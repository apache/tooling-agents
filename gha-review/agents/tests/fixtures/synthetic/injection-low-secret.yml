name: Deploy
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Login
        run: echo "${{ secrets.DEPLOY_TOKEN }}" | docker login -u user --password-stdin
