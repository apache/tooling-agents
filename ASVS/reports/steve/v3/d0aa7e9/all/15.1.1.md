# Security Audit Report: ASVS 15.1.1

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 15.1.1 |
| **Files Analyzed** | 8 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that application documentation defines risk based remediation time frames for 3rd party component versions with vulnerabilities and for updating libraries in general, to minimize the risk from these components.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---

## ASVS-15.1.1: Risk-based Remediation Timeframes

### Findings for 15.1.1

### [MEDIUM]

**Finding ID**: ASVS-1511-MED-001

**Title**: No Documented Vulnerability Remediation Timeframes for Third-Party Components

**File**: `v3/pyproject.toml`

**Relevant Code**:
```toml
dependencies = [
    "asfpy>=0.56",
    "asfquart>=0.1.12",
    "ezt>=1.1",
    "easydict>=1.13",
    "cryptography>=46.0.5,<47",
    "argon2-cffi>=25.1.0,<26",
]
```

**Issue Description**: The project defines dependencies with version constraints but lacks any documented policy or process for:

1. **Risk-based remediation timeframes** — No documentation specifying how quickly critical, high, medium, or low severity vulnerabilities in dependencies must be addressed (e.g., critical within 24h, high within 7 days, medium within 30 days).
2. **Update cadence** — No documented schedule for reviewing and updating dependencies.
3. **Vulnerability monitoring** — No evidence of automated vulnerability scanning integration (e.g., Dependabot, Snyk, or `uv audit` configuration).
4. **Risk classification** for dependencies — No documentation identifying which components contain "dangerous functionality" (e.g., `cryptography` handles key material, `argon2-cffi` handles password hashing).

The `cryptography` library is particularly critical as it handles vote encryption (Fernet, HKDF), and `argon2-cffi` handles key derivation — vulnerabilities in either would compromise vote secrecy.

**Impact**: Without defined remediation timeframes, known vulnerabilities in critical dependencies (especially `cryptography` and `argon2-cffi`) could remain unpatched indefinitely, potentially compromising vote encryption or election integrity.

**Remediation**:
Create a `SECURITY.md` or `docs/vulnerability-management.md`:
```markdown
# Vulnerability Remediation Policy

## Remediation Timeframes
| Severity | Max Remediation Time | Examples |
|----------|---------------------|----------|
| Critical | 24 hours | RCE in cryptography, argon2-cffi |
| High | 7 days | Auth bypass in asfquart, data leak in asfpy |
| Medium | 30 days | DoS vulnerabilities, minor info leaks |
| Low | 90 days | Cosmetic issues, non-exploitable bugs |

## Critical Components (Dangerous Functionality)
- `cryptography` — Vote encryption, key derivation
- `argon2-cffi` — Password/token hashing
- `asfquart` — Authentication, session management

## Update Cadence
- Security patches: Per remediation timeframe above
- Routine updates: Monthly review of all dependencies
- Automated scanning: [Dependabot/Snyk/uv audit] configured

## Monitoring
- GitHub Security Advisories enabled
- `uv audit` run in CI pipeline
```

---

### Security Controls Inventory for 15.1.1

| Control | Location | Status |
|---------|----------|--------|
| Vulnerability remediation policy document | Not found | **MISSING** |
| Remediation timeframe definitions | Not found | **MISSING** |
| Automated vulnerability scanning | Not found in provided files | **MISSING** |
| Dangerous component identification | Not found | **MISSING** |
| Dependency update schedule | Not found | **MISSING** |

### Positive Patterns for 15.1.1

- Dependencies use upper-bound version constraints (`<47`, `<26`) for critical security libraries, preventing unexpected major version upgrades that could introduce breaking changes.
- The `cryptography` library is pinned to a specific major version range (`>=46.0.5,<47`), indicating awareness of compatibility concerns.
- Development dependencies are separated from production dependencies using dependency groups.
- The project uses `uv` as a build system, which supports `uv.lock` for reproducible builds and has `uv audit` capabilities.

---



## Cross-cutting Architecture Observations

1. **Incomplete Security Controls Pattern**: The application exhibits a recurring pattern of security controls being _defined_ or _planned_ but not _implemented_:
   - CSRF token: defined as `'placeholder'` (line 83 of pages.py)
   - Authorization checks: marked with `### check authz` comments throughout
   - Path validation in `serve_doc`: marked with `### verify the propriety of DOCNAME`
   - These represent Type B gaps (control exists in concept but is not applied)

2. **Server-Side Security vs. Client-Side Security Imbalance**: The application demonstrates strong server-side security patterns (parameterized queries, cryptographic vote protection, state assertions, anti-tamper detection) but lacks corresponding client-side protections (no cache control, no Clear-Site-Data, no CSP headers).

3. **Cryptographic Maturity vs. Infrastructure Maturity**: The cryptographic design (Argon2 for key derivation, Fernet encryption, vote token system, tamper detection) is sophisticated, but the surrounding infrastructure (no SBOM, no vulnerability policy, no security headers) lags significantly.

4. **Single-Responsibility Concern**: `pages.py` handles routing, authentication, authorization, template rendering, and business logic. Security controls would benefit from middleware extraction (headers, CSRF, caching) applied uniformly.

5. **Framework Dependency for Security**: The application relies heavily on `asfquart` for authentication and session management. The security posture of these operations is opaque from the provided code — a single vulnerability in the framework could undermine all authentication.

---

## Cross-cutting Recommendations

### Immediate (0-7 days)
1. **Add anti-caching headers** via `@APP.after_request` handler for all non-static responses (`Cache-Control: no-store`)
2. **Implement logout endpoint** that sets `Clear-Site-Data: "cache", "cookies", "storage"` header
3. **Generate and commit `uv.lock`** to ensure reproducible builds and enable transitive dependency auditing

### Short-term (1-4 weeks)
1. **Create vulnerability remediation policy** documenting timeframes for critical/high/medium/low severity issues in dependencies
2. **Configure SBOM generation** in CI pipeline using `cyclonedx-py` or equivalent
3. **Add `uv audit`** to CI pipeline to detect known vulnerabilities in dependencies
4. **Implement Content-Security-Policy** header to restrict client-side script behavior
5. **Pin Apache-specific package sources** to prevent dependency confusion attacks

### Long-term (1-3 months)
1. **Extract security middleware** — Create a unified security headers middleware that applies Cache-Control, CSP, X-Content-Type-Options, and other security headers
2. **Implement proper CSRF protection** — Replace placeholder with cryptographically secure tokens validated server-side
3. **Complete authorization system** — Implement the `### check authz` checks throughout the application
4. **Establish dependency review process** — Monthly review cadence, automated PR generation for updates
5. **Add client-side session cleanup** — JavaScript that detects session expiration and clears sensitive DOM data
        