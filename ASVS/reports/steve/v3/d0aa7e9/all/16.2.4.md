# Security Audit Report: ASVS 16.2.4

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 16.2.4 |
| **Files Analyzed** | 5 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that logs can be read and correlated by the log processor that is in use, preferably by using a common logging format.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 2 |
| 🟢 Low | 0 |

---

## ASVS-16.2.4: General Logging - Log Format and Correlation

### Findings for 16.2.4

### [MEDIUM]

**Finding ID:** ASVS-1624-MED-001

**Title:** Log Format is Semi-Structured Text, Not Machine-Parseable Standard Format

**File:** `v3/server/main.py`, line 55

**Vulnerable Code:**
```python
logging.basicConfig(
    level=logging.DEBUG,
    style='{',
    format='[{asctime}|{levelname}|{name}] {message}',
    datefmt=DATE_FORMAT,
)
```

Combined with message patterns in `v3/server/pages.py`:
```python
_LOGGER.info(
    f'User[U:{result.uid}] created election[E:{election.eid}];'
    f' title: "{form.title}"'
)
```

**Impact:** The log format uses a custom text-based structure:
- Metadata in brackets: `[01/15 14:30|INFO|pages]`
- Message body uses ad-hoc notation: `User[U:xxx] action election[E:yyy]`

This format:
1. Is not a standard log format (not JSON, not CEF, not syslog RFC 5424)
2. Requires custom parsing rules for any log processor (Splunk, ELK, etc.)
3. Inconsistent delimiters (pipes in header, brackets/semicolons in body)
4. Free-text title field could break parsers: `title: "something with [brackets]"`
5. No request correlation ID for tracing multi-step operations

A log processor would need custom regex patterns to extract fields, making correlation error-prone.

**Remediation:**

```python
import json
import logging

class JSONFormatter(logging.Formatter):
    def format(self, record):
        log_entry = {
            'timestamp': self.formatTime(record, datefmt='%Y-%m-%dT%H:%M:%S.%fZ'),
            'level': record.levelname,
            'logger': record.name,
            'message': record.getMessage(),
            'module': record.module,
            'function': record.funcName,
            'line': record.lineno,
        }
        # Add extra fields if present
        if hasattr(record, 'user_id'):
            log_entry['user_id'] = record.user_id
        if hasattr(record, 'request_id'):
            log_entry['request_id'] = record.request_id
        return json.dumps(log_entry)

# Apply:
handler = logging.StreamHandler()
handler.setFormatter(JSONFormatter())
logging.root.addHandler(handler)
```

---

### [MEDIUM]

**Finding ID:** ASVS-1624-MED-002

**Title:** No Request Correlation ID Prevents Cross-Log Event Correlation

**File:** `v3/server/pages.py` (all log statements)

**Evidence:** Representative log entries from a single vote submission flow:

```
[01/15 14:30|DEBUG|pages] Loaded: <Election>
[01/15 14:30|INFO|pages] User[U:alice] voted on issue[I:abc123] in election[E:def456]
[01/15 14:30|INFO|steve.election] Created issue[I:abc123] in election[E:def456]
```

**Impact:** When multiple users perform actions simultaneously, there is no correlation ID to tie log entries from the same request together. For example, if `load_election` logs a debug entry and then `do_vote_endpoint` logs the vote, there's no way to definitively tie these two entries to the same HTTP request, especially under concurrent load. This makes timeline reconstruction during security incidents extremely difficult.

**Remediation:** Add request-scoped correlation IDs using Quart's context:
```python
import uuid
from quart import g

@APP.before_request
async def add_request_id():
    g.request_id = str(uuid.uuid4())[:12]

# Then in log messages:
_LOGGER.info(f'[RID:{g.request_id}] User[U:{result.uid}] voted...')
```

### Security Controls Inventory for 16.2.4

| Control | Location | Status |
|---------|----------|--------|
| Standard log format (JSON/CEF/syslog) | Not used (custom text format) | **MISSING** |
| Request correlation ID | Not present | **MISSING** |
| Consistent identifier format | Present (`User[U:]`, `election[E:]`, `issue[I:]`) | **Partial** |
| Log processor compatibility documentation | Not present | **MISSING** |
| Format consistency across components | Inconsistent (main app vs mail script) | **FAILING** |

### Positive Patterns for 16.2.4

- Consistent use of identifier markers (`U:`, `E:`, `I:`) across log messages enables regex-based field extraction
- All log messages in pages.py follow a similar `Subject Action Object` pattern
- Pipe-delimited header fields in the format string provide some structure for basic parsing
- Entity identifiers are unique (UUIDs) enabling cross-reference between log entries mentioning the same election or issue

---



## Cross-cutting Architecture Observations

1. **Logging Architecture Maturity**: The application has basic logging in place with consistent patterns (module loggers, action logging), but lacks the maturity markers expected for ASVS Level 2/3: no documentation, no structured format, no correlation IDs, and insufficient timestamp precision.

2. **Async/Sync Impedance Mismatch**: The application uses Quart (async) but all database operations via `asfpy.db`/`sqlite3` are synchronous. This creates a fundamental architectural issue affecting both concurrency fairness (15.4.4) and potentially logging reliability (if the event loop is blocked, log writes may also be delayed).

3. **Centralized Logging Configuration with Gaps**: While `main.py` centralizes logging configuration, the `print()` statements in `pages.py` and the separate `basicConfig()` in `mail-voters.py` create inconsistencies that undermine all logging requirements simultaneously.

4. **Security Event Coverage**: The application logs the right categories of events (CRUD operations, state changes, access decisions) but the metadata quality (missing source IP, missing correlation ID, imprecise timestamps) significantly reduces the investigative value of these logs.

5. **No Separation of Security vs. Operational Logs**: All logs go to the same destination with the same format. There's no differentiation between security-relevant events (votes, election state changes, access denials) and operational events (loading elections, rendering templates).

## Cross-cutting Recommendations

### Immediate (Sprint 1)

1. **Fix timestamp format** — Change `DATE_FORMAT` to `'%Y-%m-%dT%H:%M:%S%z'` and set `logging.Formatter.converter = time.gmtime` for UTC. Single-line fix with highest impact on 16.2.2.

2. **Remove `print()` statements** — Replace the two `print('FORM:', form)` calls in `pages.py` with `_LOGGER.debug()` or remove entirely. Fixes 16.2.3 compliance.

3. **Add source IP to log messages** — Include `quart.request.remote_addr` in security-relevant log entries. Improves 16.2.1 metadata.

### Short-term (1-2 Sprints)

4. **Implement request correlation IDs** — Add a `before_request` hook that generates and propagates a request ID through all log entries. Addresses 16.2.4 correlation.

5. **Create logging inventory document** — Document all event types, destinations, formats, access controls, and retention. Addresses 16.1.1 entirely.

6. **Adopt structured logging format** — Migrate to JSON logging format (e.g., `python-json-logger`) for machine parseability. Addresses 16.2.4.

### Long-term (Next Quarter)

7. **Implement async database layer** — Use `aiosqlite` or wrap synchronous calls in `asyncio.to_thread()` with a bounded thread pool to prevent event loop starvation. Addresses 15.4.4.

8. **Add request timeout middleware** — Configure Hypercorn's `keep_alive_timeout` and add application-level timeouts for database operations. Further addresses 15.4.4.

9. **Implement log access controls** — Define and enforce who can access logs, implement log rotation with retention policies, and consider separating security logs from operational logs. Addresses 16.1.1 comprehensively.
        