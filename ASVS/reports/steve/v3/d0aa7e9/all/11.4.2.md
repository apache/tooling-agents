# Security Audit Report: ASVS 11.4.2

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 11.4.2 |
| **Files Analyzed** | 6 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that passwords are stored using an approved, computationally intensive, key derivation function (also known as a "password hashing function"), with parameter settings configured based on current guidance. The settings should balance security and performance to make brute-force attacks…

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---

## ASVS-11.4.2: Password Storage with Approved KDF

### Findings for 11.4.2

### [MEDIUM]

**Finding ID**: ASVS-1142-MED-001  
**File**: `v3/steve/crypto.py`, function `_hash`, lines 85-93  

```python
def _hash(data: bytes, salt: bytes) -> bytes:
    "Use Argon2 to hash the data, with default tuning parameters."

    return argon2.low_level.hash_secret_raw(
        secret=data,
        salt=salt,
        time_cost=2,  # Passlib default
        memory_cost=65536,  # Passlib default
        parallelism=4,  # Passlib default
        hash_len=32,  # Standard Argon2 digest length
        type=argon2.low_level.Type.D,
    )
```

**Data flow**: Election data / vote credentials → `_hash()` with Argon2**d** → cryptographic keys  
**Issue**: The production code uses `argon2.low_level.Type.D` (Argon2d), while the OWASP Password Storage Cheat Sheet and NIST SP 800-63B recommend **Argon2id** as the preferred variant. Argon2d is vulnerable to side-channel attacks (timing attacks based on memory access patterns). The benchmark function at line 116 correctly uses `Type.ID` (Argon2id), indicating the developer is aware of the preferred variant but used the wrong one in production.

**Proof of concept**: An attacker with physical/VM co-location access could observe memory access patterns during vote token generation to recover secret data, since Argon2d's memory accesses are data-dependent.

**Impact**: Reduced resistance to side-channel attacks. While this is a server-side computation (reducing practical exploitability), cloud/shared-hosting environments increase risk.

**Remediation**:
```python
def _hash(data: bytes, salt: bytes) -> bytes:
    "Use Argon2 to hash the data, with default tuning parameters."

    return argon2.low_level.hash_secret_raw(
        secret=data,
        salt=salt,
        time_cost=2,
        memory_cost=65536,
        parallelism=4,
        hash_len=32,
        type=argon2.low_level.Type.ID,  # Argon2id: recommended variant
    )
```

### Security Controls Inventory for 11.4.2

| Control | Location | Coverage Status |
|---------|----------|-----------------|
| Argon2 KDF | `crypto.py:_hash()` line 85 | ✅ Applied, but wrong variant (Type.D vs Type.ID) |
| Memory-hard parameters | `memory_cost=65536` (64 MB) | ✅ Reasonable for server-side |
| Time cost parameter | `time_cost=2` | ⚠️ Minimum acceptable; consider increasing |
| Parallelism | `parallelism=4` | ✅ Standard |
| Benchmark utility | `crypto.py:benchmark_argon2()` line 108 | ✅ Available for parameter tuning |

### Positive Patterns for 11.4.2

1. **Argon2 usage**: The system uses a memory-hard KDF (Argon2) rather than PBKDF2 or bcrypt, which is the current best practice.
2. **Benchmark function**: A utility exists (`benchmark_argon2()`) to help administrators tune parameters for their hardware.
3. **No plaintext credential storage**: The system never stores passwords — authentication is delegated to external LDAP/ASF infrastructure.
4. **Adequate memory cost**: 64 MB memory cost provides meaningful GPU resistance.

---



## Cross-cutting Architecture Observations

1. **Cryptographic Architecture is Well-Designed**: The multi-layered approach (election data → BLAKE2b → Argon2 → opened_key; then opened_key + identifiers → Argon2 → vote_token → HKDF → encryption key) provides defense in depth with appropriate separation of concerns.

2. **Argon2 Variant Inconsistency**: The benchmark function uses `Type.ID` (Argon2id) while production uses `Type.D` (Argon2d). This appears to be an oversight rather than a deliberate design choice, as there's no documented rationale for choosing Argon2d.

3. **Fernet as Transitional Algorithm**: The code explicitly marks Fernet as temporary (comments mention switching to XChaCha20-Poly1305). The HKDF `info` parameter is already set for XChaCha20, which could cause key equivalence issues during migration if not properly managed.

4. **No Password Storage**: The system delegates authentication to external infrastructure (ASF/LDAP), meaning traditional password hashing requirements (11.4.2) have limited direct applicability. The Argon2 usage is for key derivation and anti-tamper rather than password verification.

5. **Anti-Tamper Integrity**: The `opened_key` mechanism provides cryptographic assurance that election configuration hasn't been modified after opening — a strong integrity control binding election metadata, issues, and voter lists.

## Cross-cutting Recommendations

### Immediate (Critical/High Priority)
1. **Switch Argon2 variant to Argon2id** (`Type.ID`): Single-line change in `_hash()` function, aligning production with benchmark code and OWASP recommendations. This affects all derived keys.

### Short-term (Medium Priority)
2. **Plan migration path for Fernet → XChaCha20-Poly1305**: Ensure the HKDF `info` parameter changes during migration to prevent key reuse across algorithms. Document the migration strategy for existing encrypted votes.
3. **Review Argon2 parameters against current hardware**: Run `benchmark_argon2()` on production hardware to determine if `time_cost` can be increased (current value of 2 is the minimum recommended).

### Long-term (Low Priority)
4. **Consider BLAKE2b → SHA-512 substitution**: If strict NIST compliance is required, replace BLAKE2b with SHA-512 for the data reduction step. Functionally equivalent with no performance concern for the single-call use case.
5. **Add cryptographic agility documentation**: Document the algorithm choices, migration strategy, and key derivation chain in a security design document to support future audits.
        