# Security Audit Report: ASVS 15.3.1

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 15.3.1 |
| **Files Analyzed** | 10 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that the application only returns the required subset of fields from a data object. For example, it should not return an entire data object, as some individual fields should not be accessible to users.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 1 |

---

## ASVS-15.3.1: Return Only Required Subset of Fields

### Findings for 15.3.1

### [MEDIUM]

**Finding ID:** ASVS-1531-MED-001

**Title:** Database Queries Use SELECT * Returning Sensitive Fields (Salt, Cryptographic Keys)

**File:** `v3/queries.yaml`, lines 63, 65, 67, 72, 98-100

**Vulnerable code:**
```yaml
q_metadata: SELECT * FROM election WHERE eid = ?
q_get_issue: SELECT * FROM issue WHERE iid = ?
q_get_mayvote: SELECT * FROM mayvote WHERE pid = ? AND iid = ?
q_tally: SELECT * FROM mayvote WHERE iid = ?
q_find_issues: |
    SELECT m.*
    FROM mayvote m
    JOIN issue i ON m.iid = i.iid
    WHERE m.pid = ? AND i.eid = ?
```

**Data flow:** Database (all columns including salt/opened_key) → query result → application code → (currently filtered before template rendering)

**Issue:** Multiple queries retrieve all columns from tables containing security-sensitive data (cryptographic salts, opened_key). While the application code currently filters these before returning to users (e.g., `get_metadata()` excludes salt and opened_key), this is a Type B gap — the control (filtering) exists in the application layer but relies on every consumer correctly filtering. If any new code path accesses these query results without filtering, sensitive data would be exposed.

The `q_tally` and `q_find_issues` queries return the `salt` column from the `mayvote` table. While used internally for vote token generation, the broad query pattern increases the attack surface.

**Impact:** If a future code change returns raw query results to a template or API response, cryptographic salts and election keys would be exposed, potentially allowing vote decryption or election tampering.

**Remediation:** Use explicit column lists in all queries:
```yaml
q_metadata_internal: |
    SELECT eid, title, owner_pid, authz, salt, opened_key, closed, open_at, close_at
    FROM election WHERE eid = ?
q_metadata_public: |
    SELECT eid, title, owner_pid, authz, closed, open_at, close_at
    FROM election WHERE eid = ?
q_get_issue: SELECT iid, eid, title, description, type, kv FROM issue WHERE iid = ?
q_get_mayvote: SELECT pid, iid, salt FROM mayvote WHERE pid = ? AND iid = ?
q_tally: SELECT pid, iid, salt FROM mayvote WHERE iid = ?
```

---

### [LOW]

**Finding ID:** ASVS-1531-LOW-001

**Title:** Person Query Returns All Fields Without Explicit Column Selection

**File:** `v3/queries.yaml`, lines 135-136

**Vulnerable code:**
```yaml
q_person: SELECT * FROM person ORDER BY pid
q_get_person: SELECT * FROM person WHERE pid = ?
```

**Issue:** The `person` table contains `pid`, `name`, and `email`. These queries return all fields. While the current schema only has three fields, the broad pattern means any future sensitive columns would be automatically exposed.

**Impact:** Low current risk (person table has minimal fields), but increases risk surface as schema evolves.

**Remediation:**
```yaml
q_person: SELECT pid, name, email FROM person ORDER BY pid
q_get_person: SELECT pid, name, email FROM person WHERE pid = ?
```

### Security Controls Inventory for 15.3.1

| Control | Location | Status |
|---------|----------|--------|
| `get_metadata()` field filtering | `election.py` line 144 | Active — excludes salt, opened_key |
| `get_issue()` field filtering | `election.py` line 157 | Active — excludes salt |
| `list_issues()` field filtering | `election.py` line 207 | Active — returns only iid, title, description, vtype, kv |
| `q_open_to_me` explicit columns | `queries.yaml` line 86 | Active — never returns salt/opened_key |
| `q_owned` explicit columns | `queries.yaml` line 119 | Active — never returns salt/opened_key |
| `q_upcoming_to_me` explicit columns | `queries.yaml` line 123 | Active — never returns salt/opened_key |
| `get_voters_for_email()` filtering | `election.py` line 445 | Active — returns only pid, name, email |

### Positive Patterns for 15.3.1

- **Strong**: `get_metadata()` in `election.py` explicitly constructs a new `edict` with only safe fields, rather than removing sensitive fields from a full record
- **Strong**: `q_open_to_me`, `q_owned`, and `q_upcoming_to_me` all use explicit column lists and notably use `(e.opened_key IS NOT NULL) AS is_opened` to expose only a boolean, not the actual key
- **Strong**: Comment in `queries.yaml` explicitly documents "SALT and OPENED_KEY are never returned for security"
- **Strong**: `list_issues()` uses an `extract_issue()` helper that constructs a clean object
- **Good**: `get_issue()` returns a tuple of only the needed fields, never including salt

---



## Cross-cutting Architecture Observations

1. **Authentication vs. Authorization Gap**: The application has authentication (`@asfquart.auth.require`) but lacks fine-grained authorization. All committers can manage any election — there are multiple `### check authz` placeholder comments indicating this is known but unimplemented. This creates a horizontal privilege escalation risk.

2. **CSRF Protection Entirely Missing**: The `csrf_token = 'placeholder'` on line 83 of `pages.py` means every state-changing endpoint is vulnerable to CSRF. This affects multiple requirements:
   - 15.2.5: No protection for dangerous operations (open/close election)
   - 15.3.3: Mass assignment protection is weakened when attackers can submit forms cross-origin

3. **Defense in Depth Architecture**: The application relies on a single-layer architecture where the web server, business logic, and cryptographic operations all run in the same process. This maximizes blast radius from any single vulnerability.

4. **State Machine as Security Control**: The `assert`-based state machine in `election.py` (editable → open → closed) provides an important security boundary. However, `assert` statements can be disabled with Python's `-O` flag, making this a fragile control for production.

5. **Query Design Philosophy**: The codebase shows two competing patterns — some queries (external-facing) explicitly select safe columns, while others (internal) use `SELECT *`. This split is well-documented but creates maintenance risk.

6. **Session-Based Identity**: The consistent use of `result.uid` from authenticated sessions for identity (rather than client-supplied values) is a strong positive pattern across all endpoints.

## Cross-cutting Recommendations

### Immediate (Critical/High Priority)
1. **Implement CSRF protection**: Replace `'placeholder'` with actual token generation and validation on all state-changing endpoints
2. **Convert GET state-changers to POST**: `/do-open/<eid>` and `/do-close/<eid>` must be POST endpoints
3. **Implement authorization checks**: Fill in all `### check authz` placeholders — at minimum, verify that the authenticated user is the election owner before allowing management operations
4. **Replace `assert` with explicit checks**: State validation should use `if not ... : raise` rather than `assert` which can be optimized away

### Short-term (Medium Priority)
5. **Configure trusted proxy middleware**: Add ProxyFix or equivalent with appropriate trust depth
6. **Add IP addresses to audit logs**: Include client IP (from trusted proxy chain) in security-relevant log entries
7. **Replace `SELECT *` queries**: Use explicit column lists in all queries, especially those touching tables with cryptographic material
8. **Implement formal input schemas**: Add dataclass or Pydantic-based input validation with explicit field allowlists
9. **Remove debug print statements**: Replace with structured logging at appropriate levels
10. **Pin dependency sources**: Add repository configuration and generate/commit lock file with hashes

### Long-term (Architecture)
11. **Service separation**: Split admin, voter, and cryptographic operations into separate services with network isolation
12. **Containerization**: Deploy with container-level isolation between components
13. **Least privilege**: The crypto/tally service should be the only component with access to opened_key values
14. **Rate limiting**: Implement rate limiting using correctly-resolved client IPs
15. **Dependency scanning**: Integrate automated dependency vulnerability scanning (e.g., `pip-audit`, `safety`) into CI/CD
        