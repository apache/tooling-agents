# Security Audit Report: ASVS 12.3.1

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 12.3.1 |
| **Files Analyzed** | 10 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that an encrypted protocol such as TLS is used for all inbound and outbound connections to and from the application, including monitoring systems, management tools, remote access and SSH, middleware, databases, mainframes, partner systems, or external APIs. The server must not fall back to…

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 1 |

---

## ASVS-12.3.1: General Service to Service Communication Security

### Findings for 12.3.1

#### [MEDIUM]
**Finding ID**: ASVS-1231-MED-001

**Title**: No enforcement against fallback to unencrypted protocols — server can operate in plain HTTP mode

**File**: `v3/server/main.py` (lines 80-84)

**Vulnerable Code**:
```python
kwargs = {}
if app.cfg.server.certfile:
    kwargs['certfile'] = CERTS_DIR / app.cfg.server.certfile
    kwargs['keyfile'] = CERTS_DIR / app.cfg.server.keyfile
    extra_files.update((kwargs['certfile'], kwargs['keyfile']))

# Spool up the app!
app.runx(port=app.cfg.server.port, extra_files=extra_files, **kwargs)
```

**Data flow**: Configuration decision → Conditional TLS setup → Server starts in HTTP or HTTPS mode → No fallback prevention mechanism

**Analysis**: The server can fall back to plain HTTP if:
1. TLS configuration is removed or emptied
2. Certificate files become unavailable (deleted, permissions issue)
3. A new deployment uses the default config without certificates

There is no mechanism to prevent the server from operating without encryption.

**Impact**: An election system handling confidential votes and authentication data could silently degrade to unencrypted communications, exposing sensitive data to network observers.

**Remediation**:
```python
# Enforce TLS, fail loudly if certs unavailable
certfile_path = CERTS_DIR / app.cfg.server.certfile
keyfile_path = CERTS_DIR / app.cfg.server.keyfile

if not certfile_path.exists():
    _LOGGER.critical(f'Certificate file not found: {certfile_path}')
    sys.exit(1)
if not keyfile_path.exists():
    _LOGGER.critical(f'Key file not found: {keyfile_path}')
    sys.exit(1)

kwargs = {'certfile': certfile_path, 'keyfile': keyfile_path}
```

---

#### [LOW]
**Finding ID**: ASVS-1231-LOW-001

**Title**: LDAP connection security not verified in architecture

**File**: `v3/docs/quickstart.md` (lines 38-44), `v3/ARCHITECTURE.md`

**Relevant Documentation**:
```markdown
# quickstart.md
cat > bin/bind.txt
# <2 magic lines for user/pass; talk to me on Slack>

Once bind.txt is ready, run the LDAP loading script (this may take a few minutes):
uv run bin/asf-load-ldap.py
```

**Analysis**: The system connects to an LDAP server for user data loading. The `asf-load-ldap.py` script is referenced but not provided. There's no documentation indicating whether:
1. LDAPS (LDAP over TLS) or StartTLS is used
2. Certificate validation is performed
3. Credentials in `bind.txt` are transmitted securely

**Impact**: If LDAP connections use plaintext LDAP (port 389) without StartTLS, credentials and user data are transmitted unencrypted.

**Remediation**: Ensure the LDAP loading script uses LDAPS (port 636) or StartTLS, and document this requirement.

### Security Controls Inventory for 12.3.1

| Control | Location | Coverage Status |
|---------|----------|-----------------|
| TLS for server listener | `main.py` lines 80-84 | CONDITIONAL (not enforced) |
| HTTPS for OAuth | `main.py` lines 46-49 | PRESENT (hardcoded HTTPS URLs) |
| Database encryption | N/A (SQLite is local file) | NOT APPLICABLE |
| LDAP connection encryption | `bin/asf-load-ldap.py` (not provided) | UNKNOWN |
| No-fallback mechanism | Not found | ABSENT |

### Positive Patterns for 12.3.1

- OAuth URLs hardcoded as `https://` — prevents downgrade to HTTP for authentication
- SQLite database is a local file, not a network connection — no network encryption needed for DB
- Architecture design places a proxy in front, where TLS termination would typically be enforced
- The application supports TLS natively when configured

---



## Cross-cutting Architecture Observations

1. **Monolithic Architecture Reduces TLS Surface**: The application uses SQLite (local file) and runs as a single process, meaning most "internal communication" happens in-process. The primary TLS concern is the external-facing server endpoint and the proxy-to-app connection.

2. **Proxy-Dependent Security Model**: The architecture explicitly states "a proxy sits in front of this server." This means TLS termination and certificate management may be delegated to the proxy layer (e.g., Apache httpd, nginx, HAProxy), which is not represented in this codebase. This is a common and valid pattern but creates an implicit dependency that isn't enforced.

3. **Optional TLS is the Primary Risk**: The most significant cross-cutting issue is that TLS is conditional on configuration. The server can run in plain HTTP mode, which violates multiple requirements simultaneously (12.2.2, 12.3.1, 12.3.3).

4. **External Dependencies Not Auditable**: The `asfquart` framework handles OAuth HTTP clients and potentially TLS configuration. Without access to this code, certificate validation behavior cannot be fully verified.

5. **Development vs. Production Gap**: The documentation and configuration examples target development (self-signed certs, localhost). There is no production deployment guide addressing TLS requirements.

---

## Cross-cutting Recommendations

### Immediate (Critical/High Priority)

1. **Enforce TLS at Startup**: Modify `main.py` to require TLS configuration and fail with a clear error if certificates are missing or invalid. Allow an explicit `--insecure` flag for development only:
   ```python
   if not app.cfg.server.certfile and not os.environ.get('STEVE_DEV_MODE'):
       _LOGGER.critical('TLS is required. Set certfile/keyfile or STEVE_DEV_MODE=1')
       sys.exit(1)
   ```

2. **Add HSTS Headers**: Configure Strict-Transport-Security headers to prevent protocol downgrade:
   ```python
   @APP.after_request
   async def add_security_headers(response):
       response.headers['Strict-Transport-Security'] = 'max-age=31536000; includeSubDomains'
       return response
   ```

### Short-term (Within 1-2 Sprints)

3. **Production Deployment Guide**: Create documentation for production TLS deployment covering:
   - Publicly trusted certificate acquisition (Let's Encrypt/ACME)
   - Reverse proxy TLS configuration
   - Internal communication encryption requirements

4. **Verify OAuth Client TLS**: Audit the `asfquart` library to confirm certificate validation is enabled and cannot be accidentally disabled.

5. **Certificate Validation for LDAP**: Ensure `asf-load-ldap.py` uses LDAPS or StartTLS with certificate validation.

### Long-term (Architecture Level)

6. **Internal CA Infrastructure**: If the deployment grows beyond a single-host model, establish an internal CA for service-to-service TLS with proper certificate lifecycle management.

7. **Consider mTLS for Proxy-App Communication**: Implement mutual TLS between the reverse proxy and the application server for defense-in-depth.

8. **Automated Certificate Rotation**: Integrate certificate rotation mechanisms (e.g., cert-manager, ACME) to prevent certificate expiry-related outages.
        