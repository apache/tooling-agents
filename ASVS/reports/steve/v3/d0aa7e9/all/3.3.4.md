# Security Audit Report: ASVS 3.3.4

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 3.3.4 |
| **Files Analyzed** | 11 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that if the value of a cookie is not meant to be accessible to client-side scripts (such as a session token), the cookie must have the 'HttpOnly' attribute set and the same value (e. g. session token) must only be transferred to the client via the 'Set-Cookie' header field.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 1 |

---

## ASVS-3.3.4: Cookie HttpOnly Attribute

### Findings for 3.3.4

#### [MEDIUM]
**Finding ID**: ASVS-334-MED-001

**Title**: No explicit HttpOnly configuration for session cookie

**File**: `v3/server/main.py` (lines 42-45) and `v3/server/pages.py` (line 68)

**Vulnerable code quote**:
```python
# Session read is server-side only - value should never be accessible to JS
s = await asfquart.session.read()
if s:
    basic.update(
        uid=s['uid'],
        name=s['fullname'],
        email=s['email'],
    )
```

**Data flow**: Session cookie contains authentication state → used exclusively server-side via `asfquart.session.read()` → no client-side JavaScript requires access to this cookie value → HttpOnly attribute should be enforced.

**Analysis**: The session cookie is used only for server-side operations (reading UID, name, email). No client-side script accesses the session cookie directly. The CSRF token placeholder (line 83) is passed to templates via `[csrf_token]`, not via cookie. Therefore, the session cookie qualifies as a value "not meant to be accessible to client-side scripts" and MUST have `HttpOnly`.

**Impact**: If HttpOnly is not set and an XSS vulnerability exists (note: the `rewrite_description` function on line 49 wraps user content in `<pre>` tags without full HTML escaping), an attacker could steal the session cookie via `document.cookie`.

**Remediation**:
```python
# In app configuration (explicit enforcement)
app.config['SESSION_COOKIE_HTTPONLY'] = True
```

**Note**: Quart/Flask frameworks typically default HttpOnly to True, but without seeing the `asfquart` framework source, this cannot be confirmed. Explicit configuration provides defense-in-depth.

#### [LOW]
**Finding ID**: ASVS-334-LOW-001

**Title**: Session token value potentially exposed outside Set-Cookie header

**File**: `v3/server/pages.py` (line 83)

**Vulnerable code quote**:
```python
### generate a real token and store in the session
basic.csrf_token = 'placeholder'
```

**Analysis**: The requirement states that the session token value "must only be transferred to the client via the 'Set-Cookie' header field." While the current placeholder CSRF token is not the session token, if a future implementation derives the CSRF token from the session token (a common anti-pattern), it would violate this requirement. The current code does not appear to expose the actual session token in response bodies.

**Impact**: Low — currently the session token appears to only be sent via Set-Cookie. This is a monitoring item for future changes.

### Security Controls Inventory for 3.3.4

| Control | Location | Status |
|---------|----------|--------|
| HttpOnly on session cookie | Not explicitly configured | **UNKNOWN** (likely framework default) |
| Session value not in response body | `pages.py` line 68 (read-only) | **PASS** |
| Session token only via Set-Cookie | Appears correct | **PASS** (pending verification) |

### Positive Patterns for 3.3.4

- Session data is read exclusively server-side via `asfquart.session.read()` — never exposed in response bodies
- Template data passed to client includes only derived values (uid, name, email, csrf_token) not the session cookie value itself
- The application architecture separates session identity from rendered page data

---



## Cross-cutting Architecture Observations

1. **Session Management Framework Opacity**: The `asfquart` framework handles session cookies, but its configuration is not visible in the audited code. Critical security attributes (SameSite, HttpOnly, Secure, cookie name) are delegated to the framework with no explicit override. This creates an audit gap — the security posture depends on undocumented framework defaults.

2. **No Security Header Middleware**: The application lacks any `after_request` handler or middleware for setting security headers. This means HSTS, CSP, X-Content-Type-Options, X-Frame-Options, and other security headers are all absent from responses.

3. **CSRF Defense Completely Absent**: The combination of:
   - Placeholder CSRF token (`'placeholder'`)
   - No SameSite cookie attribute (verified)
   - State-changing GET endpoints (`/do-open`, `/do-close`)
   
   Creates a complete absence of CSRF protection for the voting system. This is the most critical cross-cutting concern.

4. **Defense-in-Depth Gap**: The application relies solely on authentication (`@asfquart.auth.require`) for security. There are no layered defenses (CSRF, origin checking, SameSite cookies, authorization checks). If any single layer is bypassed, there is no fallback.

5. **Acknowledged Technical Debt**: The `TODO.md` and inline `### check authz` comments indicate the development team is aware of security gaps but has not yet addressed them. The code appears to be in active development with security controls planned but not implemented.

---

## Cross-cutting Recommendations

### Immediate (Before Any Production Deployment)

1. **Add Security Headers Middleware** (addresses ASVS 3.4.1):
```python
@APP.after_request
async def security_headers(response):
    response.headers['Strict-Transport-Security'] = 'max-age=31536000; includeSubDomains'
    response.headers['X-Content-Type-Options'] = 'nosniff'
    response.headers['X-Frame-Options'] = 'DENY'
    return response
```

2. **Configure Session Cookie Securely** (addresses ASVS 3.3.2, 3.3.3, 3.3.4):
```python
app.config.update(
    SESSION_COOKIE_NAME='__Host-steve_session',
    SESSION_COOKIE_HTTPONLY=True,
    SESSION_COOKIE_SECURE=True,
    SESSION_COOKIE_SAMESITE='Strict',
    SESSION_COOKIE_PATH='/',
)
```

3. **Convert State-Changing GET Endpoints to POST** (addresses ASVS 3.3.2):
   - `/do-open/<eid>` → POST with CSRF token
   - `/do-close/<eid>` → POST with CSRF token

### Short-term (Within Next Sprint)

4. **Implement Real CSRF Protection** (supports ASVS 3.3.2):
   - Generate cryptographically random tokens per session
   - Validate tokens on all state-changing endpoints
   - Combine with `SameSite=Strict` for defense-in-depth

5. **Add Cookie Size Monitoring** (addresses ASVS 3.3.5):
   - Log warnings if session cookie approaches 4096 bytes
   - Consider server-side session storage if flash messages grow

### Long-term (Architecture)

6. **Audit asfquart Framework**: Review the `asfquart` framework source code to verify default cookie attributes, as the application delegates all cookie security to this dependency.

7. **Implement Authorization Layer**: Address the `### check authz` placeholders throughout the codebase to ensure election owners/authorized groups are properly validated, complementing the cookie security controls.

8. **Security Testing**: Add integration tests that verify response headers include HSTS, cookie attributes include SameSite/HttpOnly/Secure, and no CORS headers leak on sensitive endpoints.
        