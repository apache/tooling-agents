# Security Audit Report: ASVS 3.3.2

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 3.3.2 |
| **Files Analyzed** | 11 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that each cookie's 'SameSite' attribute value is set according to the purpose of the cookie, to limit exposure to user interface redress attacks and browser-based request forgery attacks, commonly known as cross-site request forgery (CSRF).

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 1 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---

## ASVS-3.3.2: Cookie SameSite Attribute

### Findings for 3.3.2

#### [HIGH]
**Finding ID**: ASVS-332-HIGH-001

**Title**: No explicit SameSite cookie attribute configuration for session cookies

**File**: `v3/server/main.py` (lines 42-45) and `v3/server/pages.py` (line 83)

**Vulnerable code quote**:
```python
# main.py - App construction with no cookie security configuration
app = asfquart.construct('steve', app_dir=THIS_DIR, static_folder=None)
```

```python
# pages.py - Session usage without SameSite enforcement
s = await asfquart.session.read()
```

**Data flow**: Session cookie is created by the `asfquart` framework → delivered to browser via `Set-Cookie` → no visible `SameSite` attribute configuration → browser uses default behavior (varies by browser).

**Entry points affected**:
- ALL authenticated endpoints (19 endpoints) rely on session cookies for authentication
- State-changing GET endpoints are particularly vulnerable: `/do-open/<eid>` (line 431), `/do-close/<eid>` (line 451) — these perform state changes via GET request without CSRF protection

**Proof of concept**:
```html
<!-- Attacker page targeting /do-close/<known_eid> -->
<img src="https://steve.example.org/do-close/1a2b3c4d5e" style="display:none">
<!-- If SameSite is not Strict/Lax, cookie is sent with cross-site request -->
```

**Impact**: Combined with the acknowledged placeholder CSRF token (`basic.csrf_token = 'placeholder'` on line 83 of `pages.py`), the absence of explicit `SameSite=Strict` or `SameSite=Lax` on session cookies means the application has NO functional CSRF defense. An attacker can trick an authenticated user into closing elections, opening elections, or casting votes.

**Gap Classification**: Type B — The application has authentication controls but no SameSite enforcement configured, creating false confidence in CSRF protection.

**Remediation**:
```python
# In main.py or app configuration
app.config['SESSION_COOKIE_SAMESITE'] = 'Strict'  # Or 'Lax' for broader compatibility
app.config['SESSION_COOKIE_SECURE'] = True  # Required with HTTPS
```

---

#### [MEDIUM]
**Finding ID**: ASVS-332-MED-001

**Title**: State-changing operations via GET requests bypass SameSite=Lax protection

**File**: `v3/server/pages.py` (lines 431, 451)

**Vulnerable code quote**:
```python
@APP.get('/do-open/<eid>')
@asfquart.auth.require({R.committer})  ### need general solution
@load_election
async def do_open_endpoint(election):
    # ...
    election.open(pdb)

@APP.get('/do-close/<eid>')
@asfquart.auth.require({R.committer})  ### need general solution
@load_election
async def do_close_endpoint(election):
    # ...
    election.close()
```

**Data flow**: GET request → no CSRF check → state-changing operation executed. Even with `SameSite=Lax`, cookies are sent on top-level GET navigations.

**Impact**: Even if `SameSite=Lax` is configured, state-changing GET endpoints would still be vulnerable because Lax allows cookies on top-level navigations. An attacker could embed `<a>` tags or use JavaScript redirects to trigger these endpoints.

**Remediation**:
```python
# Change to POST with CSRF validation
@APP.post('/do-open/<eid>')
@asfquart.auth.require({R.committer})
@load_election
async def do_open_endpoint(election):
    verify_csrf_token(await quart.request.form)
    # ...
```

### Security Controls Inventory for 3.3.2

| Control | Location | Status |
|---------|----------|--------|
| SameSite cookie attribute | Not found in codebase | **MISSING** |
| CSRF token generation | `pages.py` line 83 | **PLACEHOLDER** (`'placeholder'` literal) |
| CSRF token validation | Not found | **MISSING** |
| State-changing via POST | Partial (some endpoints use POST, some use GET) | **INCOMPLETE** |

### Positive Patterns for 3.3.2

- POST method correctly used for vote submission (`/do-vote/<eid>`), election creation (`/do-create-election`), issue operations (`/do-add-issue`, `/do-edit-issue`, `/do-delete-issue`)
- Template includes CSRF token in forms (e.g., `manage.ezt` line 11: `<input type="hidden" id="csrf-token" value="[csrf_token]">`) — infrastructure is partially in place for future implementation
- The `TODO.md` (item 8) explicitly acknowledges the CSRF vulnerability

---



## Cross-cutting Architecture Observations

1. **Session Management Framework Opacity**: The `asfquart` framework handles session cookies, but its configuration is not visible in the audited code. Critical security attributes (SameSite, HttpOnly, Secure, cookie name) are delegated to the framework with no explicit override. This creates an audit gap — the security posture depends on undocumented framework defaults.

2. **No Security Header Middleware**: The application lacks any `after_request` handler or middleware for setting security headers. This means HSTS, CSP, X-Content-Type-Options, X-Frame-Options, and other security headers are all absent from responses.

3. **CSRF Defense Completely Absent**: The combination of:
   - Placeholder CSRF token (`'placeholder'`)
   - No SameSite cookie attribute (verified)
   - State-changing GET endpoints (`/do-open`, `/do-close`)
   
   Creates a complete absence of CSRF protection for the voting system. This is the most critical cross-cutting concern.

4. **Defense-in-Depth Gap**: The application relies solely on authentication (`@asfquart.auth.require`) for security. There are no layered defenses (CSRF, origin checking, SameSite cookies, authorization checks). If any single layer is bypassed, there is no fallback.

5. **Acknowledged Technical Debt**: The `TODO.md` and inline `### check authz` comments indicate the development team is aware of security gaps but has not yet addressed them. The code appears to be in active development with security controls planned but not implemented.

---

## Cross-cutting Recommendations

### Immediate (Before Any Production Deployment)

1. **Add Security Headers Middleware** (addresses ASVS 3.4.1):
```python
@APP.after_request
async def security_headers(response):
    response.headers['Strict-Transport-Security'] = 'max-age=31536000; includeSubDomains'
    response.headers['X-Content-Type-Options'] = 'nosniff'
    response.headers['X-Frame-Options'] = 'DENY'
    return response
```

2. **Configure Session Cookie Securely** (addresses ASVS 3.3.2, 3.3.3, 3.3.4):
```python
app.config.update(
    SESSION_COOKIE_NAME='__Host-steve_session',
    SESSION_COOKIE_HTTPONLY=True,
    SESSION_COOKIE_SECURE=True,
    SESSION_COOKIE_SAMESITE='Strict',
    SESSION_COOKIE_PATH='/',
)
```

3. **Convert State-Changing GET Endpoints to POST** (addresses ASVS 3.3.2):
   - `/do-open/<eid>` → POST with CSRF token
   - `/do-close/<eid>` → POST with CSRF token

### Short-term (Within Next Sprint)

4. **Implement Real CSRF Protection** (supports ASVS 3.3.2):
   - Generate cryptographically random tokens per session
   - Validate tokens on all state-changing endpoints
   - Combine with `SameSite=Strict` for defense-in-depth

5. **Add Cookie Size Monitoring** (addresses ASVS 3.3.5):
   - Log warnings if session cookie approaches 4096 bytes
   - Consider server-side session storage if flash messages grow

### Long-term (Architecture)

6. **Audit asfquart Framework**: Review the `asfquart` framework source code to verify default cookie attributes, as the application delegates all cookie security to this dependency.

7. **Implement Authorization Layer**: Address the `### check authz` placeholders throughout the codebase to ensure election owners/authorized groups are properly validated, complementing the cookie security controls.

8. **Security Testing**: Add integration tests that verify response headers include HSTS, cookie attributes include SameSite/HttpOnly/Secure, and no CORS headers leak on sensitive endpoints.
        