# Security Audit Report: ASVS 4.1.1

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 4.1.1 |
| **Files Analyzed** | 9 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that every HTTP response with a message body contains a Content-Type header field that matches the actual content of the response, including the charset parameter to specify safe character encoding (e.g., UTF-8, ISO-8859-1) according to IANA Media Types, such as "text/", "/+xml" and "/xml".

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 1 |

---

## ASVS-4.1.1: Content-Type Header with Charset

### Findings for 4.1.1

### [MEDIUM] ASVS-411-MED-001
**Finding: Custom 404 Responses Missing Charset in Content-Type**

- **File:** `v3/server/pages.py`
- **Location:** `raise_404()` function, line 647

**Vulnerable code quote:**
```python
def raise_404(template, data):
    content = asfquart.utils.render(template, data)
    quart.abort(quart.Response(content, status=404, mimetype='text/html'))
```

- **Data flow:** Client requests invalid resource → `raise_404()` called → `quart.Response()` created with `mimetype='text/html'` → Response sent with `Content-Type: text/html` (no charset) → Browser may use content-sniffing or default encoding
- **Proof of concept:**
```
GET /vote-on/nonexistent-eid HTTP/1.1
Host: example.com

Response:
HTTP/1.1 404 NOT FOUND
Content-Type: text/html
...
```
The response will have `Content-Type: text/html` without `; charset=utf-8`, potentially allowing character encoding attacks (UTF-7 XSS in older browsers).

- **Impact:** Without explicit charset, older browsers may be susceptible to character encoding-based XSS attacks. Although modern browsers largely mitigate this, the requirement explicitly calls for charset specification.
- **Gap Type:** Type B — Quart's default template rendering includes charset, but the custom `raise_404` manually creates a Response without it.

**Remediation:**
```python
def raise_404(template, data):
    content = asfquart.utils.render(template, data)
    quart.abort(quart.Response(
        content,
        status=404,
        content_type='text/html; charset=utf-8'
    ))
```

### [LOW] ASVS-411-LOW-001
**Finding: Document Serving Endpoint May Lack Charset for Text Content**

- **File:** `v3/server/pages.py`
- **Location:** `serve_doc()` function, line 574

**Vulnerable code quote:**
```python
@APP.get('/docs/<iid>/<docname>')
@asfquart.auth.require
async def serve_doc(iid, docname):
    # ...
    return await quart.send_from_directory(DOCSDIR / iid, docname)
```

- **Data flow:** Client requests `/docs/<iid>/file.txt` → `send_from_directory()` determines MIME type from extension → For text/* files, charset may not be included depending on Quart's implementation → Response sent without explicit charset
- **Proof of concept:**
```
GET /docs/abc123/notes.txt HTTP/1.1

Response may be:
Content-Type: text/plain
(missing charset)
```
- **Impact:** Low risk for most file types, but text-based documents could be misinterpreted by browsers without explicit charset declaration.
- **Gap Type:** Type A — No explicit charset handling for served documents.

**Remediation:**
```python
@APP.get('/docs/<iid>/<docname>')
@asfquart.auth.require
async def serve_doc(iid, docname):
    result = await basic_info()
    db = steve.election.Election.open_database(DB_FNAME)
    row = db.q_get_mayvote.first_row(result.uid, iid)
    if not row:
        quart.abort(404)
    
    response = await quart.send_from_directory(DOCSDIR / iid, docname)
    # Ensure text types have charset
    if response.content_type and response.content_type.startswith('text/'):
        if 'charset' not in response.content_type:
            response.content_type += '; charset=utf-8'
    return response
```

### Security Controls Inventory for 4.1.1

| Control | Location | Coverage Status |
|---------|----------|----------------|
| Template responses via `@APP.use_template` | All GET page handlers | Likely includes charset (framework default) |
| Custom 404 responses | `raise_404()` line 647 | **MISSING charset** |
| Document serving | `serve_doc()` line 574 | **MISSING explicit charset for text types** |
| Static file serving | `serve_static()` line 577 | Framework-dependent |
| JSON API responses (204) | `_set_election_date()` line 112 | N/A (empty body) |

### Positive Patterns for 4.1.1

- The `@APP.use_template` decorator (used on all standard page endpoints) likely uses Quart's default response creation which sets `Content-Type: text/html; charset=utf-8`.
- The `_set_election_date()` function returns `'', 204` — a No Content response appropriately has no body and needs no Content-Type.
- Static files served via `quart.send_from_directory()` use Quart's built-in MIME type detection.

---



## Cross-cutting Architecture Observations

1. **CSRF Protection is Completely Non-Functional:** The `basic_info()` function sets `basic.csrf_token = 'placeholder'` (line 83). This value is rendered in templates and sent in `X-CSRFToken` headers, but **no server-side validation exists**. This is a Type B gap (control exists in UI but is never validated), creating false confidence. All state-changing endpoints are vulnerable to CSRF, compounded by the GET-method issue on open/close.

2. **Authorization Gaps (Pervasive):** Multiple `### check authz` placeholders indicate that fine-grained authorization (e.g., "is this user the owner of this election?") is NOT implemented. Any authenticated committer can manage/open/close any election. This is explicitly acknowledged in code comments but represents a critical security gap for a voting system.

3. **Defense-in-Depth Layers Missing:** The application lacks:
   - Security response headers (CSP, X-Content-Type-Options, X-Frame-Options)
   - Rate limiting
   - Request size limits
   - Audit trail integrity

4. **Deployment Architecture Dependency:** The config comment "a proxy sits in front of this server" means some controls (HSTS, HTTP→HTTPS redirect, proxy header trust) might be implemented at the infrastructure layer. However, the application provides no documentation, verification, or enforcement of these assumptions.

5. **Template vs API Endpoint Confusion:** The application mixes template-rendered pages with JSON API endpoints (`/do-set-open_at`, `/do-set-close_at`) without clear architectural separation. This makes it difficult to apply appropriate security policies (Content-Type, redirect behavior, CORS) per endpoint type.

---

## Cross-cutting Recommendations

### Immediate (Critical — must fix before production)
1. **Implement CSRF protection:** Replace `'placeholder'` token with a cryptographically random token tied to the session. Validate on all POST endpoints. Consider using `quart-csrf` or similar library.
2. **Change `/do-open` and `/do-close` to POST methods** to prevent CSRF via GET and accidental triggering by prefetchers/crawlers.
3. **Add HSTS header with preload directive** and submit domain to hstspreload.org.
4. **Add charset to all responses:** Fix `raise_404()` and add an `after_request` handler ensuring all `text/*` responses include `; charset=utf-8`.

### Short-term (High — fix within current sprint)
5. **Configure trusted proxy headers:** Add Hypercorn `forwarded_allow_ips` or application-level middleware to prevent header spoofing.
6. **Implement HTTP rejection for API endpoints:** Non-browser endpoints should return 403 on HTTP rather than redirecting.
7. **Add browser feature detection:** Implement client-side checks that warn users if their browser cannot support the voting interface.
8. **Add security headers via `after_request` middleware:**
   ```python
   @app.after_request
   async def security_headers(response):
       response.headers['X-Content-Type-Options'] = 'nosniff'
       response.headers['X-Frame-Options'] = 'DENY'
       response.headers['Strict-Transport-Security'] = 'max-age=63072000; includeSubDomains; preload'
       response.headers['Content-Security-Policy'] = "default-src 'self'"
       return response
   ```

### Long-term (Medium — plan for next release)
9. **Implement fine-grained authorization** (replace all `### check authz` placeholders) — verify election ownership before allowing management operations.
10. **Architectural separation:** Create distinct middleware chains for browser-facing pages vs. JSON API endpoints with different security policies.
11. **Document deployment requirements:** Create a security deployment guide specifying required proxy configuration, TLS requirements, and header trust settings.
12. **Add integration tests** that verify security headers, method restrictions, and CSRF protection are functioning correctly in the deployed configuration.
        