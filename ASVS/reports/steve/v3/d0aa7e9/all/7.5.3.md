# Security Audit Report: ASVS 7.5.3

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 7.5.3 |
| **Files Analyzed** | 4 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that the application requires further authentication with at least one factor or secondary verification before performing highly sensitive transactions or operations.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 2 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---

## ASVS-7.5.3: Step-up Authentication for Sensitive Operations

### Findings for 7.5.3

### [HIGH]

**Finding ID:** ASVS-753-HIGH-001

**File:** `v3/server/pages.py`, lines 372-407 (`do_vote_endpoint`)

**Description:** Voting — a highly sensitive operation in an election system — does not require step-up authentication or secondary verification. Once a user has an active session, they can cast votes with no additional verification.

```python
@APP.post('/do-vote/<eid>')
@asfquart.auth.require({R.committer})
@load_election
async def do_vote_endpoint(election):
    result = await basic_info()

    ### check authz  # <-- NOT IMPLEMENTED

    # Parse the form data
    form = edict(await quart.request.form)
    if not form:
        await flash_danger('No vote data provided.')
        return quart.redirect(f'/vote-on/{election.eid}', code=303)

    # ... votes are processed and stored directly
    for iid, votestring in votes.items():
        # ... no step-up auth check
        election.add_vote(result.uid, iid, votestring)
```

**Data flow:** Authenticated session → POST `/do-vote/<eid>` → vote recorded immediately → **no secondary verification**

**Proof of concept:** An attacker with a hijacked session (via XSS exploitation facilitated by the placeholder CSRF token) can cast votes on behalf of the victim without any additional authentication challenge.

**Impact:** In a voting system, casting votes is the most sensitive operation. Without step-up authentication, a compromised session directly leads to election manipulation. Combined with the non-functional CSRF protection (line 83: `basic.csrf_token = 'placeholder'`), this creates a critical attack surface for vote manipulation via session hijacking or CSRF.

### [HIGH]

**Finding ID:** ASVS-753-HIGH-002

**File:** `v3/server/pages.py`, lines 431-448 (`do_open_endpoint`) and lines 451-466 (`do_close_endpoint`)

**Description:** Opening and closing elections — irreversible operations that affect all voters — do not require step-up authentication. Additionally, these use GET requests, making them even more susceptible to CSRF.

```python
@APP.get('/do-open/<eid>')  # <-- GET for state-changing operation!
@asfquart.auth.require({R.committer})  ### need general solution
@load_election
async def do_open_endpoint(election):
    result = await basic_info()
    ### check authz  # <-- NOT IMPLEMENTED
    pdb = steve.persondb.PersonDB.open(DB_FNAME)
    election.open(pdb)  # Irreversible operation
    # ...

@APP.get('/do-close/<eid>')  # <-- GET for state-changing operation!
@asfquart.auth.require({R.committer})  ### need general solution
@load_election
async def do_close_endpoint(election):
    result = await basic_info()
    ### check authz  # <-- NOT IMPLEMENTED
    election.close()  # Irreversible operation
```

**Proof of concept:** An attacker can craft an image tag or link: `<img src="/do-close/TARGET_EID">` that, when loaded by an authenticated election admin, would close the election prematurely.

**Impact:** Elections can be prematurely opened (before all voters are configured) or closed (disenfranchising voters who haven't voted yet) by exploiting an authenticated session without any additional verification.

### [MEDIUM]

**Finding ID:** ASVS-753-MED-001

**File:** `v3/server/pages.py`, lines 410-427 (`do_create_endpoint`)

**Description:** Creating an election — while less destructive — is a privileged operation that only requires `R.pmc_member` session authentication with no additional verification.

```python
@APP.post('/do-create-election')
@asfquart.auth.require({R.pmc_member})  ### need general solution
async def do_create_endpoint():
    "Create a new Election."
    result = await basic_info()
    ### check authz  # <-- NOT IMPLEMENTED
    form = edict(await quart.request.form)
    election = steve.election.Election.create(DB_FNAME, form.title, result.uid)
```

**Remediation for all 7.5.3 findings:**

```python
from datetime import datetime, timedelta

STEP_UP_VALIDITY = timedelta(minutes=5)

async def require_step_up_auth(operation_name: str):
    """Require recent re-authentication for sensitive operations."""
    s = await asfquart.session.read()
    last_step_up = s.get('last_step_up_auth')
    
    if not last_step_up or (datetime.utcnow() - datetime.fromisoformat(last_step_up)) > STEP_UP_VALIDITY:
        # Store the intended operation and redirect to step-up auth
        await asfquart.session.write(pending_operation=operation_name,
                                      return_url=quart.request.url)
        return quart.redirect('/auth/step-up', code=303)
    return None

@APP.post('/do-vote/<eid>')
@asfquart.auth.require({R.committer})
@load_election
async def do_vote_endpoint(election):
    # Require step-up authentication before voting
    redirect = await require_step_up_auth('vote')
    if redirect:
        return redirect
    # ... proceed with vote processing
```

### Security Controls Inventory for 7.5.3

| Control | Location | Coverage Status |
|---------|----------|-----------------|
| Step-up auth for voting | Not implemented | ❌ MISSING |
| Step-up auth for opening elections | Not implemented | ❌ MISSING |
| Step-up auth for closing elections | Not implemented | ❌ MISSING |
| Step-up auth for creating elections | Not implemented | ❌ MISSING |
| Step-up auth for deleting issues | Not implemented | ❌ MISSING |
| CSRF protection (complementary control) | Placeholder only (line 83) | ❌ NON-FUNCTIONAL (Type B gap) |

### Positive Patterns for 7.5.3

1. **Role-based access control**: Different operations require different roles (`R.committer` vs `R.pmc_member`), showing awareness of privilege separation.
2. **Election state assertions**: The `election.py` code uses `assert self.is_editable()` and `assert self.is_open()` to prevent operations in wrong states, providing defense-in-depth at the data layer.
3. **Cryptographic vote tokens**: Votes are cryptographically bound to the voter via `gen_vote_token(opened_key, pid, iid, salt)`, providing integrity protection at the storage layer.

---



## Cross-cutting Architecture Observations

1. **Session Management Delegation**: The application delegates authentication/session management to `asfquart` (ASF Quart framework). This means some session controls (lifetime, storage, IdP synchronization) may be implemented at the framework level but are not visible or configurable in application code. This creates an audit gap — the framework would need separate review.

2. **Placeholder Security Controls (Type B Gaps)**: The most critical architectural issue is the prevalence of placeholder/non-functional security controls:
   - CSRF token: `basic.csrf_token = 'placeholder'` (line 83) — **exists in templates but provides no protection**
   - Authorization checks: `### check authz` comments in 8+ locations — **control points identified but not implemented**
   - These represent **Type B gaps** (control EXISTS but NOT CALLED) — providing false confidence to developers and auditors who see the control infrastructure without realizing it's non-functional.

3. **State-Changing GET Requests**: The `/do-open/<eid>` and `/do-close/<eid>` endpoints use GET methods for irreversible state changes. This violates HTTP semantics and makes these operations trivially exploitable via CSRF (no form submission needed — simple link/image inclusion suffices).

4. **Authorization Model Immaturity**: The code repeatedly notes `### need general solution` next to auth decorators. The current model only distinguishes between `R.committer` and `R.pmc_member`, but there's no ownership-based authorization (any committer can manage any election, open/close any election, etc.). The `### check authz` placeholders indicate awareness but no implementation.

5. **Voting System Sensitivity**: This is an election/voting system where integrity is paramount. The combination of:
   - No step-up authentication for voting
   - Non-functional CSRF protection
   - No session viewing/termination for users
   - GET-based state-changing operations
   
   Creates a scenario where a single compromised session could manipulate election outcomes.

## Cross-cutting Recommendations

### Immediate (Critical — before any production deployment)

1. **Implement CSRF protection**: Replace `'placeholder'` with actual token generation and validation. All state-changing endpoints MUST validate CSRF tokens. Priority: all POST endpoints and especially convert GET state-change endpoints to POST.

2. **Convert state-changing GETs to POSTs**: `/do-open/<eid>` and `/do-close/<eid>` MUST use POST methods with CSRF protection.

3. **Implement authorization checks**: Replace all `### check authz` placeholders with actual ownership/permission verification. At minimum, verify that the authenticated user is the election owner before allowing management operations.

### Short-term (Within next development sprint)

4. **Implement user session viewing**: Add `/sessions` page allowing users to see active sessions with device/time metadata.

5. **Implement user session termination**: Allow users to terminate other sessions (requiring re-authentication with at least one factor per ASVS 7.5.2).

6. **Add step-up authentication for voting**: Require recent authentication (within 5 minutes) before casting votes and performing irreversible election operations.

7. **Document session lifetime behavior**: Explicitly configure and document the session lifetime contract between this application and the ASF SSO provider.

### Long-term (Architecture improvements)

8. **Implement admin session management**: Provide administrators the ability to terminate sessions for specific users or all users (ASVS 7.4.5).

9. **Add IdP authentication freshness checking**: Validate `auth_time` claims from the IdP and force re-authentication when the maximum authentication age is exceeded (ASVS 7.6.1).

10. **Implement session binding**: Consider binding sessions to client characteristics (IP range, user agent) to detect session hijacking, with automatic step-up challenges on anomalous changes.

11. **Role and permission system overhaul**: Replace the `### need general solution` comments with a proper RBAC system that distinguishes election owners, election administrators, voters, and system administrators with appropriate permission boundaries.
        