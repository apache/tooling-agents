# Security Audit Report: ASVS 12.2.1

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 12.2.1 |
| **Files Analyzed** | 8 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that TLS is used for all connectivity between a client and external facing, HTTP-based services, and does not fall back to insecure or unencrypted communications.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 1 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---

## ASVS-12.2.1: HTTPS Communication with External Facing Services

### Findings for 12.2.1

### [HIGH]

**Finding ID**: ASVS-1221-HIGH-001

**File**: `v3/server/config.yaml.example`, lines 6-12; `v3/server/main.py`, lines 72-82

**Vulnerable code (config.yaml.example):**
```yaml
    # Specify the .pem files to serve using TLS, or leave these two
    # fields blank for plain HTTP. These files are relative to the
    # "certs" subdirectory of main.py (the app), or they can be absolute
    # paths to their installed location.
    certfile: localhost.apache.org+3.pem
    keyfile: localhost.apache.org+3-key.pem
```

**Vulnerable code (main.py):**
```python
kwargs = {}
if app.cfg.server.certfile:
    kwargs['certfile'] = CERTS_DIR / app.cfg.server.certfile
    kwargs['keyfile'] = CERTS_DIR / app.cfg.server.keyfile
    extra_files.update((kwargs['certfile'], kwargs['keyfile']))

# Spool up the app!
app.runx(port=app.cfg.server.port, extra_files=extra_files, **kwargs)
```

**Data flow**: `config.yaml` → `app.cfg.server.certfile` → conditional TLS setup → if blank, server runs on plain HTTP

**Proof of concept**: 
1. Set `certfile:` and `keyfile:` to empty in config.yaml
2. Start the server: `uv run main.py`
3. The application serves all content over plain HTTP on port 58383
4. All authentication tokens, session cookies, votes, and election data transmitted in cleartext

**Impact**: The configuration explicitly documents that TLS can be disabled by leaving fields blank. When running without TLS:
- Session cookies are transmitted in cleartext (session hijacking)
- OAuth tokens and credentials are exposed
- Vote data is transmitted unencrypted
- No mechanism exists to redirect HTTP → HTTPS
- No HSTS header enforcement is visible in the code

This is a voting application handling sensitive election data—running without TLS completely undermines confidentiality and integrity.

**Remediation**:
```python
def run_standalone():
    app = create_app()
    
    # REQUIRE TLS - refuse to start without certificates
    if not app.cfg.server.certfile or not app.cfg.server.keyfile:
        _LOGGER.critical("TLS certificates are REQUIRED. Cannot start without certfile and keyfile.")
        sys.exit(1)
    
    kwargs = {
        'certfile': CERTS_DIR / app.cfg.server.certfile,
        'keyfile': CERTS_DIR / app.cfg.server.keyfile,
    }
    
    app.runx(port=app.cfg.server.port, extra_files=extra_files, **kwargs)
```

Additionally, implement HTTP-to-HTTPS redirect and HSTS:
```python
@APP.before_request
async def enforce_https():
    if not quart.request.is_secure:
        url = quart.request.url.replace('http://', 'https://', 1)
        return quart.redirect(url, code=301)

@APP.after_request
async def add_hsts_header(response):
    response.headers['Strict-Transport-Security'] = 'max-age=31536000; includeSubDomains'
    return response
```

---

### [MEDIUM]

**Finding ID**: ASVS-1221-MED-001

**File**: `v3/server/main.py`, function `run_asgi()`, lines 88-108

**Vulnerable code:**
```python
def run_asgi():
    "Run as an ASGI process; eg. Hypercorn"

    # NOTE: no-op if Hypercorn has set up the root logger.
    logging.basicConfig(
        level=logging.DEBUG,
        style='{',
        format='[{asctime}|{levelname}|{name}] {message}',
        datefmt=DATE_FORMAT,
    )

    _LOGGER.info(' ** Run-mode: ASGI')

    global steve_app
    steve_app = create_app()
```

**Impact**: When running in ASGI mode (production deployment via Hypercorn), TLS configuration is entirely delegated to the external ASGI server. There is no validation or enforcement within the application that TLS is being used. The application code does not:
- Verify that the ASGI server has TLS configured
- Set any TLS-related response headers (HSTS)
- Reject non-HTTPS requests
- Check `X-Forwarded-Proto` when behind a proxy

**Remediation**: Add middleware to enforce HTTPS in ASGI mode:
```python
@APP.before_request
async def require_tls():
    # Check both direct TLS and proxy-forwarded TLS
    if not quart.request.is_secure:
        forwarded_proto = quart.request.headers.get('X-Forwarded-Proto', '')
        if forwarded_proto != 'https':
            quart.abort(403, 'HTTPS required')
```

### Security Controls Inventory for 12.2.1

| Control | Location | Status |
|---------|----------|--------|
| TLS certificate configuration | `main.py:75-78` | Present but **OPTIONAL** |
| HTTP→HTTPS redirect | N/A | **NOT PRESENT** |
| HSTS header | N/A | **NOT PRESENT** |
| Fallback to HTTP prevention | N/A | **NOT PRESENT** — explicitly allowed in config |
| Secure cookie flags | N/A | Not verified in audited code |
| ASGI TLS enforcement | N/A | **NOT PRESENT** — delegated without verification |

### Positive Patterns for 12.2.1

- TLS is the default configuration in `config.yaml.example` (certificates are specified)
- Development documentation directs users to use HTTPS URLs
- The architecture acknowledges a proxy may sit in front ("Typical usage is that a proxy sits in front of this server")

---



## Cross-cutting Architecture Observations

1. **TLS Configuration is Entirely Application-Agnostic**: The application treats TLS as an optional transport layer concern, passing cert/key paths to the framework without any security controls around protocol versions, cipher suites, or enforcement. This is a systemic design gap.

2. **Dual Deployment Modes Create Inconsistency**: The standalone mode (`run_standalone()`) accepts TLS configuration via `config.yaml`, while the ASGI mode (`run_asgi()`) delegates all TLS to Hypercorn's external configuration. Neither mode enforces TLS or validates its presence.

3. **No Security Headers Infrastructure**: There is no middleware or after-request hook for setting security-critical HTTP headers (HSTS, X-Content-Type-Options, etc.). This suggests TLS enforcement at the application layer has not been considered.

4. **Development vs. Production Gap**: The development setup uses self-signed certificates (mkcert-generated `localhost.apache.org+3.pem`), which is appropriate for development but provides no path toward production TLS hardening (OCSP, ECH, cipher suite restrictions).

5. **Proxy Assumption Undocumented**: The config comment mentions "a proxy sits in front of this server," but there is no documentation of required proxy TLS configuration, no `X-Forwarded-Proto` validation, and no deployment hardening guide.

## Cross-cutting Recommendations

### Immediate (Critical/High Priority)

1. **Enforce TLS as mandatory**: Remove the option to run without TLS. If plain HTTP is needed for development behind a proxy, require an explicit `--insecure` flag or environment variable.

2. **Add TLS protocol version enforcement**: Create an SSL context with `minimum_version = TLSv1_2` for standalone mode. Document required Hypercorn TLS configuration for ASGI mode.

3. **Implement HSTS**: Add `Strict-Transport-Security` header via after-request middleware to prevent protocol downgrade attacks.

4. **Configure cipher suites**: Restrict to AEAD cipher suites with forward secrecy (ECDHE+AESGCM, ECDHE+CHACHA20).

### Short-term (Medium Priority)

5. **Document production TLS requirements**: Create a deployment guide specifying required TLS configuration for both standalone and proxy-fronted deployments.

6. **Add HTTP→HTTPS redirect**: If port 80 is accessible, redirect all traffic to HTTPS.

7. **Configure OCSP stapling**: For production deployments, configure either at the application level (if possible) or document proxy-level OCSP stapling requirements.

8. **Add secure cookie attributes**: Ensure session cookies have `Secure`, `HttpOnly`, and `SameSite=Strict` flags.

### Long-term (Low Priority)

9. **Implement ECH**: Once Python/Hypercorn gains native ECH support, or document proxy-level ECH configuration (e.g., via Cloudflare or nginx with ECH patches).

10. **Automated TLS testing**: Integrate TLS configuration testing (e.g., testssl.sh, sslyze) into CI/CD pipeline to detect configuration regressions.

11. **Certificate management**: Implement automated certificate rotation, separate certificates per environment (as noted in the ASVS section description about wildcard certificates), and certificate expiry monitoring.
        