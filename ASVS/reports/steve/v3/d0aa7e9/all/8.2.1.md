# Security Audit Report: ASVS 8.2.1

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 8.2.1 |
| **Files Analyzed** | 11 relevant / 49 total |
| **Files Skipped** | 12 | **Requirement description:** Verify that the application ensures that function-level access is restricted to consumers with explicit permissions.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 4 |
| 🟠 High | 2 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 | ---

## ASVS-8.2.1: General Authorization Design - Function-Level Access

### Findings for 8.2.1

## check authz    # <--- NOT IMPLEMENTED

        return await func(e)
    return loader
```

**Gap Type:** Type B — Control placeholder EXISTS but NOT CALLED

**Data flow:** Any authenticated committer → URL with arbitrary `eid` → `load_election` decorator → NO ownership check → election management functions execute

**Proof of concept:**
```http
GET /do-open/a1b2c3d4e5 HTTP/1.1
Cookie: session=<any_committer_session>
```
Any ASF committer can open, close, or modify ANY election regardless of ownership.

**Impact:** Complete breach of election integrity. Any of the ~1800+ ASF committers can:
- Open any election prematurely
- Close any election prematurely
- Add/edit/delete issues in any election (editable state)
- Set open/close dates for any election

**Affected endpoints using `load_election`:**
- `GET /do-open/<eid>` (line 431)
- `GET /do-close/<eid>` (line 451)
- `POST /do-add-issue/<eid>` (line 469)
- `POST /do-set-open_at/<eid>` (line 360)
- `POST /do-set-close_at/<eid>` (line 366)
- `GET /manage/<eid>` (line 318)
- `POST /do-vote/<eid>` (line 372)

**Remediation:**
```python
def load_election(func):
    """Decorator to load/pass-argument an Election from EID."""
    @functools.wraps(func)
    async def loader(eid):
        try:
            e = steve.election.Election(DB_FNAME, eid)
        except steve.election.ElectionNotFound:
            result = await basic_info()
            result.title = 'Unknown Election'
            result.eid = eid
            raise_404(T_BAD_EID, result)

        # Verify the authenticated user has permission to manage this election
        result = await basic_info()
        md = e.get_metadata()
        if md.owner_pid != result.uid:
            # Check authz group membership if defined
            if not md.authz or not await check_group_membership(result.uid, md.authz):
                quart.abort(403)

        return await func(e)
    return loader
```

---


> **Note:** 3 Critical findings have been redacted from this report and forwarded to the project's PMC private mailing list.


## check authz    # <--- NOT IMPLEMENTED

    form = edict(await quart.request.form)
    if not form:
        await flash_danger('No vote data provided.')
        return quart.redirect(f'/vote-on/{election.eid}', code=303)

    votes = {}
    for key, value in form.items():
        if key.startswith('vote-'):
            iid = key.split('-', 1)[1]
            votes[iid] = value

    issues = election.list_issues()
    issue_dict = {i.iid: i for i in issues}

    for iid, votestring in votes.items():
        if iid not in issue_dict:
            await flash_danger(f'Invalid issue ID: {iid}')
            return quart.redirect(f'/vote-on/{election.eid}', code=303)
        try:
            election.add_vote(result.uid, iid, votestring)
```

**Gap Type:** Type B — Authorization check placeholder EXISTS but NOT CALLED

**Data flow:** Authenticated committer → `POST /do-vote/<eid>` → no eligibility check → `election.add_vote()` → AttributeError if not in mayvote OR vote recorded

**Comparison with `vote_on_page` (line 236):**
```python
# vote_on_page DOES check eligibility:
election.q_find_issues.perform(result.uid, election.eid)
if not election.q_find_issues.fetchall():
    # ... deny access
```

But `do_vote_endpoint` does NOT perform this check. While `add_vote()` will crash on `mayvote.salt` if the user isn't eligible, this relies on an implicit failure rather than an explicit authorization check.

**Proof of concept:**
```http
POST /do-vote/a1b2c3d4e5 HTTP/1.1
Cookie: session=<committer_not_eligible_to_vote>
Content-Type: application/x-www-form-urlencoded

vote-1234567890=y
```

**Impact:** A committer not eligible to vote on an election can attempt to submit votes. The `add_vote()` call will produce an unhandled AttributeError (accessing `.salt` on None) which returns a 500 error rather than a proper 403, leaking implementation details.

**Remediation:**
```python
@APP.post('/do-vote/<eid>')
@asfquart.auth.require({R.committer})
@load_election
async def do_vote_endpoint(election):
    result = await basic_info()

    # Verify voter eligibility
    election.q_find_issues.perform(result.uid, election.eid)
    if not election.q_find_issues.fetchall():
        quart.abort(403, 'Not eligible to vote in this election')
    
    # ... rest of function
```

---

## need general solution
@load_election
async def do_open_endpoint(election):
    result = await basic_info()

    ### check authz    # <--- NOT IMPLEMENTED

    pdb = steve.persondb.PersonDB.open(DB_FNAME)
    election.open(pdb)

    _LOGGER.info(f'User[U:{result.uid}] opened election[E:{election.eid}]')
    # ...
```

**Gap Type:** Type B — Authorization exists conceptually ("need general solution") but NOT CALLED

**Data flow:** Any committer → `GET /do-open/<eid>` → no ownership check → `election.open()` → election irreversibly opened

**Proof of concept:**
```http
GET /do-open/a1b2c3d4e5 HTTP/1.1
Cookie: session=<any_committer_who_is_not_the_owner>
```

**Impact:** Opening an election is an IRREVERSIBLE action that:
- Generates cryptographic salts for all voter/issue pairs
- Locks the election from further editing (no adding/removing issues or voters)
- Enables voting immediately

A malicious committer could open an election before it's ready (e.g., before all voters are added), permanently corrupting the election's integrity.

**Remediation:**
```python
@APP.post('/do-open/<eid>')  # Also: should be POST not GET for state-changing
@asfquart.auth.require({R.committer})
@load_election
async def do_open_endpoint(election):
    result = await basic_info()
    
    md = election.get_metadata()
    if md.owner_pid != result.uid:
        quart.abort(403, 'Only the election owner can open the election')
    
    pdb = steve.persondb.PersonDB.open(DB_FNAME)
    election.open(pdb)
```

---

#### [HIGH]

**Finding ID:** ASVS-821-HIGH-001

**File:** `v3/server/pages.py`, `do_open_endpoint()` line 431, `do_close_endpoint()` line 451

**Vulnerable code:**
```python
@APP.get('/do-open/<eid>')
@asfquart.auth.require({R.committer})
@load_election
async def do_open_endpoint(election):
    # ...

@APP.get('/do-close/<eid>')
@asfquart.auth.require({R.committer})
@load_election
async def do_close_endpoint(election):
    # ...
```

**Issue:** State-mutating operations (open/close election) use GET method instead of POST. This:
1. Makes them vulnerable to CSRF via image tags, link prefetch, etc.
2. Violates HTTP semantics (GET should be idempotent/safe)
3. Can be triggered by browser prefetching or link preloading

**Proof of concept:**
```html
<!-- Attacker's page; victim just needs to load this while authenticated -->
<img src="https://voting-app/do-close/a1b2c3d4e5" style="display:none">
```

**Impact:** Combined with the missing ownership check, ANY webpage could trigger an election open/close for any committer who visits it.

**Remediation:**
```python
@APP.post('/do-open/<eid>')  # Change to POST
@asfquart.auth.require({R.committer})
@load_election
async def do_open_endpoint(election):
    # Validate CSRF token
    # Check ownership
    # ...
```

---

#### [HIGH]

**Finding ID:** ASVS-821-HIGH-002

**File:** `v3/server/pages.py`, `load_election_issue` decorator, lines 189-219

**Vulnerable code:**
```python
def load_election_issue(func):
    """Decorator to load/pass-argument an Election from EID."""
    @functools.wraps(func)
    async def loader(eid, iid):
        try:
            e = steve.election.Election(DB_FNAME, eid)
        except steve.election.ElectionNotFound:
            # ...

        _LOGGER.debug(f'Loaded: {e}')

        ### check authz    # <--- NOT IMPLEMENTED

        try:
            i = e.get_issue(iid)
        except steve.election.IssueNotFound:
            # ...

        issue = edict(iid=iid, title=i[0], description=i[1], vtype=i[2], kv=i[3])
        return await func(e, issue)
    return loader
```

**Gap Type:** Type B — Control placeholder EXISTS but NOT CALLED

**Affected endpoints:**
- `POST /do-edit-issue/<eid>/<iid>` (line 493)
- `POST /do-delete-issue/<eid>/<iid>` (line 516)
- `GET /manage-stv/<eid>/<iid>` (line 337)

**Impact:** Any committer can edit or delete issues in any election they don't own, as long as the election is in editable state.

**Remediation:** Same ownership check as .

---

#### [MEDIUM]

**Finding ID:** ASVS-821-MED-001

**File:** `v3/server/pages.py`, `manage_page()`, lines 318-334

**Vulnerable code:**
```python
@APP.get('/manage/<eid>')
@asfquart.auth.require({R.committer})  ### need general solution
@load_election
@APP.use_template(TEMPLATES / 'manage.ezt')
async def manage_page(election):
    result = await basic_info()
    result.title = 'Manage an Election'
    result.eid = election.eid
    result.election = election.get_metadata()
    # ... displays full management interface
```

**Issue:** Any committer can view the management interface for any election, including seeing issue titles, descriptions, and election state. While `get_metadata()` properly excludes sensitive crypto fields, the management page exposes the election's internal structure.

**Impact:** Information disclosure — any committer can enumerate and view all elections and their issues via URL guessing (10-hex-char EIDs).

**Remediation:** Add ownership/authz group verification before rendering the management page.

### Security Controls Inventory for 8.2.1

| Control | Location | Applied To | Status |
|---------|----------|-----------|--------|
| `@asfquart.auth.require({R.committer})` | All protected endpoints | Authentication only | ✅ Applied, but insufficient (only checks role, not resource ownership) |
| `@asfquart.auth.require({R.pmc_member})` | `/do-create-election` | Election creation | ✅ More restrictive, appropriate |
| `### check authz` placeholder | `load_election` line 183 | Election management | ❌ NOT IMPLEMENTED |
| `### check authz` placeholder | `load_election_issue` line 209 | Issue management | ❌ NOT IMPLEMENTED |
| `### check authz` placeholder | `do_vote_endpoint` line 379 | Voting | ❌ NOT IMPLEMENTED |
| `### check authz` placeholder | `do_create_endpoint` line 417 | Creation | ❌ NOT IMPLEMENTED |
| `### check authz` placeholder | `do_open_endpoint` line 438 | Election open | ❌ NOT IMPLEMENTED |
| `### check authz` placeholder | `do_close_endpoint` line 458 | Election close | ❌ NOT IMPLEMENTED |
| `### check authz` placeholder | `do_add_issue_endpoint` line 479 | Issue add | ❌ NOT IMPLEMENTED |
| `### check authz` placeholder | `do_edit_issue_endpoint` line 501 | Issue edit | ❌ NOT IMPLEMENTED |
| `### check authz` placeholder | `do_delete_issue_endpoint` line 524 | Issue delete | ❌ NOT IMPLEMENTED |
| Voter eligibility check | `vote_on_page` line 236 | Ballot viewing | ✅ Applied (checks mayvote) |
| Document access check | `serve_doc` line 567 | Document serving | ✅ Applied (checks mayvote) |
| `owner_pid` field | `schema.sql` election table | Ownership tracking | ✅ Stored, ❌ NEVER enforced |
| `authz` field | `schema.sql` election table | Group-based access | ✅ Stored, ❌ NEVER enforced |
| Election state assertions | `election.py` multiple methods | State-based restrictions | ✅ Applied (e.g., can't edit open election) | ### Positive Patterns for 8.2.1

- **Voter eligibility verification** in `vote_on_page()` (line 236) properly checks `q_find_issues` before showing the ballot
- **Document access control** in `serve_doc()` (line 567) properly checks `q_get_mayvote` before serving documents
- **Election state machine** enforcement via assertions (`is_editable()`, `is_open()`, `is_closed()`) prevents operations in wrong states
- **`R.pmc_member`** restriction on election creation is more granular than other endpoints
- **Database-level integrity** via foreign keys and constraints prevents data corruption even if application-level checks are bypassed

---



## Cross-cutting Architecture Observations

1. **Systematic Authorization Gap (Type B):** The most critical finding across all requirements is the pattern of authorization placeholders (`### check authz`) that are never implemented. This creates a dangerous false sense of security — developers clearly intended to add authorization but it was never done. Eight separate endpoints share this identical gap.

2. **Authentication ≠ Authorization Confusion:** The application conflates authentication (proving identity) with authorization (proving permission). Being an ASF committer is sufficient to perform ANY operation on ANY election. The `authz` and `owner_pid` fields exist in the schema but are completely unused in the application layer.

3. **Defense-in-Depth at Data Layer:** Despite the application-level authorization failures, the `election.py` module provides some defense via state assertions (`assert self.is_editable()`) and the cryptographic design (can't decrypt votes without the proper keys). However, these protect election integrity, not confidentiality or availability.

4. **Missing Documentation Pattern:** Across all 8.1.x requirements, there is no formal authorization documentation. The code comments and `TODO.md` acknowledge this debt explicitly. The `schema.md` provides data-level documentation but not security-decision documentation.

5. **HTTP Method Misuse:** State-mutating operations (`do_open`, `do_close`) use GET, making them vulnerable to CSRF even before considering the placeholder CSRF token. This compounds the authorization gap.

6. **Incomplete Enforcement Pattern:** Where authorization IS implemented (e.g., `vote_on_page` checks mayvote), the corresponding action endpoint (`do_vote_endpoint`) does NOT repeat the check. This creates a bypass where viewing is protected but acting is not.

---

## Cross-cutting Recommendations

### Immediate (Critical - must fix before deployment)

1. **Implement ownership checks in `load_election` decorator** — Compare `election.owner_pid` against `result.uid`. This single fix addresses CRIT-003, HIGH-002, and MED-001 across 8+ endpoints.

2. **Add voter eligibility check in `do_vote_endpoint`** — Mirror the check from `vote_on_page()` to prevent ineligible users from submitting votes ().

3. **Change `do_open` and `do_close` from GET to POST** — Prevents trivial CSRF via link/image tags (ASVS-821-HIGH-001).

### Short-term (High priority - within next sprint)

4. **Implement the `authz` field enforcement** — Define and implement group-based authorization for elections that specify an authz group.

5. **Create authorization documentation** (ASVS-811-HIGH-001) — Document the role hierarchy, function-level access matrix, and field-level restrictions that are already partially implemented in code.

6. **Implement CSRF token generation and validation** — Replace the placeholder with real tokens, validated on all POST endpoints.

7. **Document field-level access rules** — Formalize the implicit rules about salt/key exclusion from public APIs.

### Long-term (Strategic improvements)

8. **Consider contextual security factors** — For a voting system, logging IP addresses and implementing rate limiting would improve auditability and prevent abuse.

9. **Implement session consent mechanism** — For federated authentication, add a consent step or session confirmation to meet ASVS 7.6.2.

10. **Centralize authorization logic** — Instead of scattered checks, implement a policy-based authorization system (e.g., ABAC) that the decorators call consistently.
        