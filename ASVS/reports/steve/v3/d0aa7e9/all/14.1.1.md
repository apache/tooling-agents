# Security Audit Report: ASVS 14.1.1

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 14.1.1 |
| **Files Analyzed** | 8 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that all sensitive data created and processed by the application has been identified and classified into protection levels. This includes data that is only encoded and therefore easily decoded, such as Base64 strings or the plaintext payload inside a JWT. Protection levels need to take into…

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---

## ASVS-14.1.1: Data Protection Documentation - Data Classification

### Findings for 14.1.1

#### [MEDIUM]
**Finding ID**: ASVS-1411-MED-001

**File**: `v3/docs/schema.md` and overall application architecture

**Issue**: While the code demonstrates awareness of sensitive data handling (e.g., explicitly excluding `salt` and `opened_key` from public queries), there is no formal data classification document that identifies ALL sensitive data and assigns protection levels. The `schema.md` documents security mechanisms but does not provide a complete inventory of sensitive data with classification levels.

**Specific gaps in classification**:

1. **Personal Identifiable Information (PII)** - Not formally classified:
   - `person.email` - email addresses of voters
   - `person.name` - human-readable names
   - `person.pid` - LDAP usernames (identifying information)

2. **Session data** - Not formally classified:
   - `uid`, `fullname`, `email` from `asfquart.session.read()`

3. **Vote data** - Partially classified (encrypted, but no formal sensitivity level):
   - `vote.ciphertext` - encrypted vote content
   - Decrypted `votestring` values during tally

4. **Cryptographic material** - Implicitly sensitive but not formally classified:
   - `election.salt`, `election.opened_key`
   - `mayvote.salt`
   - `vote.vote_token`
   - Derived encryption keys (`vote_key` in `crypto.py`)

5. **Encoded data that is easily decoded** (per requirement text):
   - `issue.kv` - JSON-formatted data containing election metadata (candidates, etc.)
   - Form data in transit

**Evidence of partial awareness** (code-level, not documented):
```python
# From election.py, get_metadata():
# NOTE: do not return the SALT or OPENED_KEY
```
```yaml
# From queries.yaml, q_open_to_me:
# Note: SALT and OPENED_KEY are never returned for security.
```

**Impact**: Without formal classification, developers may inadvertently expose sensitive data in new features, logs, or API responses. There's no reference document to verify protection is adequate.

**Remediation**: Create a formal data classification document that:
- Lists all data elements
- Assigns protection levels (e.g., Public, Internal, Confidential, Restricted)
- Maps to applicable regulations (privacy laws for PII)
- References which controls apply at each level

### Security Controls Inventory for 14.1.1

| Control | Location | Coverage Status |
|---------|----------|-----------------|
| Exclusion of salt/opened_key from public queries | `queries.yaml` (q_open_to_me, q_owned, q_upcoming_to_me) | Applied |
| Election.get_metadata() filters sensitive fields | `election.py:144-155` | Applied |
| get_issue() never returns salt | `election.py:157` (comment) | Applied |
| Vote encryption | `crypto.py:72-77` | Applied |
| Formal data classification document | N/A | **NOT PRESENT** |

### Positive Patterns for 14.1.1

1. **Deliberate exclusion of sensitive columns**: Queries like `q_open_to_me`, `q_owned`, and `q_upcoming_to_me` explicitly SELECT only non-sensitive columns, with comments explaining why salt/opened_key are excluded.
2. **Code-level comments marking sensitive data**: Comments like `# NOTE: do not return the SALT or OPENED_KEY` show developer awareness.
3. **Separation of internal and public API methods**: `_all_metadata()` is explicitly marked as internal-only, while `get_metadata()` filters sensitive fields before returning.
4. **Encryption of vote content**: Votes are encrypted before storage, showing awareness of vote confidentiality requirements.

---



## Cross-cutting Architecture Observations

1. **Type B Gap Pattern (Control exists but not called)**: The most pervasive security issue across this codebase is the `### check authz` pattern. Authorization logic is acknowledged as needed (via comments) throughout `pages.py` but is never implemented. This creates false confidence — the code appears to have considered authorization, but any authenticated committer can manage any election, open/close any election, or manipulate any issue regardless of ownership.

2. **Documentation vs. Implementation Asymmetry**: The `schema.md` documentation describes a well-thought-out security architecture (non-predictable IDs, encryption, anti-tamper), but the running application has significant gaps (CSRF placeholder, missing authz, unvalidated file serving).

3. **Positive Crypto Architecture**: The cryptographic design (Argon2 for key derivation, per-voter salts, Fernet encryption, vote shuffling) is well-implemented in `crypto.py` and `election.py`. The separation of concerns (crypto operations isolated in one module) supports auditability.

4. **Data Leakage Prevention in Queries**: The `queries.yaml` demonstrates conscious effort to prevent sensitive data leakage — public-facing queries explicitly exclude `salt` and `opened_key` columns, with comments documenting this decision. The `_all_metadata()` method is correctly marked as internal-only.

5. **Incomplete Defense-in-Depth**: Authentication is applied (decorators on all sensitive endpoints), but authorization (who can do what to which resource) and input validation (file extensions, path components) represent the missing layers.

---

## Cross-cutting Recommendations

### Immediate (Critical/High Priority)

1. **Convert state-changing GET endpoints to POST**: Change `/do-open/<eid>` and `/do-close/<eid>` from GET to POST, requiring form submission with CSRF tokens. This addresses both ASVS 14.2.1 (operations in URLs) and CSRF vulnerability.

2. **Implement file extension whitelist**: Add allowed extension sets for both `/static/` and `/docs/` endpoints. This directly addresses ASVS 13.4.7 and prevents accidental file disclosure.

3. **Validate `docname` parameter**: Implement the already-planned validation noted by the `### verify the propriety of DOCNAME` comment. Block path traversal characters and restrict to allowed extensions.

### Short-term (Within Sprint)

4. **Suppress server version headers**: Add an `@APP.after_request` handler to remove `Server` and `X-Powered-By` headers, or configure Hypercorn to disable server header inclusion (ASVS 13.4.6).

5. **Implement authorization checks**: Replace all `### check authz` comments with actual ownership/permission verification. At minimum, verify `election.owner_pid == result.uid` for management operations.

6. **Create data classification document**: Formally document all sensitive data types, their protection levels, and required controls (ASVS 14.1.1, 14.1.2).

### Long-term (Architecture)

7. **Implement CSRF protection**: Replace `basic.csrf_token = 'placeholder'` with cryptographically-generated per-session tokens validated on all state-changing endpoints.

8. **Create comprehensive data protection policy**: Document encryption requirements, retention periods, logging policies, key management lifecycle, and privacy controls for each data sensitivity level.

9. **Add security headers middleware**: Implement `Content-Security-Policy`, `X-Content-Type-Options: nosniff`, `X-Frame-Options`, and `Strict-Transport-Security` headers to complement the data protection controls.
        