# Security Audit Report: ASVS 10.3.2

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 10.3.2 |
| **Files Analyzed** | 4 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that the resource server enforces authorization decisions based on claims from the access token that define delegated authorization. If claims such as 'sub', 'scope', and 'authorization_details' are present, they must be part of the decision.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 2 |
| 🟡 Medium | 0 |
| 🟢 Low | 0 |

---

## ASVS-10.3.2: OAuth Resource Server - Authorization Based on Token Claims

### Findings for 10.3.2

#### [HIGH] ASVS-1032-HIGH-001: No OAuth Access Token Claim-Based Authorization Enforcement

**File:** `v3/server/pages.py` (entire file, multiple endpoints)

**Description:** The application does not implement OAuth access token validation or claim-based authorization decisions. Authentication relies on session cookies via `asfquart.session.read()`, and authorization is limited to role-checking decorators (`R.committer`, `R.pmc_member`). No `sub`, `scope`, or `authorization_details` claims from an access token are evaluated for any authorization decision.

**Vulnerable Code:**
```python
# v3/server/pages.py, line 68-82
async def basic_info():
    s = await asfquart.session.read()
    if s:
        basic.update(
            uid=s['uid'],
            name=s['fullname'],
            email=s['email'],
        )
    else:
        basic.update(uid=None, name=None, email=None)
    ### generate a real token and store in the session
    basic.csrf_token = 'placeholder'
    return basic
```

**Data Flow:** User session → `s['uid']` → used as `result.uid` in authorization checks (e.g., `election.q_find_issues.perform(result.uid, election.eid)`)

**Impact:** If this application is intended to function as an OAuth 2.0 resource server (API), it completely lacks the ability to:
- Validate access tokens (JWT or introspection)
- Check `scope` claims to determine permitted operations
- Use `sub` for delegated authorization decisions
- Process `authorization_details` for fine-grained access control

Any client presenting a valid session cookie gets access regardless of OAuth delegation boundaries.

**Remediation:** If OAuth integration is required, implement token validation middleware:
```python
from jose import jwt, JWTError

async def validate_access_token(request):
    auth_header = request.headers.get('Authorization', '')
    if not auth_header.startswith('Bearer '):
        quart.abort(401)
    token = auth_header[7:]
    try:
        claims = jwt.decode(token, PUBLIC_KEY, algorithms=['RS256'], 
                           audience=EXPECTED_AUDIENCE, issuer=EXPECTED_ISSUER)
    except JWTError:
        quart.abort(401)
    
    # Enforce scope-based authorization
    required_scope = get_required_scope(request.endpoint)
    if required_scope and required_scope not in claims.get('scope', '').split():
        quart.abort(403)
    
    return claims
```

#### [HIGH] ASVS-1032-HIGH-002: Missing Fine-Grained Authorization (Placeholder Comments)

**File:** `v3/server/pages.py`, lines 89, 379, 417, 438, 458, 479, 501, 524

**Description:** Multiple endpoints contain `### check authz` placeholder comments where authorization checks should exist. This means no fine-grained authorization decision (ownership verification, scope-based access) is enforced after authentication.

**Vulnerable Code:**
```python
# v3/server/pages.py, line 431-445
@APP.get('/do-open/<eid>')
@asfquart.auth.require({R.committer})  ### need general solution
@load_election
async def do_open_endpoint(election):
    result = await basic_info()
    ### check authz  <-- NOT IMPLEMENTED
    pdb = steve.persondb.PersonDB.open(DB_FNAME)
    election.open(pdb)  # Any committer can open any election!
```

**Gap Type:** Type B — Control location is identified (`### check authz`) but NOT implemented. This creates false confidence that authorization was considered.

**Proof of Concept:** Any authenticated committer can:
1. Open any election: `GET /do-open/<any_eid>`
2. Close any election: `GET /do-close/<any_eid>`
3. Modify any election's issues: `POST /do-add-issue/<any_eid>`
4. Set dates on any election: `POST /do-set-open_at/<any_eid>`

**Impact:** Horizontal privilege escalation — any committer can manage elections owned by other users. In an OAuth context, this would mean the resource server is not enforcing the `sub` claim or any delegation boundaries.

**Remediation:**
```python
@APP.get('/do-open/<eid>')
@asfquart.auth.require({R.committer})
@load_election
async def do_open_endpoint(election):
    result = await basic_info()
    md = election.get_metadata()
    if md.owner_pid != result.uid:
        # Check if user has authz group access
        if not has_authz_access(result.uid, md.authz):
            quart.abort(403, 'Not authorized to manage this election')
    # proceed...
```

### Security Controls Inventory for 10.3.2

| Control | Location | Status |
|---------|----------|--------|
| Session-based authentication | `basic_info()` line 68 | APPLIED but not OAuth-based |
| Role-based decorator | `@asfquart.auth.require({R.committer})` | APPLIED at entry points |
| Authorization check (ownership) | Placeholder `### check authz` | **NOT IMPLEMENTED** (Type B gap) |
| Voter eligibility check | `vote_on_page()` line 236 | APPLIED for voting page view |
| Document access check | `serve_doc()` line 567 | APPLIED via `q_get_mayvote` |

### Positive Patterns for 10.3.2

- The voter eligibility check in `vote_on_page()` (line 236) properly queries `q_find_issues` to verify the user has mayvote entries before displaying the voting page.
- The `serve_doc()` handler verifies the user has a mayvote relationship before serving documents.
- The `add_vote()` method (election.py line 231) validates open state and retrieves the mayvote record for the specific `pid`/`iid` pair, ensuring only eligible voters can submit votes.

---



## Cross-cutting Architecture Observations

1. **No OAuth Implementation Present:** The entire codebase uses traditional session-cookie-based authentication via `asfquart.session.read()` and role-based decorators. There is no OAuth 2.0 resource server or authorization server functionality. The ASVS 10.3.x and 10.4.x requirements are architecturally not addressed because the application doesn't implement the OAuth protocol at all.

2. **Authorization Model Gap (Type B — Critical Pattern):** The most significant security concern across requirements is the repeated `### check authz` placeholder pattern. The code has **identified** where authorization checks should exist but has **not implemented** them. This creates a false sense of security. Any authenticated committer can manage any election (open, close, add issues, set dates), which represents a horizontal privilege escalation vulnerability.

3. **Single Authentication Context:** The user identity (`result.uid`) flows from a single session source through all operations. There is no federation, no multi-issuer support, and no delegation model. This simplifies the security model but means the application cannot enforce OAuth-style delegated authorization if it needs to be accessed by third-party clients.

4. **Election-Level Access Control Done Right in Some Places:** The `vote_on_page()` handler and `serve_doc()` handler properly check voter eligibility via `q_find_issues` and `q_get_mayvote` respectively. However, administrative operations (open, close, add/edit/delete issues) lack equivalent ownership checks.

5. **Cryptographic Voter Anonymity vs. Authentication:** The voting system employs strong cryptographic measures (vote tokens, encryption, salts) to ensure ballot secrecy. However, the authentication layer protecting these operations is comparatively weaker (session cookies without binding, no MFA requirement, no freshness checks).

---

## Cross-cutting Recommendations

### Immediate (Critical)

1. **Implement Authorization Ownership Checks:** Replace all `### check authz` placeholders with actual ownership verification:
   - Verify `election.get_metadata().owner_pid == result.uid` OR user is in the election's `authz` group
   - Apply to: `/do-open/`, `/do-close/`, `/do-add-issue/`, `/do-edit-issue/`, `/do-delete-issue/`, `/do-set-open_at/`, `/do-set-close_at/`
   - **Priority:** This is exploitable today by any authenticated committer

2. **Implement CSRF Protection:** Replace `basic.csrf_token = 'placeholder'` with actual CSRF token generation and validation. All POST endpoints and state-changing GET endpoints (`/do-open/`, `/do-close/`) are vulnerable.

### Short-term (High)

3. **Add Authentication Freshness for Sensitive Operations:** For vote casting and election state changes, require that the authentication occurred within a recent time window (e.g., 10–30 minutes).

4. **Convert State-Changing GETs to POSTs:** `/do-open/<eid>` and `/do-close/<eid>` use GET method for state-changing operations, making them susceptible to CSRF via image tags or link prefetching. Convert to POST with CSRF tokens.

5. **If OAuth API Access Is Planned:** Design and implement an OAuth resource server layer with:
   - JWT validation (signature, expiration, audience, issuer)
   - Scope-based access control for API operations
   - `sub` + `iss` based user identification

### Long-term (Strategic)

6. **Evaluate OAuth/OIDC Integration:** If this voting system needs to support API clients or federated access, implement:
   - An OAuth resource server with token introspection or JWT validation
   - Sender-constrained tokens (DPoP) for Level 3 compliance
   - Step-up authentication for high-sensitivity operations (vote casting)

7. **Implement Audit Logging with Authentication Context:** Log authentication method and strength alongside actions to support forensic analysis and compliance verification.
        