# Security Audit Report: ASVS 13.1.1

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 13.1.1 |
| **Files Analyzed** | 14 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that all communication needs for the application are documented. This must include external services which the application relies upon and cases where an end user might be able to provide an external location to which the application will then connect.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---

## ASVS-13.1.1: Communication Needs Documentation

### Findings for 13.1.1

#### [MEDIUM]
**Finding ID:** ASVS-1311-MED-001

**Title:** Incomplete documentation of application communication needs

**Location:** `v3/ARCHITECTURE.md`, `v3/docs/schema.md`, `v3/server/config.yaml.example`

**Description:** The application communicates with multiple external services, but there is no comprehensive communication inventory document. The following communication channels were identified through code analysis but are not formally documented in a single reference:

1. **ASF OAuth Service** (external authentication):
```python
# v3/server/main.py, lines 36-40
asfquart.generics.OAUTH_URL_INIT = (
    'https://oauth.apache.org/auth?state=%s&redirect_uri=%s'
)
asfquart.generics.OAUTH_URL_CALLBACK = 'https://oauth.apache.org/token?code=%s'
```

2. **SMTP/Email Service** (voter notification):
```python
# v3/server/bin/mail-voters.py, lines 67-73
asfpy.messaging.mail(
    sender='Apache Election Platform <voter@apache.org>',
    recipient=voter.email,
    subject=f'Vote in Election: {metadata.title}',
    message=rendered_body,
)
```

3. **SQLite Database** (local persistence):
```python
# v3/steve/election.py, line 37
return asfpy.db.DB(db_fname, yaml_fname=QUERIES, yaml_section='election')
```

4. **LDAP Service** (authorization, referenced but not implemented):
```
# v3/docs/schema.md
authz (TEXT): Specifies an LDAP group (e.g., group name or identifier)
```

5. **End-user-provided document filenames** (potential SSRF vector):
```python
# v3/server/pages.py, lines 560-574
@APP.get('/docs/<iid>/<docname>')
async def serve_doc(iid, docname):
    # ...
    return await quart.send_from_directory(DOCSDIR / iid, docname)
```

**Impact:** Without comprehensive communication documentation, security teams cannot perform complete threat modeling, firewall rule validation, or network segmentation reviews. Undocumented external dependencies may introduce unmonitored attack surfaces.

**Remediation:** Create a dedicated `COMMUNICATIONS.md` or equivalent document that inventories:
- All external service endpoints (OAuth, SMTP, LDAP)
- Protocol, port, and authentication method for each
- Direction of communication (inbound/outbound)
- User-controllable endpoints or file references
- Network security requirements (TLS versions, cipher suites)

### Security Controls Inventory for 13.1.1

| Control | Location | Coverage |
|---------|----------|----------|
| Architecture overview | `v3/ARCHITECTURE.md` | Partial — mentions OAuth, SQLite, but lacks formal communication inventory |
| Schema documentation | `v3/docs/schema.md` | Partial — mentions LDAP authorization format is "TBD" |
| Config example | `v3/server/config.yaml.example` | Partial — documents TLS cert paths and port only |
| OAuth URLs | `v3/server/main.py:36-40` | Hardcoded in code, not in dedicated documentation |
| Email service | `v3/server/bin/mail-voters.py` | Not documented in architecture docs |

### Positive Patterns for 13.1.1

- `ARCHITECTURE.md` provides a reasonable high-level overview of the system components and their interactions
- `schema.md` documents database relationships and cryptographic field purposes
- `config.yaml.example` separates deployment-specific values (TLS certs, port, DB path) from code
- The code clearly separates concerns (web layer, business logic, data layer) making communication paths traceable

---



## Cross-cutting Architecture Observations

1. **Documentation vs. Implementation Gap**: The application has good architectural documentation (`ARCHITECTURE.md`, `schema.md`) covering design intent, but lacks operational documentation required by ASVS Level 2-3 (communication inventories, resource management strategies, secrets rotation schedules).

2. **Single Database Pattern**: The use of SQLite as an embedded database simplifies many backend communication concerns (no network authentication needed, no connection pool management) but makes it impossible to apply traditional service-account or role-based database access controls.

3. **Trust Boundary Ambiguity**: The `TODO.md` file acknowledges multiple unimplemented security controls (`### check authz` placeholders, CSRF placeholder). The system currently relies primarily on the ASF OAuth committer-level authentication as its sole access control boundary, with no fine-grained authorization distinguishing election owners from other committers.

4. **Operational Tooling Without Controls**: CLI scripts (`create-election.py`, `mail-voters.py`) operate with the same privileges as the web server but without any authentication or authorization layer, creating a potential privilege escalation path for anyone with shell access.

5. **Consistent Cryptographic Approach**: The cryptographic design (Argon2 key derivation, separate salts per voter-issue pair, encrypted votes, anti-tamper keys) is well-documented and consistently applied, representing a strength in the security architecture.

---

## Cross-cutting Recommendations

### Immediate (Address within current sprint)
1. **Create a Communications Inventory document** listing all external services (OAuth, SMTP, LDAP), protocols, authentication methods, and network requirements — satisfies ASVS 13.1.1 at minimal implementation cost
2. **Document existing secrets** and their criticality in a secrets inventory, even without rotation automation — satisfies ASVS 13.1.4 documentation requirement

### Short-term (1-3 months)
3. **Add resource management documentation** defining timeouts, connection limits, and failure handling for SQLite, OAuth, and SMTP — satisfies ASVS 13.1.2 and 13.1.3
4. **Implement read-only database connections** for components that only need read access (mail script, voter-facing queries) — improves ASVS 13.2.2 compliance
5. **Implement TLS certificate rotation procedures** and document the rotation schedule

### Long-term (3-6 months)
6. **Evaluate migration from SQLite** to a database that supports role-based access control if the application scales beyond single-server deployment — would fully satisfy ASVS 13.2.1 and 13.2.2
7. **Implement connection lifecycle management** with explicit open/close patterns (context managers) for all database access paths to prevent resource leaks
8. **Add circuit breaker patterns** for OAuth and SMTP communication with documented fallback behavior
9. **Implement proper CSRF tokens and session-based secrets** with defined rotation (currently placeholder) — prerequisite for ASVS 13.1.4 compliance on session secrets
        