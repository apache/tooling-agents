# Security Audit Report: ASVS 14.1.2

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 14.1.2 |
| **Files Analyzed** | 8 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that all sensitive data protection levels have a documented set of protection requirements. This must include (but not be limited to) requirements related to general encryption, integrity verification, retention, how the data is to be logged, access controls around sensitive data in logs…

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---

## ASVS-14.1.2: Data Protection Documentation - Protection Requirements

### Findings for 14.1.2

#### [MEDIUM]
**Finding ID**: ASVS-1412-MED-001

**File**: Overall application architecture (no formal document exists)

**Issue**: There is no documented set of protection requirements for each data sensitivity level. While the `schema.md` describes WHAT security mechanisms exist, it does not specify the REQUIRED protection controls for each data category in a normative manner.

**Missing documentation for**:

| Protection Area | Current Status | Gap |
|----------------|---------------|-----|
| Encryption requirements | Fernet used for votes, documented informally | No formal requirement spec (algorithm selection rationale, key management lifecycle) |
| Integrity verification | Argon2-based `opened_key` for tamper detection | No documented integrity requirements for other data |
| Data retention | `vote` table retains all re-votes "for auditing" | No documented retention policy (how long? purge procedures?) |
| Logging requirements | Actions logged with UIDs | No specification of what MUST vs MUST NOT be logged |
| Access controls | Ad-hoc per-endpoint auth decorators | No documented access control matrix |
| Database encryption | No at-rest encryption visible | Not documented as required or not-required |
| Privacy requirements | Email addresses collected | No privacy policy or PII handling requirements documented |
| Key management | Keys derived per-vote, election salt generated once | No key rotation or lifecycle documentation |

**Evidence of undocumented decisions** (from `election.py`):
```python
# Re-voting: Older votes are retained for auditing.
# (schema.md mentions this but no retention policy)
```

**From `crypto.py`**:
```python
def _hash(data: bytes, salt: bytes) -> bytes:
    "Use Argon2 to hash the data, with default tuning parameters."
    return argon2.low_level.hash_secret_raw(
        secret=data,
        salt=salt,
        time_cost=2,  # Passlib default
        memory_cost=65536,  # Passlib default
        parallelism=4,  # Passlib default
        hash_len=32,  # Standard Argon2 digest length
        type=argon2.low_level.Type.D,
    )
```
Parameters use "Passlib defaults" without a documented justification or requirement specification.

**Impact**: Without documented requirements, security decisions are made ad-hoc and inconsistently. New developers may not understand the rationale behind controls, potentially weakening them during refactoring.

**Remediation**: Create a data protection requirements document specifying:
- Required encryption algorithms and minimum key sizes per data level
- Integrity verification mechanisms
- Data retention and purge schedules
- Logging requirements (what to log, what never to log)
- Access control matrices
- Key management lifecycle (generation, storage, rotation, destruction)
- Privacy controls for PII

### Security Controls Inventory for 14.1.2

| Control | Location | Coverage Status |
|---------|----------|-----------------|
| Encryption specification | `crypto.py` (implementation exists) | **No formal requirement document** |
| Integrity verification spec | `election.py:320-332` (anti-tamper exists) | **No formal requirement document** |
| Retention policy | N/A | **NOT DOCUMENTED** |
| Logging policy | N/A | **NOT DOCUMENTED** |
| Access control policy | N/A | **NOT DOCUMENTED** (placeholders only) |
| Key management policy | N/A | **NOT DOCUMENTED** |

### Positive Patterns for 14.1.2

1. **Cryptographic implementation is centralized**: All crypto operations are in `crypto.py`, making them auditable and consistent.
2. **Anti-tamper mechanism**: The `opened_key` approach provides integrity verification for election data.
3. **Vote confidentiality through encryption**: Votes are encrypted with per-voter keys derived from strong primitives (Argon2 + HKDF).
4. **Shuffle for privacy**: `crypto.shuffle()` uses cryptographically secure randomness to prevent database-order leakage during tally.
5. **Future-aware design**: Comments indicate planned transition from Fernet to XChaCha20-Poly1305, showing security improvement awareness.

---



## Cross-cutting Architecture Observations

1. **Type B Gap Pattern (Control exists but not called)**: The most pervasive security issue across this codebase is the `### check authz` pattern. Authorization logic is acknowledged as needed (via comments) throughout `pages.py` but is never implemented. This creates false confidence — the code appears to have considered authorization, but any authenticated committer can manage any election, open/close any election, or manipulate any issue regardless of ownership.

2. **Documentation vs. Implementation Asymmetry**: The `schema.md` documentation describes a well-thought-out security architecture (non-predictable IDs, encryption, anti-tamper), but the running application has significant gaps (CSRF placeholder, missing authz, unvalidated file serving).

3. **Positive Crypto Architecture**: The cryptographic design (Argon2 for key derivation, per-voter salts, Fernet encryption, vote shuffling) is well-implemented in `crypto.py` and `election.py`. The separation of concerns (crypto operations isolated in one module) supports auditability.

4. **Data Leakage Prevention in Queries**: The `queries.yaml` demonstrates conscious effort to prevent sensitive data leakage — public-facing queries explicitly exclude `salt` and `opened_key` columns, with comments documenting this decision. The `_all_metadata()` method is correctly marked as internal-only.

5. **Incomplete Defense-in-Depth**: Authentication is applied (decorators on all sensitive endpoints), but authorization (who can do what to which resource) and input validation (file extensions, path components) represent the missing layers.

---

## Cross-cutting Recommendations

### Immediate (Critical/High Priority)

1. **Convert state-changing GET endpoints to POST**: Change `/do-open/<eid>` and `/do-close/<eid>` from GET to POST, requiring form submission with CSRF tokens. This addresses both ASVS 14.2.1 (operations in URLs) and CSRF vulnerability.

2. **Implement file extension whitelist**: Add allowed extension sets for both `/static/` and `/docs/` endpoints. This directly addresses ASVS 13.4.7 and prevents accidental file disclosure.

3. **Validate `docname` parameter**: Implement the already-planned validation noted by the `### verify the propriety of DOCNAME` comment. Block path traversal characters and restrict to allowed extensions.

### Short-term (Within Sprint)

4. **Suppress server version headers**: Add an `@APP.after_request` handler to remove `Server` and `X-Powered-By` headers, or configure Hypercorn to disable server header inclusion (ASVS 13.4.6).

5. **Implement authorization checks**: Replace all `### check authz` comments with actual ownership/permission verification. At minimum, verify `election.owner_pid == result.uid` for management operations.

6. **Create data classification document**: Formally document all sensitive data types, their protection levels, and required controls (ASVS 14.1.1, 14.1.2).

### Long-term (Architecture)

7. **Implement CSRF protection**: Replace `basic.csrf_token = 'placeholder'` with cryptographically-generated per-session tokens validated on all state-changing endpoints.

8. **Create comprehensive data protection policy**: Document encryption requirements, retention periods, logging policies, key management lifecycle, and privacy controls for each data sensitivity level.

9. **Add security headers middleware**: Implement `Content-Security-Policy`, `X-Content-Type-Options: nosniff`, `X-Frame-Options`, and `Strict-Transport-Security` headers to complement the data protection controls.
        