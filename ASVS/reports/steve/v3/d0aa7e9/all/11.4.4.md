# Security Audit Report: ASVS 11.4.4

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 11.4.4 |
| **Files Analyzed** | 6 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that the application uses approved key derivation functions with key stretching parameters when deriving secret keys from passwords. The parameters in use must balance security and performance to prevent brute-force attacks from compromising the resulting cryptographic key.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 3 |
| 🟢 Low | 1 |

---

## ASVS-11.4.4: Approved KDF with Key Stretching for Key Derivation from Passwords

### Findings for 11.4.4

### [MEDIUM]

**Finding ID**: ASVS-1144-MED-001  
**File**: `v3/steve/crypto.py`, function `_hash`, lines 85-93  

```python
type=argon2.low_level.Type.D,
```

**Data flow**: `opened_key + pid + iid` → Argon2d → `vote_token` → HKDF → encryption key  
**Issue**: Same as ASVS-1142-MED-001. The Argon2d variant is used for key derivation where Argon2id is recommended. Since `vote_token` derivation feeds directly into encryption key generation (via HKDF), the key stretching step uses a suboptimal algorithm variant.

**Impact**: Side-channel vulnerability in the key stretching phase. If an attacker can observe memory access patterns during `gen_vote_token()` execution, they could potentially recover the vote_token and thus the encryption key.

**Remediation**: Switch to `argon2.low_level.Type.ID` (same fix as ASVS-1142-MED-001).

### [LOW]

**Finding ID**: ASVS-1144-LOW-001  
**File**: `v3/steve/crypto.py`, function `_b64_vote_key`, lines 60-67  

```python
keymaker = hkdf.HKDF(
    algorithm=hashes.SHA256(),
    length=32,
    salt=salt,
    info=b'xchacha20_key',
)
vote_key = keymaker.derive(vote_token)
```

**Issue**: HKDF is not a key-stretching function — it provides no computational cost against brute-force. However, since the input (`vote_token`) is already the 32-byte output of Argon2 (which provides the key stretching), HKDF is being used appropriately as a key derivation function to transform already-stretched material into an encryption key. This is acceptable architecture but worth documenting that the stretching occurs upstream.

**Impact**: Negligible — the overall key derivation chain provides adequate brute-force resistance through the Argon2 stage.

**Remediation**: No immediate action required. Add a comment clarifying the security model:

```python
def _b64_vote_key(vote_token: bytes, salt: bytes) -> bytes:
    """Derive encryption key from vote_token.
    
    Note: Key stretching is provided by Argon2 in gen_vote_token().
    HKDF here only transforms the already-stretched token into a
    key suitable for the encryption algorithm.
    """
```

### Security Controls Inventory for 11.4.4

| Control | Location | Coverage Status |
|---------|----------|-----------------|
| Argon2 key stretching | `crypto.py:_hash()` line 85 | ✅ Applied (wrong variant) |
| HKDF key derivation | `crypto.py:_b64_vote_key()` line 60 | ✅ Appropriate for post-stretch derivation |
| Memory-hard cost (64 MB) | `crypto.py:_hash()` `memory_cost=65536` | ✅ Provides GPU resistance |
| Time cost | `crypto.py:_hash()` `time_cost=2` | ⚠️ Minimum; benchmark utility exists for tuning |

### Positive Patterns for 11.4.4

1. **Two-stage key derivation**: Argon2 provides the computationally expensive stretching, followed by HKDF for algorithm-specific key formatting. This is a sound cryptographic architecture.
2. **Unique salts per derivation**: Each person-issue pair has a unique salt, preventing rainbow table attacks across vote tokens.
3. **32-byte key output**: The Argon2 output length (256 bits) provides adequate entropy for downstream key derivation.

---



## Cross-cutting Architecture Observations

1. **Cryptographic Architecture is Well-Designed**: The multi-layered approach (election data → BLAKE2b → Argon2 → opened_key; then opened_key + identifiers → Argon2 → vote_token → HKDF → encryption key) provides defense in depth with appropriate separation of concerns.

2. **Argon2 Variant Inconsistency**: The benchmark function uses `Type.ID` (Argon2id) while production uses `Type.D` (Argon2d). This appears to be an oversight rather than a deliberate design choice, as there's no documented rationale for choosing Argon2d.

3. **Fernet as Transitional Algorithm**: The code explicitly marks Fernet as temporary (comments mention switching to XChaCha20-Poly1305). The HKDF `info` parameter is already set for XChaCha20, which could cause key equivalence issues during migration if not properly managed.

4. **No Password Storage**: The system delegates authentication to external infrastructure (ASF/LDAP), meaning traditional password hashing requirements (11.4.2) have limited direct applicability. The Argon2 usage is for key derivation and anti-tamper rather than password verification.

5. **Anti-Tamper Integrity**: The `opened_key` mechanism provides cryptographic assurance that election configuration hasn't been modified after opening — a strong integrity control binding election metadata, issues, and voter lists.

## Cross-cutting Recommendations

### Immediate (Critical/High Priority)
1. **Switch Argon2 variant to Argon2id** (`Type.ID`): Single-line change in `_hash()` function, aligning production with benchmark code and OWASP recommendations. This affects all derived keys.

### Short-term (Medium Priority)
2. **Plan migration path for Fernet → XChaCha20-Poly1305**: Ensure the HKDF `info` parameter changes during migration to prevent key reuse across algorithms. Document the migration strategy for existing encrypted votes.
3. **Review Argon2 parameters against current hardware**: Run `benchmark_argon2()` on production hardware to determine if `time_cost` can be increased (current value of 2 is the minimum recommended).

### Long-term (Low Priority)
4. **Consider BLAKE2b → SHA-512 substitution**: If strict NIST compliance is required, replace BLAKE2b with SHA-512 for the data reduction step. Functionally equivalent with no performance concern for the single-call use case.
5. **Add cryptographic agility documentation**: Document the algorithm choices, migration strategy, and key derivation chain in a security design document to support future audits.
        