# Security Audit Report: ASVS 3.5.2

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 3.5.2 |
| **Files Analyzed** | 8 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that, if the application relies on the CORS preflight mechanism to prevent disallowed cross-origin use of sensitive functionality, it is not possible to call the functionality with a request which does not trigger a CORS-preflight request. This may require checking the values of the 'Origin'…

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 1 |

---

## ASVS-3.5.2: Browser Origin Separation - CORS Preflight Mechanism

### Findings for 3.5.2

### [MEDIUM]

**Finding ID:** ASVS-352-MED-001

**Title:** Form-Based POST Endpoints Accept CORS-Safelisted Content Types Without Origin Validation

**File:** `v3/server/pages.py`, multiple POST endpoints

**Vulnerable Code:**
```python
@APP.post('/do-vote/<eid>')
@asfquart.auth.require({R.committer})
@load_election
async def do_vote_endpoint(election):
    result = await basic_info()
    form = edict(await quart.request.form)
    # No Content-Type validation
    # No Origin header validation
```

Similarly for `/do-create-election`, `/do-add-issue/<eid>`, `/do-edit-issue/<eid>/<iid>`, `/do-delete-issue/<eid>/<iid>`.

**Data Flow:** Cross-origin form POST with `Content-Type: application/x-www-form-urlencoded` (CORS-safelisted) → No preflight triggered → Server processes request without origin checks

**Explanation:** Requests with `Content-Type: application/x-www-form-urlencoded` are CORS-safelisted and do NOT trigger a preflight `OPTIONS` request. Since there is no CSRF token validation, no Origin header validation, and no requirement for non-safelisted headers, these endpoints cannot rely on the CORS preflight mechanism for protection.

**Proof of Concept:**
A cross-origin form submission uses `application/x-www-form-urlencoded` by default:
```html
<form action="https://target.example.com/do-vote/ELECTION_ID" method="POST">
  <input name="vote-issue123" value="y">
</form>
<script>document.forms[0].submit();</script>
```
This request is simple (no preflight), carries session cookies, and will be processed.

**Impact:** Since the application does not validate Origin headers or enforce non-safelisted Content-Type on form endpoints, cross-origin requests can reach sensitive functionality without triggering a CORS preflight.

**Remediation:**
```python
def validate_origin(func):
    """Validate the Origin header matches expected values."""
    @functools.wraps(func)
    async def wrapper(*args, **kwargs):
        origin = quart.request.headers.get('Origin')
        # If Origin is present (cross-origin request), validate it
        if origin and origin not in APP.cfg.allowed_origins:
            quart.abort(403, 'Invalid origin')
        return await func(*args, **kwargs)
    return wrapper
```

---

### [LOW]

**Finding ID:** ASVS-352-LOW-001

**Title:** JSON Endpoints Use Content-Type That Triggers Preflight But Lack Explicit CORS Policy

**File:** `v3/server/pages.py`, lines 360-366

**Vulnerable Code:**
```python
@APP.post('/do-set-open_at/<eid>')
@asfquart.auth.require({R.committer})
@load_election
async def do_set_open_at_endpoint(election):
    return await _set_election_date(election, 'open_at')
```

**Analysis:** The client sends `Content-Type: application/json` (from `manage.ezt` line 251), which IS non-safelisted and WOULD trigger a CORS preflight. However, without explicit CORS configuration visible in the code, it's unclear whether the server properly rejects unauthorized origins during the OPTIONS preflight. If a permissive CORS policy were added in the future (e.g., `Access-Control-Allow-Origin: *`), these endpoints would become vulnerable.

**Impact:** Low — currently protected by the non-safelisted Content-Type requirement, but protection is implicit rather than explicit.

**Remediation:** Add explicit CORS policy configuration that restricts allowed origins, and validate Content-Type server-side:
```python
async def _set_election_date(election, field):
    # Explicitly verify Content-Type
    if quart.request.content_type != 'application/json':
        quart.abort(415, 'Content-Type must be application/json')
    # ... rest of handler
```

---

### Security Controls Inventory for 3.5.2

| Control | Location | Status |
|---------|----------|--------|
| CORS middleware/configuration | Not found in provided code | **UNKNOWN** — may be configured at framework level |
| Origin header validation | Not found | **MISSING** |
| Content-Type enforcement on JSON endpoints | `_set_election_date` uses `get_json()` | Implicit (framework may reject non-JSON) |
| Content-Type enforcement on form endpoints | Not found | **MISSING** |
| Non-safelisted header requirement | `X-CSRFToken` sent in fetch calls but not validated | **NOT ENFORCED** |

### Positive Patterns for 3.5.2

- JSON-based endpoints (`/do-set-open_at`, `/do-set-close_at`) use `application/json` Content-Type from the client, which triggers CORS preflight for cross-origin requests
- The `X-CSRFToken` header is sent by the client for fetch-based requests, which is a non-safelisted header that would trigger preflight (though it's not validated server-side)

---



## Cross-cutting Architecture Observations

1. **CSRF Protection is Entirely Absent Despite Apparent Intent:** The codebase shows clear architectural intent to implement CSRF protection (token fields in templates, header passing in fetch calls, TODO items acknowledging it), but the actual enforcement is completely missing. This creates a dangerous false sense of security — developers and reviewers may believe protection exists because they see the tokens in templates.

2. **GET-Based State Changes Compound the CSRF Problem:** The `/do-open/<eid>` and `/do-close/<eid>` endpoints using GET method means that even implementing CSRF tokens for POST requests would leave these critical operations unprotected. These must be converted to POST before any CSRF solution is effective.

3. **Authorization Model is Incomplete:** Multiple `### check authz` placeholders indicate that ownership verification (e.g., "does this user own this election?") is not enforced. Any authenticated committer can manage any election. While this is beyond the scope of ASVS 3.5.x, it amplifies the impact of CSRF — an attacker doesn't need to target the election owner specifically.

4. **Template Security is Well-Implemented:** The use of `[format "js,html"]` in EZT templates for user-supplied data injected into JavaScript contexts shows awareness of XSS prevention, which is relevant because XSS can bypass CSRF protections.

5. **Client-Server Contract Mismatch:** The client (templates) sends CSRF tokens and custom headers that the server completely ignores. This indicates the frontend was developed with security in mind but the backend implementation was deferred.

## Cross-cutting Recommendations

### Immediate (Critical — deploy blockers)

1. **Implement CSRF token validation on all POST endpoints.** Generate a cryptographically random token per session, store it server-side, and validate it on every state-changing request. Use a decorator pattern for consistency.

2. **Convert `/do-open/<eid>` and `/do-close/<eid>` from GET to POST.** These perform irreversible operations and must not be triggerable via simple navigation, image loads, or link prefetch.

3. **Add Origin header validation** as defense-in-depth for all POST endpoints, rejecting requests with an Origin header that doesn't match the application's own origin.

### Short-term (High priority — within sprint)

4. **Enforce Content-Type validation** on form endpoints — reject requests that don't match `application/x-www-form-urlencoded` or `multipart/form-data` as appropriate. For JSON endpoints, reject non-`application/json` requests.

5. **Configure SameSite=Lax (minimum) or SameSite=Strict on session cookies.** This provides browser-level CSRF protection for most scenarios while the token-based approach covers older browsers.

6. **Add Sec-Fetch-Site header checking** as an additional defense layer — reject requests where `Sec-Fetch-Site: cross-site` on sensitive endpoints.

### Long-term (Architecture improvements)

7. **Consider hostname-based separation** for admin and voter functionality to leverage Same-Origin Policy restrictions between functional domains.

8. **Implement a comprehensive security middleware stack** that handles CSRF, CORS, and Content-Type validation in one place rather than requiring per-endpoint implementation.

9. **Add integration tests** that verify CSRF protection by attempting state-changing requests without valid tokens and asserting they fail with 403.
        