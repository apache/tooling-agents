# Security Audit Report: ASVS 7.4.2

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 7.4.2 |
| **Files Analyzed** | 7 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that the application terminates all active sessions when a user account is disabled or deleted (such as an employee leaving the company).

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 1 |
| 🟡 Medium | 0 |
| 🟢 Low | 0 |

---

## ASVS-7.4.2: Session Termination

### Findings for 7.4.2

#### [HIGH]

**Finding ID:** ASVS-742-HIGH-001

**File:** `v3/queries.yaml` — line defining `c_delete_person`, and absence of session termination in application logic

**Vulnerable Code:**
```yaml
person:
    c_delete_person: DELETE FROM person WHERE pid = ?
```

```python
# In election.py - no session termination on person operations
# In pages.py - no endpoint or logic to terminate sessions on account disable/delete

# The schema supports person deletion:
# schema.sql:
# FOREIGN KEY (owner_pid) REFERENCES person(pid) ON DELETE RESTRICT
# FOREIGN KEY (pid) REFERENCES person(pid) ON DELETE RESTRICT
```

**Data Flow:** Admin/system deletes person (via `c_delete_person` query) → Person's row removed from `person` table → No corresponding session invalidation occurs → Person's active sessions remain valid → Deleted person retains access.

**Analysis:** The system has:
1. A `c_delete_person` SQL query defined in `queries.yaml`
2. No application-level code that terminates sessions when a person is deleted
3. No session store indexed by person ID (PID) for bulk invalidation
4. No mechanism to link active sessions to person records
5. No "disabled" flag in the `person` table schema for soft-disabling accounts

The `person` table has `RESTRICT` foreign keys from `mayvote` and `election.owner_pid`, suggesting person deletion is constrained but possible. When it occurs, no session management action is taken.

**Impact:** If a person's account is deleted (e.g., they leave the organization), their active sessions continue to work. They could continue to vote, manage elections, or access sensitive election data until their session naturally expires (which, per 7.3.1 and 7.3.2 findings, may be never).

**Remediation:**
```python
async def delete_person(pid):
    """Delete a person and terminate all their active sessions."""
    # First, terminate all active sessions for this PID
    await terminate_all_sessions_for_user(pid)
    
    # Then delete the person record
    pdb = steve.persondb.PersonDB.open(DB_FNAME)
    pdb.delete_person(pid)
    
    _LOGGER.info(f'Person[P:{pid}] deleted and all sessions terminated')

async def terminate_all_sessions_for_user(pid):
    """Invalidate all active sessions belonging to PID."""
    # Implementation depends on session store:
    # For server-side sessions: DELETE FROM sessions WHERE user_id = ?
    # For JWTs: add PID to revocation list or update per-user invalidation timestamp
    session_store.invalidate_by_user(pid)
```

### Security Controls Inventory for 7.4.2

| Control | Location | Status |
|---------|----------|--------|
| Person deletion query | `queries.yaml` — `c_delete_person` | Present, DB-only |
| Session termination on deletion | Not found anywhere | **MISSING (Type A)** |
| Account disable mechanism | Not in schema or code | **MISSING** |
| Session-to-user mapping | Not visible | **MISSING** |
| Bulk session invalidation | Not found | **MISSING** |

### Positive Patterns for 7.4.2

- Foreign key constraints with `RESTRICT` in the schema prevent accidental deletion of persons who have active election relationships, providing some protection against premature deletion

---



## Cross-cutting Architecture Observations

1. **Session Management Delegation**: The application delegates all session management to the `asfquart` framework. While this is architecturally sound for separation of concerns, it means the application code has ZERO visibility into session lifecycle events. There is no evidence the framework provides the required controls (timeouts, token rotation, termination).

2. **Missing Security Layer**: The application has authentication (via decorators) but lacks a complete session management layer. There are no:
   - Session creation/destruction endpoints
   - Session metadata tracking (creation time, last activity)
   - Session inventory or listing
   - Per-user session management

3. **Election Lifecycle vs Session Lifecycle Mismatch**: Elections have well-defined states (editable → open → closed) with proper state transitions and assertions. Sessions have NO lifecycle management at all — a stark contrast suggesting sessions were not part of the security design.

4. **TODO Acknowledgment**: The `TODO.md` acknowledges CSRF issues but does NOT mention any session management concerns, indicating these requirements were not considered during design.

5. **Person Deletion Without Session Awareness**: The database schema and queries support person deletion (`c_delete_person`), but the application has no mechanism to link person records to active sessions. The `person` table has no "active/disabled" status field.

6. **No State-Change-Triggered Session Actions**: Sensitive state changes (opening/closing elections, casting votes) do not trigger any session-related security actions. There's no step-up authentication for high-privilege operations.

## Cross-cutting Recommendations

### Immediate (Critical)

1. **Implement a logout endpoint** — Users currently have no way to terminate their sessions. This is the most basic session management requirement.
   
2. **Add inactivity timeout** — Configure the `asfquart` session framework or implement middleware to enforce a 30-minute inactivity timeout for this election management context.

3. **Add absolute session lifetime** — Implement an 8-hour maximum session lifetime to bound the impact of session compromise.

### Short-term (High)

4. **Implement session-to-user mapping** — Add a session store or index that allows finding all sessions for a given PID. This enables requirements 7.4.2 and 7.4.3.

5. **Add session termination on person deletion** — Before or after calling `c_delete_person`, invalidate all sessions for that PID.

6. **Verify session token regeneration** — Audit the `asfquart.auth` framework to confirm it regenerates session tokens on authentication. If not, add this capability.

### Long-term (Medium)

7. **Add "terminate all other sessions" feature** — Implement in the `/settings` page, allowing users to invalidate all sessions except their current one.

8. **Document session security decisions** — Create a security design document that specifies chosen timeout values, session lifetime limits, and the risk analysis supporting these decisions (as required by ASVS Level 2).

9. **Implement session activity dashboard** — Allow users to view their active sessions (device, last activity, IP) and terminate individual ones.

10. **Add step-up authentication** — For critical operations (opening/closing elections, person management), require re-authentication even within a valid session.
        