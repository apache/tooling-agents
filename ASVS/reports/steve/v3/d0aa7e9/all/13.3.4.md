# Security Audit Report: ASVS 13.3.4

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 13.3.4 |
| **Files Analyzed** | 7 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that secrets are configured to expire and be rotated based on the application's documentation.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 1 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---

## ASVS-13.3.4: Secret Expiration and Rotation

### Findings for 13.3.4

### [HIGH]

**Finding ID**: ASVS-1334-HIGH-001

**File**: `v3/steve/election.py` (lines 70-80) and `v3/schema.sql` (lines 48-56)

**Vulnerable Code**:
```python
# election.py - Keys persist indefinitely once generated
def open(self, pdb):
    assert self.is_editable()
    self.add_salts()
    edata = self.gather_election_data(pdb)
    salt = crypto.gen_salt()
    opened_key = crypto.gen_opened_key(edata, salt)
    self.c_open.perform(salt, opened_key, self.eid)
```

```sql
-- schema.sql - No expiry columns for secrets
salt  BLOB  CHECK (salt IS NULL OR length(salt) = 16),
opened_key  BLOB  CHECK (opened_key IS NULL OR length(opened_key) = 32),
```

**Data Flow**: `crypto.gen_salt()` → `salt` stored in `election` table → never expires or rotates. `crypto.gen_opened_key()` → `opened_key` stored in `election` table → persists through open → closed states indefinitely.

**Impact**: Cryptographic keys and salts have no configured expiration or rotation mechanism. Once an election is closed, its key material remains in the database indefinitely, increasing the window for key compromise. Even the `mayvote.salt` values persist forever. If any key is compromised (e.g., through backup theft), there is no mechanism to detect staleness or force rotation.

**Proof of Concept**: An old election's `opened_key` and `mayvote.salt` values remain valid indefinitely. An attacker obtaining an old database backup can decrypt votes from elections that ended months or years ago, since no keys have been rotated or marked expired.

**Remediation**:
```python
# Add expiry tracking and rotation support
class Election:
    KEY_EXPIRY_DAYS = 90  # Configurable based on policy
    
    def check_key_expiry(self):
        md = self._all_metadata()
        if md.key_created_at:
            age = time.time() - md.key_created_at
            if age > self.KEY_EXPIRY_DAYS * 86400:
                raise KeyExpiredError(self.eid)
    
    def rotate_keys_post_close(self):
        """After tallying is complete, remove key material."""
        assert self.is_closed()
        # Zero out key material after results are finalized
        self.c_clear_keys.perform(self.eid)
```

```sql
-- Add expiry tracking to schema
ALTER TABLE election ADD COLUMN key_created_at INTEGER;
ALTER TABLE election ADD COLUMN key_expires_at INTEGER;
```

### [MEDIUM]

**Finding ID**: ASVS-1334-MED-001

**File**: `v3/schema.sql` (lines 130-139) and `v3/steve/election.py`

**Vulnerable Code**:
```sql
-- mayvote table - salts persist indefinitely
CREATE TABLE mayvote (
    pid  TEXT NOT NULL,
    iid  TEXT NOT NULL,
    salt  BLOB  CHECK (salt IS NULL OR length(salt) = 16),
    PRIMARY KEY (pid, iid),
    ...
) STRICT;
```

**Impact**: Per-voter-per-issue salt values in the `mayvote` table have no expiration or rotation mechanism. These salts are critical for deriving `vote_token` and encryption keys. Once set during `add_salts()`, they remain unchanged for the lifetime of the database.

**Remediation**: Implement a key lifecycle policy that:
1. Documents expected lifetimes for all secret material
2. Removes or re-encrypts key material after election finalization
3. Implements automated cleanup of secrets past their useful life

### Security Controls Inventory for 13.3.4

| Control | Location | Coverage Status |
|---------|----------|-----------------|
| Secret expiration mechanism | N/A | **MISSING** - no expiry on any secrets |
| Secret rotation mechanism | N/A | **MISSING** - no rotation capability |
| Key lifecycle documentation | N/A | **MISSING** - no documented policy |
| Election close timestamp | `schema.sql:close_at` | EXISTS but only advisory, not tied to key expiry |

### Positive Patterns for 13.3.4

- Election lifecycle states (editable → open → closed) provide a logical framework that could support key rotation at state transitions
- The `close_at` field exists which could serve as a basis for computing key expiry
- The anti-tamper mechanism (`is_tampered()`) demonstrates awareness of key integrity concerns

---



## Cross-cutting Architecture Observations

1. **Key Material Exposure Pattern**: The application handles all cryptographic operations in-process with no isolation boundary. The `opened_key` (master election key) flows through multiple code paths: `election.open()` → SQLite storage → `add_vote()` → `tally_issue()` → `has_voted_upon()`. Any single compromise point in this chain exposes all election vote data.

2. **Lifecycle Gap**: The election lifecycle (editable → open → closed) provides a natural framework for key management, but key material persists in the `closed` state indefinitely. There's no "archived" or "destroyed" state where key material would be removed.

3. **Defense-in-Depth Gaps**: Multiple requirements share a common theme of missing explicit hardening. The framework provides reasonable defaults (no TRACE, no directory listing, method restrictions) but there's no explicit defense-in-depth configuration. This creates risk if framework behavior changes or if deployment differs from assumptions.

4. **Development Artifacts in Production Code**: Multiple indicators suggest the code is not fully production-hardened: placeholder CSRF token, active `print()` statements, `### check authz` comments indicating incomplete authorization, and commented-out debug statements for sensitive values.

5. **Single Database as Secret Store**: SQLite serves as both the application database and the secrets store. There's no separation between operational data and key material, making it impossible to apply different access controls or audit requirements to secrets vs. general data.

## Cross-cutting Recommendations

### Immediate (Critical Path)
1. **Remove debug `print()` statements** from `do_add_issue_endpoint` and `do_edit_issue_endpoint` - these actively leak form data to stdout in production
2. **Add explicit source control metadata blocking** to the `/static/` route to prevent serving `.git` contents if accidentally deployed
3. **Document deployment requirements** explicitly stating that TRACE must be disabled at reverse proxy level and directory listing must be disabled

### Short-term (1-3 months)
4. **Implement key lifecycle management** - add `key_created_at` tracking to the schema, implement secret cleanup after election finalization (e.g., zeroing `opened_key` after results are exported/verified)
5. **Add explicit TRACE blocking middleware** and source control path filtering as defense-in-depth measures
6. **Remove all commented-out debug code** that could expose sensitive key material if uncommented
7. **Implement environment-based configuration** to ensure debug-level features are disabled in production builds

### Long-term (3-12 months)
8. **Integrate with a key management service** (HashiCorp Vault, AWS KMS, or similar) to perform cryptographic operations in an isolated security boundary - this is the most significant gap for a Level 3 ASVS target
9. **Implement secret rotation** - design a mechanism to rotate election keys for long-running elections, or implement a maximum election duration policy
10. **Separate secret storage** from operational database - consider storing key material in a dedicated secrets backend with its own access controls and audit trail
        