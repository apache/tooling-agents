# Security Audit Report: ASVS 14.3.2

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 14.3.2 |
| **Files Analyzed** | 8 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that the application sets sufficient anti-caching HTTP response header fields (i.e., Cache-Control: no-store) so that sensitive data is not cached in browsers.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 1 |
| 🟡 Medium | 0 |
| 🟢 Low | 0 |

---

## ASVS-14.3.2: Anti-Caching HTTP Response Headers

### Findings for 14.3.2

### [HIGH]

**Finding ID**: ASVS-1432-HIGH-001

**Title**: No Cache-Control Headers Set on Authenticated/Sensitive Endpoints

**File**: `v3/server/pages.py` (all authenticated endpoints)

**Vulnerable Code** (representative example):
```python
@APP.get('/voter')
@asfquart.auth.require({R.committer})  ### need general solution
@APP.use_template(TEMPLATES / 'voter.ezt')
async def voter_page():
    result = await basic_info()
    result.title = 'Voting'
    # ... fetches open elections, upcoming elections, etc.
    result.open_elections = [postprocess_election(e) for e in election]
    # ... no cache-control headers set
    return result
```

**Data Flow**: Sensitive data (election details, voter eligibility, admin data) → template rendering → HTTP response → browser cache (no `Cache-Control: no-store`) → persists on disk

**Issue Description**: None of the authenticated endpoints in `pages.py` set anti-caching response headers. The following endpoints serve sensitive data without cache-control directives:

| Endpoint | Sensitive Data |
|----------|---------------|
| `/voter` | Voter's elections, participation status |
| `/vote-on/<eid>` | Election issues, voter eligibility |
| `/admin` | Owned elections, admin status |
| `/manage/<eid>` | Election management data |
| `/manage-stv/<eid>/<iid>` | STV issue candidates |
| `/profile` | User profile data |
| `/settings` | User settings |
| `/docs/<iid>/<docname>` | Election documents |

**Proof of Concept**:
1. Authenticated user visits `/admin` in a shared browser
2. Response is cached by the browser (default behavior without `Cache-Control: no-store`)
3. Another user on the same machine opens browser history or uses back/forward navigation
4. Cached page with election administration data is displayed without re-authentication

**Impact**: Sensitive election data, voter participation status, and administrative information may be cached in the browser's disk cache, accessible to subsequent users of the same machine or through forensic analysis.

**Remediation**:
```python
# Add a decorator or after_request handler for all authenticated routes
@APP.after_request
async def set_security_headers(response):
    # For authenticated endpoints, prevent caching
    if quart.request.endpoint and hasattr(quart.request, 'authenticated'):
        response.headers['Cache-Control'] = 'no-store, no-cache, must-revalidate, max-age=0'
        response.headers['Pragma'] = 'no-cache'
        response.headers['Expires'] = '0'
    return response

# Or as middleware applied to all routes:
@APP.after_request
async def add_security_headers(response):
    # Static files can be cached; everything else should not be
    if not quart.request.path.startswith('/static/'):
        response.headers['Cache-Control'] = 'no-store'
        response.headers['Pragma'] = 'no-cache'
    return response
```

---

### Security Controls Inventory for 14.3.2

| Control | Location | Status |
|---------|----------|--------|
| `Cache-Control: no-store` header | Not found in any file | **MISSING** |
| `Pragma: no-cache` header | Not found | **MISSING** |
| `after_request` hook for security headers | Not found | **MISSING** |
| Per-endpoint cache control | Not found | **MISSING** |

### Positive Patterns for 14.3.2

- Static files are served through a separate `/static/` route, which would allow differential cache policies (static files can be cached, dynamic pages should not).
- The application uses a template system that generates dynamic content for authenticated pages, making it straightforward to apply blanket no-cache policies.

---



## Cross-cutting Architecture Observations

1. **Incomplete Security Controls Pattern**: The application exhibits a recurring pattern of security controls being _defined_ or _planned_ but not _implemented_:
   - CSRF token: defined as `'placeholder'` (line 83 of pages.py)
   - Authorization checks: marked with `### check authz` comments throughout
   - Path validation in `serve_doc`: marked with `### verify the propriety of DOCNAME`
   - These represent Type B gaps (control exists in concept but is not applied)

2. **Server-Side Security vs. Client-Side Security Imbalance**: The application demonstrates strong server-side security patterns (parameterized queries, cryptographic vote protection, state assertions, anti-tamper detection) but lacks corresponding client-side protections (no cache control, no Clear-Site-Data, no CSP headers).

3. **Cryptographic Maturity vs. Infrastructure Maturity**: The cryptographic design (Argon2 for key derivation, Fernet encryption, vote token system, tamper detection) is sophisticated, but the surrounding infrastructure (no SBOM, no vulnerability policy, no security headers) lags significantly.

4. **Single-Responsibility Concern**: `pages.py` handles routing, authentication, authorization, template rendering, and business logic. Security controls would benefit from middleware extraction (headers, CSRF, caching) applied uniformly.

5. **Framework Dependency for Security**: The application relies heavily on `asfquart` for authentication and session management. The security posture of these operations is opaque from the provided code — a single vulnerability in the framework could undermine all authentication.

---

## Cross-cutting Recommendations

### Immediate (0-7 days)
1. **Add anti-caching headers** via `@APP.after_request` handler for all non-static responses (`Cache-Control: no-store`)
2. **Implement logout endpoint** that sets `Clear-Site-Data: "cache", "cookies", "storage"` header
3. **Generate and commit `uv.lock`** to ensure reproducible builds and enable transitive dependency auditing

### Short-term (1-4 weeks)
1. **Create vulnerability remediation policy** documenting timeframes for critical/high/medium/low severity issues in dependencies
2. **Configure SBOM generation** in CI pipeline using `cyclonedx-py` or equivalent
3. **Add `uv audit`** to CI pipeline to detect known vulnerabilities in dependencies
4. **Implement Content-Security-Policy** header to restrict client-side script behavior
5. **Pin Apache-specific package sources** to prevent dependency confusion attacks

### Long-term (1-3 months)
1. **Extract security middleware** — Create a unified security headers middleware that applies Cache-Control, CSP, X-Content-Type-Options, and other security headers
2. **Implement proper CSRF protection** — Replace placeholder with cryptographically secure tokens validated server-side
3. **Complete authorization system** — Implement the `### check authz` checks throughout the application
4. **Establish dependency review process** — Monthly review cadence, automated PR generation for updates
5. **Add client-side session cleanup** — JavaScript that detects session expiration and clears sensitive DOM data
        