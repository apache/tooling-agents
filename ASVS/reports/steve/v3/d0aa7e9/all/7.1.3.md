# Security Audit Report: ASVS 7.1.3

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 7.1.3 |
| **Files Analyzed** | 8 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that all systems that create and manage user sessions as part of a federated identity management ecosystem (such as SSO systems) are documented along with controls to coordinate session lifetimes, termination, and any other conditions that require re-authentication.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---

## ASVS-7.1.3: Federated Identity Management Documentation

### Findings for 7.1.3

#### [MEDIUM]

**Finding ID:** ASVS-713-MED-001

**Title:** Federated identity system (ASF SSO) interaction undocumented

**File:** `v3/server/pages.py` (lines 1-36, imports and comments)

**Vulnerable Code:**
```python
### NOTE: the voting handlers require login for ASF committers only.
### Obviously, this is not a general purpose solution. Something for
### the future, to figure out how we'd like to do configuration
### authorization for various install scenarios and authn systems.

import asfquart.session
from asfquart.auth import Requirements as R
```

**Analysis:**
- The application uses `asfquart.auth` and `asfquart.session`, which integrate with Apache Software Foundation's identity management ecosystem (likely involving LDAP and OAuth/SSO).
- The code comments explicitly acknowledge this is ASF-committer-specific authentication: `"the voting handlers require login for ASF committers only"`.
- There is no documentation detailing:
  - How the ASF SSO session coordinates with the application session
  - What happens when the SSO session expires but the local session persists
  - How session termination propagates between systems
  - Conditions that require re-authentication (e.g., sensitive operations like opening/closing elections)
  - Session lifetime coordination between the Identity Provider and this application
- The `schema.md` documentation covers database schema but not authentication/session architecture.
- Multiple comments (`### need general solution`) indicate incomplete authorization design.

**Impact:** Without documented federated session management, there is a risk of:
- Orphaned local sessions after SSO logout
- Inconsistent session lifetimes between the IdP and this application
- Missing re-authentication for high-privilege operations (opening elections, tallying votes)
- No coordinated session termination in incident response scenarios

**Remediation:**
Create federated session management documentation:
```markdown
# Federated Session Management Documentation

## Systems in Ecosystem
1. ASF OAuth/SSO Identity Provider (authentication source)
2. STeVe Application (this system, session consumer)
3. LDAP Directory (authorization source for committer/PMC membership)

## Session Coordination
- Local session MUST NOT exceed IdP session lifetime
- Application checks IdP token validity on sensitive operations
- Logout from IdP triggers backchannel logout notification

## Re-authentication Requirements
- Opening an election: re-verify IdP session is active
- Closing an election: re-verify IdP session is active  
- Tallying votes: re-verify IdP session is active

## Session Termination
- User-initiated logout invalidates both local and IdP sessions
- IdP-initiated revocation must invalidate local session within 5 minutes
- Administrative session revocation propagates immediately
```

### Security Controls Inventory for 7.1.3

| Control | Location | Status |
|---------|----------|--------|
| SSO integration | `asfquart.session`, `asfquart.auth` (imported) | EXISTS (external) |
| Session lifetime coordination | Not found | MISSING |
| Federated logout | Not found | MISSING |
| Re-authentication for sensitive ops | Not found | MISSING |
| Documentation of SSO interaction | Not found | MISSING |

### Positive Patterns for 7.1.3

- The application uses framework-level SSO integration (`asfquart`) rather than custom authentication code.
- Role-based authorization (`R.committer`, `R.pmc_member`) is applied consistently, suggesting the SSO system provides role claims.

---



## Cross-cutting Architecture Observations

1. **Session Management Delegation:** The application delegates all session management to `asfquart.session` and `asfquart.auth`. While this is architecturally sound (avoiding custom session code), it means critical session security properties (timeouts, entropy, concurrent limits) cannot be audited from the application code alone. The framework becomes a trust boundary that must be separately verified.

2. **Authentication vs. Authorization Gap:** Authentication is consistently enforced (`@asfquart.auth.require`), but fine-grained authorization (election ownership checks) is marked with `### check authz` placeholder comments throughout `pages.py`. This means any authenticated committer can currently manage any election. This is not directly a session management issue but creates a significant authorization gap that session controls alone cannot mitigate.

3. **CSRF Token Placeholder:** The `basic.csrf_token = 'placeholder'` (line 83) represents a critical gap. While not a session *token* issue per ASVS 7.2.x, it means that session-based CSRF protection is non-functional. An attacker could forge requests that ride on a victim's valid session.

4. **State-Changing Operations via GET:** The `/do-open/<eid>` and `/do-close/<eid>` endpoints use GET requests for state-changing operations. Combined with the absent CSRF protection, this creates a trivially exploitable session riding vulnerability.

5. **No Session Invalidation on Privilege Change:** When election state changes (opened, closed), there is no session re-validation or re-authentication requirement. A session established before an election opened remains valid for voting without any freshness check.

## Cross-cutting Recommendations

### Immediate (Critical/High Priority)

1. **Document Session Management Policy:** Create a dedicated session management policy document covering:
   - Inactivity timeout (recommend 30 minutes per NIST SP 800-63B)
   - Absolute session lifetime (recommend 8-12 hours)
   - Concurrent session limit (recommend 3-5)
   - Federated session coordination with ASF SSO
   - Re-authentication triggers for sensitive operations

2. **Verify Framework Session Security:** Audit `asfquart.session` to confirm:
   - Session tokens use CSPRNG with ≥128 bits of entropy
   - Tokens are unique (collision-resistant)
   - Session store supports timeout/expiration

3. **Implement CSRF Protection:** Replace the placeholder token with a real CSRF mechanism tied to the session, as this directly undermines session security by enabling session riding attacks.

### Short-term (1-2 Sprints)

4. **Implement Authorization Checks:** Replace all `### check authz` placeholders with actual ownership/permission verification to ensure sessions only provide access to authorized resources.

5. **Convert State-Changing GETs to POSTs:** Change `/do-open/<eid>` and `/do-close/<eid>` to POST endpoints with CSRF protection.

6. **Add Concurrent Session Controls:** Implement session counting and enforcement, with user notification of other active sessions.

### Long-term (Architecture)

7. **Session Lifecycle Documentation:** Maintain living documentation of session management decisions as the application evolves from ASF-specific to a general-purpose solution (as noted in code comments).

8. **Re-authentication for Critical Operations:** Implement step-up authentication for election lifecycle operations (open, close, tally).

9. **Session Monitoring:** Add logging for session creation, destruction, and anomalous patterns (multiple geographic locations, rapid session cycling).
        