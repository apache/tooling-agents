# Security Audit Report: ASVS 16.5.1

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 16.5.1 |
| **Files Analyzed** | 8 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that a generic message is returned to the consumer when an unexpected or security-sensitive error occurs, ensuring no exposure of sensitive internal system data such as stack traces, queries, secret keys, and tokens.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 1 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---

## ASVS-16.5.1: Generic Error Messages

### Findings for 16.5.1

### [HIGH]
**Finding ID**: ASVS-1651-HIGH-001

**File**: `v3/server/pages.py`, lines 476, 497  
**Function**: `do_add_issue_endpoint`, `do_edit_issue_endpoint`

**Vulnerable code quote**:
```python
@APP.post('/do-add-issue/<eid>')
@asfquart.auth.require({R.committer})  ### need general solution
@load_election
async def do_add_issue_endpoint(election):
    result = await basic_info()
    ### check authz
    form = edict(await quart.request.form)
    print('FORM:', form)
    # ...

@APP.post('/do-edit-issue/<eid>/<iid>')
@asfquart.auth.require({R.committer})  ### need general solution
@load_election_issue
async def do_edit_issue_endpoint(election, issue):
    result = await basic_info()
    ### check authz
    form = edict(await quart.request.form)
    print('FORM:', form)
```

**Description**: Debug `print()` statements output full form data to stdout/stderr. In production deployments, stdout/stderr may be captured in process logs, container logs, or shared monitoring systems, potentially exposing user-submitted data including election titles, descriptions, and other form fields to parties with access to system-level logs.

**Data flow**: User form submission → `quart.request.form` → `print()` → stdout/stderr → potentially visible in deployment logs

**Proof of concept**: An administrator or monitoring system viewing application process output would see all form data submitted by users when adding or editing issues.

**Impact**: Sensitive internal data exposure through debug output. In containerized environments, this data may be aggregated alongside other services.

**Remediation**:
```python
# Remove debug print statements
# Replace with appropriate-level logging if needed:
_LOGGER.debug(f'Form received for issue in election[E:{election.eid}]')
```

### [MEDIUM]
**Finding ID**: ASVS-1651-MED-001

**File**: `v3/server/pages.py`  
**Function**: No global error handler defined

**Description**: There is no application-wide error handler that ensures unhandled exceptions return generic messages. The Quart framework's default error handling may expose stack traces, internal paths, or exception details in non-production configurations. Without an explicit error handler, the application relies on framework defaults which vary by environment settings (DEBUG mode, etc.).

Key concerns:
- `election.py` raises `ElectionBadState` with internal state details: `f'Election[E:{self.eid}] is "{self.current}" but should be "{self.required}"'`
- `AssertionError` messages (if assertions are left in production) reveal internal validation logic
- Database errors from sqlite3 may include SQL fragments or file paths
- No `@APP.errorhandler(500)` or similar catch-all handler is defined

**Data flow**: Internal exception → framework default handler → potentially detailed error page to client

**Proof of concept**: Trigger an unhandled state violation:
```
POST /do-vote/VALID_EID
# Submit a vote for a closed election (if assertion is enabled)
# The assertion message or traceback could be returned to the user
```

**Impact**: Stack traces, internal paths, election state details, or database information could be exposed to end users.

**Remediation**:
```python
@APP.errorhandler(Exception)
async def handle_generic_error(error):
    _LOGGER.exception('Unhandled exception')
    return quart.Response(
        'An unexpected error occurred. Please try again later.',
        status=500,
        mimetype='text/plain'
    )

@APP.errorhandler(500)
async def handle_500(error):
    _LOGGER.error(f'Internal server error: {error}')
    return quart.Response(
        'An unexpected error occurred. Please try again later.',
        status=500,
        mimetype='text/plain'
    )
```

---

### Security Controls Inventory for 16.5.1

| Control | Location | Status |
|---------|----------|--------|
| Generic 404 responses | `raise_404()` at line 647 | APPLIED for specific cases |
| Flash messages (generic) | `do_vote_endpoint` line 402 | APPLIED - uses generic messages |
| HTTP abort with generic message | `_set_election_date` lines 95, 100, 104 | APPLIED |
| Debug print statements | Lines 476, 497 | VULNERABILITY - leaks form data |
| Global exception handler | Not found | ABSENT |

### Positive Patterns for 16.5.1

- `do_vote_endpoint` catches exceptions and returns a generic flash message (`'Error submitting vote for issue {iid}.'`) without exposing the underlying error details to the user.
- `_set_election_date` uses `quart.abort(400, ...)` with controlled error messages.
- Custom 404 pages via `raise_404()` use templates that don't expose internal details.
- The `load_election` and `load_election_issue` decorators handle "not found" cases with user-friendly 404 pages.

---



## Cross-cutting Architecture Observations

1. **Security control existence vs. application gap (Type B)**: The most significant systemic issue is that security controls are *identified* (via `### check authz` comments and `assert` statements) but either not implemented or implemented using an unreliable mechanism (`assert`). This creates false confidence that security is handled.

2. **Logging infrastructure immaturity**: The application generates good audit events but lacks the infrastructure to protect, transmit, and store those events securely. This is a foundational gap that affects multiple ASVS requirements.

3. **Error handling philosophy inconsistency**: Some endpoints (like `do_vote_endpoint`) have try/except blocks while others (like `do_open_endpoint`, `do_close_endpoint`) have no error handling at all. There's no consistent strategy.

4. **Single-tier architecture**: The application, database, and logs all reside on the same system/process. This means a compromise of any one component grants access to all others, including logs. A multi-tier or defense-in-depth approach is absent.

5. **Admin tooling (`tally.py`) shares the same deficiencies**: The tally script uses `raise` to "fail hard" on errors, but has no structured error handling, no audit logging to a secure system, and outputs results directly to stdout where they could be captured or lost.

6. **Assertion-based validation in a cryptographic voting system**: Using `assert` for state checks in a system designed to guarantee election integrity is a fundamental design flaw. The security model depends on state transitions being enforced, but the enforcement mechanism can be silently disabled.

---

## Cross-cutting Recommendations

### Immediate (Critical/High - address before deployment)

1. **Replace all `assert` statements with proper exception raising** in `election.py`. Every `assert self.is_editable()`, `assert self.is_open()`, and `assert self.is_closed()` must be converted to `if not ...: raise ElectionBadState(...)`. This is the single highest-impact change.

2. **Implement a global exception handler** (`@APP.errorhandler(Exception)`) that:
   - Logs the full exception to the structured logger
   - Returns a generic error page to the user
   - Prevents stack trace disclosure

3. **Implement authorization checks** at all `### check authz` locations. At minimum, verify the authenticated user owns the election for management operations.

### Short-term (Medium - address within sprint)

4. **Configure log transmission** to a logically separate system (remote syslog, centralized logging service, or at minimum a separate filesystem with restricted permissions).

5. **Add database error handling** with graceful degradation. Wrap database operations in try/except for `sqlite3.OperationalError` and related exceptions.

6. **Remove debug `print()` statements** from production endpoints.

7. **Add log file protection**: If logs are stored locally, configure append-only permissions and ensure the application process cannot delete or modify existing log entries.

### Long-term (Architecture improvements)

8. **Implement a circuit breaker pattern** for database access, with health checks and graceful degradation.

9. **Add log integrity verification**: Implement hash chaining or digital signatures on log entries to detect tampering.

10. **Separate the logging infrastructure**: Deploy a dedicated log aggregation system (e.g., ELK stack, Loki, or cloud-native logging) that is network-isolated from the application.

11. **Add comprehensive integration tests** that verify error handling paths, including simulated database failures and invalid state transitions.
        