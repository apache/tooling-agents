# Security Audit Report: ASVS 13.3.1

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 13.3.1 |
| **Files Analyzed** | 7 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that a secrets management solution, such as a key vault, is used to securely create, store, control access to, and destroy backend secrets. These could include passwords, key material, integrations with databases and third-party systems, keys and seeds for time-based tokens, other internal…

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 1 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---

## ASVS-13.3.1: Secrets Management Solution

### Findings for 13.3.1

### [HIGH]

**Finding ID**: ASVS-1331-HIGH-001

**Title**: No secrets management solution used; cryptographic keys stored directly in SQLite database

**File**: `v3/steve/election.py`, `v3/schema.sql`

**Vulnerable Code** (schema.sql lines 63-72):
```sql
/* A salt value to use for hashing this Election. 16 bytes.
   This will be NULL until the Election is opened.  */
salt  BLOB  CHECK (salt IS NULL OR length(salt) = 16),

/* If this Election has been opened for voting, then we store
   the OpenedKey here to avoid recomputing. 32 bytes.
   This will be NULL until the Election is opened.  */
opened_key  BLOB  CHECK (opened_key IS NULL OR length(opened_key) = 32),
```

**Data flow**: `crypto.gen_salt()` / `crypto.gen_opened_key()` → stored directly in SQLite `election` and `mayvote` tables → accessed by application code.

**Description**: The application stores cryptographic secrets (election salts, opened_keys, mayvote salts, encrypted vote ciphertexts) directly in an unencrypted SQLite database file. There is no:
- Key vault or secrets management solution (e.g., HashiCorp Vault, AWS KMS, Azure Key Vault)
- Hardware Security Module (HSM) integration
- Encryption at rest for the database file
- Access control beyond filesystem permissions on the SQLite file

**Impact**: 
- If the SQLite database file is compromised (backup theft, file system access), all cryptographic material is exposed
- The `opened_key` (32 bytes) is the master key for vote token derivation — its compromise allows vote de-anonymization
- Election salts allow regeneration of vote tokens
- No audit trail for secret access

**Remediation**:
```python
# Integration with a secrets management solution
from vault_client import VaultClient  # example

class SecureElection(Election):
    vault = VaultClient(addr=os.environ['VAULT_ADDR'], token=os.environ['VAULT_TOKEN'])
    
    def open(self, pdb):
        assert self.is_editable()
        self.add_salts()
        edata = self.gather_election_data(pdb)
        salt = crypto.gen_salt()
        opened_key = crypto.gen_opened_key(edata, salt)
        
        # Store sensitive key material in vault, not database
        self.vault.write(
            f'secret/elections/{self.eid}',
            data={'salt': salt.hex(), 'opened_key': opened_key.hex()}
        )
        # Store reference in database (not the actual key)
        self.c_open.perform(salt, opened_key, self.eid)
```

### [MEDIUM]

**Finding ID**: ASVS-1331-MED-001

**Title**: CSRF token is a hardcoded placeholder string in source code

**File**: `v3/server/pages.py`, function `basic_info()`, line 83

**Vulnerable Code**:
```python
### generate a real token and store in the session
basic.csrf_token = 'placeholder'
```

**Impact**: While not a backend service secret per se, this is a security-critical token that should be cryptographically generated and properly managed. Its placeholder nature means CSRF protection is non-functional.

**Remediation**:
```python
import secrets

async def basic_info():
    # ...
    s = await asfquart.session.read()
    if s:
        # Generate or retrieve CSRF token from session
        if 'csrf_token' not in s:
            s['csrf_token'] = secrets.token_hex(32)
            await asfquart.session.write(s)
        basic.csrf_token = s['csrf_token']
```

### Security Controls Inventory for 13.3.1

| Control | Location | Coverage Status |
|---------|----------|-----------------|
| Key vault / secrets manager | Not present | **MISSING** |
| HSM integration | Not present | **MISSING** |
| Database encryption at rest | Not configured | **MISSING** |
| Runtime secret generation | `crypto.py:gen_salt()`, `gen_opened_key()` | Applied (good entropy source) |
| Secret exclusion from API | `election.py:get_metadata()` lines 144-158 | Applied (SALT/OPENED_KEY excluded) |
| Secrets in source code | None found (except CSRF placeholder) | **PASS** (no API keys/passwords in source) |
| Build artifact secrets | None detected | **PASS** |

### Positive Patterns for 13.3.1

1. **No secrets in source code**: No API keys, passwords, or connection strings are hardcoded in the source.
2. **Strong entropy for key generation**: Uses `secrets.token_bytes()` (CSPRNG) for all cryptographic material generation.
3. **Sensitive columns excluded from public APIs**: `get_metadata()` explicitly excludes `salt` and `opened_key`. Queries like `q_open_to_me` and `q_owned` do not select sensitive columns.
4. **Database path from configuration**: `APP.cfg.db` — not hardcoded, loaded from external config.

---



## Cross-cutting Architecture Observations

1. **Authentication without Authorization**: The application has robust authentication (via `asfquart.auth.require`) but authorization is largely unimplemented. Multiple `### check authz` placeholders throughout `pages.py` mean any authenticated committer can manage any election. This impacts the backend communication security posture because it means the application's internal access control doesn't properly protect backend resources.

2. **Local-First Architecture**: The application uses SQLite (local file database) rather than remote services. This significantly reduces the attack surface for ASVS 13.2.x requirements about backend communication, as there are no remote database credentials, API keys, or network connections to external services to protect.

3. **Cryptographic Material Lifecycle**: The application has a well-designed cryptographic lifecycle (salt generation → key derivation → vote encryption) but all material resides in the same SQLite file. There's no separation of privilege between the application accessing election metadata and accessing cryptographic secrets.

4. **Single Process Model**: The application appears to run as a single Quart process, connecting to local SQLite. This means connection configuration (13.2.6) is less critical than in distributed architectures, but still relevant for concurrency handling.

5. **Defense in Depth Gap**: While queries are properly parameterized (SQL injection protection) and sensitive data is filtered at the API layer, the single-layer design means a compromise at the application level exposes all secrets simultaneously.

---

## Cross-cutting Recommendations

### Immediate (Critical/High Priority)

1. **Implement secrets management**: Integrate a key vault (e.g., HashiCorp Vault) to store `opened_key` values separately from the election database. At minimum, enable SQLite encryption (SQLCipher) for the database file.

2. **Restrict `__getattr__` proxy**: Limit the Election class's attribute proxy to only expose a defined set of public operations, preventing unintended access to raw database cursors that return sensitive columns.

### Short-term (Medium Priority)

3. **Document and configure connection behavior**: Add SQLite `PRAGMA busy_timeout`, WAL mode, and explicit connection lifecycle management. Document expected concurrency limits.

4. **Implement file serving allowlist**: Add file extension and name validation to `serve_doc()` to define permitted document types.

5. **Add database encryption at rest**: Use SQLCipher or similar to encrypt the SQLite database, ensuring that file system access alone cannot expose secrets.

### Long-term (Architecture)

6. **Separate secrets storage**: Move cryptographic material (election salts, opened_keys) to a dedicated secrets store with access control, audit logging, and key rotation capabilities.

7. **Implement connection pooling**: Replace ad-hoc `open_database()` calls with an application-level connection pool with documented configuration for timeouts, max connections, and retry behavior.

8. **Add outbound request allowlist**: If the application evolves to include email notifications or external integrations (suggested by `get_voters_for_email()`), implement an allowlist of permitted outbound destinations at the application and network layers.
        