# Security Audit Report: ASVS 14.2.5

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 14.2.5 |
| **Files Analyzed** | 9 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that caching mechanisms are configured to only cache responses which have the expected content type for that resource and do not contain sensitive, dynamic content. The web server should return a 404 or 302 response when a non-existent file is accessed rather than returning a different…

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 2 |
| 🟢 Low | 0 |

---

## ASVS-14.2.5: Cache Mechanisms and Web Cache Deception Prevention

### Findings for 14.2.5

### [MEDIUM]

**Finding ID:** ASVS-1425-MED-001

**Title:** Document Serving Endpoint Lacks Content-Type Validation, Enabling Potential Cache Deception

**File:** `v3/server/pages.py`, lines 560-574  
**Function:** `serve_doc`

**Vulnerable Code:**
```python
@APP.get('/docs/<iid>/<docname>')
@asfquart.auth.require
async def serve_doc(iid, docname):
    result = await basic_info()
    db = steve.election.Election.open_database(DB_FNAME)
    row = db.q_get_mayvote.first_row(result.uid, iid)
    if not row:
        quart.abort(404)
    ### verify the propriety of DOCNAME.
    return await quart.send_from_directory(DOCSDIR / iid, docname)
```

**Data Flow:** Attacker crafts URL like `/docs/<iid>/profile.css` → `send_from_directory` returns 404 or serves file → **intermediate cache may cache the response based on file extension** → subsequent requests served from cache.

**Impact:** The comment `### verify the propriety of DOCNAME` indicates the developers are aware this needs validation. Without explicit content-type enforcement and proper 404 handling at the cache layer, an attacker could potentially:
1. Request `/docs/<iid>/nonexistent.css` — if the server returns a redirect or different file instead of a clean 404, caches may store the response under the `.css` extension.
2. Since `send_from_directory` should return 404 for missing files, the primary risk is in misconfigured caching layers that cache 404s or error pages.

**Remediation:**
```python
@APP.get('/docs/<iid>/<docname>')
@asfquart.auth.require
async def serve_doc(iid, docname):
    import re
    result = await basic_info()
    
    # Validate docname format (alphanumeric, hyphens, dots only)
    if not re.match(r'^[a-zA-Z0-9][a-zA-Z0-9._-]*$', docname):
        quart.abort(404)
    
    db = steve.election.Election.open_database(DB_FNAME)
    row = db.q_get_mayvote.first_row(result.uid, iid)
    if not row:
        quart.abort(404)
    
    response = await quart.send_from_directory(DOCSDIR / iid, docname)
    # Prevent caching of authenticated document responses
    response.headers['Cache-Control'] = 'no-store, private'
    response.headers['X-Content-Type-Options'] = 'nosniff'
    return response
```

---

### [MEDIUM]

**Finding ID:** ASVS-1425-MED-002

**Title:** No Content-Type Response Header Enforcement or Cache-Deception Prevention on Dynamic Pages

**File:** `v3/server/pages.py`  
**Functions:** All template-rendered endpoints

**Vulnerable Code:**
```python
@APP.get('/vote-on/<eid>')
@asfquart.auth.require({R.committer})
@load_election
@APP.use_template(TEMPLATES / 'vote-on.ezt')
async def vote_on_page(election):
    # ... Returns HTML content with sensitive voting data
    # No X-Content-Type-Options header
    # No explicit Content-Type enforcement
    return result
```

**Impact:** Without `X-Content-Type-Options: nosniff` and explicit `Content-Type` headers, intermediate caches might misinterpret response content types. Combined with the absence of `Cache-Control: no-store`, a Web Cache Deception attack could theoretically cause a cache to store a user's personalized voting page under a path that looks like a static resource.

For example, if an attacker tricks a victim into visiting `/vote-on/abc1234567/nonexistent.css`, and the application returns the voting page HTML (because the EID includes the path suffix), a cache could store this as a CSS resource. However, the `load_election` decorator validates the EID format, making this specific attack path unlikely.

**Remediation:**
```python
@APP.after_request
async def security_headers(response):
    response.headers['X-Content-Type-Options'] = 'nosniff'
    if 'Cache-Control' not in response.headers:
        response.headers['Cache-Control'] = 'no-store, private'
    return response
```

### Security Controls Inventory for 14.2.5

| Control | Location | Status |
|---------|----------|--------|
| X-Content-Type-Options: nosniff | `pages.py` | **NOT PRESENT** |
| Cache-Control on dynamic pages | `pages.py` | **NOT PRESENT** |
| Content-Type validation on document serving | `serve_doc()` | **NOT PRESENT** |
| 404 for non-existent files | `send_from_directory` (framework) | **PRESENT** ✓ (framework default) |
| EID format validation (10 hex chars) | `schema.sql` CHECK constraint | **PRESENT** ✓ |
| IID format validation (10 hex chars) | `schema.sql` CHECK constraint | **PRESENT** ✓ |
| URL path structure prevents extension-based deception on main routes | Route definitions | **PARTIAL** ✓ |

### Positive Patterns for 14.2.5

- **EID/IID validation** — The database schema enforces 10 hex character format via CHECK constraints (`eid GLOB '[0-9a-f]{10}'`), and the `load_election` decorator validates existence. This prevents most URL-manipulation-based cache deception attacks on election endpoints because appended suffixes would change the EID parameter.
- **`send_from_directory`** — Quart's built-in function returns proper 404 responses for non-existent files, which is the expected behavior to prevent serving alternative content.
- **Route structure** — Dynamic routes like `/vote-on/<eid>` capture the entire path segment, meaning appended extensions (`.css`, `.js`) would be part of the EID and fail validation.

---



## Cross-cutting Architecture Observations

1. **Security Control Consistency Gap**: The application has strong cryptographic controls (Argon2, Fernet encryption, BLAKE2b, CSPRNG for IDs and shuffling) but lacks fundamental HTTP-layer security headers (Cache-Control, X-Content-Type-Options). This suggests the security design focused on cryptographic vote integrity but did not address transport/caching layer concerns.

2. **Internal vs. External API Surface**: The `Election._all_metadata()` method is documented as "INTERNAL ONLY" and returns sensitive fields (salt, opened_key), while `get_metadata()` is the safe public API. This separation is correct but relies on convention rather than enforcement (no access modifier, just naming convention with underscore prefix).

3. **Authorization Gap (Cross-cutting)**: Multiple endpoints have `### check authz` placeholder comments. While authentication is enforced via decorators, fine-grained authorization (e.g., only election owner can open/close/manage) is not implemented. This affects multiple ASVS requirements indirectly — if an unauthorized user can access management endpoints, they could view sensitive election data that should be minimized.

4. **Data Flow Segregation**: The system properly segregates sensitive cryptographic material (salt, opened_key) from public-facing queries at the SQL level (separate queries for internal vs. external use). This is a strong architectural pattern.

5. **Template Security**: The EZT templating engine used for HTML rendering requires careful attention to output encoding. The `vote-on.ezt` template includes a client-side `escapeHtml()` function for JavaScript-rendered content, but server-side rendering relies on EZT's default behavior.

6. **Operational Tooling**: The `mail-voters.py` script runs as a standalone process with direct database access. It accesses voter PII (email addresses) without the web application's session/auth layer, relying instead on OS-level access controls to the script and database file.

---

## Cross-cutting Recommendations

### Immediate (Within Sprint)

1. **Remove debug print statements** (`pages.py` lines 481, 497) — Direct PII/data exposure risk.
2. **Add `Cache-Control: no-store, private` header** to all authenticated responses via an `@APP.after_request` handler.
3. **Add `X-Content-Type-Options: nosniff` header** globally.

### Short-term (Within Quarter)

4. **Implement authorization checks** at all `### check authz` placeholder locations — verify the authenticated user is the election owner or in the authorized group before allowing management operations.
5. **Document data retention policy** — classify each data type (elections, votes, persons) with explicit retention periods and deletion triggers.
6. **Implement CSRF token validation** — replace the placeholder token with a real per-session token and validate on all POST endpoints.
7. **Validate `docname` parameter** in `serve_doc()` — restrict to safe filename characters.

### Long-term (Within Year)

8. **Implement automated data purge** — scheduled job to delete closed elections past retention period and purge superseded votes after tally certification.
9. **Complete migration to XChaCha20-Poly1305** with AAD binding vote ciphertext to vote_token.
10. **Implement log classification** — ensure logs containing user-action mappings have appropriate retention limits and access controls distinct from application data.
11. **Add per-response caching policy** — static assets should have appropriate cache headers (`max-age`), while dynamic authenticated content should have `no-store`.
        