# Security Audit Report: ASVS 6.7.1

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 6.7.1 |
| **Files Analyzed** | 6 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that the certificates used to verify cryptographic authentication assertions are stored in a way protects them from modification.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---

## ASVS-6.7.1: Cryptographic Authentication Mechanism - Certificate Storage Protection

### Findings for 6.7.1

The codebase implements a voting system with cryptographic operations (Argon2, HKDF, Fernet) but does not implement certificate-based cryptographic authentication mechanisms (smart cards, FIDO keys). The `opened_key` stored in the database serves as an anti-tamper integrity check, not as a certificate for verifying cryptographic authentication assertions.

However, the following observations are relevant to the spirit of this requirement regarding protection of cryptographic material:

#### [MEDIUM]

**Finding ID**: ASVS-671-MED-001

**Title**: Cryptographic keys stored in SQLite without integrity protection mechanisms

**File**: `v3/schema.sql` (lines 62-63), `v3/steve/election.py` (line 78)

**Vulnerable code**:
```sql
/* election table */
salt  BLOB  CHECK (salt IS NULL OR length(salt) = 16),
opened_key  BLOB  CHECK (opened_key IS NULL OR length(opened_key) = 32),
```

**Data flow**: `crypto.gen_opened_key()` → `c_open.perform(salt, opened_key, eid)` → SQLite `election.opened_key` column → unprotected file-system storage

**Description**: The `opened_key` (which serves as the root of trust for election integrity via `is_tampered()`) and per-voter `salt` values are stored in a plain SQLite database file. Any process or user with file-system access to `steve.db` can modify these values, potentially allowing an attacker to:
1. Replace `opened_key` after tampering with election data to defeat the anti-tamper check
2. Modify `mayvote.salt` values to invalidate legitimate votes

**Impact**: An attacker with file-system access could modify the `opened_key` to match tampered election data, bypassing the `is_tampered()` integrity check entirely.

**Remediation**: Consider storing the `opened_key` in a write-protected or append-only store, using an HMAC over the database file, or implementing database-level encryption with integrity verification:
```python
# Example: Add an HMAC over critical fields using a server-side secret
import hmac
def store_opened_key(conn, eid, salt, opened_key, server_secret):
    integrity_mac = hmac.new(server_secret, salt + opened_key + eid.encode(), 'sha256').digest()
    conn.execute("UPDATE election SET salt=?, opened_key=?, integrity_mac=? WHERE eid=?",
                 (salt, opened_key, integrity_mac, eid))
```

### Security Controls Inventory for 6.7.1

| Control | Location | Status |
|---------|----------|--------|
| Anti-tamper check (`is_tampered()`) | `v3/steve/election.py:320-332` | Implemented but bypassable if DB is writable |
| Salt length constraint (16 bytes) | `v3/schema.sql:62` | Enforced via CHECK constraint |
| Opened_key length constraint (32 bytes) | `v3/schema.sql:63` | Enforced via CHECK constraint |
| Metadata query excludes sensitive fields from public APIs | `v3/queries.yaml:q_open_to_me`, `v3/steve/election.py:144-156` | Applied consistently |

### Positive Patterns for 6.7.1

- The `get_metadata()` method (election.py:144) explicitly excludes `salt` and `opened_key` from public-facing data
- Public queries (`q_open_to_me`, `q_owned`, `q_upcoming_to_me`) do not SELECT `salt` or `opened_key` columns
- The `is_tampered()` function provides a runtime integrity verification mechanism by recomputing the opened_key
- Schema CHECK constraints enforce expected lengths for cryptographic material

---



## Cross-cutting Architecture Observations

1. **Single Trust Boundary**: The application places complete trust in the `asfquart` authentication framework. There is no defense-in-depth for authentication - if the framework is bypassed, all controls fail simultaneously.

2. **Cryptographic Architecture vs. Authentication Architecture Mismatch**: The election voting system has sophisticated cryptographic protections (Argon2 hashing, per-voter salts, Fernet encryption, anti-tamper detection), but the authentication and authorization layer is incomplete with numerous `### check authz` placeholder comments.

3. **No Separation of Duties**: The same `R.committer` role can both manage elections and vote in them. There is no enforcement that the election owner cannot also modify votes or bypass the anti-tamper mechanism.

4. **CSRF Protection Gap**: The placeholder CSRF token (`'placeholder'`) means that even with proper IdP integration, session-based attacks remain possible across all POST endpoints and the GET-based state-changing endpoints (`/do-open/`, `/do-close/`).

5. **Opaque Authentication Dependencies**: The security posture of the application is heavily dependent on the `asfquart` library, which is not included in the audit scope. The application cannot be fully assessed for ASVS 6.8.x requirements without visibility into this dependency.

6. **Authorization is Role-Based Only**: The system checks if a user is a "committer" or "pmc_member" but does not check if they are the *owner* of a specific election or have been specifically authorized for it. Any committer can manage any election.

## Cross-cutting Recommendations

### Immediate (Critical Priority)
1. **Implement CSRF protection**: Replace the placeholder token with a real CSRF mechanism. This affects all POST endpoints and the GET-based state-changing endpoints.
2. **Convert state-changing GET endpoints to POST**: `/do-open/<eid>` and `/do-close/<eid>` must not use GET for irreversible operations.
3. **Implement authorization checks**: Replace all `### check authz` placeholders with actual ownership/permission verification before allowing election management operations.

### Short-term (High Priority)
4. **Add authentication strength verification**: Capture and validate `acr`, `amr`, and `auth_time` from the IdP for sensitive operations (election open/close, vote submission).
5. **Namespace user identities by IdP**: Add an IdP identifier to the `person.pid` to prevent future identity collision if multiple IdPs are supported.
6. **Document authentication framework requirements**: Create explicit documentation of what `asfquart.auth` must verify (signature validation, replay prevention, token expiry) as a contractual interface.
7. **Add database integrity protection**: Implement an HMAC or signature over the `opened_key` to detect unauthorized modification at the database level.

### Long-term (Architectural)
8. **Implement step-up authentication**: For critical operations (opening/closing elections), require re-authentication or MFA verification.
9. **Add audit logging for authentication events**: Log authentication method, strength, and time for security monitoring.
10. **Consider database encryption**: Protect the SQLite database at rest to prevent direct modification of cryptographic material.
11. **Implement proper secret management**: The cryptographic keys and salts should be managed according to the "Secret Management" principles referenced in the ASVS section description, potentially using a dedicated key management service.
        