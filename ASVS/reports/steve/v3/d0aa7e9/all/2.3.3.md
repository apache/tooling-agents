# Security Audit Report: ASVS 2.3.3

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 2.3.3 |
| **Files Analyzed** | 17 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that transactions are being used at the business logic level such that either a business logic operation succeeds in its entirety or it is rolled back to the previous correct state.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 2 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---

## ASVS-2.3.3: Transaction Usage at Business Logic Level

### Findings for 2.3.3

### [HIGH]

**Finding ID**: ASVS-233-HIGH-001
**Title**: Multi-Vote Submission Not Wrapped in Transaction — Partial Failure Leaves Inconsistent State

**Location**: `v3/server/pages.py`, `do_vote_endpoint()` (line 372)

**Vulnerable code**:
```python
@APP.post('/do-vote/<eid>')
@asfquart.auth.require({R.committer})
@load_election
async def do_vote_endpoint(election):
    ...
    # Process each vote
    for iid, votestring in votes.items():
        if iid not in issue_dict:
            await flash_danger(f'Invalid issue ID: {iid}')
            return quart.redirect(f'/vote-on/{election.eid}', code=303)

        try:
            election.add_vote(result.uid, iid, votestring)
            ...
        except Exception as e:
            _LOGGER.error(...)
            await flash_danger(f'Error submitting vote for issue {iid}.')
            return quart.redirect(f'/vote-on/{election.eid}', code=303)

    await flash_success('Votes submitted successfully!')
    return quart.redirect('/voter', code=303)
```

**Data flow**: User submits ballot with 10 issues → Loop processes votes one by one → Vote #5 fails → Votes #1-4 already committed → User told "error" but partial votes persist

**Proof of Concept**: Submit a ballot with 10 YNA votes. If the 5th issue has a database error (e.g., mayvote row missing due to race condition), votes 1-4 are committed but 6-10 are not. The user receives an error message and may attempt to re-vote, but the partial submission already changed their recorded ballot.

**Impact**: Atomic ballot submission is not guaranteed. A voter's intent is a complete ballot, but partial submission creates an inconsistent state where some issues have new votes and others retain old votes. This violates the principle that business operations should succeed entirely or roll back.

**Remediation**:
```python
async def do_vote_endpoint(election):
    ...
    # Wrap all votes in a single transaction
    election.db.conn.execute('BEGIN TRANSACTION')
    try:
        for iid, votestring in votes.items():
            if iid not in issue_dict:
                election.db.conn.execute('ROLLBACK')
                await flash_danger(f'Invalid issue ID: {iid}')
                return quart.redirect(f'/vote-on/{election.eid}', code=303)
            election.add_vote(result.uid, iid, votestring)
        election.db.conn.execute('COMMIT')
    except Exception as e:
        election.db.conn.execute('ROLLBACK')
        await flash_danger('Error submitting votes. No changes were saved.')
        return quart.redirect(f'/vote-on/{election.eid}', code=303)
    ...
```

---

### [HIGH]

**Finding ID**: ASVS-233-HIGH-002
**Title**: Election Open Operation Not Fully Atomic — Salt Addition and State Change in Separate Transactions

**Location**: `v3/steve/election.py`, `open()` method (line 70)

**Vulnerable code**:
```python
def open(self, pdb):
    # Double-check the Election is in the editing state.
    assert self.is_editable()

    # Add salts first. If this is gonna fail, then make sure it
    # happens before we move to the "opened" state.
    self.add_salts()  # <-- Transaction 1: BEGIN/COMMIT

    edata = self.gather_election_data(pdb)
    salt = crypto.gen_salt()
    opened_key = crypto.gen_opened_key(edata, salt)

    self.c_open.perform(salt, opened_key, self.eid)  # <-- Auto-commit (Transaction 2)
```

And `add_salts()`:
```python
def add_salts(self):
    ...
    self.db.conn.execute('BEGIN TRANSACTION')
    self.q_all_issues.perform(self.eid)
    for mayvote in self.q_all_issues.fetchall():
        salt = crypto.gen_salt()
        self.c_salt_mayvote.perform(salt, mayvote.rowid)
    self.db.conn.execute('COMMIT')
```

**Data flow**: `open()` → `add_salts()` [Transaction 1 commits] → `gather_election_data()` → `c_open.perform()` [Auto-commit] — If `c_open` fails, salts are already written but election remains in editable state

**Proof of Concept**: If an error occurs between `add_salts()` completing and `c_open.perform()` succeeding (e.g., disk full, power loss, exception in `gather_election_data`), the `mayvote` table has salts set (indicating an opened election) but the election record still shows editable state. Attempting to open again would regenerate salts, invalidating any votes that might have been cast in the interim.

**Impact**: The election open operation is not atomic. A failure mid-way leaves the database in an inconsistent state that cannot be trivially recovered, potentially compromising the anti-tamper mechanism.

**Remediation**:
```python
def open(self, pdb):
    if not self.is_editable():
        raise ElectionBadState(...)

    self.db.conn.execute('BEGIN TRANSACTION')
    try:
        # Add salts within the same transaction
        self.q_all_issues.perform(self.eid)
        for mayvote in self.q_all_issues.fetchall():
            salt = crypto.gen_salt()
            self.c_salt_mayvote.perform(salt, mayvote.rowid)

        edata = self.gather_election_data(pdb)
        salt = crypto.gen_salt()
        opened_key = crypto.gen_opened_key(edata, salt)
        self.c_open.perform(salt, opened_key, self.eid)
        
        self.db.conn.execute('COMMIT')
    except Exception:
        self.db.conn.execute('ROLLBACK')
        raise
```

---

### [MEDIUM]

**Finding ID**: ASVS-233-MED-001
**Title**: Election Creation Script Has Transaction Code Commented Out

**Location**: `v3/server/bin/create-election.py` (lines 83-87, 109-112)

**Vulnerable code**:
```python
    # TODO: Re-enable transactions for safety once database setup allows.
    # pdb = steve.persondb.PersonDB(DB_FNAME)
    # pdb.db.conn.execute('BEGIN TRANSACTION')

    try:
        election = steve.election.Election.create(DB_FNAME, title, owner_pid, authz, open_at, close_at)
        ...
        for issue_data in issues:
            iid = election.add_issue(...)
        ...
        for pid in record:
            election.add_voter(pid)
        # pdb.db.conn.execute('COMMIT')
    except Exception as e:
        # pdb.db.conn.execute('ROLLBACK')
        ...
        raise
```

**Impact**: If voter addition fails partway through (e.g., PID not found), the election is left with some but not all eligible voters. No rollback occurs.

**Remediation**: Re-enable the transaction wrapping.

### Security Controls Inventory for 2.3.3

| Control | Location | Coverage Status |
|---------|----------|-----------------|
| `BEGIN TRANSACTION`/`COMMIT` | `election.py` `delete()` line 55 | APPLIED correctly for deletion |
| `BEGIN TRANSACTION`/`COMMIT` | `election.py` `add_salts()` line 122 | APPLIED — but isolated from `open()` |
| Transaction in `open()` | `election.py` line 70 | NOT APPLIED — salts and state change in separate transactions |
| Transaction in vote submission | `pages.py` `do_vote_endpoint()` | NOT APPLIED — each vote auto-commits |
| Transaction in script | `create-election.py` line 83 | DEFINED — COMMENTED OUT |

### Positive Patterns for 2.3.3

- `delete()` correctly wraps all deletion operations (mayvote → issues → election) in a single transaction with proper ordering for referential integrity.
- `add_salts()` uses a transaction for the bulk salt generation, preventing partial salt states.
- The schema uses `FOREIGN KEY ... ON DELETE RESTRICT` to prevent inconsistent deletions outside of proper transaction-wrapped code.

---



## Cross-cutting Architecture Observations

1. **Type B Gaps Dominate**: The most critical pattern is controls that EXIST in the codebase (owner_pid, authz, CSRF tokens, state validation) but are NOT APPLIED at the enforcement point. This creates false confidence — the schema and comments suggest security is in place, but enforcement is absent.

2. **Assert vs. Exception Pattern Inconsistency**: The codebase uses `assert` for state checks in some methods (`open()`, `close()`, `add_issue()`) but proper exceptions in others (`add_vote()` via `_all_metadata(required_state)`). This inconsistency means security enforcement depends on which code path is taken.

3. **GET for State Changes**: The use of GET requests for irreversible operations (`/do-open/<eid>`, `/do-close/<eid>`) combines with the CSRF gap to create a particularly dangerous attack surface.

4. **Single-Layer Defense**: Security controls exist primarily at the model layer (`election.py`) with minimal web-layer enforcement. The web layer (pages.py) trusts that the model will enforce constraints, but the model uses bypassable assertions.

5. **SQLite Concurrency Model**: The application relies on SQLite's implicit serialization for concurrency safety, but the check-then-act patterns in the application layer create TOCTOU windows that could be exploited in multi-process deployments.

6. **Audit Trail Strength**: Logging of user actions is consistently implemented, providing after-the-fact accountability even where preventive controls are missing.

## Cross-cutting Recommendations

### Immediate (P0 — Address Before Production Use)

1. **Replace all `assert` statements** with proper exception raising (`ElectionBadState`) in `election.py`. This is the most critical fix as it affects all state enforcement.

2. **Implement authorization checks** in all management endpoints — compare `result.uid` against `election.owner_pid`/`election.authz`. This closes the most impactful Type B gap.

3. **Implement CSRF protection** — generate real tokens per session and validate them in all POST endpoints. Change `/do-open/` and `/do-close/` to POST methods.

4. **Add vote string validation** in `add_vote()` — validate against issue type before encryption.

### Short-term (P1 — Within Next Sprint)

5. **Wrap vote submission in a transaction** — ensure atomic ballot submission in `do_vote_endpoint()`.

6. **Make election open operation atomic** — combine `add_salts()` and the state change in a single transaction.

7. **Add rate limiting** on all mutating endpoints, prioritizing `/do-vote/<eid>` and `/do-create-election`.

8. **Add prerequisite validation** before election opening (verify issues exist, voters are registered).

### Long-term (P2 — Architecture Improvements)

9. **Implement multi-user approval** for irreversible state changes (open/close elections), particularly for elections with many eligible voters.

10. **Add explicit optimistic locking** (e.g., version column) on the election table to prevent concurrent state modifications.

11. **Re-enable and complete transactions** in the `create-election.py` script.

12. **Consider vote garbage collection** — periodically remove superseded vote rows to prevent unbounded table growth.
        