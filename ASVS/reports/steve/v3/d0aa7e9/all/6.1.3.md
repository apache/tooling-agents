# Security Audit Report: ASVS 6.1.3

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 6.1.3 |
| **Files Analyzed** | 4 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that, if the application includes multiple authentication pathways, these are all documented together with the security controls and authentication strength which must be consistently enforced across them.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---

## ASVS-6.1.3: Authentication Documentation - Multiple Authentication Pathways

### Findings for 6.1.3

### [MEDIUM]

**Finding ID**: ASVS-613-MED-001

**File**: `v3/server/pages.py`, multiple locations

**Description**: The application implements multiple authentication levels/pathways but they are not documented together, nor are the security controls and authentication strength consistently enforced.

**Evidence of multiple pathways:**
```python
# Pathway 1: No authentication (public)
@APP.get('/')
async def home_page():

@APP.get('/about')
async def about_page():

@APP.get('/static/<path:filename>')
async def serve_static(filename):

# Pathway 2: Valid session only (any authenticated user)
@APP.get('/profile')
@asfquart.auth.require  # Bare decorator means just require a valid session

@APP.get('/settings')
@asfquart.auth.require

@APP.get('/docs/<iid>/<docname>')
@asfquart.auth.require

# Pathway 3: ASF Committer
@APP.get('/voter')
@asfquart.auth.require({R.committer})  ### need general solution

@APP.post('/do-vote/<eid>')
@asfquart.auth.require({R.committer})

@APP.get('/do-open/<eid>')
@asfquart.auth.require({R.committer})  ### need general solution

# Pathway 4: PMC Member (elevated)
@APP.post('/do-create-election')
@asfquart.auth.require({R.pmc_member})  ### need general solution
```

**The `### need general solution` comments (appearing 9+ times) explicitly acknowledge that the authentication/authorization model is incomplete and undocumented.**

**Inconsistency example** — state-changing GET endpoints:
```python
# These state-changing operations use GET (vulnerable to CSRF via link/img):
@APP.get('/do-open/<eid>')
@asfquart.auth.require({R.committer})

@APP.get('/do-close/<eid>')  
@asfquart.auth.require({R.committer})
```

**Impact**: Without documentation of authentication pathways and their required security strengths:
- Security auditors cannot verify completeness of controls
- Developers may apply incorrect auth levels to new endpoints
- The "need general solution" comments indicate the current model is known to be inadequate
- Inconsistent enforcement (e.g., any committer can manage any election, not just the owner)

**Remediation**: Create authentication pathway documentation:

```markdown
# Authentication Pathways

## Pathway 1: Public (No Authentication)
- Endpoints: /, /about, /static/*, /favicon.ico
- Controls: None required
- Strength: N/A

## Pathway 2: Authenticated User (Any Valid Session)
- Endpoints: /profile, /settings, /docs/<iid>/<docname>
- Controls: Valid ASF OAuth session
- Strength: Single-factor (ASF credentials)
- Additional: /docs requires per-document mayvote authorization

## Pathway 3: ASF Committer  
- Endpoints: /voter, /vote-on/<eid>, /admin, /manage/<eid>, 
             /do-vote/<eid>, /do-open/<eid>, /do-close/<eid>,
             /do-add-issue/<eid>, /do-edit-issue/<eid>/<iid>,
             /do-delete-issue/<eid>/<iid>
- Controls: ASF committer role verification + ownership checks (TODO)
- Strength: Single-factor (ASF credentials) + role-based
- Note: Ownership verification (election.owner_pid == current_user) 
        must be enforced on all management operations

## Pathway 4: PMC Member (Elevated)
- Endpoints: /do-create-election
- Controls: ASF PMC membership verification
- Strength: Single-factor (ASF credentials) + elevated role
```

### Security Controls Inventory for 6.1.3

| Control | Location | Status |
|---------|----------|--------|
| Authentication documentation | Not found | **MISSING** |
| Public pathway | `/`, `/about`, `/static/*` | Defined implicitly |
| Session-only pathway | `/profile`, `/settings`, `/docs` | Defined via bare `@asfquart.auth.require` |
| Committer pathway | Most operational endpoints | Defined via `@asfquart.auth.require({R.committer})` |
| PMC member pathway | `/do-create-election` | Defined via `@asfquart.auth.require({R.pmc_member})` |
| Ownership authorization | Multiple endpoints (commented `### check authz`) | **NOT IMPLEMENTED (Type B gap)** |

### Positive Patterns for 6.1.3

- Consistent use of `@asfquart.auth.require` decorator pattern across endpoints
- Separation between public, session-required, committer, and PMC-member levels
- The code authors are aware of the gap (comments acknowledge need for "general solution")
- Per-document authorization in `serve_doc()` verifies voter eligibility

---



## Cross-cutting Architecture Observations

1. **Authentication Delegation Pattern**: The application consistently delegates authentication to ASF infrastructure (`asfquart.auth`). This means ASVS requirements 6.2.1 and 6.2.10 (password security) are the responsibility of the external identity provider, not this application. However, documentation requirements (6.1.1, 6.1.2, 6.1.3) still apply to the application.

2. **Authorization Gap (Type B — Control EXISTS but NOT CALLED)**: The code has 9+ instances of `### check authz` placeholder comments. While authentication (identity verification) is enforced, authorization (permission verification — e.g., "is this user the election owner?") is NOT implemented. Any committer can manage any election, open/close elections they don't own, and delete others' issues. This is a critical Type B gap.

3. **CSRF Protection is a Placeholder**: `basic.csrf_token = 'placeholder'` (line 83) combined with no server-side validation means all state-changing endpoints are vulnerable to CSRF. The template includes the token in forms/headers, creating false confidence (Type B gap).

4. **State-Changing GET Requests**: `/do-open/<eid>` and `/do-close/<eid>` use GET methods for state-changing operations, making CSRF exploitation trivial (no form submission needed — a simple `<img>` tag suffices).

5. **File Serving Pipeline**: Documents flow from upload (not shown in provided code) → storage in `DOCSDIR` → serving via `serve_doc()` with no content inspection or antivirus scanning at any stage.

## Cross-cutting Recommendations

### Immediate (Critical/High Priority)

1. **Implement authorization checks**: Replace all `### check authz` comments with actual ownership/permission verification:
   ```python
   if election.get_metadata().owner_pid != result.uid:
       quart.abort(403)
   ```

2. **Implement CSRF protection**: Replace placeholder token with a real implementation (e.g., `quart-csrf` or similar), and validate on all POST endpoints.

3. **Convert state-changing GETs to POSTs**: `/do-open/<eid>` and `/do-close/<eid>` must use POST method with CSRF validation.

4. **Document authentication pathways**: Create a security document covering all auth levels, their required controls, and configuration guidance.

### Short-term (Medium Priority)

5. **Implement rate limiting**: Add request rate limiting middleware (e.g., `quart-rate-limiter`) with documented thresholds for authentication, voting, and administrative operations.

6. **Add antivirus scanning**: Integrate ClamAV or similar scanner into the document upload pipeline and/or download endpoint.

7. **Validate `docname` in `serve_doc()`**: While `send_from_directory` provides some protection, add explicit path traversal validation:
   ```python
   safe_path = (DOCSDIR / iid / docname).resolve()
   if not safe_path.is_relative_to(DOCSDIR / iid):
       quart.abort(404)
   ```

8. **Document context-specific password blocklist**: Even though authentication is delegated, document the blocklist and communicate it to the ASF identity management team.

### Long-term (Low Priority)

9. **Remove debug statements**: Remove `print('FORM:', form)` from production endpoints.

10. **Add Content-Security-Policy headers**: For served documents, add appropriate headers to prevent execution of embedded scripts.

11. **Implement comprehensive security documentation**: Cover all ASVS documentation requirements in a single security architecture document.
        