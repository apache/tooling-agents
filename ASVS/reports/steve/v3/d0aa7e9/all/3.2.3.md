# Security Audit Report: ASVS 3.2.3

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 3.2.3 |
| **Files Analyzed** | 10 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that the application avoids DOM clobbering when using client-side JavaScript by employing explicit variable declarations, performing strict type checking, avoiding storing global variables on the document object, and implementing namespace isolation.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 1 |
| 🟡 Medium | 1 |
| 🟢 Low | 1 |

---

## ASVS-3.2.3: DOM Clobbering Prevention

### Findings for 3.2.3

### [MEDIUM]
**Finding ID:** ASVS-323-MED-001

**Title:** Global Variable Declarations Without Namespace Isolation in Management Templates

**File:** `v3/server/templates/manage.ezt`, JavaScript section (approximately lines 240-360)

**Vulnerable code:**
```javascript
const openModal = new bootstrap.Modal(document.getElementById('openConfirmModal'));
const closeModal = new bootstrap.Modal(document.getElementById('closeConfirmModal'));
const dateSaveErrorModal = new bootstrap.Modal(document.getElementById('dateSaveErrorModal'));
const savedToast = new bootstrap.Toast(document.getElementById('saved-toast'));

const csrfToken = document.getElementById('csrf-token').value;
```

**Analysis:** These variables are declared at the top level of a `<script>` block (global scope). If an attacker can inject HTML with matching `id` attributes (e.g., via unsanitized issue descriptions rendered in the same page), they could clobber `document.getElementById()` results.

The description rendering:
```html
<div id="description-[issues.iid]" class="description mt-2">[issues.description]</div>
```

Since `[issues.description]` is rendered as raw HTML (see ASVS-322-HIGH-001), an attacker could inject:
```html
<form id="csrf-token"><input name="value" value="attacker-controlled"></form>
```

This would cause `document.getElementById('csrf-token').value` to return the attacker's value instead of the real CSRF token.

**Impact:** DOM clobbering could override the CSRF token variable, security-related DOM lookups, or modal instances. Combined with the stored XSS in descriptions, this enables manipulation of the application's client-side security controls.

**Remediation:**
```javascript
(function() {
  'use strict';
  
  // Use explicit null checks and type validation
  const csrfEl = document.getElementById('csrf-token');
  if (!csrfEl || csrfEl.tagName !== 'INPUT') {
    throw new Error('Security control missing: csrf-token element');
  }
  const csrfToken = csrfEl.value;
  
  // Wrap all functionality in IIFE
  // ... rest of code ...
})();
```

---

### [LOW]
**Finding ID:** ASVS-323-LOW-001

**Title:** steve.js Functions Defined in Global Scope Without Namespace Isolation

**File:** `v3/server/static/js/steve.js`, lines 27-79

**Vulnerable code:**
```javascript
function showModal(modalId) {
  const modal = new bootstrap.Modal(document.getElementById(modalId));
  modal.show();
  return modal;
}

function validateRequiredField(fieldId) {
  const field = document.getElementById(fieldId);
  // ...
}

function submitFormWithLoading(formId, buttonId, loadingText = 'Submitting...') {
  const form = document.getElementById(formId);
  const button = document.getElementById(buttonId);
  // ...
}
```

**Impact:** Global functions can be overridden by DOM clobbering (e.g., `<img name="showModal">` would shadow the function via `window.showModal`). While exploitation requires content injection, the lack of namespace isolation makes the application fragile.

**Remediation:**
```javascript
const STeVe = Object.freeze({
  showModal(modalId) {
    const el = document.getElementById(modalId);
    if (!el || !(el instanceof HTMLElement)) return null;
    const modal = new bootstrap.Modal(el);
    modal.show();
    return modal;
  },
  // ... other methods
});
```

### Security Controls Inventory for 3.2.3

| Control | Location | Status |
|---------|----------|--------|
| IIFE namespace isolation | `vote-on.ezt` JavaScript | ✅ Applied (`(function() { 'use strict'; ... })()`) |
| `let`/`const` declarations | All JS files | ✅ Consistently used |
| IIFE in `manage.ezt` | `manage.ezt` JavaScript | ❌ MISSING |
| IIFE in `manage-stv.ezt` | `manage-stv.ezt` JavaScript | ❌ MISSING |
| Namespace isolation in `steve.js` | `steve.js` | ❌ MISSING |
| Type checking on getElementById results | All JS | ❌ MISSING |
| Avoidance of document object globals | All templates | ⚠️ Partial (voter.ezt uses IIFE, others don't) |

### Positive Patterns for 3.2.3

- The `vote-on.ezt` template wraps its JavaScript in an IIFE with `'use strict'`, providing proper namespace isolation.
- The `vote-on.ezt` template exposes only necessary functions to global scope via explicit `window.functionName` assignments.
- All templates use `const`/`let` instead of `var`, preventing accidental global variable leakage within their scope.

---



## Cross-cutting Architecture Observations

1. **Defense-in-Depth Gap:** The application relies heavily on authentication (`@asfquart.auth.require`) as its primary security boundary but lacks multiple defense layers. There is no CSP, no CSRF validation, no rate limiting, and incomplete authorization — if authentication is bypassed, nearly all controls fail.

2. **Type B Gaps (Control Exists but Not Applied):**
   - CSRF token is generated (`basic.csrf_token = 'placeholder'`) and sent to templates, but NEVER validated server-side on any POST endpoint.
   - The `[format "js,html"]` EZT escaping is used in `manage.ezt` for issue titles in onclick handlers but NOT used in `vote-on.ezt` for STV_CANDIDATES data.
   - Authorization check comments (`### check authz`) exist at 8+ locations but authorization logic is never implemented.

3. **Mixed Security Maturity:** The `vote-on.ezt` template demonstrates good practices (IIFE, escapeHtml, textContent) while other templates (`manage.ezt`, `manage-stv.ezt`) use global scope and raw HTML rendering. The `election.py` module has strong cryptographic vote protection while `pages.py` has fundamental web security gaps.

4. **State-Changing GET Requests:** `/do-open/<eid>` and `/do-close/<eid>` use GET methods for state-changing operations. This violates HTTP semantics and makes CSRF trivially exploitable (no CSRF token needed for GET requests). The `manage.ezt` template triggers these via `window.location.href`, confirming this is by design.

5. **Document Serving as Attack Vector:** The `serve_doc()` endpoint combined with `rewrite_description()` creating links to documents creates a complete attack chain: upload malicious HTML → link is auto-generated in issue description → voters click link → XSS in application origin.

## Cross-cutting Recommendations

### Immediate (Critical/High — deploy blockers)

1. **Fix `rewrite_description()`** to HTML-escape description content before wrapping in `<pre>` tags (ASVS-322-HIGH-001).
2. **Add `Content-Disposition: attachment`** to `serve_doc()` responses and validate `docname` for path traversal (ASVS-321-HIGH-001).
3. **Implement CSRF validation** — the token infrastructure exists but is never verified. Add a decorator for POST endpoints that validates the token from the form/header matches the session value.
4. **Add `[format "js"]` escaping** to all EZT values rendered inside JavaScript string literals in `vote-on.ezt` (ASVS-322-MED-002).

### Short-term (1-2 sprints)

5. **Implement CSP headers** — start with `Content-Security-Policy: default-src 'self'` and nonce-based script allowance. This mitigates multiple XSS vectors.
6. **Configure session cookies** with `Secure`, `HttpOnly`, `SameSite=Lax`, and `__Host-` prefix (ASVS-331-HIGH-001).
7. **Add rate limiting middleware** for vote submission and election creation endpoints (ASVS-242-HIGH-001).
8. **Convert state-changing GET endpoints** (`/do-open/`, `/do-close/`) to POST with CSRF validation.
9. **Wrap `manage.ezt` JavaScript** in IIFE with strict mode and add type checking on `getElementById` results (ASVS-323-MED-001).

### Long-term (architectural improvements)

10. **Implement authorization framework** — replace all `### check authz` placeholders with ownership/permission verification.
11. **Create security documentation** per ASVS 3.1.1, specifying browser requirements and degradation behavior.
12. **Migrate `steve.js`** global functions to a namespaced module object to prevent DOM clobbering.
13. **Add X-Content-Type-Options: nosniff** globally via middleware for all responses.
14. **Implement comprehensive anti-automation** including per-endpoint rate limits, minimum timing between sensitive operations, and account-level throttling.
        