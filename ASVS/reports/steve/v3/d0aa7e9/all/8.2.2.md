# Security Audit Report: ASVS 8.2.2

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 8.2.2 |
| **Files Analyzed** | 13 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that the application ensures that data-specific access is restricted to consumers with explicit permissions to specific data items to mitigate insecure direct object reference (IDOR) and broken object level authorization (BOLA).

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 2 |
| 🟠 High | 1 |
| 🟡 Medium | 0 |
| 🟢 Low | 0 |

---

## ASVS-8.2.2: General Authorization Design — IDOR/BOLA

### Findings for 8.2.2

### [CRITICAL]
**Finding ID**: ASVS-822-CRIT-001  
**Title**: Missing Object-Level Authorization on Election Management Endpoints (IDOR/BOLA)  
**File**: `v3/server/pages.py`, `load_election` decorator (line 165) and all consuming endpoints  

**Vulnerable Code:**
```python
def load_election(func):
    """Decorator to load/pass-argument an Election from EID."""
    @functools.wraps(func)
    async def loader(eid):
        try:
            e = steve.election.Election(DB_FNAME, eid)
        except steve.election.ElectionNotFound:
            ...
        _LOGGER.debug(f'Loaded: {e}')
        ### check authz       # <--- NOT IMPLEMENTED
        return await func(e)
    return loader
```

**Data Flow**: User-controllable `eid` URL parameter → `Election(DB_FNAME, eid)` loads ANY election → no ownership/permission check → mutating operations performed on the loaded election.

**Affected Endpoints (all are Type B — control placeholder exists but NOT called)**:
| Endpoint | Line | Operation |
|----------|------|-----------|
| `GET /manage/<eid>` | 318 | View election details |
| `GET /do-open/<eid>` | 431 | Open election for voting |
| `GET /do-close/<eid>` | 451 | Close election permanently |
| `POST /do-add-issue/<eid>` | 469 | Add ballot issues |
| `POST /do-edit-issue/<eid>/<iid>` | 493 | Modify ballot issues |
| `POST /do-delete-issue/<eid>/<iid>` | 516 | Delete ballot issues |
| `POST /do-set-open_at/<eid>` | 360 | Change election dates |
| `POST /do-set-close_at/<eid>` | 366 | Change election dates |

**Proof of Concept**:
```http
GET /do-close/a1b2c3d4e5 HTTP/1.1
Cookie: <any-valid-committer-session>
```
Any authenticated committer can permanently close another user's election by guessing or enumerating the 10-hex-character EID.

**Impact**: Complete loss of integrity for all elections. Any committer can:
- Prematurely close/open elections they don't own
- Add/remove/modify ballot issues on others' elections
- Tamper with election timing

**Remediation**:
```python
def load_election(func):
    @functools.wraps(func)
    async def loader(eid):
        e = steve.election.Election(DB_FNAME, eid)
        # ... 404 handling ...
        
        # AUTHORIZATION CHECK
        result = await basic_info()
        md = e.get_metadata()
        if md.owner_pid != result.uid:
            # Optionally check md.authz for group-based access
            quart.abort(403)
        
        return await func(e)
    return loader
```

---

### [CRITICAL]
**Finding ID**: ASVS-822-CRIT-002  
**Title**: Missing Voter Eligibility Check in Vote Submission Endpoint  
**File**: `v3/server/pages.py`, `do_vote_endpoint()` (line 372)

**Vulnerable Code:**
```python
@APP.post('/do-vote/<eid>')
@asfquart.auth.require({R.committer})
@load_election
async def do_vote_endpoint(election):
    result = await basic_info()
    ### check authz           # <--- NOT IMPLEMENTED
    form = edict(await quart.request.form)
    # ...
    for iid, votestring in votes.items():
        if iid not in issue_dict:
            await flash_danger(f'Invalid issue ID: {iid}')
            return quart.redirect(f'/vote-on/{election.eid}', code=303)
        try:
            election.add_vote(result.uid, iid, votestring)
```

**Data Flow**: Authenticated committer → submits form to `/do-vote/<eid>` with arbitrary `iid` values → no eligibility check → `add_vote()` calls `q_get_mayvote.first_row(pid, iid)` → if user is NOT eligible, `mayvote` is None → accessing `mayvote.salt` raises AttributeError (500 error, not proper 403).

**Contrast with `vote_on_page` (line 236)** which DOES check eligibility:
```python
election.q_find_issues.perform(result.uid, election.eid)
if not election.q_find_issues.fetchall():
    raise_404(T_BAD_EID, result)
```

**Gap Classification**: Type B — The control exists in the read path (`vote_on_page`) but is NOT replicated in the write path (`do_vote_endpoint`).

**Proof of Concept**:
```http
POST /do-vote/a1b2c3d4e5 HTTP/1.1
Cookie: <valid-committer-session-not-eligible-for-this-election>
Content-Type: application/x-www-form-urlencoded

vote-<valid-iid>=y
```

**Impact**: Implicit failure (500 error) rather than proper authorization denial. If the `add_vote` implementation changes (e.g., to handle None gracefully), unauthorized votes could be recorded.

**Remediation**:
```python
async def do_vote_endpoint(election):
    result = await basic_info()
    # Verify voter eligibility
    election.q_find_issues.perform(result.uid, election.eid)
    if not election.q_find_issues.fetchall():
        quart.abort(403)
    # ... proceed with voting
```

---

### [HIGH]
**Finding ID**: ASVS-822-HIGH-001  
**Title**: `load_election_issue` Decorator Also Missing Authorization  
**File**: `v3/server/pages.py`, `load_election_issue` (line 189)

**Vulnerable Code:**
```python
def load_election_issue(func):
    @functools.wraps(func)
    async def loader(eid, iid):
        try:
            e = steve.election.Election(DB_FNAME, eid)
        except steve.election.ElectionNotFound:
            ...
        ### check authz       # <--- NOT IMPLEMENTED
        try:
            i = e.get_issue(iid)
        ...
```

**Impact**: Same as CRIT-001 but for issue-level endpoints (`/do-edit-issue/<eid>/<iid>`, `/do-delete-issue/<eid>/<iid>`, `/manage-stv/<eid>/<iid>`).

---

### Security Controls Inventory for 8.2.2

| Control | Location | Status |
|---------|----------|--------|
| `@asfquart.auth.require({R.committer})` | All management endpoints | ✅ Applied — verifies authenticated committer |
| Ownership check (`owner_pid`) | `load_election` decorator (line 183) | ❌ **NOT IMPLEMENTED** (placeholder comment only) |
| Voter eligibility check | `vote_on_page` (line 236) | ✅ Applied for READ path |
| Voter eligibility check | `do_vote_endpoint` (line 379) | ❌ **NOT APPLIED** for WRITE path |
| `q_get_mayvote` check | `serve_doc` (line 567) | ✅ Properly checks per-document access |
| `Election.owned_elections()` scoped by PID | `admin_page` (line 287) | ✅ Only shows own elections |
| `Election.open_to_pid()` scoped by PID | `voter_page` (line 147) | ✅ Only shows eligible elections |

### Positive Patterns for 8.2.2

1. **Document access control** (`serve_doc`, line 560): Properly queries `q_get_mayvote` to verify the user has explicit permission to access the specific issue's documents before serving files.
2. **Election listing scoped by PID**: `open_to_pid()`, `owned_elections()`, and `upcoming_to_pid()` all filter by the user's PID at the database query level.
3. **Data model supports IDOR prevention**: The `election.owner_pid` and `election.authz` fields exist in the schema specifically for object-level authorization decisions.

---



## Cross-cutting Architecture Observations

1. **Authorization Architecture Gap (Type B Pattern)**: The application has a clear architectural intent for object-level authorization — the database schema includes `owner_pid` and `authz` columns, `q_owned` queries filter by PID, and `### check authz` comments exist at 8+ locations. However, NONE of these placeholder checks are implemented. This creates false confidence: developers and auditors see the comments and may assume authorization is handled.

2. **Inconsistent Authorization Granularity**: 
   - `/vote-on/<eid>` (GET) verifies voter eligibility via `q_find_issues`
   - `/do-vote/<eid>` (POST) does NOT verify voter eligibility before attempting the vote
   - `/serve_doc/<iid>/<docname>` (GET) properly checks `q_get_mayvote`
   - Management endpoints check NOTHING beyond role

3. **GET for State Changes**: Using HTTP GET for `do-open` and `do-close` violates REST semantics and eliminates CSRF protection even if tokens were properly implemented (CSRF tokens protect POST/PUT/DELETE, not GET).

4. **Placeholder Security Controls**: The CSRF token system illustrates a dangerous anti-pattern — templates include tokens, JavaScript sends them in headers, but the server never validates them. This provides zero actual security while appearing secure during code review.

5. **Assertion-Based State Enforcement**: The `election.py` model uses Python `assert` statements for state checks. While these work in development, `assert` statements are removed when Python runs with optimization (`-O` flag). Production deployments MUST NOT use `-O` or these security checks disappear silently.

---

## Cross-cutting Recommendations

### Immediate (Critical — Address Before Production)

1. **Implement Object-Level Authorization**: Add ownership/authz checks in `load_election` and `load_election_issue` decorators. Compare `election.owner_pid` against `result.uid` (or check `election.authz` group membership).

2. **Implement CSRF Protection**: Replace the placeholder token with real per-session tokens. Add server-side validation in a `@before_request` handler or decorator for all POST endpoints.

3. **Change `do-open`/`do-close` to POST**: These perform irreversible state changes and must not be accessible via GET requests.

4. **Add Eligibility Check to `do_vote_endpoint`**: Replicate the `q_find_issues` check from `vote_on_page` before processing votes.

### Short-term (High Priority)

5. **Replace `assert` with Explicit Checks**: Replace `assert self.is_editable()` patterns with explicit `if not self.is_editable(): raise ElectionBadState(...)` to ensure enforcement regardless of Python optimization flags.

6. **Add Comprehensive Access Logging**: Log authorization denials (not just successful actions) to support security monitoring.

7. **Validate `docname` in `serve_doc`**: While `send_from_directory` likely prevents path traversal, add explicit validation per the code comment (`### verify the propriety of DOCNAME`).

### Long-term (Architecture Enhancement)

8. **Implement Adaptive Controls**: For Level 3 compliance, add IP/device tracking to sessions with re-authentication on context changes.

9. **Centralize Authorization Logic**: Create a dedicated authorization module rather than scattering checks across decorators and handlers. Consider a policy-based approach (e.g., checking owner, authz group, and election state in one call).

10. **Add Real-Time State Notifications**: Implement WebSocket or polling to notify active voting sessions when an election closes, preventing wasted user effort.
        