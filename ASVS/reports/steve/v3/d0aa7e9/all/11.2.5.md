# Security Audit Report: ASVS 11.2.5

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 11.2.5 |
| **Files Analyzed** | 5 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that all cryptographic modules fail securely, and errors are handled in a way that does not enable vulnerabilities, such as Padding Oracle attacks.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---

## ASVS-11.2.5: Secure Cryptographic Failure

### Findings for 11.2.5

### [MEDIUM]

**Finding ID:** ASVS-1125-MED-001

**File:** `v3/steve/election.py`, function `tally_issue()`, lines 283-290

**Vulnerable Code:**
```python
# Get the original votestring using the token/salt.
votestring = crypto.decrypt_votestring(
    vote_token,
    mayvote.salt,
    row.ciphertext,
)
votes.append(votestring)
```

**And in `crypto.py`:**
```python
def decrypt_votestring(vote_token: bytes, salt: bytes, ciphertext: bytes) -> str:
    "Decrypt CIPHERTEXT into a VOTESTRING."

    b64key = _b64_vote_key(vote_token, salt)
    f = cryptography.fernet.Fernet(b64key)
    return f.decrypt(ciphertext).decode()
```

**Data Flow:** `ciphertext` from database → `Fernet.decrypt()` → no exception handling → unhandled `InvalidToken` propagates → entire tally operation fails

**Impact:** If any single vote's ciphertext is corrupted (database corruption, malicious tampering with the SQLite file), the entire `tally_issue()` operation will fail with an unhandled `cryptography.fernet.InvalidToken` exception. This is a denial-of-service vector against the tallying function. While Fernet's encrypt-then-MAC construction prevents Padding Oracle attacks specifically (HMAC is verified before decryption), the lack of error handling means:
1. A single corrupted vote prevents ALL votes from being tallied
2. The exception type/message could potentially differentiate between HMAC failure and other issues in some library versions

**Remediation:**
```python
def tally_issue(self, iid):
    # ... existing code ...
    
    for mayvote in self.q_tally.fetchall():
        vote_token = crypto.gen_vote_token(
            md.opened_key, mayvote.pid, iid, mayvote.salt,
        )
        row = self.q_recent_vote.first_row(vote_token)
        if row is None:
            continue

        voters.add(mayvote.pid)

        try:
            votestring = crypto.decrypt_votestring(
                vote_token, mayvote.salt, row.ciphertext,
            )
            votes.append(votestring)
        except Exception:
            # Log the failure generically without exposing details
            _LOGGER.error(f'Failed to decrypt vote for issue {iid} - vote skipped')
            # Do NOT include exception details that could differentiate
            # between HMAC failure vs decryption failure
            continue
```

---

### Security Controls Inventory for 11.2.5

| Control | Location | Secure Failure? | Status |
|---------|----------|----------------|--------|
| Fernet encrypt-then-MAC | `cryptography` library | ✅ HMAC verified before decrypt | Compliant (Padding Oracle prevented) |
| Decryption error handling | `election.py:tally_issue()` | ❌ No try/except | Gap - DoS risk |
| Election state assertions | `election.py` multiple | ✅ AssertionError on invalid state | Acceptable |

### Positive Patterns for 11.2.5

1. **Fernet's encrypt-then-MAC design**: The `cryptography.fernet.Fernet` class verifies HMAC-SHA256 before attempting AES-CBC decryption, completely preventing Padding Oracle attacks. The `InvalidToken` exception is raised uniformly regardless of whether the HMAC or the token format check failed.
2. **Single exception type**: Fernet raises only `InvalidToken` for all failure modes, preventing error-based oracle attacks.
3. **State assertions**: The election module uses `assert` statements and `_all_metadata(required_state=...)` to enforce state machine transitions, preventing cryptographic operations from being called in invalid states.

---



## Cross-cutting Architecture Observations

1. **Cryptographic key hierarchy is well-designed**: The system uses a layered key derivation approach: Election data → BLAKE2b → Argon2 → `opened_key` → per-voter tokens → per-vote encryption keys. This provides good key separation and ensures that compromise of one vote doesn't compromise others.

2. **Incomplete migration from Fernet to AEAD**: The codebase is in a transitional state, as evidenced by the `### still using Fernet now, but will switch soon` comment and the `info=b'xchacha20_key'` parameter. The HKDF derives 32 bytes intended for XChaCha20-Poly1305 but actually feeds it to Fernet which only uses 16 bytes for AES-128. This means the full key material isn't utilized.

3. **Security margin inconsistency**: The system uses strong 256-bit outputs from Argon2 and HKDF but then feeds them into Fernet which only provides 128-bit encryption security (AES-128). The 40-bit IDs (`create_id()`) are the weakest link in the entropy chain.

4. **Side-channel considerations partially addressed**: The use of `secrets` module throughout is excellent. However, Argon2d and non-constant-time comparison in `is_tampered()` show that side-channel resistance wasn't uniformly applied.

5. **Fernet's timestamp exposure**: Fernet tokens include a plaintext timestamp of when encryption occurred. In a voting system, this could leak information about when a specific vote was cast, potentially de-anonymizing voters if combined with server access logs.

## Cross-cutting Recommendations

### Immediate (Before Next Release)
1. **Fix `is_tampered()` to use `hmac.compare_digest()`** — Simple one-line fix that eliminates the timing oracle (ASVS-1124-HIGH-001).
2. **Switch from Argon2d to Argon2id** — Change `Type.D` to `Type.ID` in `_hash()` to gain side-channel resistance (ASVS-1124-MED-001).
3. **Add error handling around `decrypt_votestring()`** — Wrap in try/except to prevent DoS via corrupted votes (ASVS-1125-MED-001).

### Short-term (Next Development Cycle)
4. **Complete migration to XChaCha20-Poly1305** — Replace Fernet with a proper AEAD cipher as already planned. This addresses ASVS 11.3.2 and eliminates the key material waste (ASVS-1132-MED-001).
5. **Increase `create_id()` entropy to at least 128 bits** — Use `secrets.token_hex(16)` for 32-character hex IDs, updating schema constraints accordingly (ASVS-1123-MED-001).

### Long-term (Architecture)
6. **Implement a unified cryptographic abstraction layer** — Create a single module interface for all crypto operations with versioning, allowing cipher agility without code changes throughout the application.
7. **Add integration tests for timing resistance** — Automated tests that verify constant-time behavior of security-critical comparisons.
8. **Consider removing Fernet timestamp from vote tokens** — Or migrate to a scheme that doesn't embed plaintext metadata in ciphertext.
        