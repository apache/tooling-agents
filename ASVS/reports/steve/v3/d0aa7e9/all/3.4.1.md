# Security Audit Report: ASVS 3.4.1

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 3.4.1 |
| **Files Analyzed** | 11 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that a Strict-Transport-Security header field is included on all responses to enforce an HTTP Strict Transport Security (HSTS) policy. A maximum age of at least 1 year must be defined, and for L2 and up, the policy must apply to all subdomains as well.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 1 |
| 🟡 Medium | 0 |
| 🟢 Low | 0 |

---

## ASVS-3.4.1: Strict-Transport-Security (HSTS) Header

### Findings for 3.4.1

#### [HIGH]
**Finding ID**: ASVS-341-HIGH-001

**Title**: No Strict-Transport-Security header configured

**File**: `v3/server/main.py` (entire file) and `v3/server/pages.py` (entire file)

**Vulnerable code quote**:
```python
# main.py - TLS is configured but no HSTS header
kwargs = {}
if app.cfg.server.certfile:
    kwargs['certfile'] = CERTS_DIR / app.cfg.server.certfile
    kwargs['keyfile'] = CERTS_DIR / app.cfg.server.keyfile
    extra_files.update((kwargs['certfile'], kwargs['keyfile']))

# Spool up the app!
app.runx(port=app.cfg.server.port, extra_files=extra_files, **kwargs)
```

**Data flow**: Application serves HTTPS (certfile configured) → HTTP responses sent to browser → **NO `Strict-Transport-Security` header** → browser does not cache HTTPS-only requirement → user vulnerable to SSL stripping on subsequent visits.

**Entry points affected**: ALL responses (every route defined in `pages.py`) — 21+ endpoints.

**Proof of concept**:
```
# User visits http://steve.example.org (before HTTPS redirect)
# Attacker on same network performs SSL strip attack
# Without HSTS, browser has no memory that this site requires HTTPS
```

**Impact**: 
- First visit (or after cache expiry): User can be MITM'd via SSL stripping
- Session cookies could be intercepted if `Secure` flag is also missing
- For L2 compliance: `includeSubDomains` directive is also required but absent
- Voting system integrity compromised — an attacker could intercept/modify votes in transit

**Gap Classification**: Type A — No control exists anywhere in the codebase for HSTS.

**Remediation**:
```python
# Option 1: After-request handler
@APP.after_request
async def set_security_headers(response):
    response.headers['Strict-Transport-Security'] = 'max-age=31536000; includeSubDomains'
    return response

# Option 2: In app construction (if asfquart supports it)
# app.config['HSTS_MAX_AGE'] = 31536000
# app.config['HSTS_INCLUDE_SUBDOMAINS'] = True
```

**ASVS Specifics**: 
- Minimum `max-age`: 31536000 (1 year) ✗ Not present
- `includeSubDomains` for L2: ✗ Not present
- On all responses: ✗ Not present

### Security Controls Inventory for 3.4.1

| Control | Location | Status |
|---------|----------|--------|
| HSTS header | Not found anywhere | **MISSING** |
| TLS configuration | `main.py` lines 75-78 | **PRESENT** (conditional) |
| After-request security headers | Not found | **MISSING** |
| Framework-level HSTS config | Not found | **MISSING** |

### Positive Patterns for 3.4.1

- TLS is supported and configurable via `certfile`/`keyfile` in `main.py` — the infrastructure for HTTPS exists
- The application uses OAuth callbacks over HTTPS (`https://oauth.apache.org/...` in `main.py` lines 36-40)

---



## Cross-cutting Architecture Observations

1. **Session Management Framework Opacity**: The `asfquart` framework handles session cookies, but its configuration is not visible in the audited code. Critical security attributes (SameSite, HttpOnly, Secure, cookie name) are delegated to the framework with no explicit override. This creates an audit gap — the security posture depends on undocumented framework defaults.

2. **No Security Header Middleware**: The application lacks any `after_request` handler or middleware for setting security headers. This means HSTS, CSP, X-Content-Type-Options, X-Frame-Options, and other security headers are all absent from responses.

3. **CSRF Defense Completely Absent**: The combination of:
   - Placeholder CSRF token (`'placeholder'`)
   - No SameSite cookie attribute (verified)
   - State-changing GET endpoints (`/do-open`, `/do-close`)
   
   Creates a complete absence of CSRF protection for the voting system. This is the most critical cross-cutting concern.

4. **Defense-in-Depth Gap**: The application relies solely on authentication (`@asfquart.auth.require`) for security. There are no layered defenses (CSRF, origin checking, SameSite cookies, authorization checks). If any single layer is bypassed, there is no fallback.

5. **Acknowledged Technical Debt**: The `TODO.md` and inline `### check authz` comments indicate the development team is aware of security gaps but has not yet addressed them. The code appears to be in active development with security controls planned but not implemented.

---

## Cross-cutting Recommendations

### Immediate (Before Any Production Deployment)

1. **Add Security Headers Middleware** (addresses ASVS 3.4.1):
```python
@APP.after_request
async def security_headers(response):
    response.headers['Strict-Transport-Security'] = 'max-age=31536000; includeSubDomains'
    response.headers['X-Content-Type-Options'] = 'nosniff'
    response.headers['X-Frame-Options'] = 'DENY'
    return response
```

2. **Configure Session Cookie Securely** (addresses ASVS 3.3.2, 3.3.3, 3.3.4):
```python
app.config.update(
    SESSION_COOKIE_NAME='__Host-steve_session',
    SESSION_COOKIE_HTTPONLY=True,
    SESSION_COOKIE_SECURE=True,
    SESSION_COOKIE_SAMESITE='Strict',
    SESSION_COOKIE_PATH='/',
)
```

3. **Convert State-Changing GET Endpoints to POST** (addresses ASVS 3.3.2):
   - `/do-open/<eid>` → POST with CSRF token
   - `/do-close/<eid>` → POST with CSRF token

### Short-term (Within Next Sprint)

4. **Implement Real CSRF Protection** (supports ASVS 3.3.2):
   - Generate cryptographically random tokens per session
   - Validate tokens on all state-changing endpoints
   - Combine with `SameSite=Strict` for defense-in-depth

5. **Add Cookie Size Monitoring** (addresses ASVS 3.3.5):
   - Log warnings if session cookie approaches 4096 bytes
   - Consider server-side session storage if flash messages grow

### Long-term (Architecture)

6. **Audit asfquart Framework**: Review the `asfquart` framework source code to verify default cookie attributes, as the application delegates all cookie security to this dependency.

7. **Implement Authorization Layer**: Address the `### check authz` placeholders throughout the codebase to ensure election owners/authorized groups are properly validated, complementing the cookie security controls.

8. **Security Testing**: Add integration tests that verify response headers include HSTS, cookie attributes include SameSite/HttpOnly/Secure, and no CORS headers leak on sensitive endpoints.
        