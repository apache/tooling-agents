# Security Audit Report: ASVS 13.4.5

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 13.4.5 |
| **Files Analyzed** | 8 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that documentation (such as for internal APIs) and monitoring endpoints are not exposed unless explicitly intended.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 1 |

---

## ASVS-13.4.5: Unintended Information Leakage - Documentation/Monitoring Endpoints

### Findings for 13.4.5

#### [MEDIUM]
**Finding ID**: ASVS-1345-MED-001

**File**: `v3/server/pages.py`, function `serve_doc`, line 560-574

**Vulnerable Code**:
```python
@APP.get('/docs/<iid>/<docname>')
@asfquart.auth.require  # fine-grained per-doc authz within the handler
async def serve_doc(iid, docname):
    result = await basic_info()  # get PID  ### grr: called uid

    db = steve.election.Election.open_database(DB_FNAME)
    row = db.q_get_mayvote.first_row(result.uid, iid)
    if not row:
        # Nothing fancy. Just pretend the file does not exist
        quart.abort(404)
        # NOTREACHED

    ### verify the propriety of DOCNAME.

    # Return per-issue document.
    return await quart.send_from_directory(DOCSDIR / iid, docname)
```

**Data Flow**: User request to `/docs/<iid>/<docname>` → `docname` unvalidated → `send_from_directory()` serves arbitrary file

**Issue**: The comment `### verify the propriety of DOCNAME.` indicates awareness of needed validation but no implementation exists (Type B gap - control planned but not implemented). Without validation, any file in the `DOCSDIR/iid/` directory can be served regardless of type, potentially including configuration files, internal documentation, or source code placed alongside legitimate documents.

**Proof of Concept**: If an admin places a README, configuration file, or internal notes in a document directory:
```
GET /docs/a1b2c3d4e5/README.md HTTP/1.1
GET /docs/a1b2c3d4e5/.gitkeep HTTP/1.1
GET /docs/a1b2c3d4e5/internal-notes.txt HTTP/1.1
```

**Impact**: Unintended disclosure of internal documentation or files co-located with ballot documents.

**Remediation**:
```python
import os

ALLOWED_DOC_EXTENSIONS = {'.pdf', '.txt', '.html', '.png', '.jpg'}

@APP.get('/docs/<iid>/<docname>')
@asfquart.auth.require
async def serve_doc(iid, docname):
    result = await basic_info()

    # Validate extension
    _, ext = os.path.splitext(docname)
    if ext.lower() not in ALLOWED_DOC_EXTENSIONS:
        quart.abort(404)

    # Validate no path traversal characters
    if '..' in docname or '/' in docname or '\\' in docname:
        quart.abort(400)

    db = steve.election.Election.open_database(DB_FNAME)
    row = db.q_get_mayvote.first_row(result.uid, iid)
    if not row:
        quart.abort(404)

    return await quart.send_from_directory(DOCSDIR / iid, docname)
```

---

#### [LOW]
**Finding ID**: ASVS-1345-LOW-001

**File**: `v3/docs/schema.md` (repository-level documentation)

**Relevant Code**:
```markdown
## Key Features
- **Security**:
  - Cryptographic fields ensure vote and election integrity:
    - `salt` (16 bytes) in `election` and `mayvote` uses `secrets.token_bytes()`.
    - `opened_key` (32 bytes) in `election` uses Argon2 to hash election data for tamper detection.
    - `vote_token` (32 bytes) in `vote` uses Argon2 to hash person-issue pairs.
    - `ciphertext` in `vote` uses Fernet encryption (to be replaced with XChaCha20-Poly1305).
```

**Issue**: While this documentation file is NOT in a served directory (`v3/docs/` vs `v3/server/docs/`), it contains detailed internal architecture and security mechanism information. If deployment procedures are not carefully controlled, this could end up accessible. The `TODO.md` file explicitly lists security vulnerabilities (CSRF placeholder, path traversal risk).

**Impact**: If deployment is misconfigured or the repository root becomes web-accessible, attackers gain detailed knowledge of cryptographic primitives, parameters, and known vulnerabilities.

**Remediation**: Ensure deployment procedures explicitly exclude non-served documentation. Add a `.dockerignore` or deployment manifest that only includes required runtime files.

### Security Controls Inventory for 13.4.5

| Control | Location | Coverage Status |
|---------|----------|-----------------|
| Authentication on /docs endpoint | `pages.py:560` (`@asfquart.auth.require`) | Applied |
| Mayvote authorization check | `pages.py:567` (`q_get_mayvote.first_row`) | Applied |
| No monitoring/health/metrics endpoints | N/A | Positive (not present) |
| No Swagger/OpenAPI documentation endpoint | N/A | Positive (not present) |
| No debug/profiling endpoints | N/A | Positive (not present) |
| Document name validation | `pages.py:571` | **NOT IMPLEMENTED** (comment only) |

### Positive Patterns for 13.4.5

1. **No monitoring endpoints exposed**: The application has no `/health`, `/metrics`, `/status`, or similar monitoring endpoints that could leak information.
2. **No API documentation endpoints**: No Swagger, OpenAPI, or similar auto-generated documentation is served.
3. **Documentation separated from served content**: `v3/docs/schema.md` is at a different path than `DOCSDIR` (`v3/server/docs/`).
4. **Authentication required for document serving**: The `/docs/` endpoint requires a valid session and checks voter eligibility before serving documents.

---



## Cross-cutting Architecture Observations

1. **Type B Gap Pattern (Control exists but not called)**: The most pervasive security issue across this codebase is the `### check authz` pattern. Authorization logic is acknowledged as needed (via comments) throughout `pages.py` but is never implemented. This creates false confidence — the code appears to have considered authorization, but any authenticated committer can manage any election, open/close any election, or manipulate any issue regardless of ownership.

2. **Documentation vs. Implementation Asymmetry**: The `schema.md` documentation describes a well-thought-out security architecture (non-predictable IDs, encryption, anti-tamper), but the running application has significant gaps (CSRF placeholder, missing authz, unvalidated file serving).

3. **Positive Crypto Architecture**: The cryptographic design (Argon2 for key derivation, per-voter salts, Fernet encryption, vote shuffling) is well-implemented in `crypto.py` and `election.py`. The separation of concerns (crypto operations isolated in one module) supports auditability.

4. **Data Leakage Prevention in Queries**: The `queries.yaml` demonstrates conscious effort to prevent sensitive data leakage — public-facing queries explicitly exclude `salt` and `opened_key` columns, with comments documenting this decision. The `_all_metadata()` method is correctly marked as internal-only.

5. **Incomplete Defense-in-Depth**: Authentication is applied (decorators on all sensitive endpoints), but authorization (who can do what to which resource) and input validation (file extensions, path components) represent the missing layers.

---

## Cross-cutting Recommendations

### Immediate (Critical/High Priority)

1. **Convert state-changing GET endpoints to POST**: Change `/do-open/<eid>` and `/do-close/<eid>` from GET to POST, requiring form submission with CSRF tokens. This addresses both ASVS 14.2.1 (operations in URLs) and CSRF vulnerability.

2. **Implement file extension whitelist**: Add allowed extension sets for both `/static/` and `/docs/` endpoints. This directly addresses ASVS 13.4.7 and prevents accidental file disclosure.

3. **Validate `docname` parameter**: Implement the already-planned validation noted by the `### verify the propriety of DOCNAME` comment. Block path traversal characters and restrict to allowed extensions.

### Short-term (Within Sprint)

4. **Suppress server version headers**: Add an `@APP.after_request` handler to remove `Server` and `X-Powered-By` headers, or configure Hypercorn to disable server header inclusion (ASVS 13.4.6).

5. **Implement authorization checks**: Replace all `### check authz` comments with actual ownership/permission verification. At minimum, verify `election.owner_pid == result.uid` for management operations.

6. **Create data classification document**: Formally document all sensitive data types, their protection levels, and required controls (ASVS 14.1.1, 14.1.2).

### Long-term (Architecture)

7. **Implement CSRF protection**: Replace `basic.csrf_token = 'placeholder'` with cryptographically-generated per-session tokens validated on all state-changing endpoints.

8. **Create comprehensive data protection policy**: Document encryption requirements, retention periods, logging policies, key management lifecycle, and privacy controls for each data sensitivity level.

9. **Add security headers middleware**: Implement `Content-Security-Policy`, `X-Content-Type-Options: nosniff`, `X-Frame-Options`, and `Strict-Transport-Security` headers to complement the data protection controls.
        