# Security Audit Report: ASVS 6.3.1

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 6.3.1 |
| **Files Analyzed** | 4 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that controls to prevent attacks such as credential stuffing and password brute force are implemented according to the application's security documentation.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 1 |

---

## ASVS-6.3.1: Controls to Prevent Credential Stuffing and Brute Force

### Findings for 6.3.1

### [MEDIUM]

**Finding ID:** ASVS-631-MED-001

**Title:** No Rate Limiting or Brute Force Protection on Authenticated Endpoints

**File:** `v3/server/pages.py` — multiple endpoints

**Vulnerable code:**

```python
@APP.post('/do-vote/<eid>')
@asfquart.auth.require({R.committer})
@load_election
async def do_vote_endpoint(election):
    result = await basic_info()
    ### check authz
    form = edict(await quart.request.form)
    # ... no rate limiting ...
```

**Data flow:** HTTP request → Quart framework → `asfquart.auth.require` → endpoint handler (no throttling between auth check and sensitive operation)

**Analysis:** While authentication is delegated to `asfquart.auth`, there are no visible rate-limiting controls within the application layer itself. This matters because:

1. **Session-authenticated endpoints** have no request throttling after authentication
2. **State-changing GET endpoints** (`/do-open/<eid>`, `/do-close/<eid>`) are particularly susceptible to automated replay
3. **No CSRF protection** (token is `'placeholder'` on line 83) compounds this by allowing cross-site automated requests

```python
# pages.py line 83
basic.csrf_token = 'placeholder'
```

4. **No account lockout or anomaly detection** for rapid consecutive requests from authenticated sessions

**Impact:** An attacker who compromises a valid session cookie could automate election manipulation (open/close elections, submit votes, delete issues) without any throttling defense. The lack of CSRF protection means even without session compromise, a victim's browser could be used to automate requests.

**Proof of concept:** After obtaining a valid session (or exploiting CSRF):
```bash
# Rapid automated election manipulation - no rate limiting stops this
for eid in $(seq 1000); do
  curl -X GET "https://target/do-close/$(printf '%010x' $eid)" \
    -H "Cookie: session=<valid_session>"
done
```

**Remediation:**
```python
from quart_rate_limiter import RateLimiter, rate_limit

rate_limiter = RateLimiter(APP)

@APP.post('/do-vote/<eid>')
@asfquart.auth.require({R.committer})
@rate_limit(10, timedelta(minutes=1))  # 10 votes per minute max
@load_election
async def do_vote_endpoint(election):
    ...
```

---

### [LOW]

**Finding ID:** ASVS-631-LOW-001

**Title:** State-Changing Operations on GET Requests Enable Easier Automated Attacks

**File:** `v3/server/pages.py`, lines 431 and 451

**Vulnerable code:**

```python
@APP.get('/do-open/<eid>')
@asfquart.auth.require({R.committer})  ### need general solution
@load_election
async def do_open_endpoint(election):
    # ...
    election.open(pdb)
    # ...

@APP.get('/do-close/<eid>')
@asfquart.auth.require({R.committer})  ### need general solution
@load_election
async def do_close_endpoint(election):
    # ...
    election.close()
    # ...
```

**Impact:** GET requests for state-changing operations can be triggered via image tags, link prefetching, or browser history attacks. Combined with no rate limiting, this makes automated attacks trivial.

**Remediation:** Convert to POST requests and implement CSRF protection:
```python
@APP.post('/do-open/<eid>')
@asfquart.auth.require({R.committer})
@validate_csrf_token
@load_election
async def do_open_endpoint(election):
    ...
```

### Security Controls Inventory for 6.3.1

| Control | Location | Coverage | Status |
|---------|----------|----------|--------|
| Authentication requirement | `@asfquart.auth.require` decorators | All sensitive endpoints | ✅ Applied |
| Rate limiting | Not found | No endpoints | ❌ Missing (Type A gap) |
| CSRF protection | `pages.py:83` | Placeholder only | ❌ Not functional (Type B gap — control exists but is placeholder) |
| Account lockout | Not found | No endpoints | ❌ Missing (Type A gap) |
| Request throttling | Not found | No endpoints | ❌ Missing (Type A gap) |

### Positive Patterns for 6.3.1

- All sensitive endpoints require authentication via `@asfquart.auth.require({R.committer})` or `{R.pmc_member}`.
- Election state assertions (`assert self.is_editable()`, `assert self.is_open()`) in `election.py` provide defense-in-depth against invalid state transitions.
- The external ASF authentication system likely has its own brute force protections (not verifiable from this code).

---



## Cross-cutting Architecture Observations

1. **Delegated Authentication:** The application entirely delegates authentication to the `asfquart.auth` module (ASF OAuth/CAS). This means password-specific ASVS requirements (6.2.6–6.2.9) are not directly applicable to this codebase but would need to be verified against the external identity provider.

2. **Authorization Gap Pattern:** Multiple endpoints have `### check authz` placeholder comments without implementation. While authentication (authn) is enforced via decorators, authorization (authz — "is this user the owner of this election?") is not verified. This is a cross-cutting concern that affects the trust model around brute-force protections — any authenticated committer can access any election's management functions.

3. **CSRF Placeholder:** The `csrf_token = 'placeholder'` pattern creates a false sense of security. Templates reference it, JavaScript sends it in headers, but no server-side validation occurs. This is a **Type B gap** (control exists but not enforced).

4. **External Auth Boundary:** The security boundary between this application and `asfquart.auth` is critical. Rate limiting, brute force protection, password policies, and account management all depend on the external system's implementation, which is outside the audit scope.

5. **Cryptographic Design (Positive):** The vote encryption scheme using Argon2 key derivation, per-voter salts, and Fernet encryption demonstrates strong cryptographic practices for protecting ballot secrecy. The anti-tamper mechanism (`is_tampered()`) provides integrity verification.

---

## Cross-cutting Recommendations

### Immediate (Critical/High Priority)

1. **Implement real CSRF protection** — Replace the placeholder token with a cryptographically random per-session token and validate it on all state-changing endpoints. This directly impacts ASVS 6.3.1 by preventing automated cross-site attacks.

2. **Convert state-changing GET endpoints to POST** — `/do-open/<eid>` and `/do-close/<eid>` must use POST to prevent trivial automation and link-based attacks.

3. **Implement authorization checks** — Replace all `### check authz` placeholders with actual ownership verification (e.g., verify `result.uid == election.owner_pid`).

### Short-term (Medium Priority)

4. **Add rate limiting** — Implement per-user, per-endpoint rate limiting for sensitive operations (voting, election creation/modification). This addresses ASVS 6.3.1 at the application layer.

5. **Document external auth requirements** — Create security documentation specifying what brute force protections must be provided by the `asfquart.auth` system to satisfy ASVS 6.3.1.

6. **Remove debug prints** — Remove `print('FORM:', form)` statements in `do_add_issue_endpoint` and `do_edit_issue_endpoint`.

### Long-term (Architecture)

7. **Verify external IdP compliance** — Audit the ASF identity provider against ASVS 6.2.6–6.2.9 to ensure password handling requirements are met at the authentication boundary.

8. **Implement request anomaly detection** — Add logging and alerting for unusual patterns (rapid successive requests, bulk operations) to complement rate limiting.

9. **Security documentation** — Create an application-level security document describing the authentication architecture, trust boundaries, and how each ASVS requirement is satisfied (either locally or by the external IdP).
        