# Security Audit Report: ASVS 11.1.2

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 11.1.2 |
| **Files Analyzed** | 8 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that a cryptographic inventory is performed, maintained, regularly updated, and includes all cryptographic keys, algorithms, and certificates used by the application. It must also document where keys can and cannot be used in the system, and the types of data that can and cannot be protected…

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---

## ASVS-11.1.2: Cryptographic Inventory

### Findings for 11.1.2

#### [MEDIUM]
**Finding ID:** ASVS-1112-MED-001  
**Title:** Incomplete Cryptographic Inventory - Missing Algorithm and Key Usage Documentation

**Location:** Project-wide

**Description:**  
While `schema.md` documents some cryptographic assets, it does not constitute a complete cryptographic inventory. The following are not formally inventoried:

1. **Algorithms not documented:**
   - BLAKE2b (64-byte digest, used as pre-hash for Argon2)
   - HKDF-SHA256 (used for key stretching vote_token → vote_key)
   - The specific Fernet composition (AES-128-CBC + HMAC-SHA256)

2. **Key usage boundaries not documented:**
   - Where each key type CAN be used vs. CANNOT be used
   - What data types each key protects
   - Which components have access to which keys

3. **No centralized inventory document:**
   - Crypto information is scattered across `schema.md`, `crypto.py` comments, and `schema.sql` comments

**Evidence of partial documentation in `schema.md`:**
```markdown
- `salt` (16 bytes) in `election` and `mayvote` uses `secrets.token_bytes()`.
- `opened_key` (32 bytes) in `election` uses Argon2 to hash election data for tamper detection.
- `vote_token` (32 bytes) in `vote` uses Argon2 to hash person-issue pairs.
- `ciphertext` in `vote` uses Fernet encryption (to be replaced with XChaCha20-Poly1305).
```

**Missing from inventory:**
```python
# crypto.py - BLAKE2b usage not inventoried
digest = hashlib.blake2b(edata).digest()  # Line 49

# crypto.py - HKDF usage not inventoried  
keymaker = hkdf.HKDF(
    algorithm=hashes.SHA256(),
    length=32,
    salt=salt,
    info=b'xchacha20_key',
)  # Lines 61-66
```

**Impact:** Without a complete inventory, it's impossible to systematically assess cryptographic risk, plan migrations, or ensure all cryptographic assets are properly managed.

**Remediation:**  
Create a dedicated `CRYPTO_INVENTORY.md` document containing:

| Asset | Algorithm | Key Length | Purpose | Protected Data | Access Scope | Location |
|-------|-----------|------------|---------|---------------|--------------|----------|
| Election salt | N/A (random) | 128 bits | Argon2 input | N/A | Server-only | election.salt |
| Opened key | Argon2d(BLAKE2b(edata)) | 256 bits | Tamper detection + vote token derivation | Election integrity | Server-only | election.opened_key |
| Mayvote salt | N/A (random) | 128 bits | Per-voter differentiation | N/A | Server-only | mayvote.salt |
| Vote token | Argon2d(opened_key‖pid‖iid, salt) | 256 bits | Voter identification + key derivation | Voter-issue binding | Server-only | vote.vote_token |
| Vote key | HKDF-SHA256(vote_token, salt) | 256 bits | Vote encryption | Vote plaintext | Ephemeral | Derived in memory |
| Ciphertext | Fernet (AES-128-CBC + HMAC-SHA256) | N/A | Vote confidentiality | Votestring | Stored | vote.ciphertext |

### Security Controls Inventory for 11.1.2

| Control | Location | Status |
|---------|----------|--------|
| Partial crypto documentation | `v3/docs/schema.md` | ⚠️ Incomplete |
| Algorithm comments in code | `v3/steve/crypto.py` | ⚠️ Informal |
| Schema-level documentation | `v3/schema.sql` (comments) | ⚠️ Partial |
| Formal cryptographic inventory | Not found | ✗ Missing |
| Key usage boundary documentation | Not found | ✗ Missing |
| Data classification vs. key mapping | Not found | ✗ Missing |

### Positive Patterns for 11.1.2

- `schema.md` does document the primary cryptographic fields and their purposes
- Code comments in `crypto.py` explain algorithm choices (e.g., "BLAKE2b is the internal primitive of Argon2, so a good fit")
- The `get_metadata()` method explicitly documents that SALT and OPENED_KEY should not be returned publicly
- Database schema comments document key sizes and states

---



## Cross-cutting Architecture Observations

1. **Centralized Crypto Module:** All cryptographic operations are centralized in `v3/steve/crypto.py`. This is excellent for auditability but currently lacks the abstraction needed for agility. The centralization makes future refactoring feasible.

2. **Key Derivation Chain:** The system uses a well-designed multi-level key derivation: `election_data → BLAKE2b → Argon2 → opened_key → (+ pid + iid + salt) → Argon2 → vote_token → HKDF → vote_key → Fernet(vote)`. This provides good key separation and limits blast radius, but the entire chain is hardcoded.

3. **Documentation Gap:** The project has partial documentation (`schema.md`) but lacks formal security/crypto documentation. The existing documentation focuses on database schema rather than cryptographic design decisions.

4. **Anti-Tamper Design:** The `opened_key` serves dual purposes: (1) tamper detection via `is_tampered()` and (2) vote token derivation. This coupling means the anti-tamper mechanism is tied to the same cryptographic algorithm as the voting mechanism — upgrading one requires upgrading both.

5. **No Asymmetric Cryptography:** The system uses only symmetric primitives and key derivation. This reduces PQC urgency compared to systems using RSA/ECC, but the AES-128 effective key length under Grover's algorithm (64-bit) is a concern for long-term data confidentiality.

6. **Inconsistency Between Production and Benchmark:** Production uses Argon2d while the benchmark uses Argon2id, suggesting either incomplete migration or a deliberate but undocumented choice.

## Cross-cutting Recommendations

### Immediate (Sprint-level)
1. **Change Argon2d to Argon2id** in `_hash()` function (ASVS-1121-MED-001). This is a one-line change with significant security improvement. Note: this will change all derived values, so must be coordinated with any existing deployed databases.

2. **Add `crypto_version` column** to the `vote` table to enable future algorithm transitions without data loss.

### Short-term (1-3 months)
3. **Create formal cryptographic inventory document** mapping all algorithms, keys, their purposes, access scopes, and protection requirements (satisfies 11.1.1, 11.1.2).

4. **Implement crypto abstraction layer** in `crypto.py` with version-based algorithm selection and a configuration mechanism for algorithm parameters (satisfies 11.2.2).

5. **Add cryptographic linting** to CI/CD pipeline using `semgrep` or equivalent with rules for:
   - Flagging new crypto imports
   - Detecting weak algorithms
   - Ensuring CSPRNG usage (satisfies 11.1.3)

### Long-term (3-12 months)
6. **Complete Fernet → XChaCha20-Poly1305 migration** with proper versioning, re-encryption tooling, and rollback capability.

7. **Document PQC migration plan** assessing quantum risk for each primitive and establishing upgrade timelines, particularly upgrading to AES-256 for long-term vote confidentiality (satisfies 11.1.4).

8. **Implement key lifecycle management** including maximum key age, scheduled rotation for long-lived keys, and secure key destruction after election archival (satisfies 11.1.1).
        