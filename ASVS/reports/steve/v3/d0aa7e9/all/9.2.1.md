# Security Audit Report: ASVS 9.2.1

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 9.2.1 |
| **Files Analyzed** | 6 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that, if a validity time span is present in the token data, the token and its content are accepted only if the verification time is within this validity time span. For example, for JWTs, the claims 'nbf' and 'exp' must be verified.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 1 |

---

## ASVS-9.2.1: Token Validity Time Span Verification

### Findings for 9.2.1

### [LOW]
**Finding ID:** ASVS-921-LOW-001
**Location:** `v3/steve/crypto.py` - `decrypt_votestring()` (line 74)

**Vulnerable Code:**
```python
def decrypt_votestring(vote_token: bytes, salt: bytes, ciphertext: bytes) -> str:
    "Decrypt CIPHERTEXT into a VOTESTRING."

    b64key = _b64_vote_key(vote_token, salt)
    f = cryptography.fernet.Fernet(b64key)
    return f.decrypt(ciphertext).decode()
```

**Description:** Fernet tokens contain an embedded timestamp and support time-to-live (TTL) validation via the `ttl` parameter to `decrypt()`. The code does not pass a `ttl` value, meaning Fernet tokens are accepted regardless of age.

**Mitigating Factors:**
- This is intentional by design — votes must be decryptable during tallying, which can occur long after voting
- The election state machine (`S_OPEN` → `S_CLOSED`) provides temporal controls at a higher level
- The `_all_metadata(self.S_CLOSED)` assertion in `tally_issue()` ensures tallying only happens when election is closed
- The `_all_metadata(self.S_OPEN)` assertion in `add_vote()` ensures voting only occurs when election is open

**Impact:** Minimal. The Fernet token here serves as encrypted storage for votes, not as an authorization mechanism with temporal validity. The temporal controls are correctly implemented at the application layer via election state management.

**Note:** This is included for completeness but the design decision is appropriate for the use case. The Fernet ciphertext is not a "self-contained token" used for security decisions in the ASVS sense — it's encrypted data at rest.

---

### [MEDIUM]
**Finding ID:** ASVS-921-MED-001
**Location:** `v3/server/pages.py` - `basic_info()` (line 58)

**Vulnerable Code:**
```python
async def basic_info():
    """Return base-level EZT template data."""
    # ...
    s = await asfquart.session.read()
    if s:
        basic.update(
            uid=s['uid'],
            name=s['fullname'],
            email=s['email'],
        )
    # ...
    ### generate a real token and store in the session
    basic.csrf_token = 'placeholder'
```

**Description:** The session reading does not validate any temporal claims within the session. While `asfquart.session` is external and may handle expiration internally, there is no visible session expiration enforcement in the audited code. The comment `### generate a real token` indicates security controls are incomplete.

**Gap Type:** Cannot determine (external dependency) — flagged for verification.

**Impact:** If session tokens lack expiration enforcement, stolen sessions could be used indefinitely.

**Remediation:** Verify that `asfquart.session.read()` validates session expiration. If not, implement session lifetime checks:
```python
s = await asfquart.session.read()
if s:
    if time.time() - s.get('created_at', 0) > MAX_SESSION_AGE:
        await asfquart.session.invalidate()
        return basic  # No authenticated user
```

---

### Security Controls Inventory for 9.2.1

| Control | Location | Status |
|---------|----------|--------|
| Election state temporal control | `election.py` `_all_metadata(required_state)` | IMPLEMENTED — prevents voting on closed elections |
| Fernet TTL validation | `crypto.py` `decrypt_votestring()` | NOT USED (by design — appropriate for vote storage) |
| Session expiration | `asfquart.session` (external) | UNKNOWN — not visible in audited code |

### Positive Patterns for 9.2.1

1. **Application-level temporal controls**: The election state machine (`editable` → `open` → `closed`) provides proper temporal boundaries for operations. `add_vote()` requires `S_OPEN` state, `tally_issue()` requires `S_CLOSED` state.
2. **Trigger-based protection**: The SQL trigger `prevent_open_close_update` prevents modification of `open_at`/`close_at` after an election is closed, maintaining temporal integrity at the database level.

---



## Cross-cutting Architecture Observations

1. **Authorization Model Gap**: The system has a well-designed data model for authorization (`owner_pid`, `authz` fields, `mayvote` table) but the web layer does not enforce ownership boundaries. This creates a Type B vulnerability pattern — the security infrastructure exists but is not applied.

2. **Defense-in-Depth Absence**: The application relies on a single authentication boundary (`R.committer`). Once past this boundary, all operations within the system are available regardless of role or ownership. The only exception is `do_create_election` which requires `R.pmc_member`.

3. **Cryptographic Architecture Strength**: The cryptographic design is sound — Argon2 for key derivation, Fernet for authenticated encryption, HKDF for key stretching, cryptographic shuffling for vote privacy. The security issues are entirely in the authorization/access-control layer, not the cryptographic layer.

4. **No Self-Contained Auth Tokens**: The system does not use JWTs or similar self-contained tokens for authentication/authorization. This means ASVS 9.x requirements for token validation are largely not applicable, with security relying instead on server-side session management (external `asfquart.session`).

5. **State Machine as Security Control**: The election state machine (`editable` → `open` → `closed`) enforced via assertions provides a secondary authorization layer for operations — certain operations are only possible in certain states, regardless of who requests them.

---

## Cross-cutting Recommendations

### Immediate (Critical)

1. **Implement ownership verification in `load_election`**: Compare `election.owner_pid` against `result.uid` and check `authz` group membership. This single change fixes ASVS-841-CRIT-001 across all endpoints using the decorator.

2. **Replace `assert` with proper access control**: Python assertions can be disabled with `-O` flag. Replace `assert self.is_editable()` with proper exception raising:
   ```python
   if not self.is_editable():
       raise ElectionBadState(self.eid, self.get_state(), self.S_EDITABLE)
   ```

3. **Convert state-changing GETs to POSTs**: `/do-open/<eid>` and `/do-close/<eid>` must be POST endpoints to prevent CSRF via simple link/image attacks.

### Short-term (High)

4. **Implement CSRF protection**: Replace placeholder token with cryptographic CSRF tokens validated on all state-changing endpoints.

5. **Create separate admin decorator**: Build a `load_election_admin` decorator that verifies ownership/authz, distinct from voter-facing `load_election`:
   ```python
   def load_election_admin(func):
       """Load election and verify administrative access."""
       @functools.wraps(func)
       async def loader(eid):
           e = steve.election.Election(DB_FNAME, eid)
           result = await basic_info()
           md = e.get_metadata()
           if md.owner_pid != result.uid and not check_authz(result.uid, md.authz):
               quart.abort(403)
           return await func(e)
       return loader
   ```

6. **Verify session token expiration**: Audit `asfquart.session` to confirm tokens have bounded lifetimes.

### Long-term (Architecture)

7. **Implement multi-layer admin security**: For administrative operations, add step-up authentication (re-verification for destructive operations), contextual risk scoring (new IP/device detection), and rate limiting on management operations.

8. **Implement proper RBAC system**: Resolve the `authz` field usage — integrate with LDAP group membership verification to support the multi-tenant model described in the schema documentation.

9. **Add audit logging with context**: Enhance logging to include IP addresses, device fingerprints, and request context for forensic analysis of administrative actions.
        