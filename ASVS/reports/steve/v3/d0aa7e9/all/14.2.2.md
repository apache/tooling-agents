# Security Audit Report: ASVS 14.2.2

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 14.2.2 |
| **Files Analyzed** | 9 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that the application prevents sensitive data from being cached in server components, such as load balancers and application caches, or ensures that the data is securely purged after use.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 1 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---

## ASVS-14.2.2: Sensitive Data Caching Prevention

### Findings for 14.2.2

### [HIGH]

**Finding ID:** ASVS-1422-HIGH-001

**Title:** No Cache-Control Headers on Responses Containing Sensitive Election and Voter Data

**File:** `v3/server/pages.py`  
**Functions:** All authenticated endpoints (lines 133-574)

**Vulnerable Code:**
```python
@APP.get('/vote-on/<eid>')
@asfquart.auth.require({R.committer})
@load_election
@APP.use_template(TEMPLATES / 'vote-on.ezt')
async def vote_on_page(election):
    result = await basic_info()
    # ... returns sensitive election data, voter eligibility, issue details
    # NO cache-control headers set
    return result
```

**Data Flow:** User request → authenticated endpoint → sensitive data (voter eligibility, election crypto state, issue details, person names/emails) → HTTP response → intermediate caches (load balancers, CDN, reverse proxies) **without** `Cache-Control: no-store` or equivalent headers.

**Affected Endpoints:**
- `/voter` — returns open elections with voter eligibility
- `/vote-on/<eid>` — returns election issues, voter status, candidates
- `/admin` — returns owned elections, management data
- `/manage/<eid>` — returns election metadata and issues
- `/docs/<iid>/<docname>` — serves potentially sensitive election documents
- `/profile` — returns user email and identity

**Impact:** Sensitive data including election metadata, voter eligibility status, election cryptographic state indicators (`is_opened`), person names, email addresses, and issue details could be cached by intermediate infrastructure (reverse proxies, CDNs, load balancers). A subsequent user or attacker accessing cached responses could obtain another user's election state or voting eligibility information.

**Remediation:**
```python
import quart

@APP.after_request
async def set_cache_control(response):
    """Prevent caching of authenticated/sensitive responses."""
    # For authenticated pages, prevent all caching
    if response.status_code == 200 and response.content_type and 'text/html' in response.content_type:
        response.headers['Cache-Control'] = 'no-store, no-cache, must-revalidate, private'
        response.headers['Pragma'] = 'no-cache'
        response.headers['Expires'] = '0'
    return response
```

---

### [MEDIUM]

**Finding ID:** ASVS-1422-MED-001

**Title:** Document Serving Endpoint Lacks Cache Prevention Headers for Sensitive Election Documents

**File:** `v3/server/pages.py`, line 574  
**Function:** `serve_doc`

**Vulnerable Code:**
```python
@APP.get('/docs/<iid>/<docname>')
@asfquart.auth.require
async def serve_doc(iid, docname):
    # ...authz check...
    # Returns file without any cache-control headers
    return await quart.send_from_directory(DOCSDIR / iid, docname)
```

**Data Flow:** Authenticated user → authorization check → `send_from_directory` → document response with default caching headers → possible intermediate cache retention.

**Impact:** Election-related documents (referenced via `doc:filename` in issue descriptions) may contain sensitive ballot information. Without cache-control headers, these documents could be retained in proxy caches and served to unauthorized users.

**Remediation:**
```python
@APP.get('/docs/<iid>/<docname>')
@asfquart.auth.require
async def serve_doc(iid, docname):
    result = await basic_info()
    db = steve.election.Election.open_database(DB_FNAME)
    row = db.q_get_mayvote.first_row(result.uid, iid)
    if not row:
        quart.abort(404)
    
    response = await quart.send_from_directory(DOCSDIR / iid, docname)
    response.headers['Cache-Control'] = 'no-store, private'
    return response
```

### Security Controls Inventory for 14.2.2

| Control | Location | Status |
|---------|----------|--------|
| Cache-Control headers on sensitive endpoints | `pages.py` (all endpoints) | **NOT PRESENT** |
| `@APP.after_request` cache middleware | `pages.py` | **NOT PRESENT** |
| Secure purge mechanism | N/A | **NOT PRESENT** |
| Static file caching (appropriate) | `serve_static()` line 577 | Present (framework default) |

### Positive Patterns for 14.2.2

- The application uses authentication decorators on all sensitive endpoints, limiting who can access data in the first place.
- Static assets are served separately from authenticated content via a distinct route (`/static/`), allowing for different caching policies.

---



## Cross-cutting Architecture Observations

1. **Security Control Consistency Gap**: The application has strong cryptographic controls (Argon2, Fernet encryption, BLAKE2b, CSPRNG for IDs and shuffling) but lacks fundamental HTTP-layer security headers (Cache-Control, X-Content-Type-Options). This suggests the security design focused on cryptographic vote integrity but did not address transport/caching layer concerns.

2. **Internal vs. External API Surface**: The `Election._all_metadata()` method is documented as "INTERNAL ONLY" and returns sensitive fields (salt, opened_key), while `get_metadata()` is the safe public API. This separation is correct but relies on convention rather than enforcement (no access modifier, just naming convention with underscore prefix).

3. **Authorization Gap (Cross-cutting)**: Multiple endpoints have `### check authz` placeholder comments. While authentication is enforced via decorators, fine-grained authorization (e.g., only election owner can open/close/manage) is not implemented. This affects multiple ASVS requirements indirectly — if an unauthorized user can access management endpoints, they could view sensitive election data that should be minimized.

4. **Data Flow Segregation**: The system properly segregates sensitive cryptographic material (salt, opened_key) from public-facing queries at the SQL level (separate queries for internal vs. external use). This is a strong architectural pattern.

5. **Template Security**: The EZT templating engine used for HTML rendering requires careful attention to output encoding. The `vote-on.ezt` template includes a client-side `escapeHtml()` function for JavaScript-rendered content, but server-side rendering relies on EZT's default behavior.

6. **Operational Tooling**: The `mail-voters.py` script runs as a standalone process with direct database access. It accesses voter PII (email addresses) without the web application's session/auth layer, relying instead on OS-level access controls to the script and database file.

---

## Cross-cutting Recommendations

### Immediate (Within Sprint)

1. **Remove debug print statements** (`pages.py` lines 481, 497) — Direct PII/data exposure risk.
2. **Add `Cache-Control: no-store, private` header** to all authenticated responses via an `@APP.after_request` handler.
3. **Add `X-Content-Type-Options: nosniff` header** globally.

### Short-term (Within Quarter)

4. **Implement authorization checks** at all `### check authz` placeholder locations — verify the authenticated user is the election owner or in the authorized group before allowing management operations.
5. **Document data retention policy** — classify each data type (elections, votes, persons) with explicit retention periods and deletion triggers.
6. **Implement CSRF token validation** — replace the placeholder token with a real per-session token and validate on all POST endpoints.
7. **Validate `docname` parameter** in `serve_doc()` — restrict to safe filename characters.

### Long-term (Within Year)

8. **Implement automated data purge** — scheduled job to delete closed elections past retention period and purge superseded votes after tally certification.
9. **Complete migration to XChaCha20-Poly1305** with AAD binding vote ciphertext to vote_token.
10. **Implement log classification** — ensure logs containing user-action mappings have appropriate retention limits and access controls distinct from application data.
11. **Add per-response caching policy** — static assets should have appropriate cache headers (`max-age`), while dynamic authenticated content should have `no-store`.
        