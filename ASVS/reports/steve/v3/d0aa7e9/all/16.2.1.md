# Security Audit Report: ASVS 16.2.1

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 16.2.1 |
| **Files Analyzed** | 5 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that each log entry includes necessary metadata (such as when, where, who, what) that would allow for a detailed investigation of the timeline when an event happens.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 2 |
| 🟢 Low | 1 |

---

## ASVS-16.2.1: General Logging - Metadata in Log Entries

### Findings for 16.2.1

### [MEDIUM]

**Finding ID:** ASVS-1621-MED-001

**Title:** Log Entries Missing Structured "Where" (Source IP/Endpoint) Metadata

**File:** `v3/server/pages.py` (multiple log statements)

**Vulnerable Code (representative examples):**

Line 108:
```python
_LOGGER.info(
    f'User[U:{result.uid}] set {field} for election[E:{election.eid}] to {date_str}'
)
```

Line 407:
```python
_LOGGER.info(
    f'User[U:{result.uid}] voted on issue[I:{iid}] in election[E:{election.eid}]'
)
```

Line 425:
```python
_LOGGER.info(
    f'User[U:{result.uid}] created election[E:{election.eid}];'
    f' title: "{form.title}"'
)
```

**Analysis:**

Current metadata per log entry:
| Metadata | Present | Source |
|----------|---------|--------|
| **When** | Partial | `{asctime}` in format string (but format `'%m/%d %H:%M'` lacks year and seconds) |
| **Who** | Yes | `User[U:{result.uid}]` in message |
| **What** | Yes | Action description in message |
| **Where** | No | No source IP, request path, or session ID |

Missing for a detailed timeline investigation:
- Source IP address of the requester
- HTTP method and request path
- Request/correlation ID for tracing
- Session identifier

**Impact:** During incident investigation, it's impossible to determine the network origin of actions, correlate multiple actions from the same session, or distinguish between legitimate and compromised sessions for the same user.

**Remediation:**

```python
import uuid

async def get_request_context():
    """Generate structured context for logging."""
    request_id = str(uuid.uuid4())[:8]
    return {
        'request_id': request_id,
        'source_ip': quart.request.remote_addr,
        'method': quart.request.method,
        'path': quart.request.path,
    }

# Usage in handlers:
_LOGGER.info(
    f'[R:{ctx["request_id"]}] User[U:{result.uid}] '
    f'from[IP:{ctx["source_ip"]}] '
    f'voted on issue[I:{iid}] in election[E:{election.eid}]'
)
```

---

### [MEDIUM]

**Finding ID:** ASVS-1621-MED-002

**Title:** Error Logging Includes Exception Details But Not Request Context

**File:** `v3/server/pages.py`, line 402

**Vulnerable Code:**
```python
except Exception as e:
    _LOGGER.error(
        f'Error adding vote for user[U:{result.uid}] on issue[I:{iid}]: {e}'
    )
```

**Impact:** When an error occurs during vote submission, the log includes the user and issue but not the request origin, full stack trace, or the input that caused the error. This makes root cause analysis during an incident significantly harder.

**Remediation:**
```python
except Exception as e:
    _LOGGER.error(
        f'[R:{request_id}] Error adding vote for user[U:{result.uid}] '
        f'on issue[I:{iid}] from[IP:{request.remote_addr}] '
        f'in election[E:{election.eid}]',
        exc_info=True  # includes full traceback
    )
```

---

### [LOW]

**Finding ID:** ASVS-1621-LOW-001

**Title:** Mail Script Logging Lacks Structured Metadata

**File:** `v3/server/bin/mail-voters.py`, lines 72, 83

**Vulnerable Code:**
```python
_LOGGER.info(f'No eligible voters found for election {eid}')
# ...
_LOGGER.info(f'Sent email to {voter.email} for election {eid}')
```

**Impact:** The mail script logs use default `logging.basicConfig()` format (no custom format string), meaning timestamps use the default format and there's no consistent structure matching the main application.

**Remediation:**
```python
logging.basicConfig(
    level=logging.INFO,
    style='{',
    format='[{asctime}|{levelname}|{name}] {message}',
    datefmt='%Y-%m-%dT%H:%M:%S%z',
)
```

### Security Controls Inventory for 16.2.1

| Control | Location | Status |
|---------|----------|--------|
| WHO metadata (user ID) | `pages.py` log messages | **Present** in action logs |
| WHAT metadata (action) | `pages.py` log messages | **Present** |
| WHEN metadata (timestamp) | `main.py` format string | **Partial** (missing year, seconds) |
| WHERE metadata (source IP) | Not present | **MISSING** |
| Request correlation ID | Not present | **MISSING** |
| Structured exception logging | Partial (`exc_info` not used) | **INADEQUATE** |

### Positive Patterns for 16.2.1

- Consistent identifier format: `User[U:...]`, `election[E:...]`, `issue[I:...]` enables grep-based correlation
- All security-relevant actions (CRUD, votes, state changes) have corresponding log entries
- Error conditions in vote submission are logged with user context
- Logger names via `__name__` provide component identification

---



## Cross-cutting Architecture Observations

1. **Logging Architecture Maturity**: The application has basic logging in place with consistent patterns (module loggers, action logging), but lacks the maturity markers expected for ASVS Level 2/3: no documentation, no structured format, no correlation IDs, and insufficient timestamp precision.

2. **Async/Sync Impedance Mismatch**: The application uses Quart (async) but all database operations via `asfpy.db`/`sqlite3` are synchronous. This creates a fundamental architectural issue affecting both concurrency fairness (15.4.4) and potentially logging reliability (if the event loop is blocked, log writes may also be delayed).

3. **Centralized Logging Configuration with Gaps**: While `main.py` centralizes logging configuration, the `print()` statements in `pages.py` and the separate `basicConfig()` in `mail-voters.py` create inconsistencies that undermine all logging requirements simultaneously.

4. **Security Event Coverage**: The application logs the right categories of events (CRUD operations, state changes, access decisions) but the metadata quality (missing source IP, missing correlation ID, imprecise timestamps) significantly reduces the investigative value of these logs.

5. **No Separation of Security vs. Operational Logs**: All logs go to the same destination with the same format. There's no differentiation between security-relevant events (votes, election state changes, access denials) and operational events (loading elections, rendering templates).

## Cross-cutting Recommendations

### Immediate (Sprint 1)

1. **Fix timestamp format** — Change `DATE_FORMAT` to `'%Y-%m-%dT%H:%M:%S%z'` and set `logging.Formatter.converter = time.gmtime` for UTC. Single-line fix with highest impact on 16.2.2.

2. **Remove `print()` statements** — Replace the two `print('FORM:', form)` calls in `pages.py` with `_LOGGER.debug()` or remove entirely. Fixes 16.2.3 compliance.

3. **Add source IP to log messages** — Include `quart.request.remote_addr` in security-relevant log entries. Improves 16.2.1 metadata.

### Short-term (1-2 Sprints)

4. **Implement request correlation IDs** — Add a `before_request` hook that generates and propagates a request ID through all log entries. Addresses 16.2.4 correlation.

5. **Create logging inventory document** — Document all event types, destinations, formats, access controls, and retention. Addresses 16.1.1 entirely.

6. **Adopt structured logging format** — Migrate to JSON logging format (e.g., `python-json-logger`) for machine parseability. Addresses 16.2.4.

### Long-term (Next Quarter)

7. **Implement async database layer** — Use `aiosqlite` or wrap synchronous calls in `asyncio.to_thread()` with a bounded thread pool to prevent event loop starvation. Addresses 15.4.4.

8. **Add request timeout middleware** — Configure Hypercorn's `keep_alive_timeout` and add application-level timeouts for database operations. Further addresses 15.4.4.

9. **Implement log access controls** — Define and enforce who can access logs, implement log rotation with retention policies, and consider separating security logs from operational logs. Addresses 16.1.1 comprehensively.
        