# Security Audit Report: ASVS 13.3.3

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 13.3.3 |
| **Files Analyzed** | 7 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that all cryptographic operations are performed using an isolated security module (such as a vault or hardware security module) to securely manage and protect key material from exposure outside of the security module.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 2 |
| 🟡 Medium | 0 |
| 🟢 Low | 0 |

---

## ASVS-13.3.3: Cryptographic Operations Using Isolated Security Module

### Findings for 13.3.3

### [HIGH]

**Finding ID**: ASVS-1333-HIGH-001

**File**: `v3/steve/crypto.py` (lines 43-95) and `v3/steve/election.py` (lines 70-80, 231-243)

**Vulnerable Code**:
```python
# crypto.py - Key generation in application memory
def gen_opened_key(edata: bytes, salt: bytes) -> bytes:
    digest = hashlib.blake2b(edata).digest()
    return _hash(digest, salt)

def _b64_vote_key(vote_token: bytes, salt: bytes) -> bytes:
    keymaker = hkdf.HKDF(
        algorithm=hashes.SHA256(),
        length=32,
        salt=salt,
        info=b'xchacha20_key',
    )
    vote_key = keymaker.derive(vote_token)
    return base64.urlsafe_b64encode(vote_key)
```

```python
# election.py - opened_key stored in plain SQLite
self.c_open.perform(salt, opened_key, self.eid)
```

**Data Flow**: Election data → `gen_opened_key()` in application process memory → stored as BLOB in SQLite `election` table → retrieved from SQLite for vote encryption/decryption operations.

**Impact**: All cryptographic key material (election salts, opened_key, vote encryption keys) is generated, stored, and used entirely within the application process and a SQLite database file on disk. If the application server or database is compromised, all key material is immediately exposed. There is no hardware boundary protecting keys from memory dumps, side-channel attacks, or direct file access.

**Proof of Concept**: An attacker with read access to the SQLite file can extract `opened_key` and all `mayvote.salt` values, then recompute `vote_token` for any voter/issue pair and decrypt all votes.

**Remediation**:
```python
# Example using HashiCorp Vault Transit secrets engine
import hvac

class VaultCrypto:
    def __init__(self):
        self.client = hvac.Client(url='https://vault.example.com:8200')
    
    def encrypt_vote(self, plaintext: bytes, key_name: str) -> bytes:
        response = self.client.secrets.transit.encrypt_data(
            name=key_name,
            plaintext=base64.b64encode(plaintext).decode()
        )
        return response['data']['ciphertext'].encode()
    
    def decrypt_vote(self, ciphertext: bytes, key_name: str) -> bytes:
        response = self.client.secrets.transit.decrypt_data(
            name=key_name,
            ciphertext=ciphertext.decode()
        )
        return base64.b64decode(response['data']['plaintext'])
```

### [HIGH]

**Finding ID**: ASVS-1333-HIGH-002

**File**: `v3/steve/election.py` (lines 144-158)

**Vulnerable Code**:
```python
def _all_metadata(self, required_state=None):
    # NOTE: this returns the SALT and OPENED_KEY columns. This
    # API is not for public use.
    md = self.q_metadata.first_row(self.eid)
    ...
    return md
```

**Data Flow**: SQLite database → `q_metadata` (SELECT *) → `md` object containing `salt` and `opened_key` → used in multiple methods including `add_vote`, `tally_issue`, `has_voted_upon`, `is_tampered`.

**Impact**: The `opened_key` (the master election key material) is loaded into application memory in multiple request paths. The sensitive key material is only protected by an internal comment ("not for public use") with no memory protection, access control at the data layer, or process isolation.

**Remediation**: Integrate with a key management service (KMS) or HSM where the key never leaves the security boundary. Operations requiring the key should be delegated to the security module via API calls.

### Security Controls Inventory for 13.3.3

| Control | Location | Coverage Status |
|---------|----------|-----------------|
| Argon2 key derivation | `crypto.py:_hash()` | DEFINED and CALLED - provides computational cost but no isolation |
| HKDF key stretching | `crypto.py:_b64_vote_key()` | DEFINED and CALLED - derives encryption keys in-process |
| Salt generation via `secrets` | `crypto.py:gen_salt()` | DEFINED and CALLED - uses CSPRNG but no HSM |
| No HSM/vault integration | N/A | **MISSING** - no isolated security module exists |

### Positive Patterns for 13.3.3

- Use of `secrets.token_bytes()` for cryptographically secure random salt generation
- Argon2 with reasonable parameters (time_cost=2, memory_cost=65536, parallelism=4) for key derivation
- HKDF for proper key stretching from vote_token to encryption key
- Code comments indicating awareness of security sensitivity (e.g., "NEVER return issue.salt")

---



## Cross-cutting Architecture Observations

1. **Key Material Exposure Pattern**: The application handles all cryptographic operations in-process with no isolation boundary. The `opened_key` (master election key) flows through multiple code paths: `election.open()` → SQLite storage → `add_vote()` → `tally_issue()` → `has_voted_upon()`. Any single compromise point in this chain exposes all election vote data.

2. **Lifecycle Gap**: The election lifecycle (editable → open → closed) provides a natural framework for key management, but key material persists in the `closed` state indefinitely. There's no "archived" or "destroyed" state where key material would be removed.

3. **Defense-in-Depth Gaps**: Multiple requirements share a common theme of missing explicit hardening. The framework provides reasonable defaults (no TRACE, no directory listing, method restrictions) but there's no explicit defense-in-depth configuration. This creates risk if framework behavior changes or if deployment differs from assumptions.

4. **Development Artifacts in Production Code**: Multiple indicators suggest the code is not fully production-hardened: placeholder CSRF token, active `print()` statements, `### check authz` comments indicating incomplete authorization, and commented-out debug statements for sensitive values.

5. **Single Database as Secret Store**: SQLite serves as both the application database and the secrets store. There's no separation between operational data and key material, making it impossible to apply different access controls or audit requirements to secrets vs. general data.

## Cross-cutting Recommendations

### Immediate (Critical Path)
1. **Remove debug `print()` statements** from `do_add_issue_endpoint` and `do_edit_issue_endpoint` - these actively leak form data to stdout in production
2. **Add explicit source control metadata blocking** to the `/static/` route to prevent serving `.git` contents if accidentally deployed
3. **Document deployment requirements** explicitly stating that TRACE must be disabled at reverse proxy level and directory listing must be disabled

### Short-term (1-3 months)
4. **Implement key lifecycle management** - add `key_created_at` tracking to the schema, implement secret cleanup after election finalization (e.g., zeroing `opened_key` after results are exported/verified)
5. **Add explicit TRACE blocking middleware** and source control path filtering as defense-in-depth measures
6. **Remove all commented-out debug code** that could expose sensitive key material if uncommented
7. **Implement environment-based configuration** to ensure debug-level features are disabled in production builds

### Long-term (3-12 months)
8. **Integrate with a key management service** (HashiCorp Vault, AWS KMS, or similar) to perform cryptographic operations in an isolated security boundary - this is the most significant gap for a Level 3 ASVS target
9. **Implement secret rotation** - design a mechanism to rotate election keys for long-running elections, or implement a maximum election duration policy
10. **Separate secret storage** from operational database - consider storing key material in a dedicated secrets backend with its own access controls and audit trail
        