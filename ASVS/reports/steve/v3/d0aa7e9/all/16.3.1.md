# Security Audit Report: ASVS 16.3.1

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 16.3.1 |
| **Files Analyzed** | 9 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that all authentication operations are logged, including successful and unsuccessful attempts. Additional metadata, such as the type of authentication or factors used, should also be collected.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 1 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---

## ASVS-16.3.1: Authentication Operations Logging

### Findings for 16.3.1

### [HIGH]
**Finding ID:** ASVS-1631-HIGH-001
**Location:** `v3/server/pages.py` — all endpoints using `@asfquart.auth.require`
**Vulnerable Code:**
```python
@APP.get('/voter')
@asfquart.auth.require({R.committer})  ### need general solution
@APP.use_template(TEMPLATES / 'voter.ezt')
async def voter_page():
    result = await basic_info()
    # ... no authentication logging ...
```
**Data Flow:** HTTP request → `@asfquart.auth.require` decorator → success/failure → NO LOG ENTRY
**Proof of Concept:** An attacker attempting to brute-force or replay session tokens against protected endpoints would generate no audit trail. Both successful authentications (e.g., first access after login) and failed attempts (invalid/expired sessions) would be invisible in logs.
**Impact:** Without authentication event logging, there is no ability to detect credential stuffing, session hijacking attempts, or account takeover. Security monitoring and incident response capabilities are severely limited. The application has 16+ authenticated endpoints, none of which log authentication outcomes.
**Remediation:** Add authentication logging at the middleware/decorator level:
```python
# In the auth framework (asfquart.auth) or via a wrapper:
async def auth_logging_wrapper(func):
    @functools.wraps(func)
    async def wrapper(*args, **kwargs):
        s = await asfquart.session.read()
        if s:
            _LOGGER.info(f'AUTH_SUCCESS: user={s["uid"]} endpoint={quart.request.path} method={quart.request.method}')
        else:
            _LOGGER.warning(f'AUTH_FAILURE: endpoint={quart.request.path} method={quart.request.method} ip={quart.request.remote_addr}')
        return await func(*args, **kwargs)
    return wrapper
```

### [MEDIUM]
**Finding ID:** ASVS-1631-MED-001
**Location:** `v3/server/pages.py` — no login/logout endpoint logging visible
**Vulnerable Code:**
```python
# No login/logout endpoints present in pages.py
# Authentication is delegated to asfquart framework
# No evidence of auth event logging in the visible codebase
```
**Data Flow:** Login/logout operations → asfquart framework → unknown logging state
**Impact:** Login and logout events (including failed login attempts, session creation, and session termination) are not demonstrably logged by this application. While the auth framework may handle this, there's no evidence in the audited code. The type of authentication (session-based, OAuth, etc.) and factors used are not recorded.
**Remediation:** Ensure the authentication framework logs all auth events, or add application-level hooks:
```python
@APP.after_request
async def log_auth_metadata(response):
    if quart.request.path in ('/login', '/logout', '/oauth/callback'):
        s = await asfquart.session.read()
        _LOGGER.info(f'AUTH_EVENT: path={quart.request.path} status={response.status_code} '
                     f'user={s["uid"] if s else "anonymous"} auth_type=session')
    return response
```

### Security Controls Inventory for 16.3.1

| Control | Location | Coverage Status |
|---------|----------|-----------------|
| Authentication success logging | Not found | ❌ NOT IMPLEMENTED |
| Authentication failure logging | Not found | ❌ NOT IMPLEMENTED |
| Auth type/factor metadata | Not found | ❌ NOT IMPLEMENTED |
| Session creation logging | Not found in scope | ❌ Unknown (may be in asfquart framework) |
| `@asfquart.auth.require` decorator | `pages.py` (16 endpoints) | ✅ Auth enforced but ❌ not logged |

### Positive Patterns for 16.3.1

1. **Consistent auth decorator usage** — All sensitive endpoints use `@asfquart.auth.require` with explicit role requirements (`R.committer`, `R.pmc_member`), providing a consistent enforcement point where logging could be added.
2. **User identity in action logs** — While auth events aren't logged, subsequent authorized actions do include the user identifier (e.g., `User[U:{result.uid}]`), which provides partial traceability.

---



## Cross-cutting Architecture Observations

1. **Logging maturity is low** — The application has action logging for administrative operations but lacks authentication event logging, authorization failure logging, input validation failure logging, and error logging across most endpoints. Only `do_vote_endpoint()` demonstrates the full pattern of try/except with error logging.

2. **Type B gaps are pervasive** — The codebase contains numerous `### check authz` comments indicating the developer intended to add authorization checks. These are never implemented, creating a false sense of security and making authorization failure logging impossible.

3. **No centralized logging infrastructure** — There is no logging middleware, no structured log format (e.g., JSON), no log sanitization layer, and no consistent error handling pattern. Each endpoint handles (or doesn't handle) logging independently.

4. **Debug code in production paths** — `print('FORM:', form)` statements exist in two production endpoints, bypassing all logging controls and dumping raw user data.

5. **Security assertions vs. proper error handling** — The `election.py` module uses Python `assert` statements for security-critical state checks. These are: (a) disabled with `-O` flag, (b) generate no log entries when they fire, and (c) provide no graceful error handling.

6. **Single responsibility gap** — The authentication framework (`asfquart.auth`) handles enforcement but there's no evidence it handles logging. The application layer logs actions but not auth events. This creates a gap where neither layer owns auth logging.

## Cross-cutting Recommendations

### Immediate (Critical/High priority)
1. **Remove debug `print()` statements** — Lines 481 and 497 in `pages.py` dump raw form data to stdout. Remove immediately.
2. **Implement log sanitization** — Create a wrapper or formatter that strips control characters (`\r`, `\n`, `\x00`-`\x1f`) from all logged values before output.
3. **Add error handling with logging to all state-changing endpoints** — Wrap `election.open()`, `election.close()`, `Election.create()` in try/except with `_LOGGER.error()`.
4. **Log authorization denials** — Add `_LOGGER.warning()` calls in `vote_on_page()` and `serve_doc()` where access is denied.

### Short-term (1-2 sprints)
5. **Implement authentication event logging** — Add a middleware or decorator that logs all authentication successes and failures with metadata (user, endpoint, method, IP, auth type).
6. **Replace `assert` with proper exceptions** — Convert all security-relevant `assert` statements in `election.py` to explicit checks with `ElectionBadState` exceptions and logging.
7. **Implement authorization checks** — Address all `### check authz` placeholders with ownership verification and log failures.
8. **Add input validation failure logging** — Log when date parsing fails, when invalid IIDs are submitted, and when other validation rejects input.

### Long-term (Architecture)
9. **Adopt structured logging (JSON)** — Migrate to structured log format with consistent fields (`timestamp`, `level`, `event_type`, `user_id`, `resource_id`, `action`, `outcome`) for machine-readable analysis.
10. **Implement centralized security event framework** — Create a `SecurityEvent` class or module that provides typed events (`AUTH_SUCCESS`, `AUTH_FAILURE`, `AUTHZ_DENIED`, `INPUT_VALIDATION_FAILED`, `BUSINESS_LOGIC_VIOLATION`) with automatic metadata collection.
11. **Add anti-automation detection** — Implement rate limiting with logging of threshold violations to detect brute-force or automated abuse.
12. **PII handling policy in logs** — Establish a logging policy that defines which fields may be logged in full, which must be masked, and which must never be logged, then enforce via the structured logging framework.
        