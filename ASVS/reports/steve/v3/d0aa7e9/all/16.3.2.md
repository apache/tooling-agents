# Security Audit Report: ASVS 16.3.2

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 16.3.2 |
| **Files Analyzed** | 9 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that failed authorization attempts are logged. For L3, this must include logging all authorization decisions, including logging when sensitive data is accessed (without logging the sensitive data itself).

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 1 |
| 🟠 High | 1 |
| 🟡 Medium | 0 |
| 🟢 Low | 0 |

---

## ASVS-16.3.2: Authorization Failure Logging

### Findings for 16.3.2

### [CRITICAL]
**Finding ID:** ASVS-1632-CRIT-001
**Location:** `v3/server/pages.py`, multiple endpoints — all `### check authz` placeholder comments
**Vulnerable Code:**
```python
@APP.post('/do-vote/<eid>')
@asfquart.auth.require({R.committer})
@load_election
async def do_vote_endpoint(election):
    result = await basic_info()

    ### check authz    # <-- NOT IMPLEMENTED

    form = edict(await quart.request.form)
    # ... proceeds without authorization check ...
```
**Locations with missing authz (Type B gap — control commented but not called):**
- `_set_election_date()` — line 89
- `do_vote_endpoint()` — line 379
- `do_create_endpoint()` — line 417
- `do_open_endpoint()` — line 438
- `do_close_endpoint()` — line 458
- `do_add_issue_endpoint()` — line 479
- `do_edit_issue_endpoint()` — line 501
- `do_delete_issue_endpoint()` — line 524
- `load_election` decorator — line 185
- `load_election_issue` decorator — line 215

**Data Flow:** Authenticated user → endpoint → `### check authz` (placeholder) → sensitive operation proceeds
**Proof of Concept:** Any authenticated committer can open, close, modify, or delete ANY election regardless of ownership:
```
POST /do-close/abc1234567 HTTP/1.1
Cookie: <any_valid_committer_session>
```
**Impact:** Authorization checks are not implemented (only placeholder comments exist). Since no authorization is enforced, no authorization failures can be logged. This is a Type B gap — the developer clearly identified the need but never implemented it. Any committer can perform any administrative action on any election. Failed authorization attempts cannot be logged because authorization is not checked.
**Remediation:** Implement and log authorization checks:
```python
async def check_election_authz(election, uid, action='manage'):
    md = election.get_metadata()
    if md.owner_pid != uid:
        _LOGGER.warning(
            f'AUTHZ_DENIED: user={uid} action={action} '
            f'election={election.eid} owner={md.owner_pid}'
        )
        quart.abort(403)
```

### [HIGH]
**Finding ID:** ASVS-1632-HIGH-001
**Location:** `v3/server/pages.py`, `vote_on_page()` (line 236) and `serve_doc()` (line 567)
**Vulnerable Code:**
```python
# vote_on_page - line 236-241
election.q_find_issues.perform(result.uid, election.eid)
if not election.q_find_issues.fetchall():
    result = await basic_info()
    result.title = 'Access Denied'
    result.eid = election.eid
    raise_404(T_BAD_EID, result)  # Reuse template for consistency

# serve_doc - line 567-570
row = db.q_get_mayvote.first_row(result.uid, iid)
if not row:
    # Nothing fancy. Just pretend the file does not exist
    quart.abort(404)
```
**Data Flow:** User request → authorization query → denial → 404 response → NO LOG
**Proof of Concept:** A user probing for elections they cannot vote in, or documents they cannot access, generates no log entries. Repeated probing attempts are invisible.
**Impact:** Authorization denials in `vote_on_page()` and `serve_doc()` are not logged, preventing detection of unauthorized access attempts or data enumeration.
**Remediation:**
```python
# In vote_on_page:
election.q_find_issues.perform(result.uid, election.eid)
if not election.q_find_issues.fetchall():
    _LOGGER.warning(
        f'AUTHZ_DENIED: user={result.uid} attempted to access '
        f'election={election.eid} without mayvote permission'
    )
    raise_404(T_BAD_EID, result)

# In serve_doc:
if not row:
    _LOGGER.warning(
        f'AUTHZ_DENIED: user={result.uid} attempted to access '
        f'doc={docname} for issue={iid} without permission'
    )
    quart.abort(404)
```

### Security Controls Inventory for 16.3.2

| Control | Location | Coverage Status |
|---------|----------|-----------------|
| Authorization check (ownership) | `### check authz` placeholders | ❌ NOT IMPLEMENTED (10 locations) |
| Mayvote check in vote_on_page | `pages.py:236` | ✅ Enforced but ❌ not logged |
| Mayvote check in serve_doc | `pages.py:567` | ✅ Enforced but ❌ not logged |
| Authorization failure logging | Not found | ❌ NOT IMPLEMENTED anywhere |
| Sensitive data access logging | Not found | ❌ NOT IMPLEMENTED |

### Positive Patterns for 16.3.2

1. **Voter eligibility check exists** — `vote_on_page()` does verify the user has mayvote entries before displaying the ballot, even though ownership authz is missing.
2. **Document access check exists** — `serve_doc()` verifies `q_get_mayvote` before serving documents, implementing per-resource authorization.

---



## Cross-cutting Architecture Observations

1. **Logging maturity is low** — The application has action logging for administrative operations but lacks authentication event logging, authorization failure logging, input validation failure logging, and error logging across most endpoints. Only `do_vote_endpoint()` demonstrates the full pattern of try/except with error logging.

2. **Type B gaps are pervasive** — The codebase contains numerous `### check authz` comments indicating the developer intended to add authorization checks. These are never implemented, creating a false sense of security and making authorization failure logging impossible.

3. **No centralized logging infrastructure** — There is no logging middleware, no structured log format (e.g., JSON), no log sanitization layer, and no consistent error handling pattern. Each endpoint handles (or doesn't handle) logging independently.

4. **Debug code in production paths** — `print('FORM:', form)` statements exist in two production endpoints, bypassing all logging controls and dumping raw user data.

5. **Security assertions vs. proper error handling** — The `election.py` module uses Python `assert` statements for security-critical state checks. These are: (a) disabled with `-O` flag, (b) generate no log entries when they fire, and (c) provide no graceful error handling.

6. **Single responsibility gap** — The authentication framework (`asfquart.auth`) handles enforcement but there's no evidence it handles logging. The application layer logs actions but not auth events. This creates a gap where neither layer owns auth logging.

## Cross-cutting Recommendations

### Immediate (Critical/High priority)
1. **Remove debug `print()` statements** — Lines 481 and 497 in `pages.py` dump raw form data to stdout. Remove immediately.
2. **Implement log sanitization** — Create a wrapper or formatter that strips control characters (`\r`, `\n`, `\x00`-`\x1f`) from all logged values before output.
3. **Add error handling with logging to all state-changing endpoints** — Wrap `election.open()`, `election.close()`, `Election.create()` in try/except with `_LOGGER.error()`.
4. **Log authorization denials** — Add `_LOGGER.warning()` calls in `vote_on_page()` and `serve_doc()` where access is denied.

### Short-term (1-2 sprints)
5. **Implement authentication event logging** — Add a middleware or decorator that logs all authentication successes and failures with metadata (user, endpoint, method, IP, auth type).
6. **Replace `assert` with proper exceptions** — Convert all security-relevant `assert` statements in `election.py` to explicit checks with `ElectionBadState` exceptions and logging.
7. **Implement authorization checks** — Address all `### check authz` placeholders with ownership verification and log failures.
8. **Add input validation failure logging** — Log when date parsing fails, when invalid IIDs are submitted, and when other validation rejects input.

### Long-term (Architecture)
9. **Adopt structured logging (JSON)** — Migrate to structured log format with consistent fields (`timestamp`, `level`, `event_type`, `user_id`, `resource_id`, `action`, `outcome`) for machine-readable analysis.
10. **Implement centralized security event framework** — Create a `SecurityEvent` class or module that provides typed events (`AUTH_SUCCESS`, `AUTH_FAILURE`, `AUTHZ_DENIED`, `INPUT_VALIDATION_FAILED`, `BUSINESS_LOGIC_VIOLATION`) with automatic metadata collection.
11. **Add anti-automation detection** — Implement rate limiting with logging of threshold violations to detect brute-force or automated abuse.
12. **PII handling policy in logs** — Establish a logging policy that defines which fields may be logged in full, which must be masked, and which must never be logged, then enforce via the structured logging framework.
        