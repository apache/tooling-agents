# Security Audit Report: ASVS 2.4.2

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 2.4.2 |
| **Files Analyzed** | 10 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that business logic flows require realistic human timing, preventing excessively rapid transaction submissions.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 1 |
| 🟡 Medium | 2 |
| 🟢 Low | 0 |

---

## ASVS-2.4.2: Anti-automation

### Findings for 2.4.2

### [HIGH]
**Finding ID:** ASVS-242-HIGH-001

**Title:** No Rate Limiting or Timing Controls on Vote Submission

**File:** `v3/server/pages.py`, function `do_vote_endpoint`, lines 372-408

**Vulnerable code:**
```python
@APP.post('/do-vote/<eid>')
@asfquart.auth.require({R.committer})
@load_election
async def do_vote_endpoint(election):
    result = await basic_info()
    ### check authz
    form = edict(await quart.request.form)
    if not form:
        await flash_danger('No vote data provided.')
        return quart.redirect(f'/vote-on/{election.eid}', code=303)
    # ... processes and submits votes immediately ...
    for iid, votestring in votes.items():
        # No timing check, no rate limiting
        election.add_vote(result.uid, iid, votestring)
```

**Data flow:** HTTP POST → `do_vote_endpoint()` → `election.add_vote()` → database write (no timing/rate check anywhere in chain)

**Proof of concept:**
```bash
# Rapidly submit votes with automated tool
for i in $(seq 1 100); do
  curl -X POST https://target/do-vote/ELECTION_ID \
    -H "Cookie: session=VALID_SESSION" \
    -d "vote-issue1=y&csrf_token=placeholder" &
done
```

**Impact:** An authenticated attacker could use automation to rapidly submit/modify votes. While this particular system overwrites votes (not duplicates), the lack of timing controls means automated scripts could enumerate issue IDs, test vote manipulation, or cause excessive load. More critically, the system has no mechanism to distinguish human-paced interactions from automated ones.

**Remediation:**
```python
import time

# Add to session or a dedicated store
VOTE_COOLDOWN_SECONDS = 5

async def do_vote_endpoint(election):
    result = await basic_info()
    s = await asfquart.session.read()
    
    last_vote_time = s.get('last_vote_time', 0)
    now = time.time()
    if now - last_vote_time < VOTE_COOLDOWN_SECONDS:
        await flash_danger('Please wait before submitting again.')
        return quart.redirect(f'/vote-on/{election.eid}', code=303)
    
    # ... process vote ...
    s['last_vote_time'] = now
    await asfquart.session.write(s)
```

---

### [MEDIUM]
**Finding ID:** ASVS-242-MED-001

**Title:** No Rate Limiting on Election/Issue Creation Endpoints

**File:** `v3/server/pages.py`, functions `do_create_endpoint` (line 410), `do_add_issue_endpoint` (line 469)

**Vulnerable code:**
```python
@APP.post('/do-create-election')
@asfquart.auth.require({R.pmc_member})
async def do_create_endpoint():
    # No rate limiting, no minimum time between creations
    form = edict(await quart.request.form)
    election = steve.election.Election.create(DB_FNAME, form.title, result.uid)
```

**Data flow:** HTTP POST → endpoint → `Election.create()` → database write (no throttling)

**Impact:** A compromised or malicious PMC member account could rapidly create hundreds of elections or issues, potentially causing resource exhaustion or data pollution.

**Remediation:** Implement per-user rate limiting (e.g., max 5 elections per hour, max 50 issues per election per minute).

---

### [MEDIUM]
**Finding ID:** ASVS-242-MED-002

**Title:** State-Changing GET Endpoints Without Timing Controls

**File:** `v3/server/pages.py`, functions `do_open_endpoint` (line 431), `do_close_endpoint` (line 451)

**Vulnerable code:**
```python
@APP.get('/do-open/<eid>')
@asfquart.auth.require({R.committer})
@load_election
async def do_open_endpoint(election):
    # No timing control - can be triggered instantly by automated scripts
    election.open(pdb)
```

**Impact:** Automated scripts could open/close elections instantly without human deliberation. Combined with the lack of ownership validation (`### check authz`), any committer could rapidly iterate through election IDs attempting to open/close them.

**Remediation:** Add confirmation tokens with timing (e.g., require a confirmation step that was issued at least N seconds prior), and convert to POST with CSRF validation.

### Security Controls Inventory for 2.4.2

| Control | Location | Status |
|---------|----------|--------|
| Rate limiting middleware | Not found | ❌ MISSING |
| Per-user request throttling | Not found | ❌ MISSING |
| Minimum inter-request timing | Not found | ❌ MISSING |
| CAPTCHA integration | Not found | ❌ MISSING |
| Client-side timing (submitFormWithLoading button disable) | `steve.js` line 68 | ⚠️ Cosmetic only (easily bypassed) |

### Positive Patterns for 2.4.2

- The `submitFormWithLoading()` function in `steve.js` disables the submit button during form submission, providing basic UX-level protection against accidental double-clicks (though trivially bypassed by direct HTTP requests).
- The `vote-on.ezt` template confirms before submission when issues are unvoted, adding a human-speed step at the UI layer.

---



## Cross-cutting Architecture Observations

1. **Defense-in-Depth Gap:** The application relies heavily on authentication (`@asfquart.auth.require`) as its primary security boundary but lacks multiple defense layers. There is no CSP, no CSRF validation, no rate limiting, and incomplete authorization — if authentication is bypassed, nearly all controls fail.

2. **Type B Gaps (Control Exists but Not Applied):**
   - CSRF token is generated (`basic.csrf_token = 'placeholder'`) and sent to templates, but NEVER validated server-side on any POST endpoint.
   - The `[format "js,html"]` EZT escaping is used in `manage.ezt` for issue titles in onclick handlers but NOT used in `vote-on.ezt` for STV_CANDIDATES data.
   - Authorization check comments (`### check authz`) exist at 8+ locations but authorization logic is never implemented.

3. **Mixed Security Maturity:** The `vote-on.ezt` template demonstrates good practices (IIFE, escapeHtml, textContent) while other templates (`manage.ezt`, `manage-stv.ezt`) use global scope and raw HTML rendering. The `election.py` module has strong cryptographic vote protection while `pages.py` has fundamental web security gaps.

4. **State-Changing GET Requests:** `/do-open/<eid>` and `/do-close/<eid>` use GET methods for state-changing operations. This violates HTTP semantics and makes CSRF trivially exploitable (no CSRF token needed for GET requests). The `manage.ezt` template triggers these via `window.location.href`, confirming this is by design.

5. **Document Serving as Attack Vector:** The `serve_doc()` endpoint combined with `rewrite_description()` creating links to documents creates a complete attack chain: upload malicious HTML → link is auto-generated in issue description → voters click link → XSS in application origin.

## Cross-cutting Recommendations

### Immediate (Critical/High — deploy blockers)

1. **Fix `rewrite_description()`** to HTML-escape description content before wrapping in `<pre>` tags (ASVS-322-HIGH-001).
2. **Add `Content-Disposition: attachment`** to `serve_doc()` responses and validate `docname` for path traversal (ASVS-321-HIGH-001).
3. **Implement CSRF validation** — the token infrastructure exists but is never verified. Add a decorator for POST endpoints that validates the token from the form/header matches the session value.
4. **Add `[format "js"]` escaping** to all EZT values rendered inside JavaScript string literals in `vote-on.ezt` (ASVS-322-MED-002).

### Short-term (1-2 sprints)

5. **Implement CSP headers** — start with `Content-Security-Policy: default-src 'self'` and nonce-based script allowance. This mitigates multiple XSS vectors.
6. **Configure session cookies** with `Secure`, `HttpOnly`, `SameSite=Lax`, and `__Host-` prefix (ASVS-331-HIGH-001).
7. **Add rate limiting middleware** for vote submission and election creation endpoints (ASVS-242-HIGH-001).
8. **Convert state-changing GET endpoints** (`/do-open/`, `/do-close/`) to POST with CSRF validation.
9. **Wrap `manage.ezt` JavaScript** in IIFE with strict mode and add type checking on `getElementById` results (ASVS-323-MED-001).

### Long-term (architectural improvements)

10. **Implement authorization framework** — replace all `### check authz` placeholders with ownership/permission verification.
11. **Create security documentation** per ASVS 3.1.1, specifying browser requirements and degradation behavior.
12. **Migrate `steve.js`** global functions to a namespaced module object to prevent DOM clobbering.
13. **Add X-Content-Type-Options: nosniff** globally via middleware for all responses.
14. **Implement comprehensive anti-automation** including per-endpoint rate limits, minimum timing between sensitive operations, and account-level throttling.
        