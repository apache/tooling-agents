# Security Audit Report: ASVS 9.2.3

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 9.2.3 |
| **Files Analyzed** | 5 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that the service only accepts tokens which are intended for use with that service (audience). For JWTs, this can be achieved by validating the 'aud' claim against an allowlist defined in the service.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 0 |
| 🟢 Low | 1 |

---

## ASVS-9.2.3: Token Audience Validation

### Findings for 9.2.3

#### [LOW]
**Finding ID**: ASVS-923-LOW-001

**Title**: No Visible Audience Validation for Authentication Tokens

**File**: `v3/server/pages.py`, all authenticated endpoints

**Description**: The application uses `asfquart.session.read()` and `@asfquart.auth.require` for authentication. There is no visible audience (`aud`) claim validation in the provided code. If the ASF OAuth/OIDC infrastructure issues tokens (JWTs) that could be used across multiple ASF services, the lack of audience validation in this application could allow tokens issued for other ASF services to be accepted here.

The code does not contain:
- Any `aud` claim extraction or validation
- An audience allowlist definition
- Any token introspection that verifies the intended recipient

**Gap Type**: Cannot definitively classify without seeing `asfquart.auth` internals. If the framework handles audience validation, this is a non-issue. If not, it's a Type A gap.

**Impact**: If the ASF identity provider issues tokens consumable by multiple services and this application doesn't validate the audience claim, a token obtained for a different ASF service could be used to authenticate to the voting system.

**Remediation**:
```python
# In application configuration or startup
EXPECTED_AUDIENCE = 'steve-voting-system'

# In session validation (ideally within asfquart.auth or at application level)
async def validate_token_audience(token_claims):
    if token_claims.get('aud') != EXPECTED_AUDIENCE:
        quart.abort(401, 'Token not intended for this service')
```

---

### Security Controls Inventory for 9.2.3

| Control | Location | Coverage Status |
|---------|----------|-----------------|
| Audience validation | Not present in provided code | NOT FOUND — delegated to framework or absent |
| `asfquart.auth.require` | Multiple endpoints in `pages.py` | Applied; cannot verify if audience is checked internally |
| Per-election voter eligibility | `q_find_issues.perform(result.uid, election.eid)` line 236 | Partial application-level audience check (verifies user is allowed for specific election) |

### Positive Patterns for 9.2.3

1. **Application-level access scoping**: Even without token audience validation, the application enforces that only eligible voters can access specific elections. The `vote_on_page()` function (line 236) checks `q_find_issues` to verify the authenticated user has mayvote entries before showing the election.
2. **Role-based access separation**: The distinction between `R.committer` and `R.pmc_member` provides granularity in who can perform which operations, reducing the impact of a token from a less-privileged service being reused.

---



## Cross-cutting Architecture Observations

1. **Single-service architecture**: This is a monolithic web application, not a microservices system. The ASVS 9.2.x requirements about token type/audience validation are primarily designed for distributed systems where tokens flow between services. The relevance is reduced (but not eliminated) for this architecture.

2. **Authentication delegation**: All authentication and session management is delegated to `asfquart.session` and `asfquart.auth`. The security posture of the application heavily depends on the correctness of these external modules, which are not in scope for this audit.

3. **Internal token design is sound**: The `vote_token` system demonstrates good cryptographic design — tokens are derived from multiple inputs making them naturally scoped and resistant to cross-purpose reuse.

4. **Authorization gaps more critical than token issues**: The repeated `### check authz` placeholders throughout the code represent a more immediate threat than token validation issues. Any authenticated committer can manage/open/close/delete any election.

5. **CSRF as a token validation failure**: The placeholder CSRF token represents a concrete failure to validate a security token for its intended purpose (9.2.2), but the root cause is incomplete implementation rather than a design flaw.

## Cross-cutting Recommendations

### Immediate (Critical)
1. **Implement CSRF token validation**: Replace the placeholder with a cryptographically random token stored in the session, validated on all state-changing endpoints.
2. **Implement authorization checks**: Replace all `### check authz` placeholders with actual ownership/permission validation to ensure only election owners can manage their elections.

### Short-term (High)
3. **Document and verify framework token validation**: Audit `asfquart.auth` to confirm it validates token type and audience claims (if using JWTs/OIDC). Document the validation chain.
4. **Add audience configuration**: If the ASF OAuth system issues tokens for multiple services, configure this application with an expected audience identifier and validate it.

### Long-term (Medium)
5. **Security architecture documentation**: Document the complete authentication and authorization flow, including which components validate which token properties, to enable future audits.
6. **Consider token binding**: If transitioning to token-based authentication, implement sender-constrained tokens (DPoP or mTLS) to prevent token theft and replay across services.
        