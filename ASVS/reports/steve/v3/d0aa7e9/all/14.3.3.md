# Security Audit Report: ASVS 14.3.3

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 14.3.3 |
| **Files Analyzed** | 8 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that data stored in browser storage (such as localStorage, sessionStorage, IndexedDB, or cookies) does not contain sensitive data, with the exception of session tokens.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---

## ASVS-14.3.3: Sensitive Data in Browser Storage

### Findings for 14.3.3

### [MEDIUM]

**Finding ID**: ASVS-1433-MED-001

**Title**: Session Data Including PII Rendered into DOM Without Storage Isolation Verification

**File**: `v3/server/pages.py`, function `basic_info()`, lines 58-83

**Vulnerable Code**:
```python
async def basic_info():
    """Return base-level EZT template data."""
    basic = edict()
    # ...
    s = await asfquart.session.read()
    if s:
        basic.update(
            uid=s['uid'],
            name=s['fullname'],
            email=s['email'],
        )
    # ...
    basic.csrf_token = 'placeholder'
    return basic
```

**Data Flow**: Server session → `basic_info()` extracts uid/name/email → passed to EZT templates → rendered into HTML → available in DOM → potential for JavaScript to store in localStorage/sessionStorage

**Issue Description**: While the server-side code itself doesn't directly write to browser storage, it provides sensitive data (user ID, full name, email) in every authenticated page response. The manage.ezt template (referenced in the code inventory) contains JavaScript that interacts with the DOM and makes fetch calls. Without seeing all template JavaScript, there's a risk that:

1. Client-side JavaScript could store user data in localStorage/sessionStorage for convenience
2. The CSRF token (currently a placeholder) is stored in a hidden input — if eventually stored in localStorage, that would be a concern
3. Session cookies may contain sensitive data beyond just the session token

However, based on the code visible, the CSRF token is in a hidden input field (not localStorage), and there's no explicit evidence of localStorage/sessionStorage usage for sensitive data.

**Impact**: LOW-MEDIUM — Without full template/JavaScript analysis, the risk is potential rather than confirmed. The architecture passes sensitive data to client-rendered pages without documented constraints on client-side storage.

**Remediation**:
```javascript
// Add to application JavaScript policy:
// 1. Never store PII in localStorage/sessionStorage
// 2. Document what can be stored client-side
// 3. Add Content-Security-Policy to limit script behavior

// Example CSP header (server-side):
// Content-Security-Policy: default-src 'self'; script-src 'self'
```

---

### Security Controls Inventory for 14.3.3

| Control | Location | Status |
|---------|----------|--------|
| Policy against storing sensitive data in browser storage | Not documented | **MISSING documentation** |
| Content-Security-Policy header | Not found | **MISSING** |
| Client-side storage audit | Not visible in provided code | Unknown |
| Session token isolation (cookie-only) | Handled by `asfquart.session` | Presumed compliant (framework) |

### Positive Patterns for 14.3.3

- CSRF token is stored in a hidden HTML input field rather than localStorage/sessionStorage.
- The application uses server-side session management (`asfquart.session.read()`) rather than client-side token storage.
- No explicit `localStorage.setItem()` or `sessionStorage.setItem()` calls observed in the provided server-side code.
- The queries (`q_open_to_me`, `q_owned`) explicitly exclude cryptographic secrets (salt, opened_key) from results returned to views.

---



## Cross-cutting Architecture Observations

1. **Incomplete Security Controls Pattern**: The application exhibits a recurring pattern of security controls being _defined_ or _planned_ but not _implemented_:
   - CSRF token: defined as `'placeholder'` (line 83 of pages.py)
   - Authorization checks: marked with `### check authz` comments throughout
   - Path validation in `serve_doc`: marked with `### verify the propriety of DOCNAME`
   - These represent Type B gaps (control exists in concept but is not applied)

2. **Server-Side Security vs. Client-Side Security Imbalance**: The application demonstrates strong server-side security patterns (parameterized queries, cryptographic vote protection, state assertions, anti-tamper detection) but lacks corresponding client-side protections (no cache control, no Clear-Site-Data, no CSP headers).

3. **Cryptographic Maturity vs. Infrastructure Maturity**: The cryptographic design (Argon2 for key derivation, Fernet encryption, vote token system, tamper detection) is sophisticated, but the surrounding infrastructure (no SBOM, no vulnerability policy, no security headers) lags significantly.

4. **Single-Responsibility Concern**: `pages.py` handles routing, authentication, authorization, template rendering, and business logic. Security controls would benefit from middleware extraction (headers, CSRF, caching) applied uniformly.

5. **Framework Dependency for Security**: The application relies heavily on `asfquart` for authentication and session management. The security posture of these operations is opaque from the provided code — a single vulnerability in the framework could undermine all authentication.

---

## Cross-cutting Recommendations

### Immediate (0-7 days)
1. **Add anti-caching headers** via `@APP.after_request` handler for all non-static responses (`Cache-Control: no-store`)
2. **Implement logout endpoint** that sets `Clear-Site-Data: "cache", "cookies", "storage"` header
3. **Generate and commit `uv.lock`** to ensure reproducible builds and enable transitive dependency auditing

### Short-term (1-4 weeks)
1. **Create vulnerability remediation policy** documenting timeframes for critical/high/medium/low severity issues in dependencies
2. **Configure SBOM generation** in CI pipeline using `cyclonedx-py` or equivalent
3. **Add `uv audit`** to CI pipeline to detect known vulnerabilities in dependencies
4. **Implement Content-Security-Policy** header to restrict client-side script behavior
5. **Pin Apache-specific package sources** to prevent dependency confusion attacks

### Long-term (1-3 months)
1. **Extract security middleware** — Create a unified security headers middleware that applies Cache-Control, CSP, X-Content-Type-Options, and other security headers
2. **Implement proper CSRF protection** — Replace placeholder with cryptographically secure tokens validated server-side
3. **Complete authorization system** — Implement the `### check authz` checks throughout the application
4. **Establish dependency review process** — Monthly review cadence, automated PR generation for updates
5. **Add client-side session cleanup** — JavaScript that detects session expiration and clears sensitive DOM data
        