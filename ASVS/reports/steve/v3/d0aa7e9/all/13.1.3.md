# Security Audit Report: ASVS 13.1.3

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 13.1.3 |
| **Files Analyzed** | 14 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that the application documentation defines resource‑management strategies for every external system or service it uses (e.g., databases, file handles, threads, HTTP connections). This should include resource‑release procedures, timeout settings, failure handling, and where retry logic is…

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---

## ASVS-13.1.3: Resource Management Strategies

### Findings for 13.1.3

#### [MEDIUM]
**Finding ID:** ASVS-1313-MED-001

**Title:** No documented resource-management strategies, timeout settings, or retry logic for external services

**Location:** `v3/ARCHITECTURE.md`, `v3/steve/election.py`, `v3/server/main.py`, `v3/server/bin/mail-voters.py`

**Description:** The application documentation does not define resource-management strategies for any external system. Code analysis reveals the following gaps:

1. **No timeout settings for database operations:**
```python
# v3/steve/election.py - no timeout parameter when opening DB
@staticmethod
def open_database(db_fname):
    return asfpy.db.DB(db_fname, yaml_fname=QUERIES, yaml_section='election')
```

SQLite's default timeout for busy/locked databases is 5 seconds, but this is never explicitly configured or documented.

2. **No resource-release procedures documented:**

Database connections are released in some cases but not others:
```python
# v3/steve/election.py, lines 65-67 — connection closed on delete
def delete(self):
    # ...
    self.db.conn.close()
    self.db = None
```

But normal request-serving paths (via `load_election` decorator) never explicitly close connections:
```python
# v3/server/pages.py, lines 165-186
async def loader(eid):
    e = steve.election.Election(DB_FNAME, eid)
    # ... connection opened but never explicitly closed
    return await func(e)
```

3. **Email sending has no retry or timeout configuration:**
```python
# v3/server/bin/mail-voters.py, lines 67-73
asfpy.messaging.mail(
    sender='Apache Election Platform <voter@apache.org>',
    recipient=voter.email,
    subject=f'Vote in Election: {metadata.title}',
    message=rendered_body,
)
# No timeout, no retry configuration, no error handling per recipient
```

4. **OAuth callback has no documented timeout:**
```python
# v3/server/main.py, lines 37-40
asfquart.generics.OAUTH_URL_CALLBACK = 'https://oauth.apache.org/token?code=%s'
# No timeout or failure handling documented
```

5. **No retry limits, delays, or back-off algorithms documented anywhere.**

**Impact:** Without defined resource management strategies:
- Database locks could cause indefinite hangs under concurrent access
- Failed email sends could silently drop voter notifications
- OAuth service outages could leave requests hanging
- Resource leaks from unclosed connections could degrade availability over time

**Remediation:** Create a resource management section in documentation covering:

```markdown
# Resource Management Strategies

## SQLite Database
- **Timeout**: 30 seconds (sqlite3 `timeout` parameter)
- **Release**: Connections closed after each request via context manager
- **Failure handling**: Return 503 if database is locked beyond timeout
- **Retry**: No retries for synchronous operations

## OAuth (ASF OAuth)
- **Timeout**: 10 seconds for token exchange
- **Retry**: None (redirect user to retry login)
- **Failure handling**: Display error page with retry option

## SMTP (Email)
- **Timeout**: 30 seconds per message
- **Retry**: Up to 2 retries with 5-second delay
- **Back-off**: Not applicable (batch script)
- **Failure handling**: Log error, continue to next recipient
```

### Security Controls Inventory for 13.1.3

| Control | Location | Coverage |
|---------|----------|----------|
| DB connection close | `v3/steve/election.py:67` | Partial — only in `delete()` and `_disappeared()` |
| Transaction management | `v3/steve/election.py:55,122` | Present for multi-step operations |
| Error handling | `v3/server/pages.py:395-408` | Partial — vote submission has try/except |
| Timeout config | None | Not implemented |
| Retry logic | None | Not implemented |
| Resource-release procedures | None | Not documented |

### Positive Patterns for 13.1.3

- Transactions are properly used for multi-step operations (`delete()`, `add_salts()`)
- The `_disappeared()` method handles the case where a referenced election no longer exists
- Exception classes (`ElectionNotFound`, `ElectionBadState`, `IssueNotFound`) provide structured error handling
- The `do_vote_endpoint` has try/except for individual vote submissions to prevent one failure from blocking all votes

---



## Cross-cutting Architecture Observations

1. **Documentation vs. Implementation Gap**: The application has good architectural documentation (`ARCHITECTURE.md`, `schema.md`) covering design intent, but lacks operational documentation required by ASVS Level 2-3 (communication inventories, resource management strategies, secrets rotation schedules).

2. **Single Database Pattern**: The use of SQLite as an embedded database simplifies many backend communication concerns (no network authentication needed, no connection pool management) but makes it impossible to apply traditional service-account or role-based database access controls.

3. **Trust Boundary Ambiguity**: The `TODO.md` file acknowledges multiple unimplemented security controls (`### check authz` placeholders, CSRF placeholder). The system currently relies primarily on the ASF OAuth committer-level authentication as its sole access control boundary, with no fine-grained authorization distinguishing election owners from other committers.

4. **Operational Tooling Without Controls**: CLI scripts (`create-election.py`, `mail-voters.py`) operate with the same privileges as the web server but without any authentication or authorization layer, creating a potential privilege escalation path for anyone with shell access.

5. **Consistent Cryptographic Approach**: The cryptographic design (Argon2 key derivation, separate salts per voter-issue pair, encrypted votes, anti-tamper keys) is well-documented and consistently applied, representing a strength in the security architecture.

---

## Cross-cutting Recommendations

### Immediate (Address within current sprint)
1. **Create a Communications Inventory document** listing all external services (OAuth, SMTP, LDAP), protocols, authentication methods, and network requirements — satisfies ASVS 13.1.1 at minimal implementation cost
2. **Document existing secrets** and their criticality in a secrets inventory, even without rotation automation — satisfies ASVS 13.1.4 documentation requirement

### Short-term (1-3 months)
3. **Add resource management documentation** defining timeouts, connection limits, and failure handling for SQLite, OAuth, and SMTP — satisfies ASVS 13.1.2 and 13.1.3
4. **Implement read-only database connections** for components that only need read access (mail script, voter-facing queries) — improves ASVS 13.2.2 compliance
5. **Implement TLS certificate rotation procedures** and document the rotation schedule

### Long-term (3-6 months)
6. **Evaluate migration from SQLite** to a database that supports role-based access control if the application scales beyond single-server deployment — would fully satisfy ASVS 13.2.1 and 13.2.2
7. **Implement connection lifecycle management** with explicit open/close patterns (context managers) for all database access paths to prevent resource leaks
8. **Add circuit breaker patterns** for OAuth and SMTP communication with documented fallback behavior
9. **Implement proper CSRF tokens and session-based secrets** with defined rotation (currently placeholder) — prerequisite for ASVS 13.1.4 compliance on session secrets
        