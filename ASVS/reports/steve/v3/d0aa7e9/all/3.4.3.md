# Security Audit Report: ASVS 3.4.3

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 3.4.3 |
| **Files Analyzed** | 3 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that HTTP responses include a Content-Security-Policy response header field which defines directives to ensure the browser only loads and executes trusted content or resources, in order to limit execution of malicious JavaScript. As a minimum, a global policy must be used which includes the…

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 1 |
| 🟠 High | 1 |
| 🟡 Medium | 0 |
| 🟢 Low | 0 |

---

## ASVS-3.4.3: Content-Security-Policy Header

### Findings for 3.4.3

### [CRITICAL]
**Finding ID:** ASVS-343-CRIT-001  
**Location:** `v3/server/pages.py` (entire file, all route handlers)  
**Title:** No Content-Security-Policy header defined anywhere in the application

**Vulnerable Code:**
```python
# All HTML-rendering endpoints return template data without any CSP header
@APP.get('/')
@APP.use_template(TEMPLATES / 'home.ezt')
async def home_page():
    result = await basic_info()
    result.title = 'Home'
    return result
```

Additionally, the explicit response creation in `raise_404` (line 647):
```python
def raise_404(template, data):
    content = asfquart.utils.render(template, data)
    quart.abort(quart.Response(content, status=404, mimetype='text/html'))
```

**Data Flow:** HTTP request → Quart route handler → template rendered → HTTP response WITHOUT Content-Security-Policy header

**Proof of Concept:** Any HTTP response from this application can be verified to lack a CSP header. An attacker who achieves XSS (e.g., via the `rewrite_description` function's `<pre>` wrapping at line 49, which doesn't escape HTML) can execute arbitrary JavaScript because no CSP restricts script sources.

**Impact:** Without CSP, the browser will load and execute any script, style, or object from any origin. This enables:
- Execution of injected malicious JavaScript
- Data exfiltration via inline scripts
- Loading of malicious third-party resources
- No mitigation against XSS attacks

The requirement specifies minimum directives of `object-src 'none'` and `base-uri 'none'` with an allowlist/nonces/hashes, none of which are present.

**Remediation:**
```python
@APP.after_request
async def add_security_headers(response):
    # Generate a per-request nonce for L3 compliance
    import secrets
    nonce = secrets.token_urlsafe(32)
    
    csp = (
        f"default-src 'self'; "
        f"script-src 'self' 'nonce-{nonce}'; "
        f"style-src 'self' 'nonce-{nonce}'; "
        f"object-src 'none'; "
        f"base-uri 'none'; "
        f"frame-ancestors 'self'; "
        f"report-uri /csp-report; "
        f"report-to csp-endpoint"
    )
    response.headers['Content-Security-Policy'] = csp
    return response
```

### [HIGH]
**Finding ID:** ASVS-343-HIGH-001  
**Location:** `v3/server/templates/header.ezt` (lines 7-9)  
**Title:** No CSP meta tag as fallback, external resources loaded without integrity or nonce controls

**Vulnerable Code:**
```html
<link href="/static/css/bootstrap.min.css" rel="stylesheet"
      integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9">
<link href="/static/css/bootstrap-icons.css" rel="stylesheet">
<link href="/static/css/steve.css" rel="stylesheet">
```

And in `footer.ezt`:
```html
<script src="/static/js/steve.js"></script>
<script src="/static/js/bootstrap.bundle.min.js" integrity="sha384-HwwvtgBNo3bZJJLYd8oVXjrBZt8cqVSpeBNS5n7C8IVInixGAoxmnlMuBnhbgrkm"></script>
```

**Impact:** While SRI attributes are present on some resources (positive), without a CSP header these provide no enforcement against injected script/style tags. The `bootstrap-icons.css` and `steve.css` lack integrity attributes entirely.

**Remediation:** Add a `<meta>` CSP fallback in `header.ezt` and add nonces to all script/style tags:
```html
<meta http-equiv="Content-Security-Policy" content="default-src 'self'; object-src 'none'; base-uri 'none';">
```

### Security Controls Inventory for 3.4.3

| Control | Location | Status |
|---------|----------|--------|
| CSP HTTP header | Not found anywhere | **MISSING** |
| CSP meta tag | Not in `header.ezt` | **MISSING** |
| Per-response nonce | Not implemented | **MISSING** |
| SRI on scripts | `footer.ezt` (bootstrap only) | Partial |
| SRI on styles | `header.ezt` (bootstrap only) | Partial |

### Positive Patterns for 3.4.3

- Subresource Integrity (SRI) is used on Bootstrap CSS and JS resources, which is a defense-in-depth measure
- Static resources are served from the same origin (`/static/`) rather than CDN, reducing attack surface
- The application uses a templating engine (`ezt`) which may provide some output encoding

---



## Cross-cutting Architecture Observations

1. **Complete Absence of Security Header Infrastructure**: The application has NO `after_request` handler, middleware, or any other mechanism to inject security headers into HTTP responses. This is a systemic gap — not a per-header oversight, but a fundamental architectural omission.

2. **Gap Type B (Control EXISTS but NOT CALLED)**: The Quart framework fully supports `after_request` hooks and response header manipulation, but neither is utilized. This creates false confidence if anyone assumes the framework provides these headers by default (it does not).

3. **Template Architecture Lacks Security Meta Tags**: The `header.ezt` template, which is included by all HTML pages, contains no security-related `<meta>` tags that could serve as a fallback for missing HTTP headers.

4. **Custom Response Objects Bypass Future Middleware**: The `raise_404` function creates raw `quart.Response` objects. If an `after_request` handler is added in the future, these responses will be processed by it (good), but the pattern of manually constructing responses should be noted for consistency.

5. **Sensitive Application Context**: This is a **voting application** handling election data. The consequences of XSS, clickjacking, or tabnabbing attacks are severe — vote manipulation, election fraud, or voter impersonation. The absence of all browser security headers in this context is particularly critical.

6. **External Resource Loading**: The application loads resources from external origins (Apache logo from `www.apache.org`) in `header.ezt`, which would need to be accounted for in any CSP policy.

## Cross-cutting Recommendations

### Immediate (Critical Priority)
1. **Create a centralized `after_request` handler** that sets ALL required security headers on every response:
   ```python
   @APP.after_request
   async def add_security_headers(response):
       response.headers['Content-Security-Policy'] = (
           "default-src 'self'; "
           "script-src 'self'; "
           "style-src 'self'; "
           "img-src 'self' https://www.apache.org; "
           "object-src 'none'; "
           "base-uri 'none'; "
           "frame-ancestors 'self'; "
           "report-uri /csp-report"
       )
       response.headers['X-Content-Type-Options'] = 'nosniff'
       response.headers['Referrer-Policy'] = 'strict-origin-when-cross-origin'
       response.headers['Cross-Origin-Opener-Policy'] = 'same-origin'
       return response
   ```

2. **Add `rel="noopener noreferrer"` to ALL external links** in templates (`footer.ezt`, `header.ezt`).

### Short-term (High Priority)
3. **Implement per-response CSP nonces** for L3 compliance — generate nonces in `basic_info()`, pass to templates, and include in CSP header.
4. **Create a CSP violation reporting endpoint** (`/csp-report`) and integrate with existing logging.
5. **Deploy CSP in report-only mode first** (`Content-Security-Policy-Report-Only`) to identify legitimate resource loads that need allowlisting.
6. **Add SRI attributes** to `bootstrap-icons.css` and `steve.css` resources that currently lack them.

### Long-term (Architectural)
7. **Consider a security header library** like `secure` (Python) or `Talisman` (adapted for Quart) that enforces headers by default and reduces risk of regression.
8. **Add integration tests** that verify security headers are present on all response types (HTML, JSON, static files, error pages).
9. **Implement Content-Security-Policy with nonces** across all templates, requiring template engine integration to inject nonces into script/style tags dynamically.
10. **Self-host the Apache feather logo** to eliminate the need for `img-src https://www.apache.org` in CSP, tightening the policy further.
        