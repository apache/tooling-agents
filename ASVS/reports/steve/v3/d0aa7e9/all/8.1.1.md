# Security Audit Report: ASVS 8.1.1

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 8.1.1 |
| **Files Analyzed** | 11 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that authorization documentation defines rules for restricting function-level and data-specific access based on consumer permissions and resource attributes.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 1 |
| 🟡 Medium | 0 |
| 🟢 Low | 0 |

---

## ASVS-8.1.1: Authorization Documentation

### Findings for 8.1.1

#### [HIGH]

**Finding ID:** ASVS-811-HIGH-001

**Files:** `v3/TODO.md`, `v3/docs/schema.md`, `v3/server/pages.py`

**Evidence of missing documentation:**

From `TODO.md`:
```
- **Authz Checks**: Many endpoints have `### check authz` comments but no implementation.
  If authorization beyond basic auth is needed, add it (e.g., ensure the user owns the election).
```

From `schema.md`:
```
- **Authorization**: The `authz` field's format is TBD but will likely use LDAP group identifiers
  to define editing permissions. Validation occurs outside the schema (e.g., via LDAP integration).
```

From `pages.py` (multiple lines):
```python
### NOTE: the voting handlers require login for ASF committers only.
### Obviously, this is not a general purpose solution. Something for
### the future, to figure out how we'd like to do configuration
### authorization for various install scenarios and authn systems.
```

**Issue:** There is no formal authorization documentation defining:
1. Which roles (committer, pmc_member, election owner) can access which functions
2. What data-specific restrictions apply (e.g., only election owner can manage their election)
3. The distinction between voter, election manager, and system administrator
4. What the `authz` field in the election table is supposed to control

The code reveals at least these distinct authorization levels that are undocumented:
- `R.committer` — used on most endpoints (very broad)
- `R.pmc_member` — used only on `/do-create-election`
- Election ownership (`owner_pid`) — exists in schema but never enforced
- Voter eligibility (`mayvote` table) — partially enforced, partially not
- Valid session — used on `/profile`, `/settings`, `/docs`

**Impact:** Without authorization documentation, developers cannot consistently implement access controls, testers cannot verify correctness, and the multiple `### check authz` placeholders indicate authorization was intended but never formally specified or implemented.

**Remediation:** Create an authorization matrix document:
```markdown
# Authorization Policy

## Roles
- **voter**: Any person in the `person` table with `mayvote` entries
- **election_owner**: Person whose PID matches `election.owner_pid`
- **authz_group**: Members of the LDAP group in `election.authz`
- **committer**: ASF committer (authenticated via IdP)
- **pmc_member**: ASF PMC member

## Function-Level Access
| Function | Required Role | Additional Constraint |
|----------|--------------|----------------------|
| Create election | pmc_member | — |
| Manage election | election_owner OR authz_group | Must own or be in authz group |
| Open/Close election | election_owner | Must own the election |
| Add/Edit/Delete issue | election_owner | Election must be editable |
| Vote | voter | Must have mayvote entry for election |
| View ballot | voter | Must have mayvote entry for election |

## Data-Specific Access
| Data | Who Can Read | Who Can Write |
|------|-------------|---------------|
| Election metadata | Any authenticated user | election_owner |
| Election salt/opened_key | System only | System only |
| Vote ciphertext | System only | voter (own vote) |
| Mayvote entries | election_owner | election_owner (editable state) |
```

### Security Controls Inventory for 8.1.1

| Control | Location | Status |
|---------|----------|--------|
| Role-based auth decorator | `pages.py`, all protected endpoints | Applied but too broad (committer for everything) |
| `owner_pid` field | `schema.sql`, `election` table | EXISTS in schema, NEVER enforced in code |
| `authz` field | `schema.sql`, `election` table | EXISTS in schema, format TBD, NEVER enforced |
| `mayvote` check | `vote_on_page()` line 236, `serve_doc()` line 567 | Partially applied (view yes, submit no) |
| Authorization documentation | N/A | DOES NOT EXIST |

### Positive Patterns for 8.1.1

- The schema design anticipates authorization via `owner_pid` and `authz` fields
- The `TODO.md` acknowledges the gap explicitly
- `do_create_endpoint` uses a more restrictive role (`R.pmc_member`)
- `vote_on_page` performs a voter eligibility check before showing the ballot

---



## Cross-cutting Architecture Observations

1. **Systematic Authorization Gap (Type B):** The most critical finding across all requirements is the pattern of authorization placeholders (`### check authz`) that are never implemented. This creates a dangerous false sense of security — developers clearly intended to add authorization but it was never done. Eight separate endpoints share this identical gap.

2. **Authentication ≠ Authorization Confusion:** The application conflates authentication (proving identity) with authorization (proving permission). Being an ASF committer is sufficient to perform ANY operation on ANY election. The `authz` and `owner_pid` fields exist in the schema but are completely unused in the application layer.

3. **Defense-in-Depth at Data Layer:** Despite the application-level authorization failures, the `election.py` module provides some defense via state assertions (`assert self.is_editable()`) and the cryptographic design (can't decrypt votes without the proper keys). However, these protect election integrity, not confidentiality or availability.

4. **Missing Documentation Pattern:** Across all 8.1.x requirements, there is no formal authorization documentation. The code comments and `TODO.md` acknowledge this debt explicitly. The `schema.md` provides data-level documentation but not security-decision documentation.

5. **HTTP Method Misuse:** State-mutating operations (`do_open`, `do_close`) use GET, making them vulnerable to CSRF even before considering the placeholder CSRF token. This compounds the authorization gap.

6. **Incomplete Enforcement Pattern:** Where authorization IS implemented (e.g., `vote_on_page` checks mayvote), the corresponding action endpoint (`do_vote_endpoint`) does NOT repeat the check. This creates a bypass where viewing is protected but acting is not.

---

## Cross-cutting Recommendations

### Immediate (Critical - must fix before deployment)

1. **Implement ownership checks in `load_election` decorator** — Compare `election.owner_pid` against `result.uid`. This single fix addresses ASVS-821-CRIT-001, CRIT-003, HIGH-002, and MED-001 across 8+ endpoints.

2. **Add voter eligibility check in `do_vote_endpoint`** — Mirror the check from `vote_on_page()` to prevent ineligible users from submitting votes (ASVS-821-CRIT-002).

3. **Change `do_open` and `do_close` from GET to POST** — Prevents trivial CSRF via link/image tags (ASVS-821-HIGH-001).

### Short-term (High priority - within next sprint)

4. **Implement the `authz` field enforcement** — Define and implement group-based authorization for elections that specify an authz group.

5. **Create authorization documentation** (ASVS-811-HIGH-001) — Document the role hierarchy, function-level access matrix, and field-level restrictions that are already partially implemented in code.

6. **Implement CSRF token generation and validation** — Replace the placeholder with real tokens, validated on all POST endpoints.

7. **Document field-level access rules** — Formalize the implicit rules about salt/key exclusion from public APIs.

### Long-term (Strategic improvements)

8. **Consider contextual security factors** — For a voting system, logging IP addresses and implementing rate limiting would improve auditability and prevent abuse.

9. **Implement session consent mechanism** — For federated authentication, add a consent step or session confirmation to meet ASVS 7.6.2.

10. **Centralize authorization logic** — Instead of scattered checks, implement a policy-based authorization system (e.g., ABAC) that the decorators call consistently.
        