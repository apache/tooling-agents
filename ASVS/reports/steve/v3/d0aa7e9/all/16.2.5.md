# Security Audit Report: ASVS 16.2.5

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 16.2.5 |
| **Files Analyzed** | 9 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that when logging sensitive data, the application enforces logging based on the data's protection level. For example, it may not be allowed to log certain data, such as credentials or payment details. Other data, such as session tokens, may only be logged by being hashed or masked, either in…

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 1 |
| 🟡 Medium | 2 |
| 🟢 Low | 0 |

---

## ASVS-16.2.5: Sensitive Data Logging

### Findings for 16.2.5

### [HIGH]
**Finding ID:** ASVS-1625-HIGH-001
**Location:** `v3/server/pages.py`, `do_add_issue_endpoint()` (line 481) and `do_edit_issue_endpoint()` (line 497)
**Vulnerable Code:**
```python
form = edict(await quart.request.form)
print('FORM:', form)
```
**Data Flow:** User form submission → `quart.request.form` → `print()` to stdout/process output
**Proof of Concept:** Any POST to `/do-add-issue/<eid>` will dump the full form contents (title, description, and any other fields) to the process output stream without any protection level consideration.
**Impact:** Raw user-submitted form data is dumped to stdout, potentially containing sensitive election descriptions, candidate information, or other data that should be treated with care. In containerized deployments, stdout is often aggregated into centralized logging systems, making this data broadly accessible.
**Remediation:** Remove debug `print()` statements. If debugging is needed, use the existing `_LOGGER.debug()` mechanism which respects log level configuration:
```python
# Remove these lines entirely:
# print('FORM:', form)

# If needed for debugging, use:
_LOGGER.debug(f'Received form for issue in election[E:{election.eid}]')
```

### [MEDIUM]
**Finding ID:** ASVS-1625-MED-001
**Location:** `v3/server/pages.py`, `do_vote_endpoint()` (line 403)
**Vulnerable Code:**
```python
except Exception as e:
    _LOGGER.error(
        f'Error adding vote for user[U:{result.uid}] on issue[I:{iid}]: {e}'
    )
```
**Data Flow:** Internal exception → `str(e)` → logger → log storage
**Proof of Concept:** If the crypto or database layer throws an exception that contains partial vote data, key material, or internal state in its message, it will be logged. For example, `sqlite3.InterfaceError` might contain query parameters.
**Impact:** Exception messages may inadvertently include sensitive internal state such as partial cryptographic material, database state, or vote-related data. The `{e}` pattern logs whatever the exception type includes.
**Remediation:** Log a generic error message and use structured metadata:
```python
except Exception as e:
    _LOGGER.error(
        f'Error adding vote for user[U:{result.uid}] on issue[I:{iid}]',
        exc_info=_LOGGER.isEnabledFor(logging.DEBUG)
    )
```

### [MEDIUM]
**Finding ID:** ASVS-1625-MED-002
**Location:** `v3/server/bin/mail-voters.py`, `main()` (line 74)
**Vulnerable Code:**
```python
_LOGGER.info(f'Sent email to {voter.email} for election {eid}')
```
**Data Flow:** Database → `voter.email` → `_LOGGER.info()` → log storage
**Proof of Concept:** Running `mail-voters.py --eid <any_eid> --template <file>` logs every voter's email address in plain text to the log file.
**Impact:** Email addresses are PII and are logged in full without masking. In environments subject to data protection regulations, logs containing PII may require additional protections or shorter retention.
**Remediation:** Mask or hash email addresses in logs:
```python
import hashlib
masked_email = voter.email[:2] + '***@' + voter.email.split('@')[-1]
_LOGGER.info(f'Sent email to {masked_email} for election {eid}')
```

### Security Controls Inventory for 16.2.5

| Control | Location | Coverage Status |
|---------|----------|-----------------|
| Vote content exclusion from logs | `pages.py:407` | ✅ Applied — votestring not logged |
| Salt/key exclusion from public APIs | `election.py:144-155` | ✅ Applied — `get_metadata()` excludes sensitive fields |
| Query design excludes secrets | `queries.yaml:q_open_to_me, q_owned` | ✅ Applied — SALT and OPENED_KEY excluded from SELECT |
| Debug print statements | `pages.py:481,497` | ❌ NOT controlled — raw form data dumped |
| Exception content logging | `pages.py:403` | ❌ Unfiltered exception message logged |
| PII in operational logs | `mail-voters.py:74` | ❌ Email logged in plain text |

### Positive Patterns for 16.2.5

1. **Vote content is never logged** — The `do_vote_endpoint()` function logs the action (user, issue, election) but explicitly avoids logging the votestring content.
2. **Cryptographic secrets excluded from metadata API** — `get_metadata()` in `election.py` explicitly excludes `salt` and `opened_key` from its return value, and comments document this decision.
3. **SQL queries designed for minimal exposure** — `q_open_to_me`, `q_owned`, and `q_upcoming_to_me` explicitly select only non-sensitive columns, with a comment noting "SALT and OPENED_KEY are never returned for security."

---



## Cross-cutting Architecture Observations

1. **Logging maturity is low** — The application has action logging for administrative operations but lacks authentication event logging, authorization failure logging, input validation failure logging, and error logging across most endpoints. Only `do_vote_endpoint()` demonstrates the full pattern of try/except with error logging.

2. **Type B gaps are pervasive** — The codebase contains numerous `### check authz` comments indicating the developer intended to add authorization checks. These are never implemented, creating a false sense of security and making authorization failure logging impossible.

3. **No centralized logging infrastructure** — There is no logging middleware, no structured log format (e.g., JSON), no log sanitization layer, and no consistent error handling pattern. Each endpoint handles (or doesn't handle) logging independently.

4. **Debug code in production paths** — `print('FORM:', form)` statements exist in two production endpoints, bypassing all logging controls and dumping raw user data.

5. **Security assertions vs. proper error handling** — The `election.py` module uses Python `assert` statements for security-critical state checks. These are: (a) disabled with `-O` flag, (b) generate no log entries when they fire, and (c) provide no graceful error handling.

6. **Single responsibility gap** — The authentication framework (`asfquart.auth`) handles enforcement but there's no evidence it handles logging. The application layer logs actions but not auth events. This creates a gap where neither layer owns auth logging.

## Cross-cutting Recommendations

### Immediate (Critical/High priority)
1. **Remove debug `print()` statements** — Lines 481 and 497 in `pages.py` dump raw form data to stdout. Remove immediately.
2. **Implement log sanitization** — Create a wrapper or formatter that strips control characters (`\r`, `\n`, `\x00`-`\x1f`) from all logged values before output.
3. **Add error handling with logging to all state-changing endpoints** — Wrap `election.open()`, `election.close()`, `Election.create()` in try/except with `_LOGGER.error()`.
4. **Log authorization denials** — Add `_LOGGER.warning()` calls in `vote_on_page()` and `serve_doc()` where access is denied.

### Short-term (1-2 sprints)
5. **Implement authentication event logging** — Add a middleware or decorator that logs all authentication successes and failures with metadata (user, endpoint, method, IP, auth type).
6. **Replace `assert` with proper exceptions** — Convert all security-relevant `assert` statements in `election.py` to explicit checks with `ElectionBadState` exceptions and logging.
7. **Implement authorization checks** — Address all `### check authz` placeholders with ownership verification and log failures.
8. **Add input validation failure logging** — Log when date parsing fails, when invalid IIDs are submitted, and when other validation rejects input.

### Long-term (Architecture)
9. **Adopt structured logging (JSON)** — Migrate to structured log format with consistent fields (`timestamp`, `level`, `event_type`, `user_id`, `resource_id`, `action`, `outcome`) for machine-readable analysis.
10. **Implement centralized security event framework** — Create a `SecurityEvent` class or module that provides typed events (`AUTH_SUCCESS`, `AUTH_FAILURE`, `AUTHZ_DENIED`, `INPUT_VALIDATION_FAILED`, `BUSINESS_LOGIC_VIOLATION`) with automatic metadata collection.
11. **Add anti-automation detection** — Implement rate limiting with logging of threshold violations to detect brute-force or automated abuse.
12. **PII handling policy in logs** — Establish a logging policy that defines which fields may be logged in full, which must be masked, and which must never be logged, then enforce via the structured logging framework.
        