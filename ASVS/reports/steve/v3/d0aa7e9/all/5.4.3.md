# Security Audit Report: ASVS 5.4.3

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/steve/v3 |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 5.4.3 |
| **Files Analyzed** | 4 relevant / 49 total |
| **Files Skipped** | 12 |

**Requirement description:** Verify that files obtained from untrusted sources are scanned by antivirus scanners to prevent serving of known malicious content.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 1 |

---

## ASVS-5.4.3: File Download - Antivirus Scanning

### Findings for 5.4.3

### [MEDIUM]

**Finding ID**: ASVS-543-MED-001

**File**: `v3/server/pages.py`, function `serve_doc`, lines 560-574

**Vulnerable code:**
```python
# Serve supporting documents for an issue
@APP.get('/docs/<iid>/<docname>')
@asfquart.auth.require  # fine-grained per-doc authz within the handler
async def serve_doc(iid, docname):
    result = await basic_info()  # get PID  ### grr: called uid

    db = steve.election.Election.open_database(DB_FNAME)
    row = db.q_get_mayvote.first_row(result.uid, iid)
    if not row:
        # Nothing fancy. Just pretend the file does not exist
        quart.abort(404)
        # NOTREACHED

    ### verify the propriety of DOCNAME.

    # Return per-issue document.
    return await quart.send_from_directory(DOCSDIR / iid, docname)
```

**Data flow**: Uploaded document → stored in `DOCSDIR/<iid>/` → served directly to users via `send_from_directory` → no antivirus scan

**Proof of concept**: An election administrator (committer/PMC member) uploads a document containing malware (e.g., a PDF with an embedded exploit). The document is stored in the `DOCSDIR/<iid>/` directory. When another voter accesses `/docs/<iid>/malicious.pdf`, the file is served without any antivirus or content inspection.

**Impact**: Malicious files uploaded as supporting election documents can be distributed to all voters in the election. Since these documents are served to authenticated ASF committers who trust the system, the social-engineering vector is significant.

**Remediation**: Implement antivirus scanning on file upload (or at minimum before serving). Example integration with ClamAV:

```python
import clamd

CLAM = clamd.ClamdUnixSocket()

@APP.get('/docs/<iid>/<docname>')
@asfquart.auth.require
async def serve_doc(iid, docname):
    result = await basic_info()
    
    db = steve.election.Election.open_database(DB_FNAME)
    row = db.q_get_mayvote.first_row(result.uid, iid)
    if not row:
        quart.abort(404)

    # Validate docname (path traversal prevention)
    safe_path = (DOCSDIR / iid / docname).resolve()
    if not safe_path.is_relative_to(DOCSDIR / iid):
        quart.abort(404)
    
    # Scan file before serving
    scan_result = CLAM.scan(str(safe_path))
    if scan_result and safe_path in scan_result:
        status, reason = scan_result[str(safe_path)]
        if status == 'FOUND':
            _LOGGER.warning(f'Malware detected in {safe_path}: {reason}')
            quart.abort(403)
    
    return await quart.send_from_directory(DOCSDIR / iid, docname)
```

---

### [LOW]

**Finding ID**: ASVS-543-LOW-001

**File**: `v3/server/pages.py`, function `rewrite_description`, lines 49-56

**Vulnerable code:**
```python
def rewrite_description(issue):
    """Rewrite issue description: wrap in <pre> and convert doc:filename to links."""
    import re
    desc = issue.description
    # Replace doc:filename with <a> link
    def repl(match):
        filename = match.group(1)
        return f'<a href="/docs/{issue.iid}/{filename}">{filename}</a>'
    desc = re.sub(r'doc:([^\s]+)', repl, desc)
    # Wrap in <pre>
    issue.description = f'<pre>{desc}</pre>'
```

**Data flow**: Issue description containing `doc:filename` → generates link to file → file served without content inspection

**Impact**: The linking mechanism creates the pathway for users to download issue documents. Without antivirus scanning at the download endpoint, any malicious document referenced via `doc:` in descriptions will be served directly.

### Security Controls Inventory for 5.4.3

| Control | Location | Status |
|---------|----------|--------|
| Antivirus scanning on upload | Not found | **MISSING** |
| Antivirus scanning on download | Not found | **MISSING** |
| Content-Type enforcement | Implicit via `send_from_directory` | Partial (framework default) |
| File type allowlist | Not found | **MISSING** |
| Authorization check for doc access | `serve_doc()` line 567 (`q_get_mayvote`) | Present |

### Positive Patterns for 5.4.3

- Authorization check exists in `serve_doc()` — only voters with `mayvote` entries for the specific issue can access documents
- Files are organized per-issue in subdirectories (`DOCSDIR / iid`), limiting scope
- `send_from_directory` is used (Quart framework utility that provides some safety against path traversal at the framework level)

---



## Cross-cutting Architecture Observations

1. **Authentication Delegation Pattern**: The application consistently delegates authentication to ASF infrastructure (`asfquart.auth`). This means ASVS requirements 6.2.1 and 6.2.10 (password security) are the responsibility of the external identity provider, not this application. However, documentation requirements (6.1.1, 6.1.2, 6.1.3) still apply to the application.

2. **Authorization Gap (Type B — Control EXISTS but NOT CALLED)**: The code has 9+ instances of `### check authz` placeholder comments. While authentication (identity verification) is enforced, authorization (permission verification — e.g., "is this user the election owner?") is NOT implemented. Any committer can manage any election, open/close elections they don't own, and delete others' issues. This is a critical Type B gap.

3. **CSRF Protection is a Placeholder**: `basic.csrf_token = 'placeholder'` (line 83) combined with no server-side validation means all state-changing endpoints are vulnerable to CSRF. The template includes the token in forms/headers, creating false confidence (Type B gap).

4. **State-Changing GET Requests**: `/do-open/<eid>` and `/do-close/<eid>` use GET methods for state-changing operations, making CSRF exploitation trivial (no form submission needed — a simple `<img>` tag suffices).

5. **File Serving Pipeline**: Documents flow from upload (not shown in provided code) → storage in `DOCSDIR` → serving via `serve_doc()` with no content inspection or antivirus scanning at any stage.

## Cross-cutting Recommendations

### Immediate (Critical/High Priority)

1. **Implement authorization checks**: Replace all `### check authz` comments with actual ownership/permission verification:
   ```python
   if election.get_metadata().owner_pid != result.uid:
       quart.abort(403)
   ```

2. **Implement CSRF protection**: Replace placeholder token with a real implementation (e.g., `quart-csrf` or similar), and validate on all POST endpoints.

3. **Convert state-changing GETs to POSTs**: `/do-open/<eid>` and `/do-close/<eid>` must use POST method with CSRF validation.

4. **Document authentication pathways**: Create a security document covering all auth levels, their required controls, and configuration guidance.

### Short-term (Medium Priority)

5. **Implement rate limiting**: Add request rate limiting middleware (e.g., `quart-rate-limiter`) with documented thresholds for authentication, voting, and administrative operations.

6. **Add antivirus scanning**: Integrate ClamAV or similar scanner into the document upload pipeline and/or download endpoint.

7. **Validate `docname` in `serve_doc()`**: While `send_from_directory` provides some protection, add explicit path traversal validation:
   ```python
   safe_path = (DOCSDIR / iid / docname).resolve()
   if not safe_path.is_relative_to(DOCSDIR / iid):
       quart.abort(404)
   ```

8. **Document context-specific password blocklist**: Even though authentication is delegated, document the blocklist and communicate it to the ASF identity management team.

### Long-term (Low Priority)

9. **Remove debug statements**: Remove `print('FORM:', form)` from production endpoints.

10. **Add Content-Security-Policy headers**: For served documents, add appropriate headers to prevent execution of embedded scripts.

11. **Implement comprehensive security documentation**: Cover all ASVS documentation requirements in a single security architecture document.
        