# Security Audit Report: ASVS 15.1.1

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/mahout |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 15.1.1 |
| **Files Analyzed** | 14 relevant / 394 total |
| **Files Skipped** | 36 |

**Requirement description:** Verify that application documentation defines risk based remediation time frames for 3rd party component versions with vulnerabilities and for updating libraries in general, to minimize the risk from these components.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---

## ASVS-15.1.1: Secure Coding and Architecture Documentation — Remediation Timeframes

### Findings for 15.1.1

#### [MEDIUM]
**Finding ID:** ASVS-1511-MED-001
**Title:** No documented risk-based remediation timeframes for third-party component vulnerabilities

**File location:** `dev/release.md` (entire file); all documentation files

**Vulnerable code quote:**
```markdown
<!-- dev/release.md does not contain any section about dependency vulnerability management -->
<!-- The release process covers: branching, building, signing, voting, publishing -->
<!-- But no mention of: -->
<!--   - Dependency vulnerability scanning -->
<!--   - Remediation timeframes (critical: X days, high: Y days, etc.) -->
<!--   - SBOM generation -->
<!--   - Dependency audit procedures -->
```

**Data flow:** N/A — this is a documentation/process gap

**Impact:** Without defined remediation timeframes:
- Known vulnerabilities in dependencies (`cudarc`, `thiserror`, `arrow`, `parquet`, CUDA runtime, PyTorch) may persist indefinitely
- No consistent standard for when updates must be applied
- Increased window of exposure for supply chain attacks
- Inconsistent risk treatment across the project

**Third-party dependencies identified from code:**
| Dependency | Usage | Risk Category |
|-----------|-------|---------------|
| `cudarc` | CUDA device management | High (GPU driver interface, FFI) |
| `qdp_kernels` | CUDA kernel bindings | High (unsafe FFI, memory operations) |
| `thiserror` | Error derivation | Low |
| `arrow`/`parquet` (implied) | Data format I/O | Medium (parser, deserialization) |
| `PyTorch` (optional) | DLPack consumer | Medium |
| `numpy` (optional) | Array input | Low |
| CUDA Runtime | GPU operations | High (system library) |

**Remediation:**
Create a `SECURITY.md` or `docs/security/dependency-policy.md` defining:

```markdown
# Dependency Vulnerability Management Policy

## Remediation Timeframes

| Severity | Definition | Remediation SLA |
|----------|-----------|----------------|
| Critical (CVSS ≥ 9.0) | RCE, data exfiltration, privilege escalation | 7 calendar days |
| High (CVSS 7.0–8.9) | Significant impact vulnerabilities | 30 calendar days |
| Medium (CVSS 4.0–6.9) | Limited impact vulnerabilities | 90 calendar days |
| Low (CVSS < 4.0) | Minimal impact | Next scheduled release |

## General Update Policy
- All dependencies reviewed quarterly
- `cargo audit` / `pip-audit` run in CI on every PR
- SBOM generated with each release

## Dangerous Components
- `qdp_kernels`: CUDA FFI — unsafe operations, direct memory manipulation
- `cudarc`: CUDA driver bindings — GPU memory allocation, raw pointers
- Parquet/Arrow readers: Binary data parsing from untrusted files
```

### Security Controls Inventory for 15.1.1

| Control | Location | Status |
|---------|----------|--------|
| Dependency remediation timeframe documentation | Not found in any provided file | ❌ **Missing** |
| Vulnerability scanning in CI | Not found | ❌ Not documented |
| SBOM generation | Not found | ❌ Not documented |
| Dangerous component identification | Not found | ❌ Not documented |
| Release process vulnerability checks | `dev/release.md` | ❌ Release process does not include dependency audit step |

### Positive Patterns for 15.1.1

1. **Apache release governance**: The ATR (Apache Trusted Releases) process with PMC voting provides a structured release gate where dependency issues could be caught, though no explicit dependency review step is documented.
2. **Feature-gated optional dependencies**: The `remote-io` and `pytorch` features are opt-in, reducing the mandatory dependency surface.

---



## Cross-cutting Architecture Observations

1. **Library vs. Web Application**: The audited codebase is a GPU computing library (Rust + Python), not a web application. Several ASVS requirements (12.2.2, 14.3.1) have limited applicability. The most impactful findings are in the documentation/process domain (15.1.1, 15.2.1).

2. **Unsafe Code Boundary**: The codebase contains significant `unsafe` Rust code for CUDA FFI operations. While the code demonstrates disciplined unsafe usage with safety documentation, pointer validation, and RAII wrappers, this represents a "dangerous functionality" category per ASVS 15.1.1 that should be formally documented.

3. **File Format Parsers as Attack Surface**: The library accepts untrusted file inputs in multiple formats (Parquet, Arrow IPC, NumPy, PyTorch, TensorFlow protobuf). These parsers process binary data from potentially untrusted sources and represent the highest-risk attack surface. The parsing is delegated to well-known libraries (Arrow, etc.), but vulnerability tracking is essential.

4. **Remote IO Feature**: The `remote-io` feature enables cloud storage access, introducing network-level security requirements (TLS, authentication). The implementation was not provided for audit, creating a significant blind spot.

5. **Credential Management in Release Process**: The `dev/release.md` correctly advises storing `.pypirc` in `~/.pypirc` with `chmod 600` and warns against placing it in the project directory — a solid operational security practice.

## Cross-cutting Recommendations

### Immediate (0–30 days)
1. **Create dependency vulnerability management policy** (ASVS-1511-MED-001): Define remediation timeframes for Critical/High/Medium/Low severity vulnerabilities in a `SECURITY.md` or equivalent document.
2. **Add `cargo audit` to CI pipeline** (ASVS-1521-MED-001): Implement automated Rust dependency vulnerability scanning on every PR and scheduled weekly runs.
3. **Document dangerous components**: Formally identify `qdp_kernels`, `cudarc`, and file format parsers as "dangerous functionality" per ASVS 15.1.1 definitions.

### Short-term (30–90 days)
4. **Audit the `remote` module** (ASVS-1222-MED-001): Verify TLS certificate validation for S3/GCS connections. Ensure publicly trusted certificates are enforced and no bypass exists.
5. **Add deployment hardening documentation** (ASVS-1341-MED-001): If QDP or its documentation is deployed as a web service, document exclusion of `.git` and other VCS metadata.
6. **Add Python dependency auditing**: Implement `pip-audit` or `safety` checks for the Python package surface.

### Long-term (90+ days)
7. **SBOM generation**: Integrate Software Bill of Materials generation into the release process for both Rust (`cargo sbom`) and Python (`cyclonedx-py`) components.
8. **Sanitize paths in error messages** (ASVS-1421-LOW-001): Implement path redaction for remote URLs in error messages to prevent information leakage of internal bucket/key names.
9. **Sandboxing for file parsers**: Consider containerization or process isolation for file format parsing (Parquet, Arrow, TensorFlow protobuf) to limit blast radius from parser vulnerabilities.
        