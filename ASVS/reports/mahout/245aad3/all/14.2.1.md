# Security Audit Report: ASVS 14.2.1

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/mahout |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 14.2.1 |
| **Files Analyzed** | 14 relevant / 394 total |
| **Files Skipped** | 36 |

**Requirement description:** Verify that sensitive data is only sent to the server in the HTTP message body or header fields, and that the URL and query string do not contain sensitive information, such as an API key or session token.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 0 |
| 🟢 Low | 1 |

---

## ASVS-14.2.1: General Data Protection — Sensitive Data in URLs

### Findings for 14.2.1

#### [LOW]
**Finding ID:** ASVS-1421-LOW-001
**Title:** Remote S3/GCS URL paths may contain sensitive bucket names or object keys in logs

**File location:** `qdp/qdp-core/src/lib.rs` (encode_from_parquet, lines referencing `path: &str`); `docs/qdp/getting-started.md` (remote URL examples)

**Vulnerable code quote:**
```markdown
<!-- docs/qdp/getting-started.md -->
engine.encode("s3://my-bucket/path/data.parquet", num_qubits=10, encoding_method="amplitude")
engine.encode("gs://my-bucket/path/data.parquet", num_qubits=10, encoding_method="amplitude")
```

```rust
// lib.rs - encode_from_parquet accepts path as &str
pub fn encode_from_parquet(
    &self,
    path: &str,
    num_qubits: usize,
    encoding_method: &str,
) -> Result<*mut DLManagedTensor> {
    platform::encode_from_parquet(self, path, num_qubits, encoding_method)
}
```

**Data flow:** User-supplied URL string (may include bucket/key paths) → `encode_from_parquet` / `encode` → `platform` module → potentially logged or included in error messages

**Impact:** While query strings are explicitly rejected (positive pattern), S3/GCS bucket names and object key paths passed as function arguments could appear in error messages or logs. Object keys may contain sensitive identifiers (customer IDs, dataset names, internal project names). The `MahoutError::Io(String)` variant could propagate these paths.

**Proof of concept:**
```python
# Path contains sensitive internal naming
engine.encode("s3://company-internal-ml-prod/customer-12345/private-dataset.parquet",
              num_qubits=10, encoding_method="amplitude")
# If this fails, error message may contain the full path
```

**Remediation:**
- Sanitize file paths in error messages to redact bucket names or keys
- Consider structured logging that separates path components for selective redaction

```rust
// Example: redact bucket/key in error messages
fn sanitize_remote_path(path: &str) -> String {
    if path.starts_with("s3://") || path.starts_with("gs://") {
        let parts: Vec<&str> = path.splitn(4, '/').collect();
        if parts.len() >= 4 {
            return format!("{}//{}/<redacted>", parts[0], parts[2]);
        }
    }
    path.to_string()
}
```

### Security Controls Inventory for 14.2.1

| Control | Location | Status |
|---------|----------|--------|
| Query string/fragment rejection | Documented in `api.md`, `getting-started.md` | ✅ Explicitly rejects `?versionId=...`, `#...` |
| URL path sanitization in errors | Error types in `error.rs` | ❌ No path sanitization in `MahoutError::Io(String)` |
| No HTTP API endpoints | Entire codebase | ✅ Library API, not a web service — no HTTP query strings for API keys |
| No session tokens in URLs | Entire codebase | ✅ Not applicable — library has no session management |

### Positive Patterns for 14.2.1

1. **Query string/fragment rejection**: The API explicitly documents and rejects query strings and fragments in remote URLs, preventing credential leakage via URL parameters like `?AWSAccessKeyId=...`.
2. **Library API design**: As a Python/Rust library (not a web service), there are no HTTP endpoints, query parameters, or URL routing that could leak sensitive data. API keys and session tokens are not part of the API surface.
3. **Typed API with `&[f64]`/`&[f32]`**: Encoding data is passed as typed arrays, not as URL-encoded strings, eliminating URL-based data leakage for the core encoding path.

---



## Cross-cutting Architecture Observations

1. **Library vs. Web Application**: The audited codebase is a GPU computing library (Rust + Python), not a web application. Several ASVS requirements (12.2.2, 14.3.1) have limited applicability. The most impactful findings are in the documentation/process domain (15.1.1, 15.2.1).

2. **Unsafe Code Boundary**: The codebase contains significant `unsafe` Rust code for CUDA FFI operations. While the code demonstrates disciplined unsafe usage with safety documentation, pointer validation, and RAII wrappers, this represents a "dangerous functionality" category per ASVS 15.1.1 that should be formally documented.

3. **File Format Parsers as Attack Surface**: The library accepts untrusted file inputs in multiple formats (Parquet, Arrow IPC, NumPy, PyTorch, TensorFlow protobuf). These parsers process binary data from potentially untrusted sources and represent the highest-risk attack surface. The parsing is delegated to well-known libraries (Arrow, etc.), but vulnerability tracking is essential.

4. **Remote IO Feature**: The `remote-io` feature enables cloud storage access, introducing network-level security requirements (TLS, authentication). The implementation was not provided for audit, creating a significant blind spot.

5. **Credential Management in Release Process**: The `dev/release.md` correctly advises storing `.pypirc` in `~/.pypirc` with `chmod 600` and warns against placing it in the project directory — a solid operational security practice.

## Cross-cutting Recommendations

### Immediate (0–30 days)
1. **Create dependency vulnerability management policy** (ASVS-1511-MED-001): Define remediation timeframes for Critical/High/Medium/Low severity vulnerabilities in a `SECURITY.md` or equivalent document.
2. **Add `cargo audit` to CI pipeline** (ASVS-1521-MED-001): Implement automated Rust dependency vulnerability scanning on every PR and scheduled weekly runs.
3. **Document dangerous components**: Formally identify `qdp_kernels`, `cudarc`, and file format parsers as "dangerous functionality" per ASVS 15.1.1 definitions.

### Short-term (30–90 days)
4. **Audit the `remote` module** (ASVS-1222-MED-001): Verify TLS certificate validation for S3/GCS connections. Ensure publicly trusted certificates are enforced and no bypass exists.
5. **Add deployment hardening documentation** (ASVS-1341-MED-001): If QDP or its documentation is deployed as a web service, document exclusion of `.git` and other VCS metadata.
6. **Add Python dependency auditing**: Implement `pip-audit` or `safety` checks for the Python package surface.

### Long-term (90+ days)
7. **SBOM generation**: Integrate Software Bill of Materials generation into the release process for both Rust (`cargo sbom`) and Python (`cyclonedx-py`) components.
8. **Sanitize paths in error messages** (ASVS-1421-LOW-001): Implement path redaction for remote URLs in error messages to prevent information leakage of internal bucket/key names.
9. **Sandboxing for file parsers**: Consider containerization or process isolation for file format parsing (Parquet, Arrow, TensorFlow protobuf) to limit blast radius from parser vulnerabilities.
        