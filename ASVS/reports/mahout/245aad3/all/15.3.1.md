# Security Audit Report: ASVS 15.3.1

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | apache/mahout |
| **Audit Date** | May 04, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 15.3.1 |
| **Files Analyzed** | 7 relevant / 394 total |
| **Files Skipped** | 36 |

**Requirement description:** Verify that the application only returns the required subset of fields from a data object. For example, it should not return an entire data object, as some individual fields should not be accessible to users.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 1 |

---

## ASVS-15.3.1: Defensive Coding — Return Only Required Field Subset

### Findings for 15.3.1

#### MEDIUM
**Finding ID:** ASVS-1531-MED-001
**Location:** `qumat/qumat.py`, `execute_circuit()` (lines ~243-262) and `get_final_state_vector()` (lines ~283-302)

**Description:** The entire `self.backend_config` dictionary is passed to backend modules, and is also mutated to accumulate state between calls. The `backend_config` contains all constructor-supplied configuration (including `backend_name`, `backend_options` with `simulator_type`, `shots`, etc.) plus injected `parameter_values`. Backend functions receive the full configuration object rather than only the fields they need.

```python
# execute_circuit mutates then passes the full config
self.backend_config["parameter_values"] = bound_parameters
return self.backend_module.execute_circuit(
    self.circuit, self.backend, self.backend_config
)
```

```python
# get_final_state_vector does the same
self.backend_config["parameter_values"] = bound_parameters
return self.backend_module.get_final_state_vector(
    self.circuit, self.backend, self.backend_config
)
```

**Data Flow:** User-supplied `backend_config` (constructor) → accumulated `parameter_values` injected → entire dict passed to backend module → backend receives fields beyond its operational need (e.g., `execute_circuit` receives `backend_name`, full `backend_options` including `simulator_type` that is only needed at init time).

**Impact:** Backend modules receive more configuration data than required for their specific operation. If a backend module logs, serializes, or exposes this config (e.g., in error messages), fields that should be scoped differently could leak. The mutation pattern also creates implicit coupling between sequential calls.

**Remediation:**
```python
def execute_circuit(self, parameter_values=None):
    self._ensure_circuit_initialized()
    # ... parameter binding logic ...
    
    # Pass only what the backend needs
    execution_config = {
        "parameter_values": bound_parameters,
        "shots": self.backend_config["backend_options"].get("shots", 1024),
    }
    return self.backend_module.execute_circuit(
        self.circuit, self.backend, execution_config
    )
```

---

#### LOW
**Finding ID:** ASVS-1531-LOW-001
**Location:** `qumat/qumat.py`, class `QuMat` (all attributes)

**Description:** All instance attributes (`backend_config`, `backend_module`, `backend`, `circuit`, `parameters`) are public Python attributes. While Python convention doesn't enforce access control, sensitive internal state (raw backend handles, full configuration) is freely accessible to any consumer of a `QuMat` instance.

**Impact:** Low risk in a library context, but consumers could inadvertently depend on or expose internal state such as the raw `backend_config` dictionary.

**Remediation:** Use underscore-prefixed attributes for internal state (`_backend_config`, `_backend_module`) and provide explicit accessor properties for fields consumers legitimately need.

---

### Security Controls Inventory for 15.3.1

| Control | Location | Coverage Status |
|---------|----------|-----------------|
| `QuantumTensor` minimal DLPack interface | `tensor.rs` (inventory) | ✅ Exposes only `__dlpack__` and `__dlpack_device__` |
| `measure_overlap` returns scalar only | `qumat.py:~360` | ✅ Returns only float overlap value |
| Backend config scoping | `qumat.py:execute_circuit`, `get_final_state_vector` | ❌ Full config dict passed |

### Positive Patterns for 15.3.1

- **`QuantumTensor`** exposes only the DLPack protocol methods (`__dlpack__`, `__dlpack_device__`) — minimal interface, no extra metadata or internal state exposed.
- **`measure_overlap`** returns a single clamped `float` — only the computed result, not intermediate circuit state or measurement distributions.
- **`calculate_prob_zero`** delegates to backend but only returns a probability float.

---



## Cross-cutting Architecture Observations

1. **Validation Asymmetry Between QDP and QuMat:** The QDP (Rust) layer has comprehensive, well-documented input validation (num_qubits 1–30, NaN/Inf rejection, type enforcement, encoding method validation). The QuMat (Python) layer has strong qubit index validation but significant gaps for other parameters (`num_qubits`, rotation angles, configuration structure). This creates a false sense of security — the Python layer appears validated but has exploitable gaps before inputs reach the Rust layer.

2. **Mutable Shared State Pattern:** `self.backend_config` serves as both a configuration store and a runtime communication channel (via injected `parameter_values`). This pattern creates implicit coupling between operations and makes reasoning about state across sequential calls difficult.

3. **Defense-in-Depth Present but Incomplete:** The architecture has validation at multiple layers (Python API → Rust core → CUDA kernels), but the Python layer validation has gaps that could cause confusing failures or resource exhaustion before inputs reach the Rust validation layer.

4. **Library vs. Service Context:** The codebase is a library, not a web service. Many ASVS web-focused requirements (HTTP headers, session management) are not directly applicable. However, input validation and defensive coding requirements remain fully relevant.

5. **Documentation Quality Split:** QDP documentation is thorough with explicit validation rules, supported formats, and error conditions. QuMat documentation covers API usage but lacks formal validation rule specifications.

---

## Cross-cutting Recommendations

### Immediate (address before next release)
1. **Add `num_qubits` validation to `create_empty_circuit`** — type check (int), non-negative, and reasonable upper bound. This is the highest-impact gap as it guards all subsequent operations. *(ASVS-221-HIGH-001)*
2. **Reset `self.parameters` in `create_empty_circuit`** — prevents stale parameter injection across circuit lifecycles. Single-line fix with high correctness impact. *(ASVS-231-MED-001)*

### Short-term (next sprint)
3. **Add angle/float validation helper** (`_validate_angle`) and apply to all rotation gate methods (`apply_rx/ry/rz_gate`, `apply_u_gate`). Check for type correctness and finiteness (no NaN/Inf). *(ASVS-221-MED-001)*
4. **Validate `backend_options` structure** in constructor — must be a `dict`, optionally validate expected keys. Add `backend_name` allow-list. *(ASVS-221-MED-002)*
5. **Stop mutating `self.backend_config`** — pass execution-specific data via a separate dict or shallow copy to avoid state leakage between calls. *(ASVS-231-LOW-001, ASVS-1531-MED-001)*
6. **Document validation rules** for QuMat class inputs in a structured format matching the QDP documentation standard. *(ASVS-211-MED-001)*

### Long-term (architectural)
7. **Create a `CircuitConfig` or `ExecutionContext` dataclass** that separates initialization config from runtime state, replacing the mutable dict pattern.
8. **Scope backend function signatures** to accept only the fields each operation needs, rather than a full configuration dict.
9. **Consider a validation decorator or mixin** that centralizes input validation for all public methods, reducing the risk of future methods being added without validation.
        