# Security Audit Report: ASVS 3.5.1

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | The-AI-Alliance/gofannon |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 3.5.1 |
| **Files Analyzed** | 2 relevant / 2 total |
| **Files Skipped** | 0 |

**Requirement description:** Verify that, if the application does not rely on the CORS preflight mechanism to prevent disallowed cross-origin requests to use sensitive functionality, these requests are validated to ensure they originate from the application itself. This may be done by using and validating anti-forgery tokens…

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 1 |
| 🟡 Medium | 0 |
| 🟢 Low | 0 |

---

## ASVS-3.5.1: CSRF / Cross-Origin Request Forgery Protection

### Findings for 3.5.1

### [HIGH]

**Finding ID:** ASVS-351-HIGH-001

**File:** `webapp/packages/api/user-service/app_factory.py` — `_configure_cors()` and `create_app()` (lines 22-75)

**Vulnerable Code:**

```python
cors_options = {
    "allow_origins": allowed_origins,
    "allow_credentials": True,
    "allow_methods": ["*"],
    "allow_headers": ["*"],
}
app.add_middleware(CORSMiddleware, **cors_options)
```

Combined with the frontend's automatic credential inclusion:

```javascript
// fetchInterceptor.js
window.fetch = async (input, init = {}) => {
  if (apiMatches(input) && init.credentials === undefined) {
    init = { ...init, credentials: 'include' };
  }
  return originalFetch(input, init);
};
```

**Data Flow:** Browser sends session cookie (`gofannon_sid`) → Server authenticates request based on cookie → No CSRF token or custom header validation → State-changing operation executes.

**Gap Type:** Type A — No CSRF protection control exists in the provided codebase. There is:
- No CSRF token generation endpoint
- No CSRF token validation middleware
- No requirement for a non-CORS-safelisted custom header (e.g., `X-Requested-With`)
- No `Sec-Fetch-*` header validation
- No `SameSite` cookie attribute configuration visible

**Proof of Concept:**

An attacker hosts the following on `https://evil.example.com`:

```html
<!-- CSRF attack using fetch from a form post (simple request, no preflight) -->
<form action="https://api.target.com/api/v1/workspaces/ws123/settings" 
      method="POST" 
      enctype="text/plain">
  <input name='{"name":"pwned","admin_email":"attacker@evil.com","ignore":"' value='"}'>
  <input type="submit" value="Click me">
</form>
```

If the browser's default `SameSite` policy is `Lax` (modern default), this specific POST from a top-level navigation would NOT send the cookie. However:
- Older browsers default to `SameSite=None`
- If the application explicitly sets `SameSite=None` for cross-origin compatibility
- GET-based state changes would still work under `Lax`

More critically, if any endpoints accept `application/x-www-form-urlencoded` or `text/plain`:

```html
<form action="https://api.target.com/api/v1/agents/agent123/execute" method="POST">
  <input type="hidden" name="code" value="malicious_code()">
  <input type="submit">
</form>
```

**Impact:** An attacker could potentially perform state-changing operations (create/delete workspaces, modify agent configurations, trigger agent executions) on behalf of an authenticated user by exploiting the cookie-based authentication without any CSRF countermeasure.

**Remediation:**

Option 1 — Require a custom header that triggers CORS preflight:

```python
# middleware/csrf.py
from starlette.middleware.base import BaseHTTPMiddleware
from starlette.responses import JSONResponse

SAFE_METHODS = {"GET", "HEAD", "OPTIONS"}

class CSRFHeaderMiddleware(BaseHTTPMiddleware):
    async def dispatch(self, request, call_next):
        if request.method not in SAFE_METHODS:
            # Require non-CORS-safelisted header
            if not request.headers.get("X-Requested-With"):
                return JSONResponse(
                    {"detail": "Missing X-Requested-With header"},
                    status_code=403,
                )
        return await call_next(request)
```

Option 2 — Validate `Origin` header on state-changing requests:

```python
class OriginValidationMiddleware(BaseHTTPMiddleware):
    def __init__(self, app, allowed_origins: list[str]):
        super().__init__(app)
        self.allowed_origins = set(allowed_origins)
    
    async def dispatch(self, request, call_next):
        if request.method not in SAFE_METHODS:
            origin = request.headers.get("origin")
            if origin and origin not in self.allowed_origins:
                return JSONResponse({"detail": "Invalid origin"}, status_code=403)
        return await call_next(request)
```

And on the frontend, add the custom header:

```javascript
window.fetch = async (input, init = {}) => {
  if (apiMatches(input)) {
    init = { ...init, credentials: 'include' };
    init.headers = { ...(init.headers || {}), 'X-Requested-With': 'XMLHttpRequest' };
  }
  return originalFetch(input, init);
};
```

### Security Controls Inventory for 3.5.1

| Control | Location | Coverage Status |
|---------|----------|-----------------|
| CSRF Token Generation | Not found | ❌ MISSING |
| CSRF Token Validation | Not found | ❌ MISSING |
| Custom Header Requirement | Not found | ❌ MISSING |
| Origin Header Validation | Not found (beyond CORS response headers) | ❌ MISSING |
| SameSite Cookie Attribute | Not visible in provided code | ❓ UNKNOWN |
| CORS Origin Restriction | `app_factory.py:24-25` | ⚠️ Exists but insufficient alone |

### Positive Patterns for 3.5.1

- The CORS origin is restricted to a single specific origin, which provides partial protection (prevents JavaScript-initiated cross-origin requests from reading responses).
- The `credentials: 'include'` pattern is only applied to API-matching URLs, not all requests.

---



## Cross-cutting Architecture Observations

1. **CORS provides false sense of security:** The `CORSMiddleware` in Starlette/FastAPI operates on the response side. It does NOT reject or block requests from non-allowed origins for simple (non-preflight) requests. The application appears to rely solely on CORS for cross-origin protection without additional server-side origin validation or CSRF tokens. This is a systemic architectural gap.

2. **Cookie-based authentication without visible CSRF countermeasures:** The combination of:
   - Server: `allow_credentials: True`
   - Client: `credentials: 'include'` (automatic via fetch interceptor)
   - No CSRF token exchange
   - No custom header requirement
   
   Creates a classic CSRF-vulnerable architecture. The only mitigations would be browser defaults (`SameSite=Lax` on cookies) which are not explicitly configured and cannot be relied upon across all browsers/configurations.

3. **Security header strategy appears delegated:** The absence of HSTS, CSP, X-Frame-Options, and other security headers in the application code suggests these are expected to be configured at the infrastructure level (reverse proxy, CDN). This is a valid architecture but creates a documentation/verification gap.

4. **Fetch interceptor centralizes credential handling:** The `fetchInterceptor.js` provides a single point of control for credential inclusion, which is architecturally sound but does not compensate for missing server-side CSRF protections.

5. **Wildcard `allow_methods` and `allow_headers`** in CORS configuration is more permissive than necessary. While this doesn't directly create vulnerabilities (CORS is a browser feature for protecting users), it signals a permissive security posture and reduces the effectiveness of CORS preflight as a defense mechanism.

---

## Cross-cutting Recommendations

### Immediate (Critical/High — Deploy within sprint)

1. **Implement CSRF protection middleware** (addresses ASVS 3.5.1 and 3.5.2):
   - Require `Content-Type: application/json` on all state-changing API endpoints (this triggers CORS preflight)
   - OR require a custom header like `X-Requested-With: XMLHttpRequest` on all authenticated requests
   - Update `fetchInterceptor.js` to always include the custom header for API requests

2. **Add server-side Origin validation** for state-changing requests:
   - Validate the `Origin` header against the allowlist before processing POST/PUT/PATCH/DELETE
   - Return 403 if Origin is present but not in the allowlist

### Short-term (Medium — Deploy within 2 weeks)

3. **Add HSTS header** (addresses ASVS 3.4.1):
   - Either via application middleware or reverse proxy configuration
   - Minimum `max-age=31536000; includeSubDomains`
   - Document where HSTS is enforced in the deployment architecture

4. **Tighten CORS configuration** (addresses ASVS 3.4.2 and 3.5.3):
   - Replace `allow_methods: ["*"]` with explicit method list
   - Replace `allow_headers: ["*"]` with explicit header list
   - Require `FRONTEND_URL` to be explicitly set (fail if unset) in production

5. **Explicitly configure SameSite cookie attribute**:
   - Set `SameSite=Lax` or `SameSite=Strict` on session cookies
   - Ensure `Secure` flag is set in production

### Long-term (Low — Deploy within quarter)

6. **Implement Sec-Fetch-* header validation** as defense-in-depth against cross-site attacks that bypass CORS
7. **Create security headers documentation** specifying which headers are set at the application vs. infrastructure level, with deployment validation tests
8. **Add integration tests** that verify CSRF protection by attempting cross-origin simple requests without custom headers and asserting rejection
        