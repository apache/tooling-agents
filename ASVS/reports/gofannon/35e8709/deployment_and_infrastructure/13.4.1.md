# ASVS 13.4.1 Security Audit Report

## Executive Summary

- **Repository:** The-AI-Alliance/gofannon
- **Audit Date:** May 05, 2026
- **Auditor:** Tooling Agents
- **ASVS Requirement:** ASVS 13.4.1
- **Description:** Verify that the application is deployed either without any source control metadata, including the .git or .svn folders, or in a way that these folders are inaccessible both externally and to the application itself.
- **Files Analyzed:** 5 relevant out of 5 total, 0 skipped

### Finding Summary

| Severity | Count | Status |
|----------|-------|--------|
| Critical | 0 | ✅ None Found |
| High | 0 | ✅ None Found |
| Medium | 1 | ⚠️ Requires Attention |
| Low | 2 | ℹ️ Minor Issues |
| **Total** | **3** | |

### Compliance Status

**PARTIAL COMPLIANCE** — Application-layer architecture is inherently safe with no static file serving, but lacks explicit defense-in-depth controls. Deployment-level verification cannot be confirmed from provided files.

---

## Security Controls Inventory

| Control | Defined | Applied | Coverage | Notes |
|---------|---------|---------|----------|-------|
| Static file serving restriction | Implicit (no `StaticFiles` mount) | ✅ By omission | App won't serve arbitrary files | FastAPI routing model provides baseline protection |
| Source control path blocking middleware | ❌ Not defined | ❌ N/A | No defense-in-depth | Gap in layered security approach |
| Debug mode disabled | Implicit (FastAPI default) | ⚠️ Not explicit | Default is `False` but not hardened | Should be explicitly set |
| API docs suppression in production | ❌ Not defined | ❌ N/A | `/docs` and `/redoc` exposed | Information disclosure vector |
| `.dockerignore`/deployment exclusion | Not visible | Cannot verify | Deployment config not provided | Critical control not verifiable |
| Error detail suppression | ❌ Not defined | ❌ N/A | Print statements expose info | Logging practices need improvement |

---

## Critical File Review

### Application Configuration Files

| File | Purpose | Security Functions | Lines Reviewed | Risk Level |
|------|---------|-------------------|----------------|------------|
| `app_factory.py` | App creation/configuration | `create_app()`, `_configure_cors()` | ~30, ~55, ~68 | Medium |
| `main.py` | Entry point | Application initialization | All | Low |

### Deployment Configuration Files

| File | Purpose | ASVS 13.4.1 Relevance | Verification Status |
|------|---------|----------------------|---------------------|
| Dockerfile (not provided) | Build/deploy | **Critical** — determines if `.git` is in image | ❌ Cannot verify |
| nginx.conf (not provided) | Reverse proxy | **Critical** — determines static file serving | ❌ Cannot verify |
| `.dockerignore` (not provided) | Build exclusion | **Critical** — should exclude `.git` | ❌ Cannot verify |

---

## Findings

### Medium Severity

#### ASVS-1341-MED-001: No Defense-in-Depth Middleware to Block Source Control Metadata Paths

**Severity:** MEDIUM  
**Gap Type:** Type A — No control at application level  
**Location:**
- **File:** `webapp/packages/api/user-service/app_factory.py`
- **Function:** `create_app()`
- **Line:** ~55

**Related Functions Checked:**
- `_configure_cors()` — No path filtering
- `create_wsgi_app()` — No middleware injection

**Description:**

The application does not register any middleware or route guard that explicitly blocks requests to well-known source control metadata paths (e.g., `/.git/config`, `/.git/HEAD`, `/.svn/entries`). While FastAPI will return 404 for undefined routes, if the application is deployed behind a reverse proxy that serves static files from the application directory (common with nginx `try_files` patterns), source control metadata could be exposed.

**Vulnerable Code:**

```python
def create_app() -> FastAPI:
    """Create and configure a FastAPI application instance."""
    app = FastAPI(lifespan=lifespan)
    app.add_middleware(ObservabilityMiddleware)
    _configure_cors(app)
    # No middleware to block /.git/, /.svn/, /.env paths
```

**Data Flow:**

```
External request 
  → Reverse proxy (if configured to serve static) 
    → `.git/config` file 
      → Source code, commit history, credentials exposed
```

**Proof of Concept:**

```bash
# If deployed behind nginx with root pointing to app directory:
curl https://target.example.com/.git/config
curl https://target.example.com/.git/HEAD
curl https://target.example.com/.git/refs/heads/main
# Could reveal remote repository URLs, branch structure
```

**Impact:**

An attacker who gains access to `.git/` can reconstruct the entire source code repository, including historical commits that may contain secrets, internal architecture details, and configuration that aids further attacks. This represents a complete compromise of intellectual property and potential credential exposure.

**Remediation:**

```python
from starlette.middleware.base import BaseHTTPMiddleware
from starlette.responses import Response

class BlockSensitivePathsMiddleware(BaseHTTPMiddleware):
    BLOCKED_PREFIXES = ("/.git", "/.svn", "/.hg", "/.env", "/.bzr")
    
    async def dispatch(self, request, call_next):
        path = request.url.path.lower()
        if any(path.startswith(prefix) for prefix in self.BLOCKED_PREFIXES):
            return Response(status_code=404)
        return await call_next(request)

def create_app() -> FastAPI:
    app = FastAPI(lifespan=lifespan)
    app.add_middleware(BlockSensitivePathsMiddleware)
    app.add_middleware(ObservabilityMiddleware)
    _configure_cors(app)
    # ...
```

---

### Low Severity

#### ASVS-1341-LOW-002: API Documentation Endpoints Exposed in Production

**Severity:** LOW  
**Gap Type:** Type A — No control  
**Location:**
- **File:** `webapp/packages/api/user-service/app_factory.py`
- **Function:** `create_app()`
- **Line:** ~55

**Related Functions Checked:**
- FastAPI constructor parameters
- OpenAPI configuration

**Description:**

The `FastAPI()` constructor is called without setting `docs_url=None` or `redoc_url=None`, which means the interactive API documentation at `/docs` (Swagger UI) and `/redoc` (ReDoc) are available to any requester. This exposes the complete API surface area, parameter types, and endpoint structure to attackers.

**Vulnerable Code:**

```python
app = FastAPI(lifespan=lifespan)
# No docs_url=None, redoc_url=None, openapi_url=None
```

**Impact:**

Attackers can enumerate all API endpoints, understand parameter schemas, and identify potential attack targets without fuzzing. This violates the principle stated in the domain context: "not expose internal API docs or monitoring endpoints publicly." This information aids reconnaissance for subsequent attacks.

**Remediation:**

```python
import os

def create_app() -> FastAPI:
    is_production = os.getenv("ENVIRONMENT", "production") == "production"
    app = FastAPI(
        lifespan=lifespan,
        docs_url=None if is_production else "/docs",
        redoc_url=None if is_production else "/redoc",
        openapi_url=None if is_production else "/openapi.json",
    )
```

---

#### ASVS-1341-LOW-003: Debug/Configuration Information Printed to stdout

**Severity:** LOW  
**Gap Type:** Type A — No control  
**Location:**
- **File:** `webapp/packages/api/user-service/app_factory.py`
- **Function:** `_configure_cors()` (line ~30), `create_app()` (line ~68)

**Related Functions Checked:**
- Observability service integration
- Logging configuration

**Description:**

The application uses `print()` statements to output configuration details and error messages. In containerized deployments, stdout is captured in logs which may be accessible to operators or log aggregation services with insufficient access controls.

**Vulnerable Code:**

```python
print(f"Configured allowed CORS origins: {allowed_origins}")
# ...
print(f"Warning: auth registry init failed: {e}")
```

**Impact:**

The first statement leaks the configured frontend URL. The second may include sensitive details about auth configuration errors (e.g., file paths, secret fragments in error messages). This contributes to information leakage that ASVS 13.4.1's section (Unintended Information Leakage) addresses.

**Remediation:**

```python
# Use structured logging through the observability service
logger = get_observability_service()
logger.log(level="INFO", event_type="config", message="CORS configured", 
           details={"origin_count": len(allowed_origins)})
# ...
logger.log(level="WARNING", event_type="auth", 
           message="Auth registry initialization failed",
           details={"error_type": type(e).__name__})
```

---

## Positive Security Patterns

| Pattern | Location | Implementation | Benefit |
|---------|----------|----------------|---------|
| No `StaticFiles` mount | `app_factory.py` | FastAPI application without static file serving | FastAPI won't serve arbitrary files from filesystem, preventing direct access to `.git` |
| Factory pattern | `app_factory.py` | Centralized `create_app()` function | Centralized app configuration makes security controls easy to audit |
| Explicit CORS origin (not wildcard) | `_configure_cors()` | Environment-based origin configuration | Only configured frontend domain allowed, prevents unauthorized cross-origin access |
| CORS from environment variable | `_configure_cors()` | `ALLOWED_ORIGINS` environment variable | Origin can differ per deployment environment without code changes |
| Conditional auth router | `create_app()` | Auth routes only mounted when providers exist | Reduces attack surface when authentication not configured |
| Minimal entry point | `main.py` | Delegates to factory pattern | Reduces attack surface in module initialization |

---

## Architecture Observations

### Strengths

1. **FastAPI's routing model inherently protects against ASVS 13.4.1** at the application layer — only explicitly defined routes return content. This is a strong baseline that prevents accidental exposure of filesystem content.

2. **Factory pattern provides centralized security control** — All application configuration flows through `create_app()`, making it straightforward to audit and add security middleware.

3. **No static file serving configured** — The absence of `StaticFiles` mount points eliminates the most common vector for `.git` exposure in web applications.

### Concerns

1. **The WSGI adapter** (`create_wsgi_app`) may introduce a web server layer not visible in the provided code. If it configures any static file serving, that could create exposure vectors.

2. **Missing deployment configuration** — The most critical aspect of ASVS 13.4.1 (whether `.git` is excluded from deployment artifacts) cannot be verified from these application-layer files alone. This is a **critical gap in audit coverage**.

3. **No `debug` parameter explicitly set** — While FastAPI defaults to `debug=False`, explicit declaration provides documentation value and prevents accidental activation via environment variable injection in some ASGI servers.

4. **Reverse proxy configuration unknown** — Common nginx configurations with `try_files` directives could bypass application-layer protections entirely.

---

## Recommendations Summary

### Immediate Actions (0-1 week)

| Priority | Action | Effort | Impact | Rationale |
|----------|--------|--------|--------|-----------|
| 1 | Add path-blocking middleware for `.git`, `.svn`, `.env` | Low | High | Eliminates risk from reverse proxy misconfiguration with minimal code change |
| 2 | Disable OpenAPI/docs endpoints in production | Low | Medium | Reduces API surface enumeration, single configuration change |
| 3 | Verify deployment pipeline excludes `.git` (Dockerfile/`.dockerignore`) | Low | Critical | Primary control for ASVS 13.4.1 compliance |

### Short-term Actions (1-4 weeks)

| Priority | Action | Effort | Impact | Rationale |
|----------|--------|--------|--------|-----------|
| 4 | Replace `print()` with structured logging | Low | Medium | Prevents information leakage in logs, improves observability |
| 5 | Explicitly set `debug=False` in FastAPI constructor | Trivial | Low | Defense-in-depth against misconfiguration |
| 6 | Document reverse proxy hardening requirements (block dot-files) | Low | Medium | Ensures operational compliance across deployment environments |

### Long-term Actions (1-3 months)

| Priority | Action | Effort | Impact |
|----------|--------|--------|--------|
| 7 | Implement automated deployment configuration scanning | Medium | High |
| 8 | Add security headers middleware (HSTS, CSP, X-Content-Type-Options) | Medium | Medium |
| 9 | Create security configuration baseline documentation | Low | Medium |

---

## Appendix: Files Analyzed

<details>
<summary>Click to expand file list (5 files)</summary>

### Application Code Files
1. **`webapp/packages/api/user-service/app_factory.py`** (Relevance: 10/10)
   - Primary application factory
   - CORS configuration
   - Middleware registration
   - Security control implementation point

2. **`webapp/packages/api/user-service/main.py`** (Relevance: 8/10)
   - Application entry point
   - WSGI adapter configuration

### Deployment Configuration Files
3. **`dev-tail.sh`** (Relevance: 10/10)
   - Development tooling
   - May reveal deployment patterns

4. **`webapp/.firebaserc`** (Relevance: 10/10)
   - Firebase configuration
   - Deployment target information

5. **`webapp/firebase.json`** (Relevance: 10/10)
   - Firebase hosting rules
   - Static file serving configuration

### Infrastructure Files
6. **`webapp/infra/docker/Dockerfile.api`** (Relevance: 10/10)
   - Container build configuration
   - **Critical for `.git` exclusion verification**

7. **`webapp/infra/docker/docker-compose.yml`** (Relevance: 10/10)
   - Service orchestration
   - Volume mount configuration

</details>

---

## Compliance Assessment

| Aspect of ASVS 13.4.1 | Status | Evidence | Gap |
|------------------------|--------|----------|-----|
| `.git` folder not served by application | ✅ PASS | No `StaticFiles` mount; FastAPI only serves defined routes | None |
| `.git` folder excluded from deployment | ⚠️ UNVERIFIABLE | No deployment config in scope | Cannot verify Docker build excludes `.git` |
| Defense-in-depth path blocking | ❌ GAP | No middleware blocks `.git` requests | Missing explicit path filtering |
| Source control metadata inaccessible to application | ⚠️ UNVERIFIABLE | No filesystem access controls visible | Cannot verify container filesystem isolation |

**Overall ASVS 13.4.1 Compliance:** **PARTIAL** — Application-layer architecture is inherently safe due to FastAPI's routing model, but lacks explicit defense-in-depth controls. Critical deployment-level verification cannot be confirmed from provided files. Recommend immediate addition of path-blocking middleware and verification of deployment pipeline configuration.

---

*Report generated: May 05, 2026*  
*Audit methodology: Static code analysis with ASVS 4.0 compliance verification*