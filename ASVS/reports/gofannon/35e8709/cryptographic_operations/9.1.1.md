# Security Audit Report: ASVS 9.1.1

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | The-AI-Alliance/gofannon |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 9.1.1 |
| **Files Analyzed** | 2 relevant / 2 total |
| **Files Skipped** | 0 |

**Requirement description:** Verify that self-contained tokens are validated using their digital signature or MAC to protect against tampering before accepting the token's contents.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---

## ASVS-9.1.1: Self-contained Token Signature Validation

### Findings for 9.1.1

### [MEDIUM]

**Finding ID:** ASVS-911-MED-001

**Title:** Abstract auth provider interface does not enforce token validation requirements for OAuth token responses

**File:** `webapp/packages/api/user-service/auth/base.py`, lines 104-116 (`exchange_code` abstract method)

```python
@abstractmethod
async def exchange_code(self, code: str, redirect_uri: str) -> UserInfo:
    """Exchange an authorization code for a ``UserInfo``.

    Subclasses may raise on any provider error; the route handler
    turns these into a 4xx response.
    """
```

**Data flow:** OAuth Provider → ID Token (JWT) → `exchange_code()` implementation → `UserInfo` (no validation contract enforced)

**Analysis:**

OAuth providers (Google, GitHub, etc.) typically return ID tokens as JWTs during the code exchange. The abstract interface:
1. Does not specify that implementations MUST validate the signature of any received JWT/ID token
2. Does not provide a validation helper or hook
3. Documentation says only "Subclasses may raise on any provider error" — no mention of signature verification

Since concrete implementations are not provided in the audit scope, this is classified as a **design gap** (Type B potential — control should exist at the interface level to prevent implementers from skipping validation).

**Impact:** A concrete provider implementation could theoretically accept unsigned or tampered ID tokens from OAuth providers if the implementer doesn't independently add signature verification. This would allow an attacker who can intercept the token exchange to forge identity claims.

**Remediation:**

Add explicit documentation and optionally a validation hook:

```python
@abstractmethod
async def exchange_code(self, code: str, redirect_uri: str) -> UserInfo:
    """Exchange an authorization code for a ``UserInfo``.

    Implementations MUST:
    - Validate the digital signature of any JWT/ID token received
    - Verify the token issuer matches the expected provider
    - Check token expiry (exp claim)
    - Verify the audience (aud claim) matches this application
    
    Subclasses may raise on any provider error; the route handler
    turns these into a 4xx response.
    """
```

**Mitigating factors:** Most Python OAuth libraries (e.g., `authlib`, `python-jose`, Google's `google-auth`) validate JWT signatures by default during the code exchange flow. The risk depends on the concrete implementation library choices.

### Security Controls Inventory for 9.1.1

| Control | Location | Coverage |
|---------|----------|----------|
| Opaque session tokens (no signature needed for sessions) | `session_service.py:_new_session_id()` | ✅ Complete — session tokens are database references |
| Abstract `exchange_code()` interface | `auth/base.py:108-116` | ⚠️ Gap — no validation contract specified |
| Session lookup validates by existence + expiry | `session_service.py:get_by_id()` | ✅ Complete — server-side validation |

### Positive Patterns for 9.1.1

- **Opaque session tokens**: The application's own session tokens (`gofannon_sid` cookie) are opaque database references, not self-contained tokens. They don't need signature validation — integrity is guaranteed by the database lookup in `get_by_id()`.
- **Server-side session validation**: Every session access checks existence and `expires_at` (line ~130 in `session_service.py`), providing integrity without cryptographic signatures on the token itself.
- **Hard expiry enforcement**: `session.expires_at <= datetime.utcnow()` ensures expired sessions cannot be reused regardless of cookie state.

---



## Cross-cutting Architecture Observations

1. **Opaque token design is the primary security control**: The decision to use server-side sessions with opaque random identifiers (`secrets.token_urlsafe(32)`) effectively eliminates most self-contained token vulnerabilities (algorithm confusion, signature bypass, key confusion, header injection). This is an excellent architectural choice that makes ASVS 9.1.x requirements largely not applicable to the session layer itself.

2. **OAuth token validation is delegated to concrete providers**: The abstract interface (`AuthProvider`) defines the contract but doesn't enforce cryptographic validation requirements. Security depends entirely on correct implementation in subclasses. Without seeing concrete providers (Google, ASF, GitHub implementations), we cannot verify that OAuth ID tokens are properly validated.

3. **No cryptographic operations in audited code**: Neither file uses encryption, hashing, or digital signatures directly. The only cryptographic primitive is `secrets.token_urlsafe(32)` for session ID generation, which is the correct choice. ASVS 11.x requirements are largely not applicable to this specific code.

4. **Session integrity model**: The application achieves session integrity through database-backed server-side state rather than cryptographic signatures on tokens. This is valid but requires:
   - Database access controls (only the application can write to `user_sessions`)
   - TLS for cookie transmission (not visible in this code)
   - Proper cookie attributes (HttpOnly, Secure, SameSite — not visible in this code)

5. **Trust boundary at provider implementations**: The `exchange_code()` abstract method is where external tokens (potentially JWTs from OAuth providers) enter the system. This is the critical point where ASVS 9.1.x controls must be applied, but enforcement is not visible in the abstract base class.

## Cross-cutting Recommendations

### Immediate (before next release)

1. **Document cryptographic requirements for provider implementers**: Add explicit documentation to `AuthProvider.exchange_code()` requiring signature validation, algorithm allowlisting, and trusted key sources for any JWT/ID tokens received during OAuth code exchange.

2. **Create a shared token validation utility**: Provide a helper function in the `auth` package that enforces algorithm allowlists and trusted JWKS resolution, so concrete providers don't each independently implement (and potentially misconfigure) JWT validation.

### Short-term (next sprint)

3. **Audit concrete provider implementations**: The security of the system depends critically on how concrete providers (not shown in this audit) implement `exchange_code()`. Verify that each:
   - Validates JWT signatures before extracting claims
   - Uses an explicit algorithm allowlist (no "none")
   - Resolves signing keys only from pre-configured JWKS URIs
   - Validates issuer, audience, and expiry claims

4. **Verify cookie security attributes**: Confirm that the `gofannon_sid` cookie is set with `HttpOnly`, `Secure`, and appropriate `SameSite` attributes at the route handler level (not visible in the audited service code).

### Long-term (architectural)

5. **Consider session binding**: For high-security environments, consider binding sessions to client fingerprints (TLS channel binding, user-agent hashing) to mitigate session token theft.

6. **Add interface-level validation hooks**: Consider making `AuthProvider` enforce validation through template method patterns rather than relying on documentation alone — e.g., a `validate_provider_token()` method that the base class calls with assertions on algorithm and issuer.
        