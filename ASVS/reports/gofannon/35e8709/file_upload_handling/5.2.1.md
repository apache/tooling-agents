# Security Audit Report: ASVS 5.2.1

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | The-AI-Alliance/gofannon |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 5.2.1 |
| **Files Analyzed** | 1 relevant / 1 total |
| **Files Skipped** | 0 |

**Requirement description:** Verify that the application will only accept files of a size which it can process without causing a loss of performance or a denial of service attack.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 1 |

---

## ASVS-5.2.1: File Upload Size Limits

### Findings for 5.2.1

#### MEDIUM
**Finding ID**: ASVS-521-MED-001

**Title**: No size constraints on swagger specification content accepted for processing

**File**: `webapp/packages/api/user-service/agent_factory/__init__.py`, function `generate_agent_code`, lines 34-41

**Vulnerable Code**:
```python
if request.swagger_specs:
    from .swagger_parser import parse_spec_and_generate_docs
    swagger_docs_parts = []
    for spec in request.swagger_specs:
        docs_for_spec = parse_spec_and_generate_docs(spec.name, spec.content)
        if docs_for_spec:
            swagger_docs_parts.append(docs_for_spec)
    swagger_docs = "\n\n".join(swagger_docs_parts)
```

**Data Flow**: User request body → `request.swagger_specs[*].content` → `parse_spec_and_generate_docs()` → concatenated into prompt → sent to LLM

**Analysis**: The function iterates over all provided `swagger_specs` and processes each spec's `content` without any visible size validation. OpenAPI/Swagger specifications can be extremely large (hundreds of MB for complex APIs). Multiple specs can be submitted in a single request, multiplying the resource consumption. The parsed content is concatenated and then included in an LLM prompt, which itself has token limits that could cause expensive processing or failure.

Additionally, the LLM call uses a 1800-second (30-minute) timeout (line 191):
```python
timeout=1800,
```

This means a single crafted request with large swagger specs could tie up server resources for up to 30 minutes.

**Proof of Concept**: Submit a `GenerateCodeRequest` containing 50 swagger_specs entries, each with `content` being a 50MB OpenAPI YAML file. This would force the server to:
1. Parse each spec with `parse_spec_and_generate_docs`
2. Concatenate results into a massive prompt
3. Send to LLM (likely exceeding context window, causing error after expensive processing)

**Impact**: Denial of service through resource exhaustion — memory consumption during parsing, CPU during doc generation, and prolonged request holding due to 30-minute timeout.

**Remediation**:
```python
MAX_SPEC_SIZE = 1_048_576  # 1 MB per spec
MAX_SPECS_COUNT = 10

if request.swagger_specs:
    if len(request.swagger_specs) > MAX_SPECS_COUNT:
        raise ValueError(f"Maximum {MAX_SPECS_COUNT} swagger specs allowed")
    
    from .swagger_parser import parse_spec_and_generate_docs
    swagger_docs_parts = []
    for spec in request.swagger_specs:
        if len(spec.content.encode('utf-8')) > MAX_SPEC_SIZE:
            raise ValueError(f"Swagger spec '{spec.name}' exceeds maximum size of {MAX_SPEC_SIZE} bytes")
        docs_for_spec = parse_spec_and_generate_docs(spec.name, spec.content)
        if docs_for_spec:
            swagger_docs_parts.append(docs_for_spec)
```

---

#### LOW
**Finding ID**: ASVS-521-LOW-001

**Title**: No count limit on MCP tool URLs or gofannon agents per request

**File**: `webapp/packages/api/user-service/agent_factory/__init__.py`, function `generate_agent_code`, lines 24-31 and 44-66

**Vulnerable Code**:
```python
if request.tools:
    tool_docs_parts = []
    for url, selected_tools in request.tools.items():
        mcp_client = RemoteMCPClient(remote_url=url)
        tools = await mcp_client.list_tools()
        # ...
```

**Data Flow**: `request.tools` (unbounded dict) → iterative remote HTTP calls to each URL → `mcp_client.list_tools()`

**Analysis**: A request with hundreds of tool URLs would cause the server to make HTTP connections to all of them sequentially. Similarly, `request.gofannon_agents` with many IDs would cause many database lookups.

**Impact**: Resource exhaustion through excessive outbound connections or database queries.

**Remediation**: Add maximum count limits on collection fields in the `GenerateCodeRequest` Pydantic model.

### Security Controls Inventory for 5.2.1

| Control | Location | Status |
|---------|----------|--------|
| Request body size limit | Not visible in audited code (may be at web server/framework level) | **Unknown** |
| Per-field size validation | Not present | **Missing** |
| Collection count limits | Not present | **Missing** |
| LLM timeout | Line 191, 1800s | **Present but excessive** |
| Pydantic model validation | `GenerateCodeRequest` model (not in scope) | **Unknown** |

### Positive Patterns for 5.2.1

- The function processes specs iteratively rather than loading all into memory at once, limiting peak memory somewhat.
- The LLM service likely has its own token limits that would reject excessively large prompts (defense in depth via external service).

---



## Cross-cutting Architecture Observations

1. **File handling is not a primary function of this module**: The `agent_factory/__init__.py` is a code generation service, not a file upload handler. File-like content (swagger specs) is accepted as structured JSON request data rather than multipart file uploads. The traditional file upload concerns (magic bytes, MIME type, storage location) apply differently here.

2. **Defense delegation pattern**: The code delegates security-critical operations to other modules (`swagger_parser`, `RemoteMCPClient`, `call_llm`) whose implementations are not available for audit. The effectiveness of file-related controls depends on whether these downstream modules implement proper validation.

3. **Input boundary is upstream**: The actual request validation (field sizes, types, counts) is likely handled at the FastAPI route handler level via Pydantic models (`GenerateCodeRequest`). Without seeing the model definition and route handler, it's impossible to confirm whether size limits and input constraints are enforced before this function is called.

4. **No file persistence in this module**: All processing is in-memory and results are returned as API responses. This eliminates several file storage risks (5.3.1, 5.3.2) at this layer, though they may exist in other parts of the system that store the generated code.

5. **MinIO storage mentioned in domain context**: The docker-compose includes MinIO, suggesting file storage exists elsewhere. This audit cannot confirm whether file upload endpoints in other modules properly implement ASVS 5.2.x and 5.3.x controls.

## Cross-cutting Recommendations

### Immediate (High Priority)
1. **Add size limits to swagger_specs**: Implement per-spec and total content size limits before parsing, either in the Pydantic model (`max_length` on content field) or at the function entry point.
2. **Add collection count limits**: Limit the number of `swagger_specs`, `tools` URLs, `gofannon_agents`, and `invokable_models` per request.
3. **Reduce LLM timeout**: The 1800-second timeout is excessive for code generation. Consider a more reasonable limit (e.g., 120-300 seconds) with retry logic.

### Short-term (Within Sprint)
4. **Audit `swagger_parser` module**: Verify that `parse_spec_and_generate_docs` does not use `spec.name` for file operations and properly handles malformed input.
5. **Validate swagger spec content format**: Before passing to the parser, perform a basic structure check (valid JSON/YAML, contains expected OpenAPI keys).
6. **Sanitize spec.name**: Apply `os.path.basename()` and character allowlisting to spec names before passing to downstream functions.

### Long-term (Architecture)
7. **Centralized file validation middleware**: If other endpoints accept file-like content, implement a shared validation layer that enforces size limits, content type checking, and filename sanitization.
8. **Document file handling architecture**: Clarify whether MinIO-based file storage has its own upload validation layer, and ensure ASVS 5.2.x/5.3.x controls are implemented wherever files enter the system.
9. **Rate limiting on code generation**: Given the expensive nature of LLM calls (30-minute potential hold time), implement per-user rate limiting on the `generate_agent_code` endpoint to prevent resource exhaustion attacks.
        