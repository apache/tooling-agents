# Security Audit Report: ASVS 8.2.2

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | The-AI-Alliance/gofannon |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 8.2.2 |
| **Files Analyzed** | 4 relevant / 4 total |
| **Files Skipped** | 0 |

**Requirement description:** Verify that the application ensures that data-specific access is restricted to consumers with explicit permissions to specific data items to mitigate insecure direct object reference (IDOR) and broken object level authorization (BOLA).

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 4 |
| 🟠 High | 3 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---


> **Note:** 3 Critical findings have been redacted from this report and forwarded to the project's PMC private mailing list.


## ASVS-8.2.2: General Authorization Design (Data-Specific / IDOR)

### Findings for 8.2.2


---


---

#### HIGH
**Finding ID:** ASVS-822-HIGH-001  
**Title:** Chat Ticket IDOR — Any User Can Read Another User's Chat Response  
**File:** `webapp/packages/api/user-service/routes.py` (lines ~369-382)

```python
@router.get("/chat/{ticket_id}")
async def get_chat_status(ticket_id: str, db: DatabaseService = Depends(get_db), user: dict = Depends(get_current_user)):
    """Get the status and result of a chat request"""
    try:
        ticket_data = db.get("tickets", ticket_id)  # No user ownership check
        return ChatResponse(
            ticket_id=ticket_data.get("_id", ticket_id),
            status=ticket_data["status"],
            result=ticket_data.get("result"),  # Contains full LLM response
            error=ticket_data.get("error")
        )
```

While ticket IDs are UUIDv4 (hard to guess), there is no validation that the authenticated user owns the ticket. If an attacker obtains a ticket ID (through logs, shared URLs, browser history, or network interception), they can read another user's complete chat response including potentially sensitive LLM output.

**Data flow:** User-controlled `ticket_id` → `db.get("tickets", ticket_id)` → full response including `result` content → returned to caller (no `userId` comparison)

**Proof of Concept:**
```bash
# If attacker obtains ticket_id from shared workspace logs or network:
curl https://api.example.com/chat/a1b2c3d4-e5f6-7890-abcd-ef1234567890 \
  -H "Cookie: gofannon_sid=attacker-session"
# Returns full chat result including potentially sensitive LLM conversation
```

**Remediation:**
```python
@router.get("/chat/{ticket_id}")
async def get_chat_status(ticket_id: str, db: DatabaseService = Depends(get_db), user: dict = Depends(get_current_user)):
    ticket_data = db.get("tickets", ticket_id)
    # Verify ownership
    if ticket_data.get("userId") != user.get("uid"):
        raise HTTPException(status_code=404, detail="Ticket not found")
    return ChatResponse(...)
```

---

#### HIGH
**Finding ID:** ASVS-822-HIGH-002  
**Title:** Session Config IDOR — Any User Can Read/Modify/Delete Another User's Session Configuration  
**File:** `webapp/packages/api/user-service/routes.py` (lines ~385-415)

```python
@router.post("/sessions/{session_id}/config")
async def update_session_config(session_id: str, config: ProviderConfig, db: DatabaseService = Depends(get_db), user: dict = Depends(get_current_user)):
    try:
        session_doc = db.get("sessions", session_id)  # No ownership check
    except HTTPException as e:
        if e.status_code == 404:
            session_doc = {"created_at": datetime.utcnow().isoformat()}
        else:
            raise
    session_doc["provider_config"] = config.dict()
    db.save("sessions", session_id, session_doc)  # Can overwrite any session

@router.delete("/sessions/{session_id}")
async def delete_session(session_id: str, db: DatabaseService = Depends(get_db), user: dict = Depends(get_current_user)):
    db.delete("sessions", session_id)  # Can delete any session
```

**Data flow:** User-controlled `session_id` path parameter → direct database operation → no user/session ownership comparison

**Proof of Concept:**
```bash
# Modify another user's session config to redirect their LLM calls
curl -X POST https://api.example.com/sessions/other-user-session-id/config \
  -H "Cookie: gofannon_sid=attacker-session" \
  -H "Content-Type: application/json" \
  -d '{"provider": "attacker-controlled", "model": "malicious-model"}'
```

**Impact:** Attacker can modify another user's session configuration, potentially redirecting their LLM calls to attacker-controlled endpoints or changing provider settings.

---


---

#### MEDIUM
**Finding ID:** ASVS-822-MED-001  
**Title:** Deployment Listing Exposes All Tenants' Deployed Agent Details  
**File:** `webapp/packages/api/user-service/routes.py` (line ~582), `webapp/packages/api/user-service/dependencies.py` (`list_deployments` and `get_available_providers`)

```python
@router.get("/deployments", response_model=List[DeployedApi])
async def list_deployments_route(db: DatabaseService = Depends(get_db), user: dict = Depends(get_current_user)):
    deployments = await list_deployments_logic(db)  # Returns ALL deployments
    return [DeployedApi(**dep) for dep in deployments]
```

And in `get_available_providers`:
```python
all_deployments = db.list_all("deployments")  # All tenants' deployments visible
```

**Impact:** All authenticated users can discover every deployed agent across all workspaces, including their friendly names, input schemas, and descriptions.

### Security Controls Inventory for 8.2.2

| Control | Location | Coverage |
|---------|----------|----------|
| User-scoped data store queries | `routes.py:911-1013` | ✅ All data store routes filter by `user_id` |
| Workspace membership in session | `routes.py:88` | ❌ Not used for resource filtering |
| Agent ownership check | N/A | ❌ Not implemented |
| Demo ownership check | N/A | ❌ Not implemented |
| Ticket ownership check | N/A | ❌ Not implemented |
| Session ownership check | N/A | ❌ Not implemented |

### Positive Patterns for 8.2.2

- **Data Store routes properly enforce user scoping:**
  ```python
  # All data store routes filter by user_id - correct pattern
  all_docs = db.find("agent_data_store", {"userId": user_id})
  ```
- The data store architecture (`AgentDataStoreProxy`) binds user_id at construction time, preventing agents from accessing other users' data
- UUIDv4 ticket IDs provide defense-in-depth against casual enumeration (though not a substitute for authorization)

---



## Cross-cutting Architecture Observations

1. **Phase B-1 vs B-3 Gap:** The workspace model (`models/workspace.py`) is well-designed with clear semantics, but workspace-based authorization enforcement is deferred to B-3. Meanwhile, the system operates as essentially a single-tenant application where all authenticated users share all resources (except data store entries). This creates a critical window where the multi-tenant claim in the domain context is aspirational, not actual.

2. **Authentication ≠ Authorization Pattern:** The codebase consistently authenticates users (positive) but conflates authentication with authorization. Every route has `user: dict = Depends(get_current_user)` but the `user` dict is used primarily for logging/context, not for access control decisions.

3. **Data Store as the Exception:** The data store routes demonstrate the correct pattern — filtering by `user_id` at the query level. This pattern should be replicated to agents, demos, deployments, and chat tickets.

4. **Audit Service Infrastructure Without Instrumentation:** `audit_service.py` is well-designed and ready for use, but no route currently calls it. This is acknowledged as B-3 scope but represents a Type B gap (control exists but is not called).

5. **Dual Admin Systems:** The `require_admin_access` (shared password) and `is_site_admin` (session-based) systems serve overlapping purposes without clear delineation, creating potential for privilege confusion.

---

## Cross-cutting Recommendations

### Immediate (Critical — before production multi-tenant deployment)

1. **Implement resource-level ownership validation** for all agent, demo, and deployment routes. At minimum, associate a `workspace_id` or `owner_uid` with each resource and validate it against the authenticated user's workspace memberships before any read/write operation.

2. **Add user ownership validation to chat ticket retrieval** (`GET /chat/{ticket_id}`). Store the user ID with the ticket during creation and verify it on retrieval.

3. **Add ownership validation to session config routes** (`/sessions/{session_id}/config`). Validate that the requesting user owns the session being modified.

### Short-term (High — within next sprint)

4. **Create a `require_workspace_access(resource_type, resource_id)` dependency** that can be applied uniformly to all resource routes, enforcing workspace-scoped access.

5. **Bind admin panel routes to user identity** by adding `get_current_user` alongside `require_admin_access`, enabling audit logging of admin actions.

6. **Scope list endpoints** (`GET /agents`, `GET /demos`, `GET /deployments`) to the user's workspace memberships rather than returning all records.

7. **Instrument cross-workspace access with the audit service** — connect `AuditService.record()` calls to the site-admin bypass path.

### Long-term (Medium — architecture alignment)

8. **Unify the admin authorization model** — replace or supplement the shared-password admin panel with the session-based `is_site_admin` flag, providing per-actor attribution.

9. **Add background task authorization refresh** — before executing long-running operations, re-validate the user's current permissions against the session service rather than relying on the snapshot from request time.

10. **Create and maintain an authorization matrix** document that maps routes → required roles/permissions → enforcement mechanism, enabling automated coverage verification in CI.
        