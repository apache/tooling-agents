# Security Audit Report: ASVS 8.1.1

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | The-AI-Alliance/gofannon |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 8.1.1 |
| **Files Analyzed** | 4 relevant / 4 total |
| **Files Skipped** | 0 |

**Requirement description:** Verify that authorization documentation defines rules for restricting function-level and data-specific access based on consumer permissions and resource attributes.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 1 |

---

## ASVS-8.1.1: Authorization Documentation

### Findings for 8.1.1

#### MEDIUM
**Finding ID:** ASVS-811-MED-001  
**Title:** Workspace Role Permissions Not Mapped to Route-Level Access Controls  
**File:** `webapp/packages/api/user-service/models/workspace.py` (lines 36-37), `webapp/packages/api/user-service/routes.py` (all route handlers)

The `WorkspaceRole` type defines `"member"` and `"admin"` roles, and `MembershipSource` tracks where memberships originate, but there is no documentation or code artifact that maps these roles to specific function-level or data-level permissions:

```python
# models/workspace.py
WorkspaceRole = Literal["member", "admin"]
```

- No documentation exists defining what "admin" vs "member" can do within a workspace
- No route annotations or permission matrices define required roles per endpoint
- The `require_admin_access` dependency in `dependencies.py` checks a shared password unrelated to workspace roles
- The `get_current_user` dependency extracts `workspaces` from session but no downstream handler inspects role values

**Impact:** Without documented authorization rules, developers cannot correctly implement or test access controls. The workspace admin/member distinction exists in the data model but has no defined semantics for API behavior.

**Remediation:** Create an authorization matrix document (or in-code decorator annotations) mapping each route to required workspace role and ownership conditions:

```python
# Example: authorization decorators or metadata
@router.delete("/agents/{agent_id}", status_code=204)
@requires_workspace_role("admin")  # Documented: only workspace admins can delete agents
async def delete_agent(...):
    ...
```

---

#### LOW
**Finding ID:** ASVS-811-LOW-001  
**Title:** Admin Panel Authorization Model Undocumented and Separate from Workspace RBAC  
**File:** `webapp/packages/api/user-service/dependencies.py` (lines 109-114)

```python
def require_admin_access(admin_password: str | None = Header(default=None, alias="X-Admin-Password")):
    if not settings.ADMIN_PANEL_ENABLED:
        raise HTTPException(status_code=403, detail="Admin panel is disabled")
    if admin_password != settings.ADMIN_PANEL_PASSWORD:
        raise HTTPException(status_code=401, detail="Invalid admin password")
```

The admin panel uses a shared secret (`X-Admin-Password`) that is unrelated to the workspace RBAC model (`WorkspaceRole`, site admin via `auth.yaml`). There is no documentation defining how these two authorization systems relate or when each should be used.

**Impact:** Two parallel authorization mechanisms (shared password vs. session-based site admin) create confusion about which grants access where, increasing risk of accidental privilege grants.

### Security Controls Inventory for 8.1.1

| Control | Location | Coverage Status |
|---------|----------|----------------|
| WorkspaceRole type definition | `models/workspace.py:36` | DEFINED only; no enforcement mapping |
| WorkspaceMembership model | `models/workspace.py:46-77` | DEFINED; cached in session |
| MembershipSource tracking | `models/workspace.py:38-48` | DEFINED; for UX only |
| require_admin_access | `dependencies.py:109-114` | Applied to 2 admin routes |
| Audit service | `services/audit_service.py` | DEFINED; not instrumented (B-3) |

### Positive Patterns for 8.1.1

- The workspace model clearly documents ID format conventions with examples in the module docstring
- `MembershipSource` tracks provenance of permissions, enabling future auditability
- The audit service infrastructure (`audit_service.py`) is pre-built for cross-workspace access logging
- Module-level docstrings in `models/workspace.py` and `audit_service.py` describe intended security semantics

---



## Cross-cutting Architecture Observations

1. **Phase B-1 vs B-3 Gap:** The workspace model (`models/workspace.py`) is well-designed with clear semantics, but workspace-based authorization enforcement is deferred to B-3. Meanwhile, the system operates as essentially a single-tenant application where all authenticated users share all resources (except data store entries). This creates a critical window where the multi-tenant claim in the domain context is aspirational, not actual.

2. **Authentication ≠ Authorization Pattern:** The codebase consistently authenticates users (positive) but conflates authentication with authorization. Every route has `user: dict = Depends(get_current_user)` but the `user` dict is used primarily for logging/context, not for access control decisions.

3. **Data Store as the Exception:** The data store routes demonstrate the correct pattern — filtering by `user_id` at the query level. This pattern should be replicated to agents, demos, deployments, and chat tickets.

4. **Audit Service Infrastructure Without Instrumentation:** `audit_service.py` is well-designed and ready for use, but no route currently calls it. This is acknowledged as B-3 scope but represents a Type B gap (control exists but is not called).

5. **Dual Admin Systems:** The `require_admin_access` (shared password) and `is_site_admin` (session-based) systems serve overlapping purposes without clear delineation, creating potential for privilege confusion.

---

## Cross-cutting Recommendations

### Immediate (Critical — before production multi-tenant deployment)

1. **Implement resource-level ownership validation** for all agent, demo, and deployment routes. At minimum, associate a `workspace_id` or `owner_uid` with each resource and validate it against the authenticated user's workspace memberships before any read/write operation.

2. **Add user ownership validation to chat ticket retrieval** (`GET /chat/{ticket_id}`). Store the user ID with the ticket during creation and verify it on retrieval.

3. **Add ownership validation to session config routes** (`/sessions/{session_id}/config`). Validate that the requesting user owns the session being modified.

### Short-term (High — within next sprint)

4. **Create a `require_workspace_access(resource_type, resource_id)` dependency** that can be applied uniformly to all resource routes, enforcing workspace-scoped access.

5. **Bind admin panel routes to user identity** by adding `get_current_user` alongside `require_admin_access`, enabling audit logging of admin actions.

6. **Scope list endpoints** (`GET /agents`, `GET /demos`, `GET /deployments`) to the user's workspace memberships rather than returning all records.

7. **Instrument cross-workspace access with the audit service** — connect `AuditService.record()` calls to the site-admin bypass path.

### Long-term (Medium — architecture alignment)

8. **Unify the admin authorization model** — replace or supplement the shared-password admin panel with the session-based `is_site_admin` flag, providing per-actor attribution.

9. **Add background task authorization refresh** — before executing long-running operations, re-validate the user's current permissions against the session service rather than relying on the snapshot from request time.

10. **Create and maintain an authorization matrix** document that maps routes → required roles/permissions → enforcement mechanism, enabling automated coverage verification in CI.
        