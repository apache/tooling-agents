# Security Audit Report: ASVS 8.2.1

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | The-AI-Alliance/gofannon |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 8.2.1 |
| **Files Analyzed** | 4 relevant / 4 total |
| **Files Skipped** | 0 |

**Requirement description:** Verify that the application ensures that function-level access is restricted to consumers with explicit permissions.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 1 |
| 🟠 High | 1 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---


> **Note:** 1 Critical finding has been redacted from this report and forwarded to the project's PMC private mailing list.


## ASVS-8.2.1: General Authorization Design (Function-Level)

### Findings for 8.2.1


---

#### HIGH
**Finding ID:** ASVS-821-HIGH-001  
**Title:** Admin Panel Routes Lack User Identity Binding — No Attribution for Admin Actions  
**File:** `webapp/packages/api/user-service/routes.py` (lines ~201-221)

Admin routes use `require_admin_access` (shared password) without `get_current_user`:

```python
@router.get("/admin/users", response_model=List[User])
def list_all_users(
    user_service: UserService = Depends(get_user_service_dep),
    _: None = Depends(require_admin_access),  # Shared password only
):
    return user_service.list_users()

@router.put("/admin/users/{user_id}", response_model=User)
def update_user_allowances(
    user_id: str,
    request: AdminUpdateUserRequest,
    user_service: UserService = Depends(get_user_service_dep),
    _: None = Depends(require_admin_access),  # No identity
):
```

**Impact:** 
- Admin operations cannot be attributed to a specific person
- The audit service cannot record who performed admin actions
- Anyone with the shared password is indistinguishable from any other admin
- Password sharing between multiple admins prevents accountability

**Remediation:** Require both authentication and admin verification:
```python
@router.put("/admin/users/{user_id}", response_model=User)
def update_user_allowances(
    user_id: str,
    request: AdminUpdateUserRequest,
    user: dict = Depends(get_current_user),      # Identity
    _: None = Depends(require_admin_access),      # Authorization
    user_service: UserService = Depends(get_user_service_dep),
):
    # Now admin action is attributable to user["uid"]
```

---

#### MEDIUM
**Finding ID:** ASVS-821-MED-001  
**Title:** Workspace Role Information Extracted But Never Evaluated for Access Decisions  
**File:** `webapp/packages/api/user-service/routes.py` (lines 65-93)

**Gap Type:** Type B — Control EXISTS (session contains workspace/role data) but is NOT CALLED for authorization decisions.

```python
# _verify_session_cookie extracts workspace memberships...
async def _verify_session_cookie(request: Request, sid: str) -> Optional[dict]:
    ...
    user = {
        "uid": session.user_uid,
        ...
        "workspaces": [w.model_dump(by_alias=True) for w in session.workspaces],
        "is_site_admin": session.is_site_admin,  # Available but unused
        ...
    }
    return user
```

No route handler inspects `user["workspaces"]` or `user["is_site_admin"]` to make authorization decisions. The workspace membership data flows through the system without being applied.

**Impact:** The system gives false confidence that workspace-based authorization exists (the data is present in the session) while providing no actual access restriction.

### Security Controls Inventory for 8.2.1

| Control | Location | Defined? | Applied? | Coverage |
|---------|----------|----------|----------|----------|
| `get_current_user` (authentication) | `routes.py:117` | ✅ | ✅ All routes except `/health` | Authentication only, not authorization |
| `require_admin_access` (shared password) | `dependencies.py:109` | ✅ | ✅ 2 admin routes | No identity binding |
| Workspace role in session | `routes.py:88` | ✅ | ❌ Never checked in route logic | Type B gap |
| `is_site_admin` in session | `routes.py:89` | ✅ | ❌ Never checked in route logic | Type B gap |
| Resource-to-workspace association | N/A | ❌ Not implemented | ❌ | Type A gap |

### Positive Patterns for 8.2.1

- All routes (except `/health`) require authentication via `get_current_user`
- The Phase B session infrastructure already carries workspace memberships and role information
- The audit service is pre-built to log cross-workspace admin access
- The admin panel can be disabled entirely via `ADMIN_PANEL_ENABLED` setting

---



## Cross-cutting Architecture Observations

1. **Phase B-1 vs B-3 Gap:** The workspace model (`models/workspace.py`) is well-designed with clear semantics, but workspace-based authorization enforcement is deferred to B-3. Meanwhile, the system operates as essentially a single-tenant application where all authenticated users share all resources (except data store entries). This creates a critical window where the multi-tenant claim in the domain context is aspirational, not actual.

2. **Authentication ≠ Authorization Pattern:** The codebase consistently authenticates users (positive) but conflates authentication with authorization. Every route has `user: dict = Depends(get_current_user)` but the `user` dict is used primarily for logging/context, not for access control decisions.

3. **Data Store as the Exception:** The data store routes demonstrate the correct pattern — filtering by `user_id` at the query level. This pattern should be replicated to agents, demos, deployments, and chat tickets.

4. **Audit Service Infrastructure Without Instrumentation:** `audit_service.py` is well-designed and ready for use, but no route currently calls it. This is acknowledged as B-3 scope but represents a Type B gap (control exists but is not called).

5. **Dual Admin Systems:** The `require_admin_access` (shared password) and `is_site_admin` (session-based) systems serve overlapping purposes without clear delineation, creating potential for privilege confusion.

---

## Cross-cutting Recommendations

### Immediate (Critical — before production multi-tenant deployment)

1. **Implement resource-level ownership validation** for all agent, demo, and deployment routes. At minimum, associate a `workspace_id` or `owner_uid` with each resource and validate it against the authenticated user's workspace memberships before any read/write operation.

2. **Add user ownership validation to chat ticket retrieval** (`GET /chat/{ticket_id}`). Store the user ID with the ticket during creation and verify it on retrieval.

3. **Add ownership validation to session config routes** (`/sessions/{session_id}/config`). Validate that the requesting user owns the session being modified.

### Short-term (High — within next sprint)

4. **Create a `require_workspace_access(resource_type, resource_id)` dependency** that can be applied uniformly to all resource routes, enforcing workspace-scoped access.

5. **Bind admin panel routes to user identity** by adding `get_current_user` alongside `require_admin_access`, enabling audit logging of admin actions.

6. **Scope list endpoints** (`GET /agents`, `GET /demos`, `GET /deployments`) to the user's workspace memberships rather than returning all records.

7. **Instrument cross-workspace access with the audit service** — connect `AuditService.record()` calls to the site-admin bypass path.

### Long-term (Medium — architecture alignment)

8. **Unify the admin authorization model** — replace or supplement the shared-password admin panel with the session-based `is_site_admin` flag, providing per-actor attribution.

9. **Add background task authorization refresh** — before executing long-running operations, re-validate the user's current permissions against the session service rather than relying on the snapshot from request time.

10. **Create and maintain an authorization matrix** document that maps routes → required roles/permissions → enforcement mechanism, enabling automated coverage verification in CI.
        