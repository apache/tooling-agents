# Security Audit Report: ASVS 12.2.2

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | The-AI-Alliance/gofannon |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 12.2.2 |
| **Files Analyzed** | 3 relevant / 3 total |
| **Files Skipped** | 0 |

**Requirement description:** Verify that external facing services use publicly trusted TLS certificates.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---

## ASVS-12.2.2: Publicly Trusted TLS Certificates

### Findings for 12.2.2

#### MEDIUM
**Finding ID:** ASVS-1222-MED-001

**Title:** No Certificate Configuration or Validation Visible in Application Code

**File:** `webapp/packages/api/user-service/app_factory.py` (entire file scope)

**Description:** The application factory does not configure any TLS certificate (neither server certificates for serving HTTPS nor CA bundles for validating upstream services). There is no evidence of certificate pinning, OCSP stapling configuration, or certificate transparency enforcement. While TLS termination is expected at the infrastructure layer, the application makes no programmatic assertion about certificate requirements.

**Vulnerable Code (absence):**
```python
def create_app() -> FastAPI:
    """Create and configure a FastAPI application instance."""
    app = FastAPI(lifespan=lifespan)
    # No certificate configuration
    # No ssl_certfile / ssl_keyfile references
    # No CA bundle configuration for outbound connections
    # No certificate validation configuration
```

**Data Flow:** Infrastructure (LB/proxy) → TLS certificate → Client (no application-level verification that publicly trusted certs are used)

**Impact:** Without application-level certificate configuration or deployment validation checks:
1. There is no defense-in-depth ensuring publicly trusted certificates are used
2. Self-signed or internal CA certificates could be deployed without application-level detection
3. Certificate rotation failures would not trigger application-level alerts

**Proof of Concept:** This is an architectural gap rather than an exploitable vulnerability. If the infrastructure is configured with a self-signed certificate:
```bash
# Client connecting would see:
curl https://api.example.com/health
# curl: (60) SSL certificate problem: self-signed certificate
# But application code has no awareness of this state
```

**Remediation:**
Add a startup health check that validates the deployment's certificate configuration:
```python
import ssl
import socket

async def verify_tls_certificate():
    """Verify the service is reachable via publicly-trusted TLS."""
    service_url = os.getenv("SERVICE_PUBLIC_URL")
    if service_url and os.getenv("ENVIRONMENT") != "development":
        hostname = service_url.replace("https://", "").split("/")[0]
        ctx = ssl.create_default_context()  # Uses system CA bundle
        try:
            with socket.create_connection((hostname, 443), timeout=5) as sock:
                with ctx.wrap_socket(sock, server_hostname=hostname) as ssock:
                    cert = ssock.getpeercert()
                    # Verify cert is valid (create_default_context does this)
                    logger.log(level="INFO", message=f"TLS cert valid for {hostname}")
        except ssl.SSLCertVerificationError as e:
            logger.log(level="ERROR", message=f"TLS cert NOT publicly trusted: {e}")
            raise
```

Additionally, document the requirement in deployment manifests:
```yaml
# deployment requirement: TLS certificates MUST be from publicly-trusted CAs
# (Let's Encrypt, DigiCert, etc.) — self-signed certificates are NOT acceptable
```

---

### Security Controls Inventory for 12.2.2

| Control | Location | Status |
|---------|----------|--------|
| Server TLS certificate configuration | Not present (infrastructure) | ⚠️ External |
| Certificate type validation (public CA) | Not present | ❌ Missing |
| Certificate expiry monitoring | Not present | ❌ Missing |
| OCSP stapling | Not present | ❌ Missing |
| Certificate transparency | Not present | ❌ Missing |

### Positive Patterns for 12.2.2

- The architectural decision to delegate TLS to cloud load balancers (GCP, AWS) is a valid pattern, as these platforms typically auto-provision and auto-renew publicly trusted certificates (e.g., AWS ACM, GCP-managed SSL certificates).
- No self-signed certificate generation or insecure certificate bypass (`verify=False`) code is present in this file.

---



## Cross-cutting Architecture Observations

1. **Complete Infrastructure Delegation for TLS**: The application code contains zero TLS-related configuration. All three ASVS requirements (12.1.1, 12.2.1, 12.2.2) are entirely delegated to infrastructure (cloud load balancers, reverse proxies). This is architecturally valid but creates a **Type B gap** — security controls exist (at infra level) but the application cannot verify they're actually applied.

2. **No Defense-in-Depth**: The application has no fallback security mechanisms if infrastructure-level TLS is misconfigured. Missing HSTS, missing HTTPS redirect, and missing origin scheme validation mean a single infrastructure misconfiguration could expose all traffic.

3. **Development Defaults in Production Path**: The `FRONTEND_URL` defaults to `http://localhost:3000` — an HTTP URL. This value flows directly into security-sensitive CORS configuration without any scheme validation, creating risk of accidental HTTP exposure in production.

4. **Observability Gap**: While `ObservabilityMiddleware` is present, there is no logging of TLS-related events (protocol version, certificate status, connection security level). This makes it impossible to detect TLS degradation in production through application-level monitoring.

5. **Single File Limitation**: This audit covers only the application factory. TLS configuration for outbound connections (to CouchDB, Firestore, DynamoDB, LDAP, LLM providers) is not visible in this file and would require inspection of service client configurations.

---

## Cross-cutting Recommendations

### Immediate (Sprint 0 / Pre-deployment)

1. **Add HSTS middleware** with `max-age=31536000; includeSubDomains` to all responses
2. **Add HTTPS scheme validation** for `FRONTEND_URL` in non-development environments — fail startup if HTTP origin is configured in production
3. **Add `HTTPSRedirectMiddleware`** for production deployments (respecting `X-Forwarded-Proto` from load balancers)

### Short-term (Next 2-4 weeks)

4. **Create a deployment security checklist** that documents TLS requirements and provides verification commands
5. **Add startup TLS verification** that confirms the service's public endpoint is using valid, publicly-trusted certificates
6. **Configure response headers** middleware that sets security headers including HSTS, `X-Content-Type-Options`, and `X-Frame-Options`
7. **Audit outbound connections** (database clients, LDAP, LLM API clients) to verify they enforce TLS 1.2+ and certificate validation

### Long-term (Quarterly)

8. **Implement TLS monitoring** — log TLS protocol version and cipher suite for each connection (available via proxy access logs or application-level inspection)
9. **Add certificate rotation automation** with alerting for expiry within 30 days
10. **Consider OCSP stapling** configuration at the infrastructure level and document verification procedures
11. **Evaluate Encrypted Client Hello (ECH)** support as browser adoption increases
        