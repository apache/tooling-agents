# Security Audit Report: ASVS 2.1.1

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | The-AI-Alliance/gofannon |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 2.1.1 |
| **Files Analyzed** | 5 relevant / 5 total |
| **Files Skipped** | 0 |

**Requirement description:** Verify that the application's documentation defines input validation rules for how to check the validity of data items against an expected structure. This could be common data formats such as credit card numbers, email addresses, telephone numbers, or it could be an internal data format.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 2 |
| 🟢 Low | 1 |

---

## ASVS-2.1.1: Validation and Business Logic Documentation

### Findings for 2.1.1

### [MEDIUM]
**Finding ID**: ASVS-211-MED-001  
**Title**: Missing documented validation rules for business-critical numeric fields  
**Location**: `models/agent.py` (LlmSettings, lines ~107-112), `routes.py` (UpdateMonthlyAllowanceRequest, UpdateSpendRemainingRequest, lines ~138-151)

```python
class LlmSettings(BaseModel):
    max_tokens: Optional[int] = Field(None, alias="maxTokens")
    temperature: Optional[float] = None
    reasoning_effort: Optional[str] = Field(None, alias="reasoningEffort")
```

```python
class UpdateMonthlyAllowanceRequest(BaseModel):
    monthly_allowance: float = Field(..., alias="monthlyAllowance")
```

**Data flow**: Client request → Pydantic model (type-checked only) → service layer → database/LLM provider  
**Issue**: No documented constraints (min/max, allowed ranges) for numerical fields with clear business limits. `temperature` has well-known provider-specific ranges (0-2 for OpenAI), `max_tokens` should have an upper bound to prevent resource abuse, and `monthly_allowance`/`spend_remaining` should not accept negative values. The lack of documented rules means developers implementing these fields cannot verify correctness.  
**Impact**: Developers have no specification to implement against; QA cannot verify business logic correctness; negative allowances or extreme token counts may propagate to downstream systems causing unexpected behavior.  
**Remediation**: Add Field constraints documenting business rules:

```python
class LlmSettings(BaseModel):
    max_tokens: Optional[int] = Field(None, alias="maxTokens", ge=1, le=200000)
    temperature: Optional[float] = Field(None, ge=0.0, le=2.0)
    reasoning_effort: Optional[str] = Field(None, pattern="^(low|medium|high)$")
```

---

### [MEDIUM]
**Finding ID**: ASVS-211-MED-002  
**Title**: No documented format rules for URL-type inputs  
**Location**: `routes.py` (FetchSpecRequest line ~131, ListMcpToolsRequest line ~124)

```python
class FetchSpecRequest(BaseModel):
    url: str

class ListMcpToolsRequest(BaseModel):
    mcp_url: str
    auth_token: Optional[str] = None
```

**Issue**: Fields that clearly represent URLs (`url`, `mcp_url`) have no documented format rules defining valid URL structures, allowed protocols (http/https only), or prohibited patterns (e.g., internal IPs, loopback addresses). Without documented rules, SSRF protection is ad-hoc.  
**Impact**: No clear specification for implementing SSRF protections; impossible to verify whether the application correctly restricts URL inputs.  
**Remediation**: Document URL constraints using Pydantic's `AnyHttpUrl` type or Field metadata:

```python
from pydantic import AnyHttpUrl

class FetchSpecRequest(BaseModel):
    url: AnyHttpUrl  # Enforces http/https scheme
```

---

### [LOW]
**Finding ID**: ASVS-211-LOW-001  
**Title**: Missing documented validation rules for string-identity fields  
**Location**: `models/user.py` (BasicInfo, line ~36), `routes.py` (UpdateApiKeyRequest, line ~218)

```python
class BasicInfo(BaseModel):
    display_name: Optional[str] = Field(default=None, alias="displayName")
    email: Optional[str] = None

class UpdateApiKeyRequest(BaseModel):
    provider: str
    api_key: str
```

**Issue**: The `email` field has no format documentation (e.g., RFC 5322 pattern), `provider` has no documented allow-list of valid values, and `display_name` has no length constraints. These are common data formats for which ASVS 2.1.1 expects explicit documented rules.  
**Remediation**: Add `EmailStr` for email, `Literal` or `pattern` for provider, and `max_length` for display_name.

### Security Controls Inventory for 2.1.1

| Control | Location | Coverage |
|---------|----------|----------|
| Pydantic type annotations | All model files | Type-level documentation for all fields |
| `ChatMessage.role` pattern | `models/chat.py:42` | Documents allowed role values |
| `DataStoreNamespaceConfig.access` Literal | `models/agent.py:27` | Documents allowed access modes |
| `_ensure_mutually_exclusive` validator | `models/chat.py:9-31` | Documents parameter constraints |
| `extra="ignore"` configuration | `models/agent.py:77,96` | Documents rejection of unexpected fields |

### Positive Patterns for 2.1.1

- `ChatMessage.role` uses explicit regex pattern `"^(user|assistant|system)$"` — a clear documented validation rule for allowed values.
- `DataStoreNamespaceConfig.access` uses `Literal["read", "write", "both"]` with docstring explaining its advisory nature — clear documentation of intent and behavior.
- `_ensure_mutually_exclusive` function with clear docstring documenting the parameter conflict rules.
- Pydantic's `alias` system provides clear documentation of the expected wire format (camelCase aliases).

---



## Cross-cutting Architecture Observations

1. **Dual HTML-serving and API-serving role**: While the domain context states "FastAPI serves API endpoints, not HTML," the auth router serves multiple HTML pages (`dev_stub_picker`, `_render_deny_page`). These HTML-generating endpoints have inconsistent security controls — one uses `html.escape()` while the other does not.

2. **Validation exists but coverage is incomplete**: The codebase demonstrates awareness of input validation (regex patterns, Literal types, mutual exclusivity validators) but these are applied inconsistently. Business-critical inputs like billing amounts, LLM parameters, and deployed agent inputs lack the same rigor.

3. **Type B gaps are the most significant risk**: The `input_schema` field exists on every agent but is never validated at the `/rest/{friendly_name}` entry point. The `html.escape` function is imported and used in one HTML generator but not another. These "control exists but not applied" gaps are more dangerous than missing controls because they create false confidence.

4. **Output encoding is generally correct for JSON contexts**: The application primarily operates as a JSON API where FastAPI/Pydantic handle serialization correctly. The XSS risks are concentrated in the few HTML-generating endpoints in the auth router.

5. **Open redirect risk in OAuth flow**: The post-authentication redirect accepts arbitrary absolute URLs, which is a common OAuth implementation mistake. The CSRF protection (state parameter) is well-implemented but doesn't protect against attacker-initiated flows with malicious `return_to` values.

---

## Cross-cutting Recommendations

### Immediate (Fix within current sprint)

1. **Fix XSS in `dev_stub_picker`** (ASVS-121-HIGH-001): Apply `html.escape()` and `urllib.parse.quote()` to all user-controlled values interpolated into HTML/URLs. This is a 5-line fix in a single function.

2. **Fix open redirect in `login_callback`** (ASVS-122-HIGH-001): Validate that absolute `return_to` URLs match the configured `FRONTEND_URL` domain. Reject or ignore external domains.

3. **Add input validation to `/rest/{friendly_name}`** (ASVS-221-HIGH-001): Validate incoming JSON against the agent's declared `input_schema` before execution.

### Short-term (Next 2-4 weeks)

4. **Add range constraints to numeric fields**: Apply `ge`, `le`, `gt`, `lt` constraints to billing amounts, LLM parameters, and other business-critical numbers.

5. **Validate provider/model against known values**: Add model validators to `ChatRequest` and `ProviderConfig` that check against `PROVIDER_CONFIG`.

6. **Add `max_length` to string fields**: Especially `ChatMessage.content`, agent `name`/`description`, and API key values.

7. **Validate URL protocols on SSRF-sensitive inputs**: Use `AnyHttpUrl` or custom validators for `FetchSpecRequest.url` and `ListMcpToolsRequest.mcp_url`.

### Long-term (Next quarter)

8. **Create a validation specification document**: Document all input validation rules in a central specification that maps each endpoint's expected formats, ranges, and constraints. This satisfies ASVS 2.1.1 fully.

9. **Implement request body size limits**: Add middleware or FastAPI configuration to limit maximum request body size, protecting against memory exhaustion attacks on unbounded string fields.

10. **Add JSON Schema validation middleware for deployed agents**: Create a reusable middleware that validates request bodies against stored schemas, making the `input_schema` field enforceable rather than advisory.
        