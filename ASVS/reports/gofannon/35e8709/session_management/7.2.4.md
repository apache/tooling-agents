# Security Audit Report: ASVS 7.2.4

## Executive Summary

| Field | Value |
|-------|-------|
| **Repository** | The-AI-Alliance/gofannon |
| **Audit Date** | May 05, 2026 |
| **Auditor** | Tooling Agents (bundled-pass mode) |
| **ASVS Requirement** | ASVS 7.2.4 |
| **Files Analyzed** | 3 relevant / 3 total |
| **Files Skipped** | 0 |

**Requirement description:** Verify that the application generates a new session token on user authentication, including re-authentication, and terminates the current session token.

### Findings Overview

| Severity | Count |
|----------|-------|
| 🔴 Critical | 0 |
| 🟠 High | 0 |
| 🟡 Medium | 1 |
| 🟢 Low | 0 |

---

## ASVS-7.2.4: Fundamental Session Management Security — New Token on Authentication

### Findings for 7.2.4

### [MEDIUM]

**Finding ID:** ASVS-724-MED-001

**Title:** Previous session not terminated on re-authentication

**File:** `webapp/packages/api/user-service/routes_auth.py` — `login_callback()` (lines 127–189)

**Vulnerable code:**

```python
@router.get("/auth/callback/{provider_type}")
async def login_callback(
    provider_type: str,
    request: Request,
    code: str = Query(...),
    state: Optional[str] = Query(default=None),
    session_svc: SessionService = Depends(_get_session_svc),
    expected_state: Optional[str] = Cookie(default=None, alias="gofannon_auth_state"),
    return_to: Optional[str] = Cookie(default=None, alias="gofannon_return_to"),
):
    # ... OAuth validation ...
    
    # New session is created (good):
    session = await session_svc.create_from_login(
        user_info=user_info,
        provider_memberships=memberships,
        is_site_admin=decision.site_admin,
    )
    
    # Cookie is set with new session ID (good):
    resp.set_cookie(
        key=SessionService.cookie_name(),
        value=session.id,
        ...
    )
    # BUT: the OLD session (if one exists in gofannon_sid cookie) is NEVER terminated
```

**Data flow:** User re-authenticates → old `gofannon_sid` cookie value is NOT read → old session document persists in CouchDB → old session remains valid until TTL expiry.

**Root cause:** The callback handler does not accept/read the existing `gofannon_sid` cookie to terminate it before creating the new session.

**Proof of concept:**
1. User Alice logs in on Browser A → session `s1` created, stored in cookie
2. Attacker captures session ID `s1` (e.g., via network sniffing before HTTPS deployment, or physical access)
3. Alice re-authenticates (logs in again) → session `s2` created, overwrites cookie in Browser A
4. Attacker uses `s1` — it remains valid in CouchDB until TTL (default 24 hours)

**Design documentation confirms:** `session_service.py` docstring states: *"Sessions are never revoked server-side at login time of another browser; concurrent sessions are allowed."*

**Impact:** Session fixation mitigation is incomplete. Compromised session tokens remain usable after re-authentication, contrary to the principle that re-authentication should reset the security state. While 256-bit session IDs make blind guessing infeasible, a stolen token retains validity.

**Remediation:**

```python
@router.get("/auth/callback/{provider_type}")
async def login_callback(
    provider_type: str,
    request: Request,
    code: str = Query(...),
    state: Optional[str] = Query(default=None),
    session_svc: SessionService = Depends(_get_session_svc),
    expected_state: Optional[str] = Cookie(default=None, alias="gofannon_auth_state"),
    return_to: Optional[str] = Cookie(default=None, alias="gofannon_return_to"),
    existing_sid: Optional[str] = Cookie(default=None, alias="gofannon_sid"),  # ADD THIS
):
    # ... existing validation ...
    
    # Terminate the previous session before creating a new one
    if existing_sid:
        await session_svc.delete(existing_sid)
    
    session = await session_svc.create_from_login(...)
    # ... rest of handler ...
```

### Security Controls Inventory for 7.2.4

| Control | Location | Status |
|---------|----------|--------|
| New session ID generation on login | `session_service.py:71` (`_new_session_id()`) | ✅ Applied |
| Old session termination on re-auth | `routes_auth.py:127-189` | ❌ **NOT IMPLEMENTED** (Type A gap) |

### Positive Patterns for 7.2.4

1. **New session ID is always generated**: `create_from_login()` unconditionally calls `_new_session_id()` — the new session is never reusing an old ID.
2. **Cookie is properly overwritten**: The response sets the new session ID in the cookie, so the client's active token does change.

---



## Cross-cutting Architecture Observations

1. **Session-per-request validation model is sound**: The reference token architecture with server-side CouchDB storage provides a strong foundation. Invalidation is effective once triggered.

2. **Concurrent sessions are an explicit design choice**: The docstring states sessions are never revoked at login time of another browser. This is documented but creates the 7.2.4 gap where old sessions persist after re-authentication.

3. **No session index by user**: The session service only supports lookup by session ID (the CouchDB `_id`). There's no secondary index or view for finding all sessions belonging to a user, which is the root cause of the 7.4.2 gap.

4. **Open redirect potential in `return_to`**: The callback handler (line 161) passes absolute URLs through without origin validation: `if raw_target.startswith(("http://", "https://")): redirect_url = raw_target`. While the `return_to` originates from an httponly cookie set during login initiation, the original query parameter is user-controlled. This is outside session management scope but worth noting.

5. **Dev stub picker XSS**: The `state` parameter in `dev_stub_picker` is embedded into HTML without escaping (line 247). While `state` is normally generated by `secrets.token_urlsafe()`, the endpoint accepts arbitrary query string input. This is a minor concern for a development-only endpoint.

6. **Legacy Firebase coexistence**: The main `routes.router` is always mounted but auth routes are conditional. The session service only handles Phase B auth. Legacy Firebase sessions presumably use a different mechanism — the security properties of that path should be verified separately.

## Cross-cutting Recommendations

### Immediate (Critical/High priority)

1. **Implement `terminate_all_for_user(user_uid)`** in `SessionService`:
   - Add a CouchDB view/index on `user_uid` in the `user_sessions` collection
   - Expose an admin API endpoint for bulk session termination
   - Wire account disable/delete actions to this method
   - *Addresses:* ASVS-742-HIGH-001

### Short-term (Medium priority)

2. **Terminate existing session on re-authentication**:
   - Read the existing `gofannon_sid` cookie in the callback handler
   - Call `session_svc.delete(existing_sid)` before creating the new session
   - *Addresses:* ASVS-724-MED-001

3. **Add session management admin capabilities**:
   - List active sessions for a user (for support/compliance)
   - Force-terminate specific or all sessions
   - Audit log session termination events

### Long-term (Architecture improvements)

4. **Consider session event bus for real-time revocation**:
   - For scenarios where the 15-minute refresh interval is too slow
   - Publish account-disable events that session validation can subscribe to
   - Useful when workspace membership removal needs immediate effect

5. **Add session metadata tracking**:
   - IP address, user agent, last-active timestamp
   - Enables anomaly detection and informed session management decisions
   - Supports "terminate sessions from unknown locations" use case
        