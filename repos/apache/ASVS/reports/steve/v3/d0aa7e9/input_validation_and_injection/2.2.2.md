# ASVS 2.2.2 Security Audit Report

**Repository:** apache/steve  
**Audit Date:** Apr 22, 2026  
**Auditor:** Tooling Agents  
**ASVS Requirement:** 2.2.2 — Verify that the application is designed to enforce input validation at a trusted service layer. While client-side validation improves usability and should be encouraged, it must not be relied upon as a security control.

---

## Executive Summary

This security audit evaluated the Apache Steve voting system against ASVS requirement 2.2.2, focusing on server-side input validation enforcement at the trusted service layer. The audit analyzed 23 files across API endpoints, service layer logic, and presentation templates.

**Key Metadata:**
- **Repository:** apache/steve
- **Audit Date:** Apr 22, 2026
- **Auditor:** Tooling Agents
- **ASVS Requirement:** 2.2.2 — Verify that the application is designed to enforce input validation at a trusted service layer. While client-side validation improves usability and should be encouraged, it must not be relied upon as a security control.
- **ASVS Level:** 1
- **Section:** Input Validation (Chapter: Validation and Business Logic)
- **Files Analyzed:** 23 relevant out of 23 total, 0 skipped

**Finding Summary:**

| Severity | Count | Description |
|----------|-------|-------------|
| **Critical** | **1** | Server-side vote content validation missing — relies solely on client-side controls |
| High | 0 | None identified |
| Medium | 0 | None identified |
| Low | 0 | None identified |

**Critical Finding:** The application contains a Type B validation gap where server-side vote content validation was explicitly identified as necessary (via TODO comment in `election.py:211`) but never implemented. This creates direct reliance on client-side form controls as the sole security mechanism for vote integrity, violating ASVS 2.2.2's core requirement.

---

## Security Controls Inventory

| Control | Location | Description | Coverage |
|---------|----------|-------------|----------|
| Election ID validation | `load_election` decorator (pages.py:163) | Validates EID exists in database | All `/manage/<eid>`, `/vote-on/<eid>`, `/do-*/<eid>` routes ✅ |
| Issue ID validation | `load_election_issue` decorator (pages.py:185) | Validates IID exists for given election | All `/<eid>/<iid>` routes ✅ |
| Date format validation | `_set_election_date` (pages.py:84) | `datetime.fromisoformat()` with error handling | `do-set-open_at`, `do-set-close_at` ✅ |
| Vote type assertion | `election.py:163,174` | `assert vtype in vtypes.TYPES` | `add_issue`, `edit_issue` ✅ |
| Election state assertions | `election.py` (multiple) | Asserts correct state before operations | `add_issue`, `edit_issue`, `delete_issue`, `add_vote`, `close` ✅ |
| Issue ID in vote form | `do_vote_endpoint` (pages.py:404) | Validates submitted IIDs against election's issue list | `do-vote` ✅ |
| Mayvote authorization | `serve_doc` (pages.py:591) | Checks user has mayvote record for issue | `/docs/<iid>/<docname>` ✅ |
| **Votestring content validation** | `election.py:211` (comment only) | **PLANNED but NOT IMPLEMENTED** | `add_vote` / `do-vote` ❌ |

---

## Critical File Review

### `v3/server/pages.py` — Entry Points

| Endpoint | Method | Server-Side Validation | Client-Side Only | Gap? |
|----------|--------|----------------------|-------------------|------|
| `/do-vote/<eid>` | POST | EID ✅, IID ✅, votestring ❌ | Radio/Modal UI | **CRITICAL (Type B)** |
| `/do-create-election` | POST | Auth ✅, form.title ❌ | `required`, JS | Type A (not CRITICAL) |
| `/do-add-issue/<eid>` | POST | EID ✅, state ✅, form fields ❌ | `required`, JS | Type A (not CRITICAL) |
| `/do-edit-issue/<eid>/<iid>` | POST | EID ✅, IID ✅, form fields ❌ | `required`, JS | Type A (not CRITICAL) |
| `/do-set-open_at/<eid>` | POST | EID ✅, date format ✅ | Date picker | None |
| `/do-set-close_at/<eid>` | POST | EID ✅, date format ✅ | Date picker | None |

### `v3/steve/election.py` — Service Layer

| Method | Input Validated | Input NOT Validated | Gap? |
|--------|----------------|---------------------|------|
| `add_vote()` | pid (via mayvote), iid (via mayvote), state (assertion) | **votestring** (TODO comment only) | **CRITICAL (Type B)** |
| `add_issue()` | vtype (assertion), eid (implicit) | title, description (no length/content) | Type A |
| `edit_issue()` | vtype (assertion), iid (rowcount check) | title, description (no length/content) | Type A |

---

## Findings

### Critical Severity

#### ASVS-222-CRT-001: Server-Side Vote Content Validation Missing — Client-Side Controls Relied Upon as Sole Security Control (Type B Gap)

**Severity:** CRITICAL

**Gap Type:** Type B — Control acknowledged as needed (`### validate VOTESTRING for ISSUE.TYPE voting`) but **never implemented**. Client-side form controls create false confidence that votes are constrained.

**Location:**
- **Entry point:** `v3/server/pages.py`, `do_vote_endpoint()`, lines 387–428
- **Service layer:** `v3/steve/election.py`, `add_vote()`, lines 208–219

**Related Functions Checked:**
- `do_vote_endpoint()` (pages.py:387-428) — validates EID and IID but not votestring content
- `add_vote()` (election.py:208-219) — contains TODO comment for validation but no implementation
- `tally_issue()` (election.py) — consumes decrypted votes without validation
- Vote type modules in `vtypes` — no `validate()` method exists

**Vulnerable Code:**

`v3/steve/election.py` lines 208-219:
```python
def add_vote(self, pid: str, iid: str, votestring: str):
    "Add VOTESTRING as the (latest) vote by PID for IID."

    # The Election should be open.
    md = self._all_metadata(self.S_OPEN)

    ### validate VOTESTRING for ISSUE.TYPE voting       # <── CONTROL IDENTIFIED BUT NOT IMPLEMENTED

    mayvote = self.q_get_mayvote.first_row(pid, iid)
    vote_token = crypto.gen_vote_token(md.opened_key, pid, iid, mayvote.salt)

    # Pass MAYVOTE.SALT for PBKDF.
    ciphertext = crypto.create_vote(vote_token, mayvote.salt, votestring)

    self.c_add_vote.perform(vote_token, ciphertext)     # <── STORED WITHOUT VALIDATION
```

`v3/server/pages.py` lines 395-421 (calling code):
```python
votes = {}
for key, value in form.items():
    if key.startswith('vote-'):
        iid = key.split('-', 1)[1]
        votes[iid] = value                              # <── Raw form value, no validation

issues = election.list_issues()
issue_dict = {i.iid: i for i in issues}                 # <── Vote type IS available here

for iid, votestring in votes.items():
    if iid not in issue_dict:                            # <── IID validated ✅
        await flash_danger(f'Invalid issue ID: {iid}')
        return quart.redirect(f'/vote-on/{election.eid}', code=303)

    try:
        election.add_vote(result.uid, iid, votestring)  # <── votestring NOT validated ❌
```

**Data Flow:**
```
HTTP POST /do-vote/<eid>
  → form["vote-<iid>"] (user-controlled string)
    → do_vote_endpoint: votes[iid] = value (NO validation)
      → election.add_vote(uid, iid, votestring) (NO validation, just a TODO comment)
        → crypto.create_vote(..., votestring) → encrypted and stored
          → Later: tally_issue() decrypts → vtypes module processes garbage data
```

**Client-side controls relied upon (NOT server-enforced):**

For **YNA** votes (`vote-on.ezt`): HTML radio buttons constrain value to `y`, `n`, `a`:
```html
<input type="radio" name="vote-[issues.iid]" value="y">
<input type="radio" name="vote-[issues.iid]" value="n">
<input type="radio" name="vote-[issues.iid]" value="a">
```

For **STV** votes (`vote-on.ezt`): JavaScript modal constructs comma-separated candidate labels from a drag-and-drop UI, stored in a hidden input. All constraints are purely client-side.

**Proof of Concept:**

YNA vote corruption — submit arbitrary string instead of `y`/`n`/`a`:
```bash
curl -X POST https://steve.example.org/do-vote/a1b2c3d4e5 \
  -H "Cookie: session=<valid_session>" \
  -d "vote-f6g7h8i9j0=MALICIOUS_STRING&csrf_token=placeholder"
```

STV vote corruption — submit invalid/duplicate candidate labels:
```bash
curl -X POST https://steve.example.org/do-vote/a1b2c3d4e5 \
  -H "Cookie: session=<valid_session>" \
  -d "vote-k1l2m3n4o5=FAKE_LABEL,FAKE_LABEL,FAKE_LABEL&csrf_token=placeholder"
```

**Impact:**
- **Vote integrity corruption:** Invalid votestrings are encrypted and stored. During tally (`tally_issue()`), the decrypted garbage is passed to `vtypes.vtype_module(issue.type).tally(votes, ...)`, which may silently miscalculate results, crash, or produce undefined behavior.
- **Election outcome manipulation:** An authenticated voter can submit strategically crafted invalid votes to disrupt tallying of a legitimate election.
- **Silent failure:** Because votes are encrypted at submission time, invalid content is undetectable until decryption during tally — long after the election closes.
- **Direct ASVS 2.2.2 violation:** The application relies entirely on client-side validation for business-critical vote content, with no server-side enforcement at the trusted service layer.

**Remediation:**

Add server-side votestring validation in **both** the handler and the service layer:

In `v3/server/pages.py` (`do_vote_endpoint`):
```python
for iid, votestring in votes.items():
    if iid not in issue_dict:
        await flash_danger(f'Invalid issue ID: {iid}')
        return quart.redirect(f'/vote-on/{election.eid}', code=303)

    issue = issue_dict[iid]
    # Server-side vote content validation
    if issue.vtype == 'yna' and votestring not in ('y', 'n', 'a'):
        await flash_danger(f'Invalid vote value for issue {iid}.')
        return quart.redirect(f'/vote-on/{election.eid}', code=303)
    elif issue.vtype == 'stv':
        labels = set(issue.kv.get('labelmap', {}).keys())
        submitted = [s.strip() for s in votestring.split(',') if s.strip()]
        if not submitted or len(submitted) != len(set(submitted)):
            await flash_danger(f'Invalid ranking for issue {iid}.')
            return quart.redirect(f'/vote-on/{election.eid}', code=303)
        if not all(s in labels for s in submitted):
            await flash_danger(f'Unknown candidate in ranking for issue {iid}.')
            return quart.redirect(f'/vote-on/{election.eid}', code=303)
```

In `v3/steve/election.py` (`add_vote`) — defense-in-depth:
```python
def add_vote(self, pid: str, iid: str, votestring: str):
    md = self._all_metadata(self.S_OPEN)

    # Validate votestring against issue type
    issue = self.q_get_issue.first_row(iid)
    if not issue:
        raise IssueNotFound(iid)
    vtype_mod = vtypes.vtype_module(issue.type)
    vtype_mod.validate(votestring, self.json2kv(issue.kv))  # Raises on invalid

    mayvote = self.q_get_mayvote.first_row(pid, iid)
    ...
```

---

## Positive Security Patterns

| Pattern | Location | Assessment |
|---------|----------|------------|
| Election/Issue existence validation via decorators | `load_election`, `load_election_issue` | Consistent application across all relevant routes |
| Date input validation with proper error handling | `_set_election_date()` | Validates format, returns 400 on invalid |
| Vote type assertion (`assert vtype in vtypes.TYPES`) | `election.py:163,174` | Prevents invalid vote types in issue creation/edit |
| State machine assertions before state-changing operations | `election.py` throughout | Prevents out-of-order operations |
| Issue ID validation in vote submission | `do_vote_endpoint` | Validates IIDs against election's actual issues |
| Parameterized queries via asfpy.db | All database operations | Prevents SQL injection |
| EZT escaping for JS/HTML contexts | Templates: `[format "js,html"]` | Properly applied for onclick handlers in manage.ezt, manage-stv.ezt |
| `send_from_directory` for file serving | `serve_doc`, `serve_static` | Framework-level path traversal protection |

**Assessment:** The application demonstrates strong architectural patterns for structural validation (identifiers, state management, SQL injection prevention) and shows clear understanding of security principles. The decorator pattern for route-level validation is particularly well-executed. The critical gap represents an incomplete implementation rather than a fundamental architectural flaw.

---

## Architecture Observations

1. **Validation architecture is partially established but incomplete.** The codebase demonstrates a clear pattern of server-side validation for structural identifiers (EID, IID, state) via decorators and assertions, but this pattern was not extended to content validation of business-critical data (vote content).

2. **The `issue_dict` with vote type information is constructed at the entry point** (`do_vote_endpoint` line 402) but is only used for IID existence checking, not for votestring content validation. All necessary information for validation is available but unused.

3. **Encrypted storage masks validation failures.** The vote encryption architecture means invalid votestrings become undetectable immediately after submission. This makes the missing validation particularly dangerous — there is no secondary check before the data causes harm during tally.

4. **TODO comment indicates awareness but not prioritization.** The explicit TODO comment in `election.py:211` demonstrates that developers identified the need for this control but did not implement it, suggesting it may have been deprioritized or overlooked during development cycles.

---

## Recommendations Summary

### Immediate Actions (Critical Priority)

1. **Implement server-side votestring validation** in both `do_vote_endpoint()` and `add_vote()` as described in ASVS-222-CRT-001 remediation. This is the single highest-priority item because it directly affects election integrity and the control was explicitly identified as needed but never implemented.

### Short-term Actions (Recommended)

2. **Add a `validate()` function to each vote type module** (`vtypes`) that can be called from `add_vote()` as a defense-in-depth layer, ensuring the service layer independently enforces business rules regardless of the caller.

3. **Implement automated testing** for vote validation with both valid and invalid inputs across all vote types (YNA, STV) to prevent regression.

### Medium-term Actions (Recommended)

4. **Add server-side presence and length validation** for form fields (title, description) in `do_create_endpoint`, `do_add_issue_endpoint`, and `do_edit_issue_endpoint` to match the client-side constraints and complete the server-side validation layer.

5. **Conduct a comprehensive audit** of all form inputs to identify any other instances where client-side validation is not mirrored server-side.

6. **Establish a security checklist** for new feature development that explicitly requires server-side validation for all user inputs at the trusted service layer.

---

## Appendix: Files Analyzed

<details>
<summary>Click to expand complete file list (23 files)</summary>

- `v3/queries.yaml` (relevance: 10)
- `v3/server/api.py` (relevance: 10)
- `v3/server/bin/asf-load-ldap.py` (relevance: 10)
- `v3/server/bin/create-election.py` (relevance: 10)
- `v3/server/pages.py` (relevance: 10)
- `v3/server/templates/about.ezt` (relevance: 10)
- `v3/server/templates/admin.ezt` (relevance: 10)
- `v3/server/templates/e_bad_eid.ezt` (relevance: 10)
- `v3/server/templates/e_bad_iid.ezt` (relevance: 10)
- `v3/server/templates/e_bad_pid.ezt` (relevance: 10)
- `v3/server/templates/flashes.ezt` (relevance: 10)
- `v3/server/templates/footer.ezt` (relevance: 10)
- `v3/server/templates/header.ezt` (relevance: 10)
- `v3/server/templates/home.ezt` (relevance: 10)
- `v3/server/templates/manage-stv.ezt` (relevance: 10)
- `v3/server/templates/manage.ezt` (relevance: 10)
- `v3/server/templates/privacy.ezt` (relevance: 10)
- `v3/server/templates/profile.ezt` (relevance: 10)
- `v3/server/templates/settings.ezt` (relevance: 10)
- `v3/server/templates/vote-on.ezt` (relevance: 10)
- `v3/server/templates/voter.ezt` (relevance: 10)
- `v3/steve/election.py` (relevance: 10)
- `v3/steve/persondb.py` (relevance: 10)

</details>

---

**End of Report**