name: Notify on PR close
on:
  pull_request:
    types: [closed]
jobs:
  notify:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - name: Send notification
        run: echo "Merged: ${{ github.event.pull_request.title }}"
